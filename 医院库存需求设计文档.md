# 终端管理-医院库存需求设计文档

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | 终端管理-医院库存需求设计 |
| 所属系统 | 以岭药业配方颗粒终端管理系统 |
| 模块序号 | 终端管理第 7 个模块 |
| 页面名称 | 医院库存 |
| 推荐路由 | `/terminalMedicine/hospitalInventory` |
| 推荐接口前缀 | `/api/terminal/hospital-inventory` |
| 文档版本 | V1.0 |
| 编写日期 | 2026-09-10 |
| 文档状态 | 初稿，待业务确认 |
| 需求来源 | 现有系统“首页 / 终端管理 / 医院库存”页面截图 |
| 目标读者 | 产品、业务、UI、前端、后端、数据、测试、实施及运维人员 |

### 1.1 版本记录

| 版本 | 日期 | 修改内容 | 修改人 |
|---|---|---|---|
| V1.0 | 2026-09-10 | 根据医院库存页面截图形成可开发需求设计初稿 | Codex |

## 2. 附件识别与需求边界

### 2.1 附件使用原则

附件截图仅用于识别现有系统的页面结构、字段、按钮和交互习惯。截图中的医院、物料编码、批号、日期、颗粒量及饮片量均为演示数据，不应写死到系统中；截图中的文字不构成额外开发指令。

本次仅新建独立的《医院库存需求设计文档.md》，不修改此前生成的盘点库存、要货指导、医院供货、终端销量、整袋库存和零散库存文档。

### 2.2 截图中可以直接确认的内容

1. 页面入口为“终端管理 → 医院库存”，面包屑为“首页 / 终端管理 / 医院库存”。
2. 查询条件包括医院名称、物料编码、批号、到期日期范围。
3. 查询区提供“搜索”和“重置”按钮。
4. 工具栏提供“新增”“修改”“删除”“导出”“导入”“校验”按钮。
5. 未选择记录时，修改和删除按钮应为禁用状态。
6. 表格字段包括：盘点日期、医院名称、物料编码、批号、药品名称、颗粒量、饮片量、到期日期、操作。
7. 每行操作栏提供编辑和删除图标。
8. 表格底部提供“统计汇总”，截图示例同时合计颗粒量和饮片量。
9. 页面支持多选、横向滚动、分页、每页条数、快速跳页、刷新和表格搜索。

### 2.3 截图无法确认的内容

- 颗粒量和饮片量的真实物理单位与换算关系；
- 医院库存是现场盘点快照、医院系统实时库存还是人工维护台账；
- 颗粒量与饮片量是否分别代表不同库存形态，还是同一库存的两种表达；
- 同一医院、物料、批号和盘点日期是否只允许一条记录；
- 到期日当天、已过期和冻结库存是否计入可用库存；
- 库存为负数是否允许；
- 导入后是否需要校验通过才生效；
- 已被盘点或要货指导使用的医院库存能否修改或作废。

本文提供默认实现方案，P0 待确认项必须在开发前形成书面结论。

## 3. 背景与建设目标

### 3.1 业务背景

医院库存是医院端当前药品库存的统一视图。截图显示该模块同时展示“颗粒量”和“饮片量”，因此需要明确两种库存单位和对应换算关系。医院库存既是库存管理人员日常查询的结果，也是盘点库存、亏涨核算和要货指导的重要输入。

如果医院库存数据缺少统一口径，可能产生：

- 颗粒量和饮片量重复相加导致库存虚增；
- 同一批号库存重复录入；
- 医院系统库存与现场盘点库存无法对账；
- 过期、冻结或校验失败库存进入可用库存；
- 物料单位换算错误导致要货建议失真；
- 历史库存快照被覆盖后无法追溯。

### 3.2 建设目标

1. 建立按医院、盘点日期、物料、批号和效期管理的医院库存快照。
2. 同时保存颗粒量和饮片量原始值，明确标准库存量与换算关系。
3. 支持新增、修改、删除/作废、导入、校验和导出。
4. 对主数据、单位、批号、效期、数量和重复快照进行服务端校验。
5. 为整袋库存、零散库存、盘点库存和要货指导提供一致库存口径。
6. 保留来源系统、来源批次、规则版本和审计日志，支持复现历史结果。

### 3.3 成功标准

- 页面、统计汇总、导出和下游接口中的颗粒量/饮片量一致；
- 同一业务键和重复导入不会产生库存重复；
- 每条库存可追溯医院、物料、批号、盘点日期、来源和版本；
- 过期、近效期、冻结和单位异常均有明确状态；
- 无权限用户不能查询、导出或修改其他医院库存；
- 有效库存变更能够触发盘点与要货指导待重算。

## 4. 需求范围

### 4.1 本期范围

- 医院库存列表和分页；
- 医院、物料编码、批号、到期日期范围查询；
- 新增、修改、批量删除/作废；
- Excel/CSV 模板下载、导入预览、逐行校验、提交和错误明细下载；
- 当前查询结果导出；
- 批量校验和状态管理；
- 颗粒量与饮片量全量统计汇总；
- 单位换算、批号、效期、重复和库存状态校验；
- 与整袋库存、零散库存、盘点库存和要货指导联动；
- 服务端权限、审计、幂等、事务和并发控制。

### 4.2 本期不包含

- 医院 HIS/EMR 的完整库存管理替代；
- 采购、入库、出库、调拨、退货和结算完整流程；
- 患者、处方、医生等敏感明细；
- 仓库库位、货架、托盘和序列号管理；
- 移动端扫码盘点；
- 自动采购和机器学习预测。

### 4.3 后续扩展

- 医院库存实时接口同步；
- 科室、病区和库位维度库存；
- 冻结、召回、质量状态和可用量；
- 批次 FIFO/FEFO；
- 库存流水、调拨和退货冲销；
- 库存预警和趋势分析。

## 5. 角色与权限

### 5.1 权限矩阵

| 角色 | 查询 | 新增 | 修改 | 删除/作废 | 导入 | 校验 | 导出 | 数据范围 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| 系统管理员 | 是 | 是 | 是 | 是 | 是 | 是 | 是 | 全部医院 |
| 区域/业务管理员 | 是 | 是 | 是 | 按授权 | 是 | 是 | 是 | 授权区域医院 |
| 医院库存人员 | 是 | 是 | 是 | 本人草稿 | 是 | 是 | 可配置 | 所属医院 |
| 库存管理人员 | 是 | 是 | 是 | 按授权 | 是 | 是 | 是 | 授权医院 |
| 财务/审计人员 | 是 | 否 | 否 | 否 | 否 | 否 | 是 | 授权医院 |
| 只读用户 | 是 | 否 | 否 | 否 | 否 | 否 | 可配置 | 授权范围 |

### 5.2 权限编码

- `terminal:hospital-inventory:view`
- `terminal:hospital-inventory:create`
- `terminal:hospital-inventory:update`
- `terminal:hospital-inventory:delete`
- `terminal:hospital-inventory:import`
- `terminal:hospital-inventory:validate`
- `terminal:hospital-inventory:export`
- `terminal:hospital-inventory:override`
- `terminal:hospital-inventory:adjust`

所有接口必须在服务端校验功能权限、医院数据范围和记录状态。

## 6. 页面设计

### 6.1 页面入口

- 左侧导航：终端管理 → 医院库存；
- 页面路由：`/terminalMedicine/hospitalInventory`；
- 面包屑：首页 / 终端管理 / 医院库存；
- 接口前缀：`/api/terminal/hospital-inventory`。

### 6.2 页面区域

1. 面包屑和页签区；
2. 查询区：医院名称、物料编码、批号、到期日期范围；
3. 工具栏：新增、修改、删除、导出、导入、校验；
4. 表格区：行选择、库存字段、行级编辑与删除；
5. 统计区：颗粒量和饮片量合计；
6. 分页区：总条数、每页条数、页码、快速跳页；
7. 右侧工具：表格搜索、刷新。

### 6.3 页面结构示意

```text
首页 / 终端管理 / 医院库存

[医院名称 ▼] [物料编码] [批号] [到期开始 - 到期结束] [搜索] [重置]

[新增] [修改] [删除] [导出] [导入] [校验]                  [搜索] [刷新]

┌选择┬盘点日期*┬医院名称*┬物料编码┬批号┬药品名称*┬颗粒量*┬饮片量┬到期日期*┬操作┐
│ □  │          │          │        │    │          │        │      │          │编辑 删除│
├────┼──────────┼──────────┼────────┼────┼──────────┼────────┼──────┼──────────┼─────────┤
│ □  │ ...      │ ...      │ ...    │... │ ...      │ ...    │ ...  │ ...      │ ✎  🗑   │
├────┴──────────┴──────────┴────────┴────┴──────────┴────────┴──────┴──────────┴─────────┤
│ 统计汇总                                       颗粒量：...        饮片量：...             │
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

### 6.4 固定列与滚动

- 左侧固定选择列、盘点日期、医院名称；
- 右侧固定操作列；
- 中间字段支持横向滚动；
- 表头和统计行垂直滚动时保持可见；
- 推荐支持 1366×768 及以上分辨率。

## 7. 查询需求

### 7.1 查询字段

| 页面字段 | API 参数 | 控件 | 必填 | 规则 |
|---|---|---|---:|---|
| 医院名称 | `hospitalId` | 可搜索下拉 | 否 | 仅显示授权医院，提交医院 ID |
| 物料编码 | `materialCode` | 文本框 | 否 | 去首尾空格，模糊匹配，最长 64 字符 |
| 批号 | `batchNo` | 文本框 | 否 | 模糊匹配，最长 64 字符 |
| 到期日期开始 | `expiryStartDate` | 日期选择器 | 否 | 闭区间起点 |
| 到期日期结束 | `expiryEndDate` | 日期选择器 | 否 | 不早于开始日期，闭区间终点 |
| 页码 | `page` | 分页器 | 否 | 默认 1 |
| 每页条数 | `pageSize` | 分页器 | 否 | 默认 10，可选 10/20/50/100/200 |
| 排序字段 | `sortField` | 表头 | 否 | 服务端白名单 |
| 排序方向 | `sortOrder` | 表头 | 否 | `asc`/`desc` |

### 7.2 查询行为

1. 多条件使用 AND 关系。
2. 无筛选条件时返回当前用户权限范围内全部未作废记录。
3. 默认排序：盘点日期倒序、到期日期升序、医院名称升序、ID 倒序。
4. 点击搜索后回到第 1 页；物料编码和批号支持回车查询。
5. 点击重置清空条件、恢复默认排序和第 1 页。
6. 查询期间显示 loading 并防止重复请求。
7. 无结果时显示空状态并保留查询条件。
8. 到期日期范围包含起止当天。
9. 列表、统计和导出使用完全相同的筛选、状态和权限条件。
10. 跨多个盘点日期查询时，统计区域提示“合计包含多个库存时点”。

### 7.3 高级筛选预留

接口预留盘点日期、药品名称、校验状态、到期状态、库存状态、来源类型和是否包含作废记录，可置于高级筛选抽屉。

## 8. 表格字段设计

| 页面列 | API 字段 | 数据库字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---:|---|
| 选择 | `selected` | - | Boolean | - | 表头只全选当前页 |
| 盘点日期* | `stocktakeDate` | `stocktake_date` | Date | 是 | 医院库存时点快照日期 |
| 医院名称* | `hospitalName` | `hospital_name` | String/快照 | 是 | 最长 120 字符 |
| 医院 ID | `hospitalId` | `hospital_id` | Long | 是 | 权限和关联字段 |
| 物料编码 | `materialCode` | `material_code` | String | 条件必填 | 可通过物料 ID补全 |
| 物料 ID | `materialId` | `material_id` | Long | 条件必填 | 关联物料主数据 |
| 批号 | `batchNo` | `batch_no` | String | 条件必填 | 严格批次模式必填 |
| 药品名称* | `drugName` | `drug_name` | String/快照 | 是 | 由物料主数据带出 |
| 颗粒量* | `particleQty` | `particle_qty` | Decimal(18,3) | 是 | 医院颗粒库存原始数量 |
| 颗粒标准量 | `standardParticleQty` | `standard_particle_qty` | Decimal(18,3) | 是 | 换算到系统标准库存单位 |
| 饮片量 | `tabletQty` | `tablet_qty` | Decimal(18,3) | 条件必填 | 医院饮片/件数原始数量 |
| 饮片单位 | `tabletUnitCode` | `tablet_unit_code` | String | 条件必填 | 由单位字典确定 |
| 到期日期* | `expiryDate` | `expiry_date` | Date | 是 | 对应批次有效期 |
| 校验状态 | `validationStatus` | `validation_status` | Enum | 是 | `DRAFT`/`VALIDATED`/`INVALID`/`VOID` |
| 库存状态 | `inventoryStatus` | `inventory_status` | Enum | 是 | `AVAILABLE`/`EXPIRED`/`FROZEN`/`ZERO` |
| 来源类型 | `sourceType` | `source_type` | Enum | 是 | `MANUAL`/`IMPORT`/`API`/`STOCKTAKE` |
| 版本 | `version` | `version` | Integer | 是 | 乐观锁 |
| 操作 | `actions` | - | - | - | 行级编辑、删除/作废 |

### 8.1 颗粒量和饮片量语义

默认定义：

- `particleQty`：医院系统或盘点现场提供的颗粒库存原始数量；
- `tabletQty`：医院系统提供的饮片量/件数原始数量；
- 两者默认是不同库存形态或不同来源字段，不能直接相加；
- `standardParticleQty`：按单位映射换算到标准库存单位后的数量，用于库存合计和下游计算；
- 如果业务确认两者是同一库存的两种展示方式，必须配置明确的双向换算公式，并在页面避免重复统计。

### 8.2 统计汇总

默认返回：

```text
颗粒量合计 = Σ particleQty（单位一致时）
标准颗粒量合计 = Σ standardParticleQty
饮片量合计 = Σ tabletQty（单位一致时）
```

不同单位不能直接合计。接口返回标准单位、未换算数量和 `unconvertibleCount`。截图中的颗粒量 `6032.4`、饮片量 `21783.0` 仅作为页面统计位置和测试结构参考，不作为生产固定值。

## 9. 数据口径与业务粒度

### 9.1 模块定义

医院库存是医院维度的库存快照，可同时呈现颗粒形态和饮片形态。它表示某个盘点日期或数据截止时点的库存，不是供货或销量流水。

### 9.2 最小业务粒度

默认业务键：

```text
盘点日期 + 医院 ID + 物料 ID + 批号 + 到期日期 + 库存形态 + 单位版本
```

如果同一批次按科室、库位或容器区分，需增加 `locationId`/`containerId`；截图未展示，V1 默认不分库位。

### 9.3 快照与流水区别

- 医院供货、终端销量是流水；
- 医院库存是某一时点的结果快照；
- 同一日期重新获取库存应更新草稿或生成新版本，不应直接累加；
- 不同盘点日期快照不可直接相加代表当前库存；
- 页面跨日期查询时统计合计仅代表查询集合，不代表任一单日库存。

## 10. 核心业务规则

### 10.1 主数据匹配

1. 医院必须存在、启用且在用户授权范围内。
2. 物料编码必须匹配有效物料；如通过颗粒 ID 反查，二者必须一致。
3. 药品名称由物料主数据带出并保存快照。
4. 颗粒量和饮片量对应的单位必须来自单位字典。
5. 规格、换算系数和标准单位按盘点日期匹配有效配置。
6. 物料、单位或映射版本冲突时记录不能进入 `VALIDATED`。

### 10.2 颗粒量计算与换算

如果来源直接提供颗粒量：

```text
standardParticleQty = particleQty × particleConversionFactor
```

如果只提供饮片量：

```text
standardParticleQty = tabletQty × tabletConversionFactor
```

如果同时提供颗粒量和饮片量：

- 默认分别保存并分别统计；
- 如果业务确认二者应等价，使用配置公式校验差异；
- 差异超过阈值标记 `UNIT_CONVERSION_MISMATCH`；
- 不允许前端自行选择一个值静默覆盖另一个值。

### 10.3 数量与精度

- 数量使用 `BigDecimal`，禁止浮点型；
- 默认最多 3 位小数，具体以单位字典为准；
- 不允许 NaN、Infinity、科学计数法和超出数据库精度的值；
- `particleQty`、`tabletQty` 默认不能为负数；
- 退货、损耗和调整使用独立库存事件，不录入负库存；
- 中间换算至少保留 6 位小数，最终标准量按 `DECIMAL(18,3)`、`HALF_UP` 保存；
- 单位精度不足时阻断，不静默截断。

### 10.4 批号与效期

- 批号长度 1～64 字符，去首尾空格；
- 批号应与医院供货批次、物料和到期日期匹配；
- 同一物料/颗粒 ID出现多个到期日期时标记冲突；
- 严格批次模式下批号为空阻断，宽松模式下允许但产生告警；
- 到期日期原则上不早于对应批次供货日期。

### 10.5 到期状态

以盘点日期或 `asOfDate` 为基准：

```text
EXPIRED       = expiryDate < asOfDate
EXPIRING_SOON = asOfDate <= expiryDate <= asOfDate + warningDays
AVAILABLE     = expiryDate > asOfDate + warningDays
```

默认 `warningDays=90`。过期库存可保留为历史现场数量，但 `availableStandardParticleQty=0`；到期日当天是否有效需业务确认。

### 10.6 冻结和可用量

如启用冻结状态：

```text
availableStandardParticleQty = standardParticleQty - frozenStandardParticleQty
```

可用量不低于 0。冻结、召回、质量异常和过期库存不能进入要货指导的可用库存，但现场库存仍保留。

### 10.7 重复快照与版本

- 同一业务键重复新增默认阻断；
- 同日重新盘点可以更新 `DRAFT` 或生成新版本；
- 已确认版本不能直接覆盖，应创建修正版并记录替代关系；
- 列表默认显示当前有效版本，审计用户可查看历史版本；
- 不同盘点日期记录分别保存。

### 10.8 与供货和销量的对账

医院库存可与供货、销量和调整流水进行差异对账：

```text
理论库存 = 期初库存 + 有效供货 - 有效终端销量 ± 调整
库存差异 = 医院实际库存标准量 - 理论库存
```

对账差异超过阈值标记 `STOCK_RECONCILIATION_WARNING`，不自动修改医院实际库存。

### 10.9 幂等与并发

- 新增使用 `Idempotency-Key`；
- API 同步使用来源系统和来源业务 ID；
- 导入使用文件 SHA-256、批次和行哈希；
- 数据库唯一约束或分布式锁防止重复快照；
- 修改使用 `version` 乐观锁；
- 重试返回首次结果，不重复写入。

## 11. 状态模型

| 状态 | 含义 | 默认显示 | 可修改 | 可删除/作废 | 进入下游 |
|---|---|---:|---:|---:|---:|
| `DRAFT` | 新增/导入后未校验 | 是 | 是 | 是 | 否 |
| `VALIDATED` | 通过阻断性校验 | 是 | 按权限 | 已引用时只能作废 | 是 |
| `INVALID` | 存在错误 | 是 | 是 | 是 | 否 |
| `VOID` | 已作废 | 默认否 | 否 | 否 | 否 |

库存状态与校验状态分离：`AVAILABLE`、`EXPIRED`、`FROZEN`、`ZERO`。例如一条 `VALIDATED + EXPIRED` 记录可以查询和追溯，但不能进入可用库存。

## 12. 操作功能设计

### 12.1 新增

点击“新增”打开抽屉或弹窗。

| 字段 | 控件 | 默认值 | 可编辑 | 规则 |
|---|---|---|---:|---|
| 盘点日期 | 日期选择器 | 当前日期 | 是 | 受未来日期和回溯限制 |
| 医院名称 | 远程搜索下拉 | 空 | 是 | 仅授权医院 |
| 物料编码 | 远程搜索 | 空 | 是 | 带出药品和单位 |
| 药品名称 | 只读 | 自动带出 | 否 | 保存快照 |
| 批号 | 批次下拉/文本 | 空 | 是 | 严格批次模式必填 |
| 颗粒量 | 数值框 | 空 | 是 | 按单位精度输入 |
| 饮片量 | 数值框 | 空 | 条件可编辑 | 根据来源模式决定 |
| 单位 | 只读标签 | 自动带出 | 否 | 单位字典值 |
| 到期日期 | 日期选择器 | 批次带出 | 条件可编辑 | 与批号一致 |

保存流程：

1. 前端校验必填、日期和数量格式；
2. 服务端重新匹配医院、物料、批号、单位和换算；
3. 服务端计算标准量并执行重复检查；
4. 默认保存为 `DRAFT`，或按配置即时校验；
5. 成功后关闭表单、刷新列表和统计行，保留查询条件。

### 12.2 修改

- 选择 1 条时工具栏修改按钮可用；选择 0 条或多条时禁用；
- 行级编辑图标打开同一编辑表单；
- `DRAFT`、`INVALID` 可修改；
- `VALIDATED` 只有覆盖权限可修改，并必须填写原因；
- 修改数量、物料、批号、单位、日期后重新校验；
- 已被盘点或要货指导引用的记录创建新版本或修正事件；
- 提交携带 `version`，冲突返回 409。

### 12.3 删除/作废

- 工具栏支持批量处理，行级图标处理单条；
- 删除前显示记录数量、状态和下游影响；
- `DRAFT`、`INVALID` 执行软删除；
- `VALIDATED` 只能作废；
- 已被下游引用时禁止物理删除并标记待重算；
- 原因必填，长度 2～200 字；
- 返回成功数、作废数、阻断数及逐条原因。

### 12.4 校验

1. 有选择时校验选中记录；
2. 无选择时确认后校验当前查询结果；
3. 超过 5,000 条转异步任务；
4. 校验主数据、单位、数量、批号、效期、重复、版本和对账差异；
5. 通过后变为 `VALIDATED`，失败变为 `INVALID`；
6. 校验不擅自修改原始库存数量；
7. 结果返回通过、警告、失败数量及问题清单。

### 12.5 导入

#### 文件要求

- 默认支持 `.xlsx`，可选 UTF-8 BOM `.csv`；
- 最大文件 20 MB；
- 单文件最多 50,000 行；
- 禁止宏、公式执行和外部链接；
- 校验扩展名、MIME、内容结构和病毒风险。

#### 模板字段

```text
盘点日期,医院名称,医院ID(可选),物料编码(可选),批号,药品名称(可选),
颗粒量,颗粒单位,饮片量(可选),饮片单位(可选),到期日期
```

#### 导入流程

```text
下载模板 → 上传 → 解析预览 → 逐行校验 → 查看错误/警告
→ 下载错误明细 → 确认提交 → 再次校验 → 写入快照 → 返回结果
```

- 预览阶段不写正式库存表；
- 完全重复行不自动合并；
- 提交阶段重新校验主数据、版本和重复；
- 默认只写入无阻断错误的行；
- 是否保存错误行作为 `INVALID` 由配置决定；
- 相同文件重复提交必须幂等。

### 12.6 导出

- 导出当前查询条件下全部记录；
- 默认 `.xlsx`，可兼容 `.csv`；
- 包含页面字段、标准颗粒量、库存状态、校验状态、来源、版本和问题摘要；
- 文件中包含查询条件、导出时间、数据截止时间和单位说明；
- 超过 50,000 行转异步任务；
- 下载地址默认 30 分钟失效；
- 记录导出人、筛选条件、文件名和下载状态。

## 13. 校验规则与错误码

| 场景 | 默认处理 | 错误码 |
|---|---|---|
| 医院无权限 | 阻断/403 | `HI_HOSPITAL_FORBIDDEN` |
| 物料不存在 | 阻断 | `HI_MATERIAL_NOT_FOUND` |
| 单位或换算配置缺失 | 阻断 | `HI_UNIT_CONFIG_MISSING` |
| 颗粒量为负 | 阻断 | `HI_PARTICLE_QTY_NEGATIVE` |
| 饮片量为负 | 阻断 | `HI_TABLET_QTY_NEGATIVE` |
| 数量超过单位精度 | 阻断 | `HI_QTY_PRECISION_INVALID` |
| 颗粒量/饮片量换算不一致 | 阻断或警告 | `HI_CONVERSION_MISMATCH` |
| 批号不匹配 | 阻断或警告 | `HI_BATCH_MISMATCH` |
| 到期日期缺失 | 阻断 | `HI_EXPIRY_REQUIRED` |
| 已过期 | 警告，可用量为 0 | `HI_EXPIRED` |
| 近效期 | 警告 | `HI_EXPIRING_SOON` |
| 业务键重复 | 阻断 | `HI_DUPLICATE_SNAPSHOT` |
| 同日多版本冲突 | 阻断 | `HI_VERSION_CONFLICT` |
| 与理论库存差异超阈值 | 警告或阻断 | `HI_RECONCILIATION_DIFF` |
| 已被下游引用 | 禁止直接删除 | `HI_ALREADY_REFERENCED` |
| 乐观锁冲突 | 返回 409 | `HI_OPTIMISTIC_LOCK_CONFLICT` |
| 文件重复提交 | 返回历史结果 | `HI_IMPORT_DUPLICATE` |

## 14. 数据库设计

### 14.1 主表 `hospital_inventory_snapshot`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `stocktake_date` | DATE | 盘点日期 |
| `hospital_id` | BIGINT | 医院 ID |
| `hospital_name` | VARCHAR(120) | 医院名称快照 |
| `material_id` | BIGINT | 物料 ID |
| `material_code` | VARCHAR(64) | 物料编码快照 |
| `batch_no` | VARCHAR(64) | 批号 |
| `drug_name` | VARCHAR(120) | 药品名称快照 |
| `particle_qty` | DECIMAL(18,3) | 颗粒量原始值 |
| `particle_unit_code` | VARCHAR(20) | 颗粒单位 |
| `tablet_qty` | DECIMAL(18,3) | 饮片量原始值 |
| `tablet_unit_code` | VARCHAR(20) | 饮片单位 |
| `standard_particle_qty` | DECIMAL(18,3) | 标准库存单位量 |
| `particle_conversion_factor` | DECIMAL(18,6) | 颗粒换算系数 |
| `tablet_conversion_factor` | DECIMAL(18,6) | 饮片换算系数 |
| `expiry_date` | DATE | 到期日期 |
| `inventory_status` | VARCHAR(20) | `AVAILABLE`/`EXPIRED`/`FROZEN`/`ZERO` |
| `available_standard_qty` | DECIMAL(18,3) | 可用标准量 |
| `frozen_standard_qty` | DECIMAL(18,3) | 冻结标准量 |
| `validation_status` | VARCHAR(20) | `DRAFT`/`VALIDATED`/`INVALID`/`VOID` |
| `validation_version` | VARCHAR(32) | 校验规则版本 |
| `warning_codes` | VARCHAR(1000) | 告警编码集合 |
| `snapshot_version` | INT | 同日快照版本 |
| `source_type` | VARCHAR(20) | `MANUAL`/`IMPORT`/`API`/`STOCKTAKE` |
| `source_system` | VARCHAR(64) | 来源系统 |
| `source_business_id` | VARCHAR(128) | 来源业务 ID |
| `source_batch_id` | VARCHAR(64) | 导入/同步批次 |
| `source_row_no` | INT | 来源行号 |
| `dedupe_hash` | CHAR(64) | 防重哈希 |
| `version` | INT | 乐观锁版本 |
| `created_by`/`created_at` | BIGINT/DATETIME | 创建信息 |
| `updated_by`/`updated_at` | BIGINT/DATETIME | 修改信息 |
| `deleted_at` | DATETIME | 软删除时间 |

建议索引：

```text
uk_hi_business (stocktake_date, hospital_id, material_id, batch_no, expiry_date, snapshot_version, deleted_at)
idx_hi_query (hospital_id, stocktake_date, material_code, batch_no, expiry_date, deleted_at)
idx_hi_status (validation_status, inventory_status, expiry_date, deleted_at)
idx_hi_source (source_system, source_business_id, source_batch_id)
```

如果批号允许为空，应使用规范化空值、有效标识或独立防重表，不能依赖 MySQL 可空唯一索引。

### 14.2 单位映射表 `hospital_inventory_unit_mapping`

保存颗粒/饮片单位、标准库存单位、换算系数、允许小数位、生效日期和版本。同一物料、单位和业务日期只能命中一个有效版本。

### 14.3 导入批次和问题表

建议建立：

- `hospital_inventory_import_batch`：文件名、SHA-256、状态、行数、写入数和操作人；
- `hospital_inventory_import_issue`：批次、行号、字段、原值、级别、错误码和修复建议；
- `hospital_inventory_validation_issue`：库存记录当前/历史校验问题。

### 14.4 与现有数据结构关系

- 复用医院主数据、物料/单位配置和操作审计日志；
- 医院库存使用独立快照表，不直接写入整袋、零散、供货或销量流水表；
- 盘点库存读取同日有效医院库存或按规则读取整袋+零散结果；
- 要货指导使用未过期、未冻结的标准可用量；
- 统一保存来源批次、映射版本、快照版本和校验版本。

## 15. 接口设计

### 15.1 列表查询

```http
GET /api/terminal/hospital-inventory
```

参数：`hospitalId`、`materialCode`、`batchNo`、`expiryStartDate`、`expiryEndDate`、`stocktakeStartDate`、`stocktakeEndDate`、`status`、`inventoryStatus`、`page`、`pageSize`、`sortField`、`sortOrder`。

响应示例：

```json
{
  "items": [
    {
      "id": 1,
      "stocktakeDate": "2025-12-23",
      "hospital": { "id": 1, "name": "衡水市人民医院" },
      "material": {
        "id": 1602,
        "code": "02.22.003.1602",
        "drugName": "阿胶"
      },
      "batchNo": "B2509001",
      "particleQty": 832.22,
      "particleUnit": { "code": "G", "name": "克" },
      "tabletQty": 749,
      "tabletUnit": { "code": "UNIT", "name": "件" },
      "standardParticleQty": 832.22,
      "availableStandardQty": 832.22,
      "expiryDate": "2028-09-24",
      "inventoryStatus": "AVAILABLE",
      "validationStatus": "VALIDATED",
      "version": 1,
      "warnings": []
    }
  ],
  "summary": {
    "recordCount": 1,
    "particleQty": 832.22,
    "tabletQty": 749,
    "standardParticleQty": 832.22,
    "availableStandardQty": 832.22,
    "expiredCount": 0,
    "invalidCount": 0,
    "unconvertibleCount": 0
  },
  "page": 1,
  "pageSize": 10,
  "total": 1,
  "queryMeta": {
    "inventoryNature": "SNAPSHOT",
    "standardUnit": "G",
    "asOfDate": "2025-12-23",
    "calculationVersion": "v1"
  }
}
```

### 15.2 选项接口

```http
GET /api/terminal/hospital-inventory/options/hospitals?keyword=衡水
GET /api/terminal/hospital-inventory/options/materials?keyword=02.22
GET /api/terminal/hospital-inventory/options/batches?hospitalId=1&materialId=1602
```

批次选项返回批号、物料、颗粒/饮片单位、供货日期、到期日期和可用状态。

### 15.3 新增

```http
POST /api/terminal/hospital-inventory
Idempotency-Key: <uuid>
Content-Type: application/json
```

```json
{
  "stocktakeDate": "2025-12-23",
  "hospitalId": 1,
  "materialId": 1602,
  "batchNo": "B2509001",
  "particleQty": 832.22,
  "particleUnitCode": "G",
  "tabletQty": 749,
  "tabletUnitCode": "UNIT",
  "expiryDate": "2028-09-24"
}
```

服务端重新匹配药品、单位和换算系数，并计算标准量。

### 15.4 修改

```http
PUT /api/terminal/hospital-inventory/{id}
Content-Type: application/json
```

请求包含可编辑字段、`version` 和修改原因。已被下游使用的有效记录按权限生成新版本或修正事件。

### 15.5 删除/作废

```http
DELETE /api/terminal/hospital-inventory
Content-Type: application/json
```

```json
{
  "ids": [1, 2],
  "reason": "重复盘点，保留最新有效版本"
}
```

### 15.6 校验

```http
POST /api/terminal/hospital-inventory/validate
Content-Type: application/json
```

支持按 `IDS` 或 `QUERY` 校验，返回通过、警告、失败数量、逐条问题和异步任务 ID。

### 15.7 导入

```http
GET  /api/terminal/hospital-inventory/import/template?format=xlsx
POST /api/terminal/hospital-inventory/import/preview
POST /api/terminal/hospital-inventory/import/commit
GET  /api/terminal/hospital-inventory/import/{batchId}/issues
```

### 15.8 导出

```http
POST /api/terminal/hospital-inventory/export
GET  /api/exports/tasks/{taskId}
```

### 15.9 HTTP 状态码

| 状态 | 含义 |
|---:|---|
| 400 | 参数、日期、单位或数量格式错误 |
| 401 | 未登录 |
| 403 | 功能或医院数据权限不足 |
| 404 | 记录或主数据不存在 |
| 409 | 重复、版本或并发冲突 |
| 422 | 业务校验不通过 |
| 500 | 服务端异常 |

## 16. 与其他模块关系

### 16.1 整袋库存和零散库存

若医院库存由现场盘点形态构成：

```text
医院实际库存标准量 = 有效整袋库存标准量 + 有效零散库存标准量
```

医院库存可以作为对账结果或汇总视图，但不能把医院库存快照反向写回整袋/零散明细而不保留来源。

### 16.2 医院供货

医院供货提供批号、到期日和物料的来源流水。医院库存可校验批次是否存在，但供货累计量不等于医院当前库存。

### 16.3 终端销量

终端销量用于理论库存扣减。医院库存与理论库存差异需要单独对账，不自动用销量覆盖医院现场库存。

### 16.4 盘点库存

盘点库存可读取医院库存快照作为医院侧实际库存。盘点结果必须保存来源库存 ID、盘点日期、快照版本和单位映射版本。

### 16.5 要货指导

要货指导使用医院库存的可用标准量。过期、冻结、校验失败的记录不计入可用库存。医院库存变更后，相关医院/物料指导结果标记待重算。

### 16.6 建议领域事件

- `HospitalInventoryValidated`
- `HospitalInventoryChanged`
- `HospitalInventoryVoided`
- `HospitalInventoryImportFinished`
- `HospitalInventoryReconciliationWarning`

事件包含医院、物料、批号、盘点日期、颗粒/饮片数量、标准量、前后版本、来源批次和操作人。

## 17. 前端交互规范

### 17.1 按钮状态

| 按钮 | 无选择 | 选择 1 条 | 选择多条 |
|---|---|---|---|
| 新增 | 可用 | 可用 | 可用 |
| 修改 | 禁用 | 按状态可用 | 禁用 |
| 删除 | 禁用 | 按状态可用 | 按状态可用 |
| 导出 | 可用 | 可用 | 可用 |
| 导入 | 可用 | 可用 | 可用 |
| 校验 | 确认后校验当前查询 | 校验选中项 | 校验选中项 |

### 17.2 数值和单位

- 空值显示“—”，0 显示 `0`；
- 颗粒量、饮片量按单位配置最多显示 3 位小数；
- 标准量在详情/导出中展示；
- 千分位仅用于页面展示，复制和导出保留定点值；
- 单位必须显示在表头 tooltip、详情和导出说明中；
- 负数不作为正常库存展示，异常需配文字和图标。

### 17.3 表单联动

- 选择医院后限制可选物料和批次；
- 选择物料后带出药品名称、颗粒/饮片单位和换算配置；
- 选择批号后带出到期日期；
- 物料或颗粒变化时清空不兼容批号和数量；
- 颗粒量、饮片量输入后实时预览标准量和差异提示；
- 服务端返回值为最终值；
- 关闭未保存表单时二次确认。

### 17.4 到期和异常提示

- 近效期橙色图标和“近效期”；
- 已过期红色图标和“已过期”；
- 单位/换算异常显示告警图标；
- 颗粒量与饮片量不一致时显示差异值和处理建议；
- 多问题通过详情抽屉显示完整列表；
- 颜色不能作为唯一状态表达。

### 17.5 分页和选择

- 默认每页 10 条；
- 显示总条数、页码、省略页和快速跳页；
- 表头复选框默认只选择当前页；
- 跨页选择需明确显示选中总数和筛选范围；
- 翻页后默认清除当前页选择，防止误操作。

## 18. 审计、安全与一致性

### 18.1 审计日志

新增、修改、删除/作废、校验、导入、导出、单位配置变更和强制修正必须记录：

- 模块、操作类型、traceId；
- 记录 ID、业务键、来源批次和快照版本；
- 修改前后颗粒量、饮片量、单位、批号、日期和状态摘要；
- 影响数量、结果和失败原因；
- 操作人、IP 和时间。

### 18.2 安全

- 全部接口按医院数据范围过滤；
- 不能通过 ID、医院参数、批量请求或导出条件越权；
- SQL 使用参数绑定，排序字段白名单；
- 上传文件检查扩展名、MIME、大小、内容和病毒风险；
- 禁止执行 Excel 宏、公式和外部链接；
- 下载地址使用短时效签名；
- 错误响应不暴露 SQL、堆栈和服务器路径。

### 18.3 一致性与事务

- 新增、修改、作废和导入提交使用事务；
- 同一业务键加唯一约束或分布式锁；
- 单位配置变化不能静默改变历史快照；
- 有效库存变更与下游待重算事件可靠提交；
- 重试不重复写入；
- 快照保存来源批次、单位版本和规则版本。

## 19. 非功能需求

### 19.1 性能

- 普通列表首屏目标不超过 2 秒，P95 不超过 4 秒；
- 支持至少 100 万条医院库存快照分页查询；
- 统计汇总在服务端执行；
- 50,000 行以内导入预览目标不超过 30 秒；
- 大批量导入、校验和导出转异步任务；
- 医院、物料、批次选项接口常用查询 1 秒内返回。

### 19.2 可用性

- 网络失败保留列表和表单内容；
- 异步任务离开页面后继续运行；
- 任务失败显示原因并支持安全重试；
- 页面可查看数据截止时间、快照版本和单位版本；
- 服务节点统一时区和定点数计算。

### 19.3 兼容性

- 支持 Chrome、Edge 最近两个大版本；
- 推荐分辨率 1366×768 及以上；
- 支持横向滚动、固定关键列和键盘操作；
- 状态不能只通过颜色表达。

## 20. 测试与验收标准

### 20.1 功能验收

1. 页面入口、查询条件、工具栏和表格字段与截图信息架构一致。
2. 可按医院、物料编码、批号和到期日期任意组合查询。
3. 新增、修改、删除/作废、导入、导出和校验符合权限及状态规则。
4. 物料/批号选择可正确带出药品名称、单位、到期日期和换算配置。
5. 颗粒量、饮片量和标准量显示及统计口径明确。
6. 列表、统计汇总和导出结果一致。
7. 统计汇总针对查询全部记录，不是当前页。
8. 过期、近效期、冻结、零库存和单位异常有明确提示。
9. 重复快照、重复导入和并发提交不会造成库存虚增。
10. 有效医院库存变更能触发盘点和要货指导待重算。

### 20.2 统计验收样例

给定截图示例颗粒量：

```text
1、832.22、1232.75、211.8、370、288.2、135.71、2194.75、335.81、430.15
```

预期：

```text
颗粒量合计 = 6032.4
```

给定截图示例饮片量：

```text
3、749、4931、1059、518、1441、380、8779、1041、2882
```

预期：

```text
饮片量合计 = 21783
```

以上用于验证统计行和小数格式；正式验收还需覆盖不同单位和换算系数。

### 20.3 单位和数量验收

| 场景 | 预期 |
|---|---|
| 颗粒量 832.22 克 | 保存为 `832.220`，标准量按系数计算 |
| 饮片量 749 件 | 按饮片单位精度保存 |
| 数量为负 | 阻断 |
| 超单位精度 | 阻断，不静默截断 |
| 换算配置缺失 | `HI_UNIT_CONFIG_MISSING` |
| 颗粒与饮片换算差异超阈值 | 警告或阻断，按配置执行 |
| 过期库存 | 可追溯，可用标准量为 0 |

### 20.4 快照和重复验收

| 场景 | 预期 |
|---|---|
| 同业务键重复 | 阻断，不自动累加 |
| 同日重新盘点 | 更新草稿或生成新版本 |
| 已确认版本修改 | 按权限生成修正版，保留旧版本 |
| 不同日期查询 | 分别保留，跨日期合计显示提示 |
| 同文件重复提交 | 返回首次结果，不重复写入 |
| 乐观锁冲突 | 返回 409 |

### 20.5 下游对账验收

给定：

```text
期初库存 = 1000
有效供货 = 500
有效终端销量 = 200
医院实际标准库存 = 1250
```

预期：

```text
理论库存 = 1000 + 500 - 200 = 1300
库存差异 = 1250 - 1300 = -50
```

差异超过配置阈值时显示对账告警，但不自动改写医院实际库存。

### 20.6 导入、权限和安全验收

- 正确模板可预览、校验和提交；
- 非法日期、物料、批号、单位、数量和重复行逐行报错；
- 预览后配置变化，提交时重新校验；
- 无权限医院无法通过列表、详情、导出或导入获取数据；
- 前端隐藏按钮后直接调用接口仍返回 403；
- 已被下游引用记录不能物理删除；
- 新增、修改、作废、校验、导入和导出都有审计日志。

### 20.7 性能验收

- 百万级数据分页查询命中索引；
- 全量统计不把全部记录加载到应用内存；
- 50,000 行任务显示进度；
- 并发提交同一业务键只生成一条有效快照；
- 异步失败可重试且不产生重复库存。

## 21. 开发拆分建议

### 21.1 后端与数据

1. 确认医院、物料、颗粒/饮片单位和批次主数据映射。
2. 新建医院库存快照、单位映射、导入批次和问题表。
3. 实现查询、分页、排序、全量统计和医院权限过滤。
4. 实现新增、修改、作废、版本和业务键防重。
5. 实现颗粒/饮片数量、换算、日期、批号、效期和对账校验。
6. 实现导入模板、预览、提交、错误文件和幂等。
7. 实现批量校验及异步任务。
8. 实现导出、短时效下载和审计。
9. 实现与整袋、零散、盘点和要货指导的来源/待重算关系。
10. 编写单元、接口、权限、并发和性能测试。

### 21.2 前端

1. 新建独立医院库存页面和路由。
2. 实现医院、物料、批号和效期查询。
3. 实现表格、固定列、操作列、分页和双数量统计行。
4. 实现新增/编辑表单及物料、批号、单位联动。
5. 实现颗粒量、饮片量和标准量预览。
6. 实现批量/行级删除、校验和异常详情。
7. 实现导入预览、错误筛选、下载和提交进度。
8. 实现导出、权限、效期、冻结和对账状态展示。

### 21.3 测试

- 页面、字段、查询、分页、统计和滚动；
- CRUD、状态、版本和审计；
- 颗粒/饮片单位、换算、精度和负值；
- 批号、效期、重复和跨日期快照；
- 与整袋、零散、供货、销量的对账；
- 导入、导出、校验、幂等、并发和任务重试；
- 权限、注入、文件安全和性能；
- Chrome、Edge、1366×768 兼容性。

## 22. 待业务确认项

| 优先级 | 待确认项 | 影响 |
|---|---|---|
| P0 | 颗粒量、饮片量的真实物理单位和标准库存单位 | 影响字段、换算和统计 |
| P0 | 颗粒量和饮片量是同一库存的两种表达还是两类库存 | 影响是否允许相加、展示和下游 |
| P0 | 医院库存是实时库存、盘点快照还是人工台账 | 影响数据来源和业务键 |
| P0 | 批号是否必填，是否必须匹配医院供货批次 | 影响追溯和校验 |
| P0 | 同日同医院同物料同批号重复时覆盖、版本还是阻断 | 影响唯一键 |
| P0 | 数量是否允许 0、负数和小数，精度是多少 | 影响数据库和校验 |
| P0 | 到期日当天及过期库存是否计入可用库存 | 影响库存和要货指导 |
| P0 | 是否支持冻结、召回和质量异常状态 | 影响可用量 |
| P0 | 保存即生效还是校验通过后生效 | 影响下游数据 |
| P0 | 已被盘点/要货指导使用的库存如何修正 | 影响版本和重算 |
| P1 | 颗粒量与饮片量差异阈值和处理策略 | 影响对账 |
| P1 | 是否按科室、库位或容器拆分 | 影响业务键和表结构 |
| P1 | 是否从医院供货自动带出批号、效期和单位 | 影响联动接口 |
| P1 | 导入错误行是否保存为 `INVALID` | 影响导入流程 |
| P1 | 近效期预警天数 | 影响状态显示 |
| P1 | 导出上限、保留时间和水印 | 影响导出实现 |
| P2 | 是否需要实时同步、扫码和库存流水 | 影响后续版本 |

## 23. 发布前检查清单

- [ ] P0 业务口径全部确认并写入数据字典/规则配置；
- [ ] 医院、物料、颗粒/饮片单位和批次映射可用；
- [ ] 医院库存快照、单位映射、导入和问题表创建完成；
- [ ] 页面字段、按钮、分页、统计行与截图逐项核对；
- [ ] 颗粒量/饮片量单位、换算和精度测试通过；
- [ ] 批号、效期、重复、版本和对账校验通过；
- [ ] 整袋/零散库存联动及下游待重算验证通过；
- [ ] 导入预览、提交、错误文件和幂等测试通过；
- [ ] 列表、统计汇总和导出结果一致；
- [ ] 权限、审计、异步任务、下载安全和性能验证通过；
- [ ] 业务 UAT 完成并确认真实样例数据。

