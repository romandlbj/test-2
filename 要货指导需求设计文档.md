# 终端管理-要货指导需求设计文档

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | 终端管理-要货指导需求设计 |
| 所属系统 | 以岭药业终端管理系统 |
| 模块序号 | 终端管理第二个模块 |
| 页面名称 | 要货指导 |
| 推荐路由 | `/terminalMedicine/goodsGuidance` |
| 推荐接口前缀 | `/api/terminal/goods-guidance` |
| 版本 | V1.0 |
| 来源 | 现有系统“首页 / 终端管理 / 要货指导”页面截图、项目现有盘点库存文档及后端数据结构 |
| 文档状态 | 初稿，待业务确认 |
| 目标读者 | 产品、业务、UI、前端、后端、数据、测试、实施和运维人员 |
| 数字精度 | 数量默认 3 位小数，金额 2 位小数，单价最多 6 位小数；数据库使用 `DECIMAL`，禁止使用浮点型 |

### 1.1 版本记录

| 版本 | 日期 | 修改内容 | 修改人 |
|---|---|---|---|
| V1.0 | 2026-09-10 | 根据页面截图和现有库存盘点实现，形成可开发的要货指导模块需求初稿 | Codex |

## 2. 页面识别与需求边界

### 2.1 截图中可以直接确认的内容

截图展示的是“终端管理 → 要货指导”列表页，页面具备以下元素：

1. 顶部面包屑为“首页 / 终端管理 / 要货指导”。
2. 查询条件包括“医院名称”“物料编码”“药品名称”“汇总日期”。
3. 查询区提供“搜索”和“重置”按钮。
4. 操作区提供“删除”“导出”“汇总”按钮；未选择数据时删除按钮为禁用状态。
5. 数据表使用多级表头，截图中可以确认以下分组：
   - 基础数据：汇总日期、医院名称、物料编码、药品名称。
   - 期初库存：颗粒量。
   - 医院供货：颗粒量。
   - 终端销量：颗粒量。
   - 理论库存：颗粒量。
   - 医院库存：饮片量、颗粒量。
   - 亏涨结果：医院亏涨（袋）、颗粒单价、医院亏涨金额。
   - 要货指导：要货指导范围、月均消耗（颗粒量），右侧还存在横向滚动未完全展示的指导字段。
6. 表格支持横向滚动、表头排序图标、行勾选和底部“统计汇总”行。
7. 负数在截图中以红色显示，数量、库存等正数使用蓝色或黑色显示。

### 2.2 需要通过业务规则确认的内容

截图不能直接确认数据来源、计算公式和状态流转。本文先给出默认实现方案，并在“待业务确认项”中列出必须在开发前确认的差异：

- “医院亏涨（袋）”列的实际计量单位，是整袋、颗粒、结算单位，还是页面历史名称；
- 期初库存、医院供货、终端销量和医院库存的来源系统及同步时点；
- 要货指导范围的默认起止日期和月均消耗的有效月份口径；
- 本月需求量、30 天需进货量是否考虑安全库存、配送周期和最小起订量；
- 无消耗记录、库存为负、物料换算配置缺失时的展示和计算策略；
- 删除和汇总对已确认、已结算数据的处理方式。

### 2.3 模块定义

“要货指导”是基于库存、供货、销量、医院库存和历史消耗生成的补货建议查询模块。它输出的是可解释的建议数量，不直接生成采购订单、配送单或财务结算单。

## 3. 背景与目标

### 3.1 业务背景

医院端需要根据当前库存和历史消耗判断是否需要向药厂要货。现有业务数据分散在期初库存、医院供货、终端销量、医院库存和库存盘点结果中，人工计算存在以下问题：

- 不同人员使用的统计周期不一致；
- 颗粒、饮片、袋等单位换算容易出错；
- 理论库存与医院实际库存的差异无法及时发现；
- 亏涨金额、月均消耗和建议进货量缺乏统一口径；
- 查询、导出和重新汇总后结果不可追溯。

### 3.2 建设目标

1. 以医院、物料和汇总日期为最小业务粒度，统一展示库存链路和要货建议。
2. 将所有计算放在服务端完成，前端只负责展示和交互。
3. 统一颗粒、饮片、袋及金额精度，保留原始值和计算规则版本。
4. 输出月均消耗、可用时长、本月需求量和 30 天需进货量等可解释指标。
5. 支持查询、排序、汇总、导出、批量删除/作废和审计追踪。
6. 当数据缺失或异常时明确提示，不生成无依据的补货建议。

### 3.3 成功标准

- 相同输入数据和计算规则版本，在重复汇总、查询和导出时结果一致；
- 业务人员可以从一行数据追溯到指导范围、消耗量、库存基准和换算配置；
- 无权限用户无法通过接口参数、导出或猜测 ID 获取其他医院数据；
- 关键计算公式、精度、边界情况均有自动化测试覆盖。

## 4. 范围

### 4.1 本期范围（MVP）

- 要货指导列表页和多级表头；
- 医院、物料编码、药品名称、汇总日期查询；
- 服务端分页、排序、统计汇总和横向滚动；
- 期初库存、医院供货、终端销量、理论库存、医院库存、亏涨结果的展示；
- 指导范围、月均消耗、本月需求量、可用时长、30 天需进货量的计算和展示；
- 汇总/重新计算、批量删除或作废、导出；
- 权限、数据范围、审计日志、异常标识和计算版本；
- 与当前项目已有 `stocktake_detail`、`stocktake_summary`、`material_unit_config` 数据的兼容。

### 4.2 本期不包含

- 自动生成采购订单、配送单或合同；
- 要货申请审批流；
- 移动端扫码盘点；
- 药品主数据、医院主数据和价格主数据的完整维护；
- 机器学习预测、季节性模型和自动安全库存优化；
- 与财务结算系统的最终对账闭环。

### 4.3 后续可扩展能力

- 指导结果转要货申请；
- 业务人员手工调整建议量并记录原因；
- 按科室、病区、剂型或供应商钻取；
- 预测模型和安全库存策略配置；
- 指导结果确认、审批和转配送。

## 5. 角色与权限

### 5.1 角色权限矩阵

| 角色 | 查询 | 查看详情 | 汇总 | 导出 | 删除/作废 | 数据范围 |
|---|---:|---:|---:|---:|---:|---|
| 系统管理员 | 是 | 是 | 是 | 是 | 是 | 全部医院 |
| 区域/业务管理员 | 是 | 是 | 是 | 是 | 按授权 | 授权区域医院 |
| 医院业务人员 | 是 | 是 | 可配置 | 可配置 | 否 | 所属医院 |
| 财务/结算人员 | 是 | 是 | 否 | 是 | 否 | 授权医院 |
| 只读用户 | 是 | 是 | 否 | 可配置 | 否 | 授权范围 |

### 5.2 权限编码

- `terminal:goods-guidance:view`：查看列表和详情；
- `terminal:goods-guidance:aggregate`：执行汇总/重新计算；
- `terminal:goods-guidance:export`：导出当前查询结果；
- `terminal:goods-guidance:delete`：删除草稿或作废已生成快照；
- `terminal:goods-guidance:adjust`：后续版本手工调整建议量；
- `terminal:goods-guidance:config`：后续版本维护指导规则。

所有接口必须在服务端校验用户权限和医院数据范围，前端按钮隐藏不能代替服务端校验。

## 6. 页面入口与布局

### 6.1 页面入口

- 左侧导航：终端管理 → 要货指导；
- 页面路径：`/terminalMedicine/goodsGuidance`；
- 面包屑：首页 / 终端管理 / 要货指导；
- 接口前缀：`/api/terminal/goods-guidance`。

### 6.2 页面区域

1. **面包屑和页签区**：显示当前模块名称和可关闭页签。
2. **查询区**：医院名称、物料编码、药品名称、汇总日期、搜索、重置。
3. **操作区**：删除、导出、汇总；右侧保留表格搜索/刷新入口。
4. **数据区**：多级表头、固定基础列、横向滚动、服务端排序、行勾选。
5. **汇总区**：当前查询结果的统计汇总行；不是当前页小计。
6. **分页区**：总条数、当前页、每页条数、页码和跳转。

### 6.3 页面结构示意

```text
首页 / 终端管理 / 要货指导

[医院名称 ▼] [物料编码] [药品名称] [开始日期 - 结束日期] [搜索] [重置]

[删除] [导出] [汇总]                                      [表格搜索] [刷新]

┌选择┬────────基础数据────────┬期初库存┬医院供货┬终端销量┬理论库存┬────医院库存────┬────亏涨结果────┬────────要货指导────────┐
│    │日期│医院│物料│药品      │颗粒量  │颗粒量  │颗粒量  │颗粒量  │饮片量│颗粒量    │亏涨│单价│金额  │指导范围│月均消耗│...│
├────┼──────────────────────┼────────┼────────┼────────┼────────┼────────┼──────────┼───────┼─────────────────────────┤
│ □  │ ...                  │ ...    │ ...    │ ...    │ ...    │ ...    │ ...      │ ...   │ ...                     │
├────┴──────────────────────┴────────┴────────┴────────┴────────┴────────┴──────────┴───────┴─────────────────────────┤
│                                    统计汇总                                                                            │
└──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

### 6.4 表格固定列建议

- 左侧固定：选择列、汇总日期、医院名称、物料编码、药品名称；
- 右侧可滚动：库存、亏涨结果和要货指导列；
- 底部固定：统计汇总行；
- 在 1366×768 分辨率下仍需可通过横向滚动访问全部列。

## 7. 查询条件

### 7.1 查询字段

| 字段 | API 参数 | 控件 | 必填 | 校验和行为 |
|---|---|---|---:|---|
| 医院名称 | `hospitalId` | 可搜索下拉 | 否 | 只返回当前用户有权限的医院；显示医院名称，提交 ID |
| 物料编码 | `materialCode` | 文本框 | 否 | 去除首尾空格；默认模糊匹配；最长 64 个字符 |
| 药品名称 | `drugName` | 文本框 | 否 | 模糊匹配；最长 120 个字符；按系统统一字符集处理 |
| 汇总日期开始 | `startDate` | 日期选择器 | 否 | `YYYY-MM-DD`；不能晚于结束日期 |
| 汇总日期结束 | `endDate` | 日期选择器 | 否 | `YYYY-MM-DD`；建议最大查询跨度 366 天 |
| 页码 | `page` | 分页器 | 否 | 最小值 1，默认 1 |
| 每页条数 | `pageSize` | 分页器 | 否 | 默认 20，允许 20/50/100/200，最大 200 |
| 排序字段 | `sortField` | 表头触发 | 否 | 只允许后端白名单字段 |
| 排序方向 | `sortOrder` | 表头触发 | 否 | `asc` 或 `desc`，默认按汇总日期倒序 |

### 7.2 查询规则

1. 多个筛选条件之间为 AND 关系。
2. 全部条件为空时，按用户数据权限返回所有有效记录。
3. 默认排序：汇总日期倒序、医院名称升序、药品名称升序、ID 倒序。
4. 点击搜索后回到第 1 页；查询按钮进入 loading，避免重复提交。
5. 物料编码输入框按回车可触发查询。
6. 点击重置后清空筛选条件，恢复默认排序和第 1 页。
7. 无结果时显示空状态，并保留已输入的筛选条件。
8. 查询条件、排序状态和当前页可写入 URL 查询参数，便于刷新和分享；分享链接仍需经过服务端权限校验。

### 7.3 医院下拉

- 支持输入关键字远程搜索；
- 最少输入 1 个字符后再请求，防止一次性返回过多数据；
- 每次最多返回 100 条；
- 选项格式：`医院名称（医院 ID）`，页面可只显示名称；
- 接口必须按当前用户数据权限过滤医院。

## 8. 表格字段设计

字段名称以截图显示为准，API 使用稳定的英文驼峰字段，数据库使用下划线命名。页面的“袋”字不代表后端可以忽略单位，必须由物料换算和指导规则配置确定。

### 8.1 基础数据

| 分组 | 页面列 | API 字段 | 类型 | 展示规则 | 数据来源 |
|---|---|---|---|---|---|
| 基础数据 | 汇总日期 | `summaryDate` | Date | `YYYY-MM-DD` | 盘点/汇总业务日期 |
| 基础数据 | 医院名称 | `hospitalName` | String | 最长 120 字符，超长省略并悬浮展示 | 医院主数据 |
| 基础数据 | 物料编码 | `materialCode` | String | 原样展示，不丢前导零 | 物料主数据 |
| 基础数据 | 药品名称 | `drugName` | String | 原样展示，超长悬浮展示 | 药品主数据 |

### 8.2 库存与经营数据

| 分组 | 页面列 | API 字段 | 类型 | 默认精度 | 口径 |
|---|---|---|---|---:|---|
| 期初库存 | 颗粒量 | `openingStockParticles` | Decimal | 3 | 汇总周期开始时的可用颗粒库存 |
| 医院供货 | 颗粒量 | `hospitalSupplyParticles` | Decimal | 3 | 指导范围内医院收到的颗粒量 |
| 终端销量 | 颗粒量 | `terminalSalesParticles` | Decimal | 3 | 指导范围内已确认的终端销售/消耗量 |
| 理论库存 | 颗粒量 | `theoreticalStockParticles` | Decimal | 3 | 按库存流水计算出的理论余额 |
| 医院库存 | 饮片量 | `hospitalTabletQty` | Decimal | 3 | 医院侧上传或盘点的饮片/件数 |
| 医院库存 | 颗粒量 | `hospitalParticleQty` | Decimal | 3 | 医院库存折算后的颗粒量 |

默认库存公式：

```text
理论库存颗粒量 = 期初库存颗粒量 + 医院供货颗粒量 - 终端销量颗粒量
```

若业务系统已经提供经过确认的理论库存，则以来源系统快照为准，同时保存公式计算值用于校验。两者差异超过配置阈值时，行上显示“库存链路异常”。

### 8.3 亏涨结果

| 页面列 | API 字段 | 类型 | 展示规则 | 说明 |
|---|---|---|---|---|
| 医院亏涨（袋） | `hospitalGainLossQty` | Decimal | 负数红色，正数蓝色，0 使用中性色 | 有正负号的业务结算数量 |
| 颗粒单价 | `particleUnitPrice` | Decimal | 最多 6 位小数 | 与亏涨数量匹配的结算单价 |
| 医院亏涨金额 | `hospitalGainLossAmount` | Decimal | 2 位小数，负数红色 | 亏涨数量乘单价，具体单位见规则配置 |

为兼容现有库表中的 `hospitalLossBags`、`hospitalLossAmount` 字段，后端可以保留旧字段，但新接口必须使用 `hospitalGainLossQty` 和 `hospitalGainLossAmount`，并在响应的 `unitMeta` 中返回单位定义。不得仅根据字段名猜测“袋”的换算关系。

### 8.4 要货指导

| 分组 | 页面列 | API 字段 | 类型 | 默认精度 | 说明 |
|---|---|---|---|---:|---|
| 要货指导 | 要货指导范围 | `guidanceRange` | Object | - | 包含 `startDate`、`endDate` |
| 要货指导 | 月均消耗（颗粒量） | `monthlyAvgConsumption` | Decimal | 3 | 指导范围内的月均有效消耗 |
| 要货指导 | 本月需求量（袋） | `currentMonthDemandBags` | Decimal | 3 | 当前自然月预计需求折算袋数 |
| 要货指导 | 现场剩余可用时长（月） | `availableMonths` | Decimal/null | 2 或 3 | 当前可用库存可支撑的月数；无消耗时为空 |
| 要货指导 | 30 天需进货量（袋） | `thirtyDayPurchaseBags` | Decimal | 3 或 0 | 未来 30 天建议补货量，默认按整袋向上取整 |
| 计算状态 | `calculationStatus` | Enum | - | `READY`、`WARNING`、`FAILED` |
| 异常提示 | `warnings` | Array | - | 返回异常编码、文案和影响字段 |

### 8.5 统计汇总行

统计汇总必须针对当前查询条件的全部结果计算，不得只汇总当前页。建议合计字段如下：

- 期初库存颗粒量、医院供货颗粒量、终端销量颗粒量、理论库存颗粒量；
- 医院饮片量、医院颗粒量；
- 医院亏涨数量、医院亏涨金额；
- 本月需求量、30 天需进货量。

月均消耗和可用时长不能简单相加。默认规则为：

- 月均消耗：按全部有效消耗量和有效指导天数重新计算；
- 可用时长：`汇总可用库存 / 汇总月均消耗`；
- 缺少有效消耗的物料不参与加权平均，并在汇总元数据中返回缺失数量。

## 9. 数据来源与业务粒度

### 9.1 最小业务粒度

一条要货指导记录的业务键建议为：

```text
医院 ID + 物料 ID + 汇总日期 + 指导规则版本
```

同一业务键重复汇总必须幂等更新，不得生成重复快照。

### 9.2 数据来源

| 数据 | 推荐来源 | 读取时点 | 缺失处理 |
|---|---|---|---|
| 医院信息 | 医院主数据 | 查询/汇总时 | 医院不存在或停用时禁止新汇总 |
| 物料编码、药品名称 | 物料主数据 | 查询/汇总时 | 编码不存在时返回配置异常 |
| 每袋颗粒数 | `material_unit_config` | 按汇总日期匹配有效版本 | 缺失或小于等于 0 时汇总失败 |
| 颗粒单价 | 价格/物料单位配置 | 按汇总日期匹配有效版本 | 缺失时金额为空并告警，是否允许汇总待确认 |
| 期初库存 | 库存快照或盘点汇总 | 指导范围开始日 | 缺失时不能静默按 0 处理 |
| 医院供货 | 供货/配送确认数据 | 指导范围内 | 未确认数据不计入有效供货 |
| 终端销量 | 终端销量/消耗数据 | 指导范围内 | 只统计已确认、未作废数据 |
| 医院库存 | 医院库存或盘点数据 | 汇总日期 | 保留饮片量和颗粒量原始值 |
| 历史消耗 | 销量数据聚合 | 指导范围内 | 记录有效天数和缺失天数 |

### 9.3 数据新鲜度

汇总结果必须返回 `sourceAsOf` 或各来源的最后同步时间。来源数据超过配置的新鲜度阈值时，显示“数据可能不是最新”，但是否允许汇总由业务配置决定。

## 10. 业务状态与操作流程

### 10.1 状态定义

| 状态 | 含义 | 可查询 | 可重新汇总 | 可删除 |
|---|---|---:|---:|---:|
| `DRAFT` | 已生成但未确认的草稿 | 是 | 是 | 是 |
| `CONFIRMED` | 计算结果已确认 | 是 | 按权限 | 只能作废 |
| `VOID` | 已作废，不参与默认查询 | 否 | 否 | 否 |
| `FAILED` | 汇总失败，保留失败原因 | 可选 | 是 | 是 |

### 10.2 首次查询

1. 用户进入页面，系统加载有权限的医院选项和默认列表。
2. 默认显示未作废记录，按汇总日期倒序。
3. 每行显示计算状态和异常标识。

### 10.3 汇总/重新计算

1. 用户可以勾选指定行汇总，也可以按当前查询条件汇总全部结果。
2. 服务端锁定业务键，读取同一时点的数据快照和规则版本。
3. 服务端执行单位校验、库存链路计算、亏涨计算和要货指导计算。
4. 计算成功后以事务方式写入或更新快照；失败行不覆盖上一次已确认结果。
5. 返回成功条数、告警条数、失败条数及逐行原因。
6. 页面刷新列表和汇总行，并保留用户筛选、排序和页码。

### 10.4 删除/作废

- 未选择行时按钮禁用；
- 删除前二次确认，显示待处理数量和是否包含已确认记录；
- `DRAFT`/`FAILED` 可软删除；`CONFIRMED` 只能在有权限时作废；
- 已经进入要货申请、配送或财务流程的数据禁止删除/作废；
- 所有操作写入审计日志，记录 ID、业务键、原因和结果。

## 11. 核心计算规则

### 11.1 单位配置

每个物料必须有生效配置：

```text
packParticleQty > 0       每袋颗粒数
particleUnitPrice >= 0    颗粒/结算单位价格
effectiveFrom <= summaryDate
effectiveTo 为空或 summaryDate <= effectiveTo
```

同一物料、同一汇总日期只能命中一个有效版本。若命中多个版本，汇总失败并提示配置冲突。

所有中间计算使用 `BigDecimal`。中间步骤保留至少 6 位小数，最终按字段精度使用 `HALF_UP` 舍入；需要整袋采购时使用 `CEILING` 向上取整。

### 11.2 库存链路

默认规则：

```text
理论库存颗粒量 = 期初库存颗粒量
             + 医院供货颗粒量
             - 终端销量颗粒量
```

如果源系统有退货、报损、调拨等业务，必须在扩展字段中体现，不允许直接覆盖销量字段：

```text
理论库存颗粒量 = 期初库存
             + 医院供货
             + 入库调整
             - 终端销量
             - 退货
             - 报损
             + 其他调整
```

### 11.3 医院库存换算

如果医院库存同时提供饮片量和颗粒量：

- 页面分别展示原始饮片量和颗粒量；
- 颗粒量优先使用医院系统已确认值；
- 若只有饮片量，则根据物料换算配置计算；
- 若同时存在两者且差异超过阈值，保留来源值并标记“单位换算异常”。

换算关系不得在前端硬编码，建议配置：

```text
医院库存颗粒量 = 饮片量 × 每饮片颗粒换算数 + 零散颗粒量
```

### 11.4 亏涨结果

建议保留一个不展示或可钻取的 `bookStockParticles`（账面库存颗粒量），默认来源为库存账簿或确认后的理论库存。

```text
库存差异颗粒量 = 实际盘点库存颗粒量 - 账面库存颗粒量
```

展示列“医院亏涨（袋）”的默认实现为：

```text
医院亏涨数量 = 库存差异颗粒量 × 亏涨展示单位换算系数
医院亏涨金额 = 医院亏涨数量 × 颗粒单价
```

其中“亏涨展示单位换算系数”由规则配置决定。若现行业务要求直接展示颗粒差异，则系数为 1；若确实要求换算为整袋，则系数为 `1 / packParticleQty`。由于截图中的列名和现有样例数据存在单位歧义，V1 开发前必须确认该系数及页面标签。

金额允许为负数：

- 正数表示医院盘盈/应收回或药厂需承担的业务方向，具体业务含义由结算规则定义；
- 负数表示医院亏损/应补足或药厂应承担的另一方向；
- 前端颜色只表达正负状态，不能替代文字和数值。

### 11.5 指导范围

指导范围由以下优先级决定：

1. 用户或批量任务明确传入的指导起止日期；
2. 物料/区域规则配置的指导周期；
3. 系统默认滚动周期。

建议默认值为“汇总日期向前滚动 90 天至汇总日期”，但截图示例仅代表历史页面数据，不得直接将示例日期写死。起止日期均包含在统计范围内。

```text
guidanceDays = endDate - startDate + 1
有效月份数 = guidanceDays / 月基准天数
月基准天数默认 30，可配置
```

### 11.6 月均消耗

默认以已确认的终端销量/消耗颗粒量计算：

```text
有效消耗颗粒量 = 指导范围内所有有效终端销量颗粒量之和
月均消耗颗粒量 = 有效消耗颗粒量 / 有效月份数
```

缺失天数是否按 0 消耗处理必须配置。默认仅对有有效数据的日期计入有效天数，避免数据同步中断导致月均消耗被人为压低；接口必须返回 `validConsumptionDays`、`missingConsumptionDays`。

### 11.7 本月需求量

默认按当前自然月的完整月需求折算：

```text
本月需求颗粒量 = 月均消耗颗粒量 × 当月自然日数 / 月基准天数
本月需求量（袋） = 本月需求颗粒量 / packParticleQty
```

页面保留小数，不在此处向上取整；是否按剩余天数预测由业务配置 `currentMonthForecastMode` 决定，可选 `FULL_MONTH`、`REMAINING_DAYS`。

### 11.8 现场剩余可用时长

```text
可用月数 = max(可用于指导的库存颗粒量, 0)
         / 月均消耗颗粒量
```

当月均消耗小于最小消耗阈值或没有有效消耗数据时，`availableMonths` 返回 `null`，页面显示“—”，并标记“无有效消耗数据”，不能显示为无穷大。

### 11.9 30 天需进货量

默认不考虑安全库存和配送周期时：

```text
30 天需求颗粒量 = 月均消耗颗粒量 × 30 / 月基准天数
建议补货颗粒量 = max(30 天需求颗粒量 - 可用指导库存颗粒量, 0)
30 天需进货量（袋） = ceil(建议补货颗粒量 / packParticleQty)
```

若启用安全库存和配送周期：

```text
目标库存颗粒量 = 月均消耗 × (配送周期天数 + 目标覆盖天数) / 30
               + 安全库存颗粒量
建议补货颗粒量 = max(目标库存颗粒量 - 可用指导库存颗粒量, 0)
```

### 11.10 库存基准选择

要货建议使用的“可用指导库存”必须明确，默认配置为：

1. 优先使用已确认医院实际颗粒库存；
2. 医院实际库存缺失时，按权限决定是否回退到理论库存；
3. 回退时必须返回 `stockBasis = THEORETICAL` 和告警，不得静默替换。

## 12. 异常与边界规则

| 场景 | 默认处理 | 页面表现 | 是否允许汇总 |
|---|---|---|---:|
| 每袋颗粒数缺失/为 0 | 行失败 | 红色告警，显示“缺少单位换算” | 否 |
| 颗粒单价缺失 | 金额为空或 0 由配置决定 | 黄色告警 | 默认否，待确认 |
| 指导范围开始日期晚于结束日期 | 请求校验失败 | 表单提示 | 否 |
| 指导范围无销量 | 月均消耗为空 | 显示“无有效消耗数据”，补货量为 0 或空由配置决定 | 是，需告警 |
| 实际库存为负 | 保留原值 | 红色告警“库存为负” | 默认否 |
| 理论库存与源值差异超阈值 | 使用源值，保存差异 | 黄色告警“库存链路异常” | 是，需权限 |
| 物料或医院已停用 | 历史记录可查 | 禁止生成新快照 | 否 |
| 来源数据未同步 | 不覆盖上次确认结果 | 显示来源时间和“数据未更新” | 默认否 |
| 同一业务键并发汇总 | 幂等锁/唯一约束 | 返回处理中或冲突 | 不重复写入 |
| 数量超过数据库精度 | 拒绝写入 | 显示字段和最大精度 | 否 |

## 13. 数据模型与数据库设计

### 13.1 与当前项目的兼容方案

当前项目已经存在：

- `stocktake_summary`：医院、物料、汇总日期主表；
- `stocktake_detail`：库存、亏涨和部分要货指导字段；
- `material_unit_config`：每袋颗粒数、颗粒单价和生效版本；
- `operation_audit_log`：操作审计日志。

MVP 可以继续复用这些表，但必须补齐截图中显示的字段，不能让前端从多个接口自行拼接。建议给 `stocktake_detail` 增加或确认以下列：

```sql
opening_stock_particles      DECIMAL(18,3) NOT NULL DEFAULT 0,
hospital_supply_particles    DECIMAL(18,3) NOT NULL DEFAULT 0,
terminal_sales_particles     DECIMAL(18,3) NOT NULL DEFAULT 0,
theoretical_stock_particles  DECIMAL(18,3) NOT NULL DEFAULT 0,
hospital_tablet_qty          DECIMAL(18,3) NOT NULL DEFAULT 0,
book_stock_particles         DECIMAL(18,3) NOT NULL DEFAULT 0,
hospital_gain_loss_qty       DECIMAL(18,3) NOT NULL DEFAULT 0,
stock_basis                  VARCHAR(20) NOT NULL DEFAULT 'ACTUAL_HOSPITAL',
calculation_status           VARCHAR(20) NOT NULL DEFAULT 'READY',
warning_codes                VARCHAR(1000) DEFAULT NULL,
source_as_of                 DATETIME DEFAULT NULL
```

现有的 `actual_stock_particles`、`hospital_loss_bags` 等字段可以作为兼容字段保留，但接口层应统一使用本需求的规范字段。迁移期间需要提供字段映射和回填脚本。

### 13.2 独立快照表（长期方案）

如果要货指导后续独立于盘点库存发布，建议新建 `goods_guidance_snapshot`：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `hospital_id` | BIGINT | 医院 ID |
| `material_id` | BIGINT | 物料 ID |
| `summary_date` | DATE | 汇总日期 |
| `guidance_start_date` / `guidance_end_date` | DATE | 指导范围 |
| `opening_stock_particles` | DECIMAL(18,3) | 期初库存 |
| `hospital_supply_particles` | DECIMAL(18,3) | 医院供货 |
| `terminal_sales_particles` | DECIMAL(18,3) | 终端销量 |
| `theoretical_stock_particles` | DECIMAL(18,3) | 理论库存 |
| `hospital_tablet_qty` | DECIMAL(18,3) | 医院饮片量 |
| `hospital_particle_qty` | DECIMAL(18,3) | 医院颗粒量 |
| `book_stock_particles` | DECIMAL(18,3) | 账面库存 |
| `hospital_gain_loss_qty` | DECIMAL(18,3) | 亏涨数量 |
| `particle_unit_price` | DECIMAL(18,6) | 单价 |
| `hospital_gain_loss_amount` | DECIMAL(18,2) | 亏涨金额 |
| `monthly_avg_consumption` | DECIMAL(18,3) | 月均消耗 |
| `current_month_demand_bags` | DECIMAL(18,3) | 本月需求袋数 |
| `available_months` | DECIMAL(18,3) | 可用月数 |
| `thirty_day_purchase_bags` | DECIMAL(18,3) | 30 天建议袋数 |
| `stock_basis` | VARCHAR(20) | `ACTUAL_HOSPITAL`/`THEORETICAL` |
| `calculation_version` | VARCHAR(32) | 规则版本 |
| `calculation_status` | VARCHAR(20) | 计算状态 |
| `warning_codes` | VARCHAR(1000) | 异常编码集合 |
| `status` | VARCHAR(20) | `DRAFT`/`CONFIRMED`/`VOID`/`FAILED` |
| `source_batch_id` | VARCHAR(64) | 来源批次 |
| `created_by`/`created_at` | BIGINT/DATETIME | 创建信息 |
| `updated_by`/`updated_at` | BIGINT/DATETIME | 更新信息 |
| `deleted_at` | DATETIME | 软删除时间 |

建议唯一索引：

```text
hospital_id + material_id + summary_date + calculation_version + deleted_at
```

### 13.3 规则配置表

建议使用 `goods_guidance_rule_config` 保存：

- `guidance_days`：默认指导天数；
- `month_base_days`：月基准天数，默认 30；
- `target_coverage_days`：目标覆盖天数；
- `lead_time_days`：配送周期；
- `safety_stock_particles` 或安全库存系数；
- `current_month_forecast_mode`；
- `no_consumption_policy`；
- `stock_basis_policy`；
- `purchase_round_mode`；
- `calculation_version`、生效时间、适用医院/区域/物料。

## 14. 接口设计

### 14.1 查询列表

```http
GET /api/terminal/goods-guidance
```

请求示例：

```text
/api/terminal/goods-guidance?hospitalId=1&materialCode=02.22&drugName=茯苓&startDate=2025-08-20&endDate=2025-12-26&page=1&pageSize=20&sortField=monthlyAvgConsumption&sortOrder=desc
```

响应示例：

```json
{
  "items": [
    {
      "id": 1,
      "summaryDate": "2025-12-26",
      "hospital": {
        "id": 1,
        "name": "河北北方学院附属第一医院"
      },
      "material": {
        "id": 1001,
        "code": "02.220001",
        "drugName": "茯苓皮"
      },
      "inventory": {
        "openingStockParticles": 468.57,
        "hospitalSupplyParticles": 0,
        "terminalSalesParticles": 21.46,
        "theoreticalStockParticles": 447.11,
        "hospitalTabletQty": 0,
        "hospitalParticleQty": 14088,
        "bookStockParticles": 604.57,
        "stockBasis": "ACTUAL_HOSPITAL"
      },
      "variance": {
        "hospitalGainLossQty": -136,
        "displayUnit": "CONFIGURED_SETTLEMENT_UNIT",
        "particleUnitPrice": 2.56,
        "hospitalGainLossAmount": -348.16
      },
      "guidance": {
        "range": {
          "startDate": "2025-08-20",
          "endDate": "2025-12-26"
        },
        "monthlyAvgConsumption": 1.29,
        "currentMonthDemandBags": 0.02,
        "availableMonths": 242.16,
        "thirtyDayPurchaseBags": 0
      },
      "calculationStatus": "READY",
      "calculationVersion": "v1",
      "warnings": []
    }
  ],
  "summary": {
    "openingStockParticles": 4545.0,
    "hospitalSupplyParticles": 300.0,
    "terminalSalesParticles": 519.7,
    "theoreticalStockParticles": 4325.4,
    "hospitalParticleQty": 29401.0,
    "hospitalGainLossQty": -247.0,
    "hospitalGainLossAmount": -764.8,
    "monthlyAvgConsumption": 37.6,
    "thirtyDayPurchaseBags": 0
  },
  "page": 1,
  "pageSize": 20,
  "total": 1,
  "queryMeta": {
    "calculationVersion": "v1",
    "unitMeta": {
      "quantityUnit": "颗粒",
      "purchaseUnit": "袋",
      "monthBaseDays": 30
    }
  }
}
```

注意：示例中的数值仅用于说明返回结构，不作为业务验收基准；真实结果必须由确认后的来源数据和规则版本计算。

### 14.2 医院下拉

```http
GET /api/terminal/goods-guidance/hospitals?keyword=河北
```

响应：

```json
[
  { "id": 1, "hospitalName": "河北北方学院附属第一医院" }
]
```

### 14.3 汇总/重新计算

```http
POST /api/terminal/goods-guidance/aggregate
Idempotency-Key: 5b8e8b0f-...
Content-Type: application/json
```

请求体支持两种模式：

```json
{
  "mode": "IDS",
  "ids": [1, 2, 3],
  "forceRecalculate": false,
  "ruleVersion": "v1"
}
```

或：

```json
{
  "mode": "QUERY",
  "query": {
    "hospitalId": 1,
    "startDate": "2025-08-20",
    "endDate": "2025-12-26"
  },
  "forceRecalculate": false,
  "ruleVersion": "v1"
}
```

响应：

```json
{
  "taskId": null,
  "successCount": 18,
  "warningCount": 2,
  "errorCount": 1,
  "errors": [
    {
      "id": 9,
      "code": "GG_UNIT_CONFIG_MISSING",
      "message": "物料 02.220009 缺少有效的每袋颗粒数"
    }
  ]
}
```

超过配置条数时转为异步任务，前端通过任务接口查询进度。

### 14.4 删除/作废

```http
DELETE /api/terminal/goods-guidance
Content-Type: application/json
```

请求体：

```json
{
  "ids": [1, 2],
  "reason": "来源数据修正，重新汇总"
}
```

响应：

```json
{
  "affectedCount": 2,
  "voidedCount": 0,
  "blockedCount": 0,
  "blockedReasons": []
}
```

### 14.5 导出

```http
POST /api/terminal/goods-guidance/export
```

请求体包含当前查询条件、排序和导出字段。导出规则：

- 默认导出当前筛选条件下的全部数据，不仅是当前页；
- 小于等于 50,000 行可同步生成；
- 超过 50,000 行创建异步任务并返回 `taskId`；
- 默认格式为 `.xlsx`，必要时兼容带 UTF-8 BOM 的 `.csv`；
- 包含多级表头、统计汇总行、查询条件、导出时间、规则版本和单位说明；
- 文件下载地址使用短时效签名，默认 30 分钟失效。

任务查询：

```http
GET /api/exports/tasks/{taskId}
```

### 14.6 单行详情（建议）

```http
GET /api/terminal/goods-guidance/{id}
```

详情至少包含：来源批次、库存流水摘要、指导范围、消耗统计、换算配置版本、计算公式版本、异常明细和操作记录。若本期不开发详情页，接口可以后置，但数据结构应预留。

### 14.7 状态码与错误码

| HTTP 状态 | 错误码示例 | 含义 |
|---:|---|---|
| 400 | `GG_INVALID_QUERY` | 查询参数格式或日期范围错误 |
| 401 | `UNAUTHORIZED` | 未登录 |
| 403 | `FORBIDDEN` | 无模块或医院数据权限 |
| 404 | `GG_NOT_FOUND` | 指导记录不存在或已作废 |
| 409 | `GG_CONCURRENT_CALCULATION` | 同一业务键正在汇总 |
| 422 | `GG_SOURCE_NOT_READY` | 来源数据不满足汇总条件 |
| 500 | `INTERNAL_ERROR` | 服务端异常 |

业务错误码至少包括：`GG_UNIT_CONFIG_MISSING`、`GG_UNIT_CONFIG_CONFLICT`、`GG_NO_CONSUMPTION`、`GG_NEGATIVE_STOCK`、`GG_DATA_STALE`、`GG_DUPLICATE_SNAPSHOT`、`GG_ALREADY_IN_FLOW`。

## 15. 前端交互与展示规范

### 15.1 查询和加载

- 首次进入显示列表 loading 或骨架屏；
- 搜索和刷新期间禁用重复操作；
- 网络失败时保留旧数据并显示重试入口；
- 接口返回 `calculationStatus=WARNING` 时，行首显示告警图标，悬浮显示完整原因。

### 15.2 数值格式

- `null` 或未计算值显示“—”；
- 数值 0 显示 `0`，不能显示为空；
- 数量默认最多 3 位小数，去除无意义尾零；
- 金额固定显示 2 位小数；
- 单价最多显示 6 位小数；
- 负数红色、正数按系统主题显示、0 使用中性颜色；
- 颜色只作为辅助信息，必须同时保留正负号。

### 15.3 表头和滚动

- 使用两级或三级表头实现截图中的业务分组；
- 表头分组名称不得由前端根据字段自动拼接；
- 列宽支持预设值，超长文本省略并在 tooltip 展示；
- 选择列和基础数据列固定，其他列横向滚动；
- 排序请求由服务端处理，前端不得只对当前页排序；
- 刷新保留当前查询、排序、页码和横向滚动位置。

### 15.4 汇总交互

点击“汇总”时弹窗展示：

- 操作范围：已选行/当前查询全部；
- 指导范围和规则版本；
- 预计处理条数；
- 是否覆盖已有草稿；
- 包含异常数据的数量和处理策略。

完成后展示成功、告警、失败三类数量，失败原因支持复制。页面自动刷新，但不强制跳回第 1 页。

### 15.5 删除交互

- 无选择时按钮置灰；
- 有选择时弹窗显示数量和状态分布；
- 已确认记录显示“将作废”而不是“删除”；
- 原因必填，长度 2～200 字符；
- 操作成功后刷新列表和统计汇总。

### 15.6 导出交互

- 导出前显示当前筛选条件和预计记录数；
- 异步导出显示任务进度，用户可以离开页面；
- 任务失败提供重试，不重复生成同一任务的多个文件；
- 导出文件中的列顺序与页面一致，并保留单位说明。

## 16. 审计、权限和安全

### 16.1 审计日志

以下动作必须记录到 `operation_audit_log` 或等价审计表：

- 查询导出；
- 汇总/重新计算；
- 删除/作废；
- 规则版本变更；
- 手工调整（后续版本）。

日志字段包括模块、操作类型、请求条件摘要、影响数量、任务 ID、结果、失败原因、操作人、IP、时间。请求摘要不得记录完整敏感信息。

### 16.2 数据权限

- 医院 ID 由服务端按用户数据范围重新过滤；
- 不能信任前端提交的医院列表；
- 不能通过修改 `id`、分页、导出条件或排序字段越权；
- 导出文件继承查询权限，下载链接使用短时效签名；
- 所有 SQL 使用参数绑定，排序字段使用白名单映射，不能直接拼接用户输入。

### 16.3 并发和一致性

- 同一医院、物料、汇总日期使用唯一键或分布式锁；
- 汇总过程在事务中完成，失败自动回滚；
- 已确认快照重新计算前保存旧版本或版本号；
- 计算结果记录 `calculationVersion` 和来源批次，保证可复现。

## 17. 非功能需求

### 17.1 性能

- 普通列表查询首屏目标不超过 2 秒，95 分位不超过 4 秒；
- 支持至少 100 万条历史明细的分页查询；
- 统计汇总使用服务端聚合，不加载全部明细到前端；
- 导出超过阈值使用异步任务，不阻塞在线请求；
- 医院下拉接口在常用关键字下 1 秒内返回。

### 17.2 可用性

- 关键操作支持重试且保证幂等；
- 来源系统暂时不可用时，保留上一次已确认快照并提示数据时间；
- 异步任务失败原因可查看，任务状态至少包括 `PENDING`、`RUNNING`、`SUCCESS`、`FAILED`、`CANCELLED`。

### 17.3 兼容性与可访问性

- 支持 Chrome、Edge 最近两个大版本；
- 推荐分辨率 1366×768 及以上；
- 表格不能因窗口变窄而隐藏关键列，必须提供横向滚动；
- 颜色不是唯一状态表达方式，异常需有图标和文字；
- 按钮、输入框和表格勾选支持键盘操作。

## 18. 测试与验收标准

### 18.1 功能验收

1. 可以按医院、物料编码、药品名称、汇总日期任意组合查询。
2. 查询结果的医院数据严格受权限范围限制。
3. 多级表头、固定列、横向滚动、服务端排序、分页和统计汇总正确。
4. 期初库存、供货、销量、理论库存和医院库存的来源及单位在详情/导出中可追溯。
5. 汇总操作能返回成功、告警、失败数量和逐行原因。
6. 相同业务键重复汇总不会插入重复记录。
7. 已确认或已进入后续流程的数据按状态规则作废或拒绝操作。
8. 导出结果包含全部筛选数据、多级表头、统计汇总、查询条件和单位说明。
9. 无消耗、缺少换算、库存为负、数据过期和库存链路差异均有明确提示。

### 18.2 计算验收样例一：库存链路

给定：

```text
期初库存颗粒量 = 500
医院供货颗粒量 = 100
终端销量颗粒量 = 50
```

预期：

```text
理论库存颗粒量 = 500 + 100 - 50 = 550
```

### 18.3 计算验收样例二：月均消耗和补货

给定：

```text
每袋颗粒数 = 100
指导范围 = 90 天
有效终端销量 = 270 颗粒
可用指导库存 = 230 颗粒
月基准天数 = 30
目标覆盖天数 = 30
安全库存 = 0
```

预期：

```text
月均消耗 = 270 / (90 / 30) = 90 颗粒/月
可用月数 = 230 / 90 = 2.555... 月
30 天需求 = 90 × 30 / 30 = 90 颗粒
建议补货颗粒量 = max(90 - 230, 0) = 0
30 天需进货量 = ceil(0 / 100) = 0 袋
```

### 18.4 计算验收样例三：需要补货

给定：

```text
每袋颗粒数 = 100
月均消耗 = 260 颗粒/月
可用指导库存 = 80 颗粒
30 天目标覆盖 = 260 颗粒
```

预期：

```text
建议补货颗粒量 = 260 - 80 = 180
30 天需进货量 = ceil(180 / 100) = 2 袋
```

### 18.5 计算验收样例四：无消耗

给定指导范围内没有有效终端销量：

- `monthlyAvgConsumption = null`；
- `availableMonths = null`；
- 页面显示“—”和“无有效消耗数据”；
- `thirtyDayPurchaseBags` 默认 0，或按确认后的 `noConsumptionPolicy` 执行；
- 不能自动生成大额补货建议。

### 18.6 权限和安全验收

- 无权限医院通过 URL、接口参数、ID 列表和导出均不可获取数据；
- 越权请求返回 403，不返回数据是否存在的详细信息；
- 排序字段不在白名单时请求失败；
- 缺少每袋颗粒数、重复有效单位版本、超精度数值均不能写入；
- 汇总网络中断后重试，结果不重复、不产生半成品快照。

## 19. 开发拆分建议

### 19.1 数据与后端

1. 确认现有 `stocktake_detail` 字段映射，完成数据库迁移或独立快照表。
2. 实现医院、物料、单位换算和价格版本读取。
3. 实现指导范围和消耗聚合服务。
4. 实现库存链路、亏涨、月均消耗、补货量计算服务。
5. 实现分页、服务端排序、统计汇总和权限过滤。
6. 实现汇总幂等、并发锁、删除/作废、审计日志。
7. 实现同步/异步导出和任务查询。
8. 为每条计算规则编写单元测试和固定输入快照测试。

### 19.2 前端

1. 实现查询表单、医院远程下拉和日期范围校验。
2. 实现多级表头、固定列、横向滚动、排序、分页和统计行。
3. 实现行选择、汇总弹窗、删除/作废确认和导出任务状态。
4. 实现数值、金额、日期、负数和告警状态格式化。
5. 实现权限指令和按钮禁用态；服务端权限仍为最终依据。
6. 保留查询条件和表格状态，支持异常重试和空状态。

### 19.3 测试

- 查询组合、分页、排序、汇总行；
- 库存链路和所有计算公式；
- 单位缺失、价格缺失、无消耗、负库存、数据过期、重复版本；
- 权限越权、导出越权、SQL 注入和参数边界；
- 并发汇总、重试、回滚和任务幂等；
- 大数据量查询和异步导出性能；
- Chrome/Edge、1366×768 分辨率和横向滚动兼容性。

## 20. 待业务确认项

以下项目在开发前必须形成书面结论，确认结果应写入规则配置或数据字典：

| 优先级 | 待确认项 | 影响 |
|---|---|---|
| P0 | “医院亏涨（袋）”的真实单位和换算系数 | 影响亏涨数量、金额和页面列名 |
| P0 | 医院库存、账面库存、期初库存的来源及字段映射 | 影响库存链路和亏涨结果 |
| P0 | 指导范围默认天数及是否允许按行/按批次调整 | 影响月均消耗和补货量 |
| P0 | 本月需求按整月还是剩余天数预测 | 影响本月需求量 |
| P0 | 无消耗数据时补货量是 0、空值还是禁止汇总 | 影响异常处理和业务风险 |
| P0 | 30 天需进货量是否按整袋向上取整 | 影响最终建议量 |
| P0 | 已确认/已结算/已进入要货流程数据能否重算或作废 | 影响状态和审计 |
| P1 | 是否启用配送周期、安全库存、最小起订量 | 影响补货算法 |
| P1 | 医院饮片量与颗粒量的换算配置 | 影响医院库存展示 |
| P1 | 来源数据同步延迟阈值 | 影响是否允许汇总 |
| P1 | 导出格式、最大行数、文件留存时间和水印 | 影响导出实现 |
| P2 | 是否需要详情抽屉、打印和手工调整 | 影响前端工作量 |

## 21. 与现有盘点库存模块的关系

要货指导与盘点库存共用医院、物料、汇总日期、单位配置、权限和审计能力，但目标不同：

| 对比项 | 盘点库存 | 要货指导 |
|---|---|---|
| 核心目标 | 记录实际盘点和库存差异 | 根据库存和消耗给出补货建议 |
| 核心输入 | 袋数、整袋颗粒、零散颗粒、医院库存 | 期初库存、供货、销量、医院库存、历史消耗 |
| 核心输出 | 实际库存、亏涨数量、亏涨金额 | 理论库存、月均消耗、可用时长、30 天需进货量 |
| 主要操作 | 汇总、删除、导出 | 汇总/重算、删除/作废、导出 |
| 依赖关系 | 可作为实际库存和账面库存来源 | 可读取盘点确认快照，但必须记录来源批次 |

若两个模块共享 `stocktake_detail`，必须通过 `module`、`viewType` 或明确接口模型区分字段语义，不能让前端根据页面名称猜测同一字段在不同模块中的含义。

## 22. 发布前检查清单

- [ ] P0 待确认项全部有结论并录入配置/数据字典；
- [ ] 数据库字段、索引和历史数据回填完成；
- [ ] 接口权限和医院数据范围测试通过；
- [ ] 计算规则单元测试、边界测试和回归测试通过；
- [ ] 页面多级表头与截图字段逐项核对；
- [ ] 统计汇总针对全量查询结果验证通过；
- [ ] 导出文件与页面字段、精度、单位一致；
- [ ] 汇总、删除/作废和导出审计日志可查询；
- [ ] 异步任务、失败重试、并发锁和回滚验证通过；
- [ ] 业务人员完成 UAT，并确认示例数据结果。

