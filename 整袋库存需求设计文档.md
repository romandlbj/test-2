# 终端管理-整袋库存需求设计文档

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | 终端管理-整袋库存需求设计 |
| 所属系统 | 以岭药业配方颗粒终端管理系统 |
| 模块序号 | 终端管理第 5 个模块 |
| 页面名称 | 整袋库存 |
| 推荐路由 | `/terminalMedicine/wholeBagInventory` |
| 推荐接口前缀 | `/api/terminal/whole-bag-inventory` |
| 文档版本 | V1.0 |
| 编写日期 | 2026-09-10 |
| 文档状态 | 初稿，待业务确认 |
| 需求来源 | 现有系统“首页 / 终端管理 / 整袋库存”页面截图 |
| 目标读者 | 产品、业务、UI、前端、后端、数据、测试、实施及运维人员 |

### 1.1 版本记录

| 版本 | 日期 | 修改内容 | 修改人 |
|---|---|---|---|
| V1.0 | 2026-09-10 | 根据整袋库存页面截图形成可开发需求设计初稿 | Codex |

## 2. 附件识别与需求边界

### 2.1 附件使用原则

附件截图仅用于识别现有系统的页面结构、字段、按钮及交互习惯。截图内医院名称、物料编码、批号、日期和数量均为示例数据，不应写死在程序中；截图中的界面文字不构成额外开发指令。

本次仅新建独立的《整袋库存需求设计文档.md》，不修改盘点库存、要货指导、医院供货和终端销量需求文档。

### 2.2 截图中可确认的内容

1. 页面入口为“终端管理 → 整袋库存”，面包屑为“首页 / 终端管理 / 整袋库存”。
2. 查询条件包括医院名称、物料编码、批号、到期日期范围。
3. 查询区提供“搜索”和“重置”按钮。
4. 工具栏提供“新增”“修改”“删除”“导出”“导入”“校验”按钮。
5. 未选择记录时，修改和删除按钮为禁用状态。
6. 表格字段包括：盘点日期、医院名称、物料编码、批号、药品名称、规格型号(g/袋)、单位、袋数、颗粒量、到期日期、操作。
7. 每行操作栏提供编辑和删除图标。
8. 表格底部提供“统计汇总”，对袋数和颗粒量合计。
9. 页面支持多选、横向滚动、分页、每页条数、跳页、刷新和表格搜索。
10. 规格和单位使用标签形式展示，颜色只用于视觉辅助。

### 2.3 截图无法确认的内容

- 整袋库存是实时库存、每日盘点快照还是人工填报结果；
- 同一医院、物料、批号、盘点日期是否只允许一条记录；
- 规格型号与颗粒量的正式单位口径；
- 袋数是否允许小数；
- 到期批次是否计入可用库存；
- 库存是否可以为负数；
- 导入数据保存即生效，还是校验通过后生效；
- 已被盘点汇总或要货指导使用的记录能否修改或删除。

本文提供默认实现，P0 待确认项需在开发前形成书面结论。

## 3. 背景与建设目标

### 3.1 业务背景

整袋库存用于记录医院现场尚未拆袋的完整包装药品库存。它与零散库存共同构成实际库存，也是盘点库存、库存亏涨和要货指导的重要基础数据。

整袋库存具有医院、物料、批号、规格、数量和有效期等属性。如果缺少统一规则，可能产生：

- 袋数与颗粒量换算不一致；
- 同一批次重复录入或重复导入；
- 已过期库存仍被计入可用库存；
- 整袋库存与医院供货、终端销量不匹配；
- 历史快照被修改后无法追溯；
- 盘点和要货指导使用错误库存量。

### 3.2 建设目标

1. 建立医院、盘点日期、物料和批号维度的整袋库存台账。
2. 统一规格、单位、袋数和颗粒量的计算口径。
3. 支持新增、修改、删除/作废、导入、校验和导出。
4. 对重复记录、批号、效期、主数据和数量关系进行校验。
5. 只有通过校验的记录才能进入盘点库存和要货指导。
6. 保存来源、版本和审计信息，使历史库存快照可复现。

### 3.3 成功标准

- 同一查询条件下列表、统计汇总和导出结果一致；
- 每条记录的颗粒量与袋数、规格换算一致；
- 重复导入不会生成重复库存；
- 过期和近效期库存具有明确状态；
- 无权限用户不能查看或变更其他医院库存；
- 有效库存变更能够触发下游待重算。

## 4. 需求范围

### 4.1 本期范围

- 整袋库存列表和分页；
- 医院、物料编码、批号、到期日期查询；
- 新增、修改、批量删除/作废；
- Excel/CSV 导入预览、校验、提交和错误明细下载；
- 当前筛选结果导出；
- 批量校验和状态管理；
- 袋数、颗粒量全量统计；
- 批号和到期状态校验；
- 服务端权限、排序、幂等、审计和并发控制；
- 为盘点库存、医院库存和要货指导提供有效整袋库存数据。

### 4.2 本期不包含

- 仓库库位、货架、托盘和序列号管理；
- 采购、配送、签收、退货和财务完整流程；
- 移动端扫码盘点；
- 自动扣减、批次分配和仓储作业；
- 温湿度、冷链和质量检验管理；
- 库存预测和自动采购。

### 4.3 后续扩展

- 扫码新增和批次识别；
- 库位和终端设备维度库存；
- 近效期预警和调拨建议；
- 整袋转零散拆袋业务；
- 盘点差异确认及审批；
- 批次库存流水和来源追踪。

## 5. 角色与权限

### 5.1 权限矩阵

| 角色 | 查询 | 新增 | 修改 | 删除/作废 | 导入 | 校验 | 导出 | 数据范围 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| 系统管理员 | 是 | 是 | 是 | 是 | 是 | 是 | 是 | 全部医院 |
| 区域/业务管理员 | 是 | 是 | 是 | 按授权 | 是 | 是 | 是 | 授权区域医院 |
| 医院盘点人员 | 是 | 是 | 是 | 本人草稿 | 是 | 是 | 可配置 | 所属医院 |
| 库存管理人员 | 是 | 是 | 是 | 按授权 | 是 | 是 | 是 | 授权医院 |
| 审计/财务人员 | 是 | 否 | 否 | 否 | 否 | 否 | 是 | 授权医院 |
| 只读用户 | 是 | 否 | 否 | 否 | 否 | 否 | 可配置 | 授权范围 |

### 5.2 权限编码

- `terminal:whole-bag-inventory:view`
- `terminal:whole-bag-inventory:create`
- `terminal:whole-bag-inventory:update`
- `terminal:whole-bag-inventory:delete`
- `terminal:whole-bag-inventory:import`
- `terminal:whole-bag-inventory:validate`
- `terminal:whole-bag-inventory:export`
- `terminal:whole-bag-inventory:override`

所有接口必须在服务端执行功能权限和医院数据范围校验。

## 6. 页面设计

### 6.1 页面入口

- 左侧导航：终端管理 → 整袋库存；
- 页面路由：`/terminalMedicine/wholeBagInventory`；
- 面包屑：首页 / 终端管理 / 整袋库存；
- 接口前缀：`/api/terminal/whole-bag-inventory`。

### 6.2 页面区域

1. 面包屑和页签区；
2. 查询区：医院名称、物料编码、批号、到期日期范围；
3. 工具栏：新增、修改、删除、导出、导入、校验；
4. 表格区：多选、字段展示、行级编辑与删除；
5. 统计区：袋数和颗粒量合计；
6. 分页区：总条数、每页条数、页码和跳页；
7. 表格右上角：快速搜索和刷新。

### 6.3 页面结构示意

```text
首页 / 终端管理 / 整袋库存

[医院名称 ▼] [物料编码] [批号] [到期开始 - 到期结束] [搜索] [重置]

[新增] [修改] [删除] [导出] [导入] [校验]                  [搜索] [刷新]

┌选择┬盘点日期*┬医院名称*┬物料编码*┬批号┬药品名称*┬规格(g/袋)*┬单位┬袋数*┬颗粒量*┬到期日期*┬操作┐
│ □  │          │          │          │    │          │           │袋  │      │        │          │编辑 删除│
├────┼──────────┼──────────┼──────────┼────┼──────────┼───────────┼────┼──────┼────────┼──────────┼─────────┤
│ □  │ ...      │ ...      │ ...      │... │ ...      │ ...       │袋  │ ...  │ ...    │ ...      │ ✎  🗑   │
├────┴──────────┴──────────┴──────────┴────┴──────────┴───────────┴────┴──────┴────────┴──────────┴─────────┤
│ 统计汇总                                                     袋数：...  颗粒量：...                        │
└───────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

### 6.4 表格固定与滚动

- 左侧固定选择列、盘点日期、医院名称、物料编码；
- 右侧固定操作列；
- 中间字段横向滚动；
- 表头和统计行垂直滚动时保持可见；
- 最小推荐分辨率为 1366×768。

## 7. 查询需求

### 7.1 查询字段

| 页面字段 | API 参数 | 控件 | 必填 | 规则 |
|---|---|---|---:|---|
| 医院名称 | `hospitalId` | 可搜索下拉 | 否 | 仅显示授权医院，提交医院 ID |
| 物料编码 | `materialCode` | 文本框 | 否 | 去首尾空格，默认模糊匹配，最长 64 字符 |
| 批号 | `batchNo` | 文本框 | 否 | 模糊匹配，最长 64 字符 |
| 到期日期开始 | `expiryStartDate` | 日期选择器 | 否 | 闭区间起点 |
| 到期日期结束 | `expiryEndDate` | 日期选择器 | 否 | 不早于开始日期，闭区间终点 |
| 页码 | `page` | 分页器 | 否 | 默认 1 |
| 每页条数 | `pageSize` | 分页器 | 否 | 默认 10，可选 10/20/50/100/200 |
| 排序字段 | `sortField` | 表头 | 否 | 服务端白名单 |
| 排序方向 | `sortOrder` | 表头 | 否 | `asc`/`desc` |

### 7.2 查询行为

1. 多条件使用 AND 关系。
2. 空条件查询当前用户有权限的全部未作废记录。
3. 默认排序：盘点日期倒序、到期日期升序、医院名称升序、ID 倒序。
4. 点击搜索回到第 1 页。
5. 物料编码和批号支持按回车搜索。
6. 重置清空筛选条件并恢复默认排序。
7. 查询期间显示 loading 并防止重复请求。
8. 无结果显示空状态并保留条件。
9. 到期日期范围包含起止当天。
10. 列表、总数和统计汇总均按相同筛选及权限条件执行。

### 7.3 建议扩展条件

接口预留盘点日期、药品名称、校验状态、到期状态、来源类型和是否包含作废记录，可放入高级筛选中。

## 8. 表格字段设计

| 页面列 | API 字段 | 数据库字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---:|---|
| 选择 | `selected` | - | Boolean | - | 当前页行选择 |
| 盘点日期* | `stocktakeDate` | `stocktake_date` | Date | 是 | 本次库存快照业务日期 |
| 医院名称* | `hospitalName` | `hospital_name` | String/快照 | 是 | 最长 120 字符 |
| 医院 ID | `hospitalId` | `hospital_id` | Long | 是 | 权限及关联字段 |
| 物料编码* | `materialCode` | `material_code` | String | 是 | 原样保留前导零，最长 64 字符 |
| 物料 ID | `materialId` | `material_id` | Long | 是 | 关联物料主数据 |
| 批号 | `batchNo` | `batch_no` | String | 条件必填 | 最长 64 字符；业务是否必填待确认 |
| 药品名称* | `drugName` | `drug_name` | String/快照 | 是 | 由物料主数据带出 |
| 规格型号(g/袋)* | `specQtyPerBag` | `spec_qty_per_bag` | Decimal(18,3) | 是 | 大于 0，按盘点日匹配有效规格 |
| 单位 | `unitCode`/`unitName` | `unit_code`/`unit_name` | String | 是 | 页面示例为“袋” |
| 袋数* | `bagCount` | `bag_count` | Decimal(18,3) | 是 | 整袋数量，默认非负整数 |
| 颗粒量* | `particleQty` | `particle_qty` | Decimal(18,3) | 是 | 服务端计算，默认只读 |
| 到期日期* | `expiryDate` | `expiry_date` | Date | 是 | 对应当前批次效期 |
| 校验状态 | `validationStatus` | `validation_status` | Enum | 是 | `DRAFT`/`VALIDATED`/`INVALID`/`VOID` |
| 到期状态 | `expiryStatus` | - | Enum | - | 查询时动态计算 |
| 版本 | `version` | `version` | Integer | 是 | 乐观锁 |
| 操作 | `actions` | - | - | - | 行级编辑和删除/作废 |

### 8.1 字段快照

医院名称、物料编码、药品名称、规格和单位应保存在库存记录中作为业务快照。主数据后续变更不能改变历史盘点记录展示。

### 8.2 统计汇总

统计当前查询结果的全部有效记录，而非当前页：

```text
袋数合计 = Σ bagCount
颗粒量合计 = Σ particleQty
```

不同标准计量单位的颗粒量不能直接合计，必须换算到统一标准库存单位；接口返回单位说明和未能换算的记录数量。

## 9. 数据口径与业务粒度

### 9.1 模块定义

整袋库存表示在 `stocktakeDate` 当日医院现场尚未拆袋、包装完整的库存快照，不代表历史累计供货量。它是时点数据，不是流水数据。

### 9.2 最小业务粒度

默认业务键：

```text
盘点日期 + 医院 ID + 物料 ID + 批号 + 到期日期 + 规格版本
```

同一业务键只能存在一条未作废快照。重复导入默认阻断，不自动累加袋数，避免把重复文件当作新增库存。

### 9.3 快照与流水区别

- 整袋库存记录某个时点的实际数量；
- 医院供货和终端销量是业务流水；
- 同一天重复盘点应更新/修正同一快照或生成新版本，不能简单相加；
- 跨日期的快照不能相加作为总库存；
- 查询统计行只对同一筛选结果显示合计，用户跨盘点日期查询时应提示“包含多个盘点日期，合计不代表单一时点库存”。

### 9.4 可用库存与账面库存

- 现场库存：盘点人员实际记录的整袋数量；
- 可用库存：排除已过期、冻结、质量异常后的可使用数量；
- 账面库存：由期初库存、供货、销量和调整计算的理论数量；
- 本模块默认保存现场库存，是否同时保存冻结量和可用量列为后续扩展。

## 10. 核心业务规则

### 10.1 主数据规则

1. 医院必须存在、启用且在用户授权范围内。
2. 物料必须存在且在盘点日期有效。
3. 选择物料后自动带出药品名称、规格和单位。
4. 同一物料存在多个规格时，必须明确选择或按批号匹配规格。
5. 历史记录保存盘点时的规格和单位版本。
6. 批号存在时，应能与医院供货记录或批次主数据匹配。

### 10.2 数量计算

默认公式：

```text
颗粒量 = 袋数 × 规格型号(g/袋)
```

规则：

- 使用 `BigDecimal`，禁止浮点型；
- 中间值至少保留 6 位小数；
- 最终按 `DECIMAL(18,3)`、`HALF_UP` 保存；
- 前端实时预览，服务端保存时重新计算；
- 导入文件提供颗粒量时，仍由服务端重新计算；
- 导入值与计算值差异超过 `0.001` 默认阻断；
- 如允许自动修正，必须展示原值和修正值并写审计日志。

### 10.3 袋数规则

- 整袋库存默认 `bagCount >= 0`；
- 默认只允许整数袋，`allowFractionalBag=false`；
- 如果袋已拆开，应计入零散库存，不能以小数袋同时计入整袋库存；
- 0 袋记录默认允许作为盘点结果，但标记 `ZERO_STOCK`，是否保留由业务配置；
- 不允许负数、NaN、Infinity、科学计数法和超精度值。

### 10.4 批号规则

- 长度 1～64 字符，去除首尾空格；
- 默认比较时大小写不敏感，原始值按录入形式保存；
- 批号应与医院、物料和到期日期匹配；
- 同一批号出现不同物料时阻断；
- 同一医院、物料、批号出现多个到期日期时标记冲突；
- 如果批号不是必填，空批号记录必须标记警告，并且不能用于严格批次扣减。

### 10.5 日期规则

- 业务日期统一按 `Asia/Shanghai` 解析；
- 盘点日期默认不能晚于当前日期；
- 历史补录最大回溯天数默认 366 天，可配置；
- 到期日期原则上不得早于该批次供货日期；
- 已过期批次允许作为实际现场库存被记录，但不计入可用库存并显示红色警告；
- 到期日期为盘点日期当天时，按业务配置视为当日有效或过期，必须统一。

### 10.6 到期状态

```text
EXPIRED       = expiryDate < stocktakeDate
EXPIRING_SOON = stocktakeDate <= expiryDate <= stocktakeDate + warningDays
NORMAL        = expiryDate > stocktakeDate + warningDays
```

默认 `warningDays=90`。列表显示状态图标或颜色，导出必须输出文字状态。

### 10.7 重复记录

同一业务键重复保存时：

- 人工新增提示已存在记录，并提供查看/编辑入口；
- 导入预览标记 `DUPLICATE_RECORD`；
- 默认不累加数量；
- 若业务选择覆盖，必须基于版本和权限更新，并记录前后值；
- 同一文件重复上传通过文件哈希和行哈希拦截。

### 10.8 校验生效

只有满足以下条件的记录进入盘点汇总和要货指导：

```text
validationStatus = VALIDATED
AND deletedAt IS NULL
AND 医院、物料、规格、单位有效
AND bagCount >= 0
AND particleQty = bagCount × specQtyPerBag
```

已过期库存可以是 `VALIDATED` 的现场库存，但其 `availableParticleQty=0`；实际库存和可用库存必须分开使用。

### 10.9 并发与幂等

- 新增使用 `Idempotency-Key`；
- 导入使用文件 SHA-256、批次号和行哈希；
- 同一业务键使用唯一约束或分布式锁；
- 修改使用 `version` 乐观锁；
- 重复请求返回首次结果，不重复写入。

## 11. 状态模型

| 状态 | 含义 | 默认显示 | 可修改 | 可删除/作废 | 进入下游 |
|---|---|---:|---:|---:|---:|
| `DRAFT` | 新增或导入后尚未校验 | 是 | 是 | 是 | 否 |
| `VALIDATED` | 已通过阻断性校验 | 是 | 按权限 | 已引用时只能作废 | 是 |
| `INVALID` | 校验失败 | 是 | 是 | 是 | 否 |
| `VOID` | 已作废 | 默认否 | 否 | 否 | 否 |

校验问题分为 `ERROR`、`WARNING` 和 `INFO`。存在 `ERROR` 时状态为 `INVALID`；只有警告时可以按规则进入 `VALIDATED`。

## 12. 操作功能设计

### 12.1 新增

点击“新增”打开抽屉或弹窗。

| 字段 | 控件 | 默认值 | 可编辑 | 规则 |
|---|---|---|---:|---|
| 盘点日期 | 日期选择器 | 当前日期 | 是 | 受未来日期和回溯限制 |
| 医院名称 | 远程搜索下拉 | 空 | 是 | 仅授权医院 |
| 物料编码 | 远程搜索 | 空 | 是 | 带出物料、规格和单位 |
| 药品名称 | 只读 | 自动带出 | 否 | 保存快照 |
| 批号 | 下拉/文本 | 空 | 是 | 优先从医院供货有效批次选择 |
| 规格型号 | 只读/选择 | 自动带出 | 条件可编辑 | 多规格时明确选择 |
| 单位 | 只读标签 | 袋 | 否 | 单位字典值 |
| 袋数 | 数值框 | 0/空 | 是 | 非负整数 |
| 颗粒量 | 只读 | 自动计算 | 否 | 服务端重算 |
| 到期日期 | 日期选择器 | 批次带出 | 条件可编辑 | 与批号一致 |

保存成功默认进入 `DRAFT`；若启用即时校验，可直接进入 `VALIDATED`。保存失败必须保留用户输入。

### 12.2 修改

- 选择 1 条时工具栏修改按钮可用；选择 0 条或多条时禁用；
- 行级编辑图标打开同一编辑表单；
- `DRAFT`、`INVALID` 可编辑；
- `VALIDATED` 只有覆盖权限可编辑，并必须填写原因；
- 任一业务字段修改后重新校验；
- 已被下游使用的记录采用新版本/修正事件，不直接覆盖历史快照；
- 提交携带 `version`，冲突返回 409。

### 12.3 删除/作废

- 工具栏支持批量处理，行级删除处理单条；
- 删除前显示记录数量、状态和下游影响；
- `DRAFT`、`INVALID` 软删除；
- `VALIDATED` 只能作废；
- 已被确认盘点或要货指导引用时，禁止物理删除并标记下游待重算；
- 原因必填，长度 2～200 字；
- 返回删除数、作废数、阻断数和逐条原因。

### 12.4 校验

1. 有选择时校验选中记录；
2. 无选择时确认后校验当前查询结果；
3. 超过 5,000 条转异步任务；
4. 校验医院权限、物料、规格、批号、日期、数量、重复和颗粒量公式；
5. 通过后变为 `VALIDATED`，失败变为 `INVALID`；
6. 校验不擅自修改有业务含义的字段；
7. 校验完成后刷新列表和统计行。

### 12.5 导入

#### 文件要求

- 默认支持 `.xlsx`，可选 UTF-8 BOM `.csv`；
- 最大文件 20 MB；
- 单文件最多 50,000 行；
- 禁止宏、公式执行和外部链接；
- 校验扩展名、MIME、内容、大小和恶意文件风险。

#### 模板字段

```text
盘点日期,医院名称,医院ID(可选),物料编码,批号,药品名称(可选),
规格型号(g/袋)(可选),单位(可选),袋数,颗粒量(可选),到期日期
```

医院名称、药品名称、规格、单位和颗粒量为辅助校验字段，服务端以主数据和公式为准。

#### 导入流程

```text
下载模板 → 上传 → 解析预览 → 逐行校验 → 查看错误/警告
→ 下载错误明细 → 确认提交 → 再次校验 → 写入 → 返回结果
```

预览阶段不写正式表。完全重复记录不自动累加。提交时再次校验，默认只写入无阻断错误的行。

### 12.6 导出

- 导出当前查询条件下的全部记录；
- 默认 `.xlsx`，可兼容 `.csv`；
- 包含页面字段、校验状态、到期状态、来源、版本和异常原因；
- 文件说明包含查询条件、导出时间、数据截止时间和单位口径；
- 超过 50,000 行转异步任务；
- 下载地址默认 30 分钟失效；
- 记录导出审计。

## 13. 校验规则与错误码

| 场景 | 默认处理 | 错误码 |
|---|---|---|
| 医院无权限 | 阻断/403 | `WBI_HOSPITAL_FORBIDDEN` |
| 物料不存在 | 阻断 | `WBI_MATERIAL_NOT_FOUND` |
| 规格或单位配置缺失 | 阻断 | `WBI_SPEC_CONFIG_MISSING` |
| 批号格式错误 | 阻断 | `WBI_BATCH_INVALID` |
| 批号与物料不一致 | 阻断 | `WBI_BATCH_MISMATCH` |
| 袋数为负 | 阻断 | `WBI_BAG_COUNT_INVALID` |
| 袋数为小数且不允许 | 阻断 | `WBI_FRACTIONAL_BAG_NOT_ALLOWED` |
| 颗粒量与公式不一致 | 阻断 | `WBI_PARTICLE_QTY_MISMATCH` |
| 到期日期缺失 | 阻断 | `WBI_EXPIRY_REQUIRED` |
| 已过期 | 警告，不计可用库存 | `WBI_EXPIRED` |
| 近效期 | 警告 | `WBI_EXPIRING_SOON` |
| 完全重复 | 阻断 | `WBI_DUPLICATE_RECORD` |
| 乐观锁冲突 | 409 | `WBI_VERSION_CONFLICT` |
| 已被下游引用 | 禁止直接删除 | `WBI_ALREADY_REFERENCED` |
| 导入文件重复 | 返回已有任务 | `WBI_IMPORT_DUPLICATE` |

## 14. 数据库设计

### 14.1 主表 `whole_bag_inventory_snapshot`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `stocktake_date` | DATE | 盘点日期 |
| `hospital_id` | BIGINT | 医院 ID |
| `hospital_name` | VARCHAR(120) | 医院名称快照 |
| `material_id` | BIGINT | 物料 ID |
| `material_code` | VARCHAR(64) | 物料编码快照 |
| `batch_no` | VARCHAR(64) | 批号 |
| `drug_name` | VARCHAR(120) | 药品名称快照 |
| `spec_qty_per_bag` | DECIMAL(18,3) | 每袋规格 |
| `standard_unit_code` | VARCHAR(20) | 规格对应标准单位 |
| `inventory_unit_code` | VARCHAR(20) | 库存单位，通常为 BAG |
| `inventory_unit_name` | VARCHAR(20) | 单位名称快照，通常为袋 |
| `bag_count` | DECIMAL(18,3) | 袋数 |
| `particle_qty` | DECIMAL(18,3) | 颗粒量 |
| `available_bag_count` | DECIMAL(18,3) | 可用袋数，可后续启用 |
| `available_particle_qty` | DECIMAL(18,3) | 排除过期/冻结后的可用量 |
| `expiry_date` | DATE | 到期日期 |
| `validation_status` | VARCHAR(20) | 校验状态 |
| `validation_version` | VARCHAR(32) | 校验规则版本 |
| `warning_codes` | VARCHAR(1000) | 告警编码 |
| `source_type` | VARCHAR(20) | `MANUAL`/`IMPORT`/`API` |
| `source_batch_id` | VARCHAR(64) | 导入/同步批次 |
| `source_row_no` | INT | 来源行号 |
| `dedupe_hash` | CHAR(64) | 防重哈希 |
| `version` | INT | 乐观锁版本 |
| `created_by`/`created_at` | BIGINT/DATETIME | 创建信息 |
| `updated_by`/`updated_at` | BIGINT/DATETIME | 修改信息 |
| `deleted_at` | DATETIME | 软删除时间 |

建议索引：

```text
uk_wbi_business (stocktake_date, hospital_id, material_id, batch_no, expiry_date, deleted_at)
idx_wbi_query (hospital_id, stocktake_date, material_code, batch_no, expiry_date, deleted_at)
idx_wbi_expiry (expiry_date, validation_status, deleted_at)
idx_wbi_source (source_batch_id, source_row_no)
```

注意 MySQL 可空字段唯一索引行为。若批号允许为空，应使用规范化批号、有效标识列或防重表，不能直接依赖 `NULL` 唯一性。

### 14.2 导入表

- `whole_bag_inventory_import_batch`：文件信息、哈希、状态、行数和操作人；
- `whole_bag_inventory_import_issue`：批次、行号、字段、原值、问题级别、错误码和修复建议；
- 可选 `whole_bag_inventory_validation_issue` 保存每次校验问题历史。

### 14.3 与现有模块的数据关系

- 复用医院主数据、物料/规格配置和操作审计日志；
- 整袋库存使用独立快照表，不直接写入供货流水或终端销量表；
- 盘点库存汇总时读取同一盘点日最近一次有效整袋库存快照；
- 与零散库存相加形成实际库存；
- 要货指导按规则读取可用整袋库存，而非包含过期库存的现场总量。

## 15. 接口设计

### 15.1 列表查询

```http
GET /api/terminal/whole-bag-inventory
```

参数：`hospitalId`、`materialCode`、`batchNo`、`expiryStartDate`、`expiryEndDate`、`stocktakeStartDate`、`stocktakeEndDate`、`status`、`page`、`pageSize`、`sortField`、`sortOrder`。

响应示例：

```json
{
  "items": [
    {
      "id": 1,
      "stocktakeDate": "2025-12-23",
      "hospital": { "id": 1, "name": "衡水市人民医院" },
      "material": {
        "id": 1880,
        "code": "02.22.003.1880",
        "drugName": "白茅根"
      },
      "batchNo": "B2511001",
      "specQtyPerBag": 100,
      "unit": { "code": "BAG", "name": "袋" },
      "bagCount": 17,
      "particleQty": 1700,
      "availableParticleQty": 1700,
      "expiryDate": "2028-12-01",
      "expiryStatus": "NORMAL",
      "validationStatus": "VALIDATED",
      "version": 1,
      "warnings": []
    }
  ],
  "summary": {
    "recordCount": 1,
    "bagCount": 17,
    "particleQty": 1700,
    "availableParticleQty": 1700,
    "expiredCount": 0,
    "invalidCount": 0
  },
  "page": 1,
  "pageSize": 10,
  "total": 1,
  "queryMeta": {
    "inventoryNature": "SNAPSHOT",
    "quantityFormula": "bagCount × specQtyPerBag",
    "calculationVersion": "v1"
  }
}
```

### 15.2 医院和物料选项

```http
GET /api/terminal/whole-bag-inventory/options/hospitals?keyword=衡水
GET /api/terminal/whole-bag-inventory/options/materials?keyword=02.22
GET /api/terminal/whole-bag-inventory/options/batches?hospitalId=1&materialId=1880
```

批次选项返回批号、规格、供货日期、到期日期和可用状态。

### 15.3 新增

```http
POST /api/terminal/whole-bag-inventory
Idempotency-Key: <uuid>
```

```json
{
  "stocktakeDate": "2025-12-23",
  "hospitalId": 1,
  "materialId": 1880,
  "batchNo": "B2511001",
  "bagCount": 17,
  "expiryDate": "2028-12-01"
}
```

服务端读取物料、规格和单位并计算颗粒量。

### 15.4 修改

```http
PUT /api/terminal/whole-bag-inventory/{id}
```

请求包含全部可编辑字段、`version` 和修改原因。

### 15.5 删除/作废

```http
DELETE /api/terminal/whole-bag-inventory
```

```json
{
  "ids": [1, 2],
  "reason": "重复盘点记录"
}
```

### 15.6 校验

```http
POST /api/terminal/whole-bag-inventory/validate
```

支持 `IDS` 和 `QUERY` 两种模式，返回通过、警告、失败条数和问题清单；大批量返回异步任务 ID。

### 15.7 导入

```http
GET  /api/terminal/whole-bag-inventory/import/template?format=xlsx
POST /api/terminal/whole-bag-inventory/import/preview
POST /api/terminal/whole-bag-inventory/import/commit
GET  /api/terminal/whole-bag-inventory/import/{batchId}/issues
```

### 15.8 导出

```http
POST /api/terminal/whole-bag-inventory/export
GET  /api/exports/tasks/{taskId}
```

### 15.9 HTTP 状态码

| 状态 | 含义 |
|---:|---|
| 400 | 参数、日期或数量格式错误 |
| 401 | 未登录 |
| 403 | 权限不足 |
| 404 | 记录或主数据不存在 |
| 409 | 重复、并发或版本冲突 |
| 422 | 业务校验不通过 |
| 500 | 服务端异常 |

## 16. 与其他模块关系

### 16.1 医院供货

批号、规格和到期日期优先从有效医院供货记录带出。整袋库存数量不是供货累计量，不得直接用供货袋数覆盖盘点袋数。

### 16.2 终端销量

终端销量用于理论库存扣减和批次核对，不直接修改整袋现场快照。如果销售涉及拆袋，整袋库存减少和零散库存增加需由独立拆袋事件处理。

### 16.3 零散库存

```text
实际库存颗粒量 = 有效整袋库存颗粒量 + 零散库存颗粒量
```

整袋和零散必须使用相同盘点日期、医院、物料和标准单位后才能相加。

### 16.4 盘点库存

盘点汇总读取指定日期有效整袋库存和零散库存。若同日存在多个版本，读取最新已确认版本并保存来源记录 ID 和版本。

### 16.5 要货指导

要货指导使用可用库存，不应把已过期、冻结或校验失败记录计入可用量。有效整袋库存变化后标记相关指导结果待重算。

### 16.6 建议领域事件

- `WholeBagInventoryValidated`
- `WholeBagInventoryChanged`
- `WholeBagInventoryVoided`
- `WholeBagInventoryImportFinished`

事件包含医院、物料、批号、盘点日期、变更前后袋数/颗粒量、来源批次和版本。

## 17. 前端交互规范

### 17.1 按钮状态

| 按钮 | 无选择 | 选择 1 条 | 选择多条 |
|---|---|---|---|
| 新增 | 可用 | 可用 | 可用 |
| 修改 | 禁用 | 按状态可用 | 禁用 |
| 删除 | 禁用 | 按状态可用 | 按状态可用 |
| 导出 | 可用 | 可用 | 可用 |
| 导入 | 可用 | 可用 | 可用 |
| 校验 | 确认后校验当前查询 | 校验选中项 | 校验选中项 |

### 17.2 展示规范

- 空值显示“—”，0 显示 `0`；
- 规格、袋数、颗粒量最多 3 位小数；
- 整数去除无意义尾零；
- 大数使用千分位，复制和导出保留原值；
- 近效期橙色、已过期红色，并附文字/图标；
- 规格和单位标签颜色不表达业务值；
- 超长字段省略并悬浮展示完整值。

### 17.3 表单联动

- 选择医院后限定可选批次范围；
- 选择物料后带出药品名称、规格和单位；
- 选择批号后带出到期日期；
- 物料变化时清空原批号、规格、到期日期和数量；
- 袋数变化时实时预览颗粒量；
- 服务端返回的计算值为最终值；
- 关闭未保存表单时二次确认。

### 17.4 分页与选择

- 默认每页 10 条；
- 显示总条数、页码、省略页、前往页；
- 表头全选默认只选择当前页；
- 如果支持跨页全选，必须显示选中总数和筛选范围；
- 翻页后默认清除当前页选择，避免误操作。

## 18. 审计、安全与一致性

### 18.1 审计

新增、修改、删除/作废、校验、导入、导出和强制修正必须记录：

- 模块、操作类型和 traceId；
- 记录 ID、业务键和来源批次；
- 修改前后摘要；
- 影响数量、结果和失败原因；
- 操作人、IP 和时间。

### 18.2 安全

- 服务端按医院范围过滤全部接口；
- 不能通过修改 ID、医院 ID、导出条件或批量请求越权；
- SQL 参数化，排序字段白名单；
- 上传文件进行格式、MIME、大小和安全校验；
- 禁止执行 Excel 宏、公式和外部链接；
- 下载文件使用短时效地址；
- 错误响应不暴露堆栈、SQL 和服务器路径。

### 18.3 一致性

- 新增、修改、作废和导入提交使用事务；
- 有效库存变化与下游待重算事件可靠提交；
- 重试不得重复写入；
- 历史记录保存规格版本、校验版本、来源批次和操作版本。

## 19. 非功能需求

### 19.1 性能

- 列表首屏目标不超过 2 秒，P95 不超过 4 秒；
- 支持至少 100 万条历史快照分页查询；
- 统计汇总在服务端执行；
- 50,000 行以内导入预览目标不超过 30 秒；
- 大批量导入、校验和导出转异步任务；
- 医院、物料和批次远程选项常用查询 1 秒内返回。

### 19.2 可用性

- 网络失败保留列表和表单内容；
- 异步任务离开页面后继续运行；
- 任务失败提供原因和安全重试；
- 来源数据和最后更新时间可查看；
- 日期和数值规则在各服务节点保持一致。

### 19.3 兼容性

- 支持 Chrome、Edge 最近两个大版本；
- 推荐分辨率 1366×768 及以上；
- 支持横向滚动、固定关键列和键盘操作；
- 状态不能仅依赖颜色。

## 20. 测试与验收标准

### 20.1 功能验收

1. 页面入口、查询条件、工具栏和表格字段与截图信息架构一致。
2. 医院、物料编码、批号和到期日期组合查询正确。
3. 新增、修改、删除、导入、导出和校验符合权限与状态规则。
4. 选择物料/批号可正确带出药名、规格、单位和到期日期。
5. 袋数修改后颗粒量正确计算。
6. 列表、统计汇总和导出结果一致。
7. 统计汇总针对当前查询全部记录，不是当前页。
8. 重复导入不产生重复库存。
9. 已过期记录可追溯但不计入可用库存。
10. 有效记录变更可触发盘点和要货指导待重算。

### 20.2 计算验收

样例一：

```text
规格 = 100 g/袋
袋数 = 17
颗粒量 = 100 × 17 = 1700
```

样例二：

```text
规格 = 200 g/袋
袋数 = 5
颗粒量 = 200 × 5 = 1000
```

样例三：

```text
袋数合计 = 17 + 5 + 105 + 3 + 13 + 9 + 4 + 18 + 6 + 13 = 193
颗粒量合计 = 1700 + 1000 + 10500 + 300 + 2600 + 900 + 400 + 3600 + 600 + 1300 = 22900
```

以上与截图统计示例一致，可作为基础验收数据。

### 20.3 边界验收

| 场景 | 预期 |
|---|---|
| 袋数为负 | 阻断 |
| 袋数为小数且不允许拆零 | 阻断 |
| 袋数为 0 | 保存为零库存或按配置跳过，并产生提示 |
| 颗粒量与袋数×规格不一致 | 阻断或明确自动修正 |
| 物料无规格 | 阻断 |
| 批号与物料不一致 | 阻断 |
| 到期日期早于盘点日期 | 标记已过期，不计可用库存 |
| 同业务键重复 | 阻断，不自动累加 |
| 同幂等键重试 | 返回首次结果 |
| 乐观锁版本冲突 | 返回 409 |

### 20.4 导入验收

- 正确模板可预览和提交；
- 缺表头、非法日期、未知物料、规格冲突和非法数量逐行报错；
- 同文件和历史重复均能识别；
- 预览后主数据变化，提交时重新校验；
- 相同文件重复提交不重复入账；
- 部分成功的统计和错误文件准确。

### 20.5 权限和安全验收

- 无权限医院无法通过列表、详情、导出、修改或导入获取数据；
- 直接调用隐藏按钮对应接口仍被拦截；
- 排序字段、批量 ID、文件名和特殊字符不能导致注入；
- 已引用记录不能物理删除；
- 所有关键操作存在审计日志。

### 20.6 性能验收

- 百万级数据分页查询命中合理索引；
- 统计汇总不在应用内加载全量数据；
- 50,000 行任务可显示进度；
- 并发提交同一业务键只生成一条有效记录。

## 21. 开发拆分建议

### 21.1 后端与数据

1. 确认医院、物料、规格、单位和批次主数据。
2. 新建整袋库存快照、导入批次和问题表。
3. 实现查询、分页、排序、全量统计和权限过滤。
4. 实现新增、修改、作废和乐观锁。
5. 实现数量公式、日期、批次、重复和到期校验。
6. 实现模板、预览、提交、错误文件和导入幂等。
7. 实现批量校验及异步任务。
8. 实现导出和短时效下载。
9. 实现下游待重算事件和版本追踪。
10. 编写单元、接口、权限、并发和性能测试。

### 21.2 前端

1. 新建独立页面和路由。
2. 实现医院、物料、批号和到期日期查询。
3. 实现表格、固定列、操作列、分页和统计行。
4. 实现新增/编辑表单及主数据联动。
5. 实现袋数输入和颗粒量实时预览。
6. 实现批量/行级删除及校验结果。
7. 实现导入预览、错误筛选、下载和提交进度。
8. 实现导出、权限和到期状态展示。

### 21.3 测试

- CRUD、查询、分页和统计；
- 数量、精度、规格和单位；
- 批号、日期、到期和重复；
- 导入、导出、校验和幂等；
- 权限、审计、并发和下游重算；
- 性能和浏览器兼容性。

## 22. 待业务确认项

| 优先级 | 待确认项 | 影响 |
|---|---|---|
| P0 | 整袋库存是实时数量、盘点快照还是日终库存 | 影响业务键和聚合方式 |
| P0 | 规格 `g/袋` 与“颗粒量”的真实单位关系 | 影响公式和字段命名 |
| P0 | 袋数是否只允许整数 | 影响整袋/零散边界 |
| P0 | 批号是否必填，空批号如何处理 | 影响唯一键和库存追溯 |
| P0 | 同日同医院同物料同批号是否只允许一条记录 | 影响防重和版本设计 |
| P0 | 到期批次是否计入实际库存和可用库存 | 影响盘点及要货指导 |
| P0 | 到期日当天是有效还是过期 | 影响状态计算 |
| P0 | 保存即生效还是校验通过后生效 | 影响状态和下游数据 |
| P0 | 已被盘点/要货指导使用的记录如何修正 | 影响版本和冲销 |
| P1 | 历史补录最大天数 | 影响日期校验 |
| P1 | 零袋记录是否保留 | 影响数据量和缺货表达 |
| P1 | 近效期预警天数 | 影响状态显示 |
| P1 | 是否从医院供货自动带出批号和到期日 | 影响联动接口 |
| P1 | 导入错误行是否保存为 `INVALID` | 影响导入流程 |
| P1 | 导出上限、保留时间和水印 | 影响导出实现 |
| P2 | 是否需要库位、扫码和拆袋业务 | 影响后续范围 |

## 23. 发布前检查清单

- [ ] P0 业务口径全部确认并写入数据字典；
- [ ] 医院、物料、规格、单位和批次映射可用；
- [ ] 快照表、导入表、问题表和索引完成；
- [ ] 页面字段、按钮和分页与截图逐项核对；
- [ ] 袋数与颗粒量公式及精度测试通过；
- [ ] 批号、到期、重复和权限校验通过；
- [ ] 导入预览、提交、错误文件和幂等通过；
- [ ] 列表、统计汇总和导出结果一致；
- [ ] 下游盘点与要货指导待重算机制通过；
- [ ] 审计、异步任务和下载安全验证通过；
- [ ] 性能、浏览器兼容性和业务 UAT 通过。

