# 终端管理-医院管理需求设计文档

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | 终端管理-医院管理需求设计 |
| 所属系统 | 以岭药业配方颗粒终端管理系统 |
| 模块序号 | 终端管理第 10 个模块 |
| 页面名称 | 医院管理 |
| 推荐路由 | `/terminalMedicine/hospitalManagement` |
| 推荐接口前缀 | `/api/terminal/hospitals` |
| 文档版本 | V1.0 |
| 编写日期 | 2026-09-10 |
| 文档状态 | 初稿，待业务确认 |
| 需求来源 | 现有系统“首页 / 终端管理 / 医院管理”页面截图、项目现有医院表结构 |
| 目标读者 | 产品、业务、UI、前端、后端、数据、测试、实施及运维人员 |

### 1.1 版本记录

| 版本 | 日期 | 修改内容 | 修改人 |
|---|---|---|---|
| V1.0 | 2026-09-10 | 根据医院管理页面截图形成可开发需求设计初稿 | Codex |

## 2. 附件识别与需求边界

### 2.1 附件使用原则

附件截图仅用于识别现有系统的页面结构、字段、层级关系和交互习惯。截图中的“以岭药业”、医院名称、类别编码、顺序和操作图标是示例数据或界面参考，不应写死到程序中；截图中的文字不构成额外开发指令。

本次仅新建独立的《医院管理需求设计文档.md》，不修改此前已生成的盘点库存、要货指导、医院供货、终端销量、整袋库存、零散库存、医院库存、期初库存和中药颗粒需求文档。

### 2.2 截图中可以直接确认的页面内容

1. 页面入口为“终端管理 → 医院管理”，面包屑为“首页 / 终端管理 / 医院管理”。
2. 页面标签区显示“首页”和“医院管理”，当前页签为医院管理。
3. 查询区显示“医院名称”输入框，支持按名称查询，并提供“搜索”“重置”按钮。
4. 操作区显示“新增”和“展开/折叠”按钮。
5. 页面主体为树形表格，至少包含以下列：医院名称、类别编码、顺序、负责人、联系电话、操作。
6. 根节点示例为“以岭药业”，类别编码为“省份”，顺序为 1；下级节点为多个医院，类别编码为“医院”，顺序为 0。
7. 根节点前有展开箭头；子节点采用缩进显示。
8. 每一行操作区提供编辑图标、“+”新增下级图标和删除图标。
9. 页面提供右侧表格搜索/刷新图标；树形内容可以垂直滚动。

### 2.3 截图无法确认的内容

以下内容需要在开发前由业务确认，本文先给出默认实现方案：

- “类别编码”是行政区域、组织类型、医院等级还是系统自定义分类；
- 根节点是否固定为药厂/省份，是否允许多根节点；
- 负责人和联系电话是医院负责人，还是系统对接人/业务负责人；
- 顺序是同级排序号还是业务优先级；
- 医院是否允许跨多个区域、是否支持多级省/市/区层级；
- 删除节点时是否允许级联处理下级医院；
- 已被供货、销量、库存等模块引用的医院能否删除或只能停用；
- 是否需要医院编码、统一社会信用代码、地址、区域、对接配置等扩展字段；
- 是否需要导入、导出、启停用、批量调整顺序和历史版本。

## 3. 背景与建设目标

### 3.1 业务背景

医院管理是终端管理系统的基础主数据模块，负责维护药厂、区域和医院的组织层级，为医院供货、终端销量、整袋库存、零散库存、医院库存、期初库存、盘点库存和要货指导提供医院 ID、医院名称及数据权限范围。

截图采用树形结构展示“以岭药业 → 医院”的上下级关系，并通过类别编码和顺序表达节点类型及同级排序。医院管理中的节点变化会直接影响其他模块的下拉选项、数据权限和历史数据关联。

### 3.2 建设目标

1. 建立可配置的医院组织树，支持根节点、区域节点和医院节点的层级管理。
2. 维护医院名称、类别编码、顺序、负责人和联系电话等基础资料。
3. 支持树形展开/折叠、名称查询、单节点新增、编辑、新增下级和删除/停用。
4. 防止重复医院、重复编码、循环挂载和错误层级。
5. 为业务模块提供稳定的医院选项、层级路径和数据权限接口。
6. 保留医院主数据快照、变更历史、操作审计和下游引用关系。

### 3.3 成功标准

- 页面树形层级、缩进、展开/折叠和排序与数据一致；
- 新增或修改医院后，授权业务模块可以正确获取最新有效医院；
- 已被业务数据引用的医院不被物理删除，历史记录仍可追溯；
- 同级顺序稳定，刷新、分页/滚动和重新打开页面后顺序不漂移；
- 无权限用户不能查看、修改或删除不属于其范围的医院节点；
- 所有结构和字段变更均有审计记录。

## 4. 需求范围

### 4.1 本期范围（MVP）

- 医院管理树形列表；
- 按医院名称模糊查询；
- 展开全部、折叠全部和单节点展开/折叠；
- 新增根节点或新增下级节点；
- 修改节点名称、类别编码、顺序、负责人和联系电话；
- 删除草稿节点、停用无引用节点和引用保护；
- 同级顺序校验及排序；
- 节点名称、类别编码、负责人、电话格式校验；
- 医院选项、层级路径和权限范围接口；
- 变更审计、软删除、状态管理和并发控制。

### 4.2 二期可选范围

- Excel/CSV 批量导入和导出；
- 批量调整顺序和批量启停用；
- 医院编码、统一社会信用代码、地址、区域、等级等扩展资料；
- 医院联系人多人员管理；
- 医院与业务员/区域的授权关系配置；
- 医院接口地址、同步账号和数据同步状态；
- 组织树版本、批量迁移和历史结构对比。

### 4.3 本期不包含

- 医院内部科室、病区和库位管理；
- 医院用户账号、角色和登录认证管理；
- 医院资质、合同、回款和结算完整流程；
- HIS/ERP 对接的完整实施；
- 医院药品、供货、销量、库存业务数据的维护；
- 删除医院后对历史业务数据的物理清理。

## 5. 角色与权限

### 5.1 权限矩阵

| 角色 | 查看树 | 查询 | 新增根节点 | 新增下级 | 修改 | 删除/停用 | 调整顺序 | 管理全部医院 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 系统管理员 | 是 | 是 | 是 | 是 | 是 | 是 | 是 | 是 |
| 主数据管理员 | 是 | 是 | 按授权 | 是 | 是 | 按流程 | 是 | 是 |
| 区域/业务管理员 | 是 | 是 | 否 | 按授权区域 | 按授权 | 否/停用 | 按授权 | 否 |
| 医院业务人员 | 是 | 是 | 否 | 否 | 否 | 否 | 否 | 否 |
| 库存/供货人员 | 是 | 是 | 否 | 否 | 否 | 否 | 否 | 否 |
| 审计/只读用户 | 是 | 是 | 否 | 否 | 否 | 否 | 否 | 否 |

### 5.2 权限编码

- `terminal:hospital-management:view`：查看医院树和医院详情；
- `terminal:hospital-management:query`：按名称和条件查询；
- `terminal:hospital-management:create`：新增根节点；
- `terminal:hospital-management:create-child`：新增下级节点；
- `terminal:hospital-management:update`：修改节点资料；
- `terminal:hospital-management:delete`：删除草稿/无引用节点；
- `terminal:hospital-management:deactivate`：停用已引用节点；
- `terminal:hospital-management:sort`：调整同级顺序；
- `terminal:hospital-management:export`：二期导出；
- `terminal:hospital-management:import`：二期导入。

服务端必须校验功能权限、节点数据范围和下游引用关系，不能依赖前端隐藏图标实现安全控制。

## 6. 页面设计

### 6.1 页面入口

- 左侧导航：终端管理 → 医院管理；
- 页面路由：`/terminalMedicine/hospitalManagement`；
- 面包屑：首页 / 终端管理 / 医院管理；
- 接口前缀：`/api/terminal/hospitals`。

### 6.2 页面区域

1. 面包屑和页签区；
2. 查询区：医院名称输入框、搜索、重置；
3. 工具栏：新增、展开/折叠；
4. 树形表格：医院名称、类别编码、顺序、负责人、联系电话、操作；
5. 右侧工具：表格搜索、刷新；
6. 滚动区域：支持大量医院节点的垂直滚动。

### 6.3 页面结构示意

```text
首页 / 终端管理 / 医院管理

[医院名称：________________] [搜索] [重置]

[新增] [展开/折叠]                                      [搜索] [刷新]

┌────────────────────┬──────────┬──────┬────────┬────────────┬──────────────┐
│ 医院名称            │ 类别编码 │ 顺序 │ 负责人 │ 联系电话   │ 操作         │
├────────────────────┼──────────┼──────┼────────┼────────────┼──────────────┤
│⌄ 以岭药业           │ 省份     │ 1    │ —      │ —          │ 编辑 + 删除  │
│ 　临城县中医医院    │ 医院     │ 0    │ —      │ —          │ 编辑 + 删除  │
│ 　石家庄市医院      │ 医院     │ 0    │ —      │ —          │ 编辑 + 删除  │
│ 　保定市医院        │ 医院     │ 0    │ —      │ —          │ 编辑 + 删除  │
└────────────────────┴──────────┴──────┴────────┴────────────┴──────────────┘
```

### 6.4 树形交互

- 根节点和有子节点的节点显示展开/折叠箭头；
- 叶子节点不显示展开箭头或显示不可点击占位；
- 点击节点名称默认选中节点并高亮；
- 点击箭头只改变展开状态，不打开编辑表单；
- 工具栏“展开/折叠”在两种状态间切换；
- 搜索命中子节点时自动展开其所有祖先，并高亮匹配文本；
- 搜索结果为空时不破坏原有展开状态，显示空状态；
- 刷新后默认恢复上次展开状态，若状态过大可只保留根节点展开。

### 6.5 操作图标语义

| 图标 | 含义 | 行为 |
|---|---|---|
| 编辑 | 修改当前节点 | 打开编辑表单 |
| `+` | 新增下级 | 以当前节点为父节点打开新增表单 |
| 删除/停用 | 删除或停用当前节点 | 按状态和引用关系弹出确认 |

## 7. 查询需求

### 7.1 查询字段

| 页面字段 | API 参数 | 控件 | 必填 | 规则 |
|---|---|---|---:|---|
| 医院名称 | `keyword` | 文本框 | 否 | 去首尾空格，模糊匹配，最长 120 字符 |
| 状态 | `status` | 高级筛选预留 | 否 | `ACTIVE`/`INACTIVE`/`DRAFT` |
| 类别编码 | `categoryCode` | 高级筛选预留 | 否 | 精确匹配类别字典 |
| 根节点 ID | `rootId` | 高级筛选预留 | 否 | 限定组织树范围 |

### 7.2 查询行为

1. 名称查询默认模糊匹配，忽略首尾空格。
2. 默认只返回 `ACTIVE` 节点；主数据管理员可查看草稿和停用节点。
3. 搜索命中节点时必须返回命中节点及其祖先路径，确保用户知道节点所在层级。
4. 搜索结果按照树形结构展示，不把节点平铺成无法定位的列表。
5. 搜索只影响显示结果，不修改数据库，也不改变节点顺序。
6. 点击搜索回到树形根节点视图；输入框回车可以触发查询。
7. 点击重置清空关键词、恢复默认状态过滤和展开状态。
8. 查询期间显示 loading，防止重复请求。
9. 无结果时显示空状态并保留查询条件。
10. 不能通过医院名称查询绕过数据权限，服务端必须重新过滤节点。

### 7.3 树形返回方式

默认接口一次返回用户可见的完整树，适用于医院规模不超过 5,000 个节点。超过该规模时支持懒加载模式：

- 首次只返回根节点；
- 展开节点请求其直接子节点；
- 搜索使用服务端查找并返回祖先路径；
- 前端不得用不完整的子节点列表判断“无子节点”。

## 8. 字段设计

### 8.1 节点字段

| 页面列/表单字段 | API 字段 | 数据库字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---:|---|
| 节点 ID | `id` | `id` | BIGINT | 是 | 主键 |
| 父节点 ID | `parentId` | `parent_id` | BIGINT/null | 根节点为空 | 树形关系 |
| 路径 | `path` | `path` | VARCHAR(1000) | 是 | 祖先 ID 路径，便于查询 |
| 层级 | `level` | `level` | INT | 是 | 根节点为 0 或 1，必须统一 |
| 医院名称 | `name` | `hospital_name` | VARCHAR(120) | 是 | 节点显示名称 |
| 类别编码 | `categoryCode` | `category_code` | VARCHAR(32) | 是 | 如 `PROVINCE`、`HOSPITAL` |
| 类别名称 | `categoryName` | -/字典 | VARCHAR(50) | 是 | 如“省份”“医院” |
| 顺序 | `sortOrder` | `sort_order` | INT | 是 | 同级排序，默认 0 |
| 负责人 | `ownerName` | `owner_name` | VARCHAR(64) | 否 | 医院负责人或业务联系人 |
| 联系电话 | `ownerPhone` | `owner_phone` | VARCHAR(32) | 否 | 联系电话，支持手机号/座机 |
| 医院编码 | `hospitalCode` | `hospital_code` | VARCHAR(64) | 条件必填 | 医院节点建议必填，截图未显示 |
| 状态 | `status` | `status` | Enum | 是 | `DRAFT`/`ACTIVE`/`INACTIVE` |
| 是否叶子 | `leaf` | - | Boolean | - | 根据子节点动态计算 |
| 子节点 | `children` | - | Array | - | 树形响应字段 |
| 版本 | `version` | `version` | INT | 是 | 乐观锁 |
| 创建信息 | `createdBy/createdAt` | 同名 | Long/DateTime | 是 | 审计字段 |
| 修改信息 | `updatedBy/updatedAt` | 同名 | Long/DateTime | 是 | 审计字段 |
| 删除时间 | `deletedAt` | `deleted_at` | DateTime/null | 否 | 软删除 |

### 8.2 负责人和联系电话

- 截图中“负责人”“联系电话”列为空，页面必须支持空值显示“—”；
- 负责人默认保存一名主负责人；
- 多联系人、手机号和座机扩展为二期联系人子表；
- 联系电话保存原始展示值，同时可保存规范化号码用于检索；
- 电话不在页面公开给无权限角色时，接口应脱敏或省略。

### 8.3 类别编码字典

建议建立 `hospital_category` 字典，不允许前端任意输入类别：

| 编码 | 名称 | 可作为父节点 | 可作为医院节点 | 默认层级 |
|---|---|---:|---:|---:|
| `GROUP` | 集团/药厂 | 是 | 否 | 0 |
| `PROVINCE` | 省份/区域 | 是 | 否 | 1 |
| `CITY` | 地市 | 是 | 否 | 2 |
| `DISTRICT` | 区县 | 是 | 否 | 3 |
| `HOSPITAL` | 医院 | 否/按配置 | 是 | 4 |

截图显示“省份”和“医院”，实际类别集合以业务字典为准。类别名称可以随字典调整，历史记录保存类别编码快照。

### 8.4 顺序规则

- `sortOrder` 为非负整数，默认 0；
- 同级节点按 `sortOrder ASC, name ASC, id ASC` 稳定排序；
- 顺序相同时不报错，但必须有确定的二级排序；
- 顺序是否从 0 还是 1 开始由业务统一，截图中医院为 0、根节点为 1，系统可以兼容但需在表单提示；
- 不建议将顺序作为全局唯一值；
- 后续拖拽排序可转换为连续整数并记录审计。

## 9. 树形数据模型和业务规则

### 9.1 组织层级模型

每个节点有且只有一个父节点（根节点除外）：

```text
根节点
└── 区域节点
    └── 医院节点
```

默认允许的父子关系：

| 父类别 | 子类别 | 默认允许 |
|---|---|---:|
| `GROUP` | `PROVINCE`/`CITY`/`HOSPITAL` | 是 |
| `PROVINCE` | `CITY`/`DISTRICT`/`HOSPITAL` | 是 |
| `CITY` | `DISTRICT`/`HOSPITAL` | 是 |
| `DISTRICT` | `HOSPITAL` | 是 |
| `HOSPITAL` | 任意 | 否 |

实际关系由类别字典的 `allowedParentCategories` 配置控制，服务端必须校验，不能只由前端限制。

### 9.2 根节点

- 默认允许一个当前有效的集团/药厂根节点；
- 是否允许多个根节点列为 P0 待确认；
- 根节点 `parentId=null`；
- 根节点不能被普通用户删除；
- 根节点改名需要主数据管理员权限并记录影响范围；
- 截图中的“以岭药业”只是示例，不写死为系统常量。

### 9.3 医院节点唯一性

默认唯一规则：

```text
同一父节点下：name 不重复
全局：hospitalCode 不重复
全局：同一有效医院节点不得有两个 active 记录
```

是否允许不同区域存在同名医院需要业务确认。若允许，必须使用 `hospitalCode` 或统一社会信用代码区分。

### 9.4 名称规则

- 名称去除首尾空格，长度 1～120 个字符；
- 不能包含控制字符；
- 默认不允许只使用标点符号；
- 是否允许同级重名由唯一性策略控制，默认不允许；
- 改名不改变节点 ID，历史业务记录继续引用原节点；
- 页面显示名称取当前主数据，历史业务模块应保存名称快照。

### 9.5 循环和孤儿节点防护

修改父节点时必须检查：

- 新父节点不能是当前节点本身；
- 新父节点不能是当前节点的任意后代；
- 被删除/停用节点不能作为新父节点；
- 事务完成后不能产生没有有效根路径的孤儿节点；
- `path`、`level` 和子树关系必须同时更新。

### 9.6 顺序和层级变更

- 修改顺序只影响同级节点；
- 修改父节点会改变当前节点及全部后代的 `path` 和 `level`；
- 大子树移动使用异步任务或分批更新，避免长事务阻塞；
- 移动操作前显示节点名称、原路径、新路径和受影响子节点数量；
- 结构变更成功后发布组织树变更事件。

## 10. 状态和删除规则

### 10.1 状态定义

| 状态 | 含义 | 默认显示 | 可作为父节点 | 可被业务选择 | 可编辑 |
|---|---|---:|---:|---:|---:|
| `DRAFT` | 新增未发布/未确认 | 主数据用户可见 | 按配置 | 否 | 是 |
| `ACTIVE` | 当前有效节点 | 是 | 按类别 | 是 | 是/生成版本 |
| `INACTIVE` | 已停用 | 默认否 | 否 | 否 | 可复制生成新节点 |
| `RETIRED` | 已退役，仅历史可查 | 审计可见 | 否 | 否 | 否 |

### 10.2 删除/停用规则

1. 无子节点、无业务引用且为 `DRAFT` 的节点可软删除。
2. 无子节点、无业务引用的 `ACTIVE` 节点可按权限停用或删除。
3. 有子节点的节点不能直接删除，必须先迁移/停用子节点，或按明确的级联策略处理。
4. 已被医院供货、销量、库存、盘点、期初或要货指导引用的医院不能物理删除。
5. 已引用节点默认执行 `INACTIVE`，历史业务记录继续可查。
6. 删除/停用前显示下级数量、业务引用数量和影响模块。
7. 删除/停用原因必填，长度 2～200 字符。
8. 软删除记录不出现在默认树和业务选项中，但保留审计和历史关联。

### 10.3 级联策略

默认采用“禁止隐式级联删除”：

- 删除父节点前必须无子节点；
- 停用父节点时默认阻断并列出子节点；
- 批量停用及子树迁移属于后续显式操作；
- 不允许用户在确认弹窗中误把整棵医院树删除。

## 11. 操作功能设计

### 11.1 新增根节点

点击工具栏“新增”打开表单，默认新增根节点。若当前系统只允许一个根节点且已有有效根节点，按钮应改为“新增下级”或提示选择父节点。

表单字段：

| 字段 | 控件 | 默认值 | 可编辑 | 规则 |
|---|---|---|---:|---|
| 上级节点 | 树形下拉 | 空/根 | 是 | 根节点为空；新增下级自动带入 |
| 名称 | 文本框 | 空 | 是 | 必填，1～120 字符 |
| 类别编码 | 下拉 | 根据父节点带出 | 是 | 只能选择允许的类别 |
| 顺序 | 数值框 | 同级最大值+1 或 0 | 是 | 非负整数 |
| 负责人 | 文本框 | 空 | 是 | 0～64 字符 |
| 联系电话 | 文本框 | 空 | 是 | 手机/座机格式校验 |
| 医院编码 | 文本框 | 空 | 条件 | 医院节点建议必填且唯一 |
| 状态 | 单选/只读 | `DRAFT` | 按权限 | 发布后变为 `ACTIVE` |

保存流程：

1. 前端执行必填、长度、电话和顺序格式校验；
2. 服务端重新校验父子类别、名称唯一、编码唯一和权限；
3. 计算 `path`、`level` 和排序位置；
4. 默认保存为 `DRAFT` 或按配置直接 `ACTIVE`；
5. 保存成功后刷新树并自动展开父节点；
6. 返回新节点 ID、完整路径和状态。

### 11.2 新增下级节点

点击行操作中的“+”图标打开新增下级表单：

- 自动带入当前节点作为父节点；
- 显示“上级节点：A / B”，但父节点 ID由服务端确认；
- 类别编码只展示允许的子类别；
- 医院节点新增后自动进入当前父节点下的排序位置；
- 保存成功后展开当前父节点并选中新节点。

### 11.3 编辑节点

- 点击编辑图标打开编辑表单；
- `DRAFT`、未锁定 `ACTIVE` 可按权限修改；
- 已被下游引用的医院修改名称、负责人或电话时保留节点 ID；
- 修改类别或父节点属于结构变更，需要显示影响子树；
- 根节点不能修改为医院节点，医院节点不能直接变成有子节点的叶子限制类别；
- 修改使用 `version` 乐观锁，版本冲突返回 HTTP 409；
- 修改成功后刷新受影响的树路径和业务选项缓存。

### 11.4 删除/停用节点

- 点击行删除图标打开确认弹窗；
- 弹窗显示节点名称、完整路径、子节点数、业务引用数和预计动作；
- 无引用草稿执行软删除；
- 已引用有效节点默认执行停用；
- 有子节点时默认阻断，并提供查看子节点入口；
- 原因必填；
- 成功后刷新父节点，并在有审计权限时可查看历史记录。

### 11.5 展开/折叠

- 工具栏按钮在“展开全部”和“折叠全部”之间切换；
- 展开全部只展开当前用户有权限看到的节点；
- 超大树使用懒加载，不强制一次请求全部后代；
- 单节点箭头支持局部展开/折叠；
- 搜索后自动展开命中节点祖先，不改变数据库；
- 刷新后保留已展开节点 ID 集合，集合失效时安全回退到根节点。

### 11.6 排序

首版支持表单输入顺序，二期支持拖拽排序：

- 拖拽只允许同级节点移动；
- 跨父节点移动需要单独“移动节点”权限；
- 排序提交带 `version` 和目标父节点 ID；
- 服务端重新生成同级连续顺序；
- 排序失败时恢复前端原顺序。

### 11.7 刷新和快速搜索

- 刷新保留查询关键词、展开状态和选中节点；
- 网络失败保留旧树并显示重试入口；
- 表格搜索图标打开快速搜索输入；
- 快速搜索与顶部医院名称查询使用同一服务端查询逻辑；
- 不允许只在当前已加载的子节点中搜索而遗漏未加载节点。

## 12. 校验规则与错误码

### 12.1 错误分级

| 级别 | 含义 | 示例 | 是否阻断 |
|---|---|---|---:|
| `ERROR` | 不能保存或发布 | 名称为空、父子类别非法、循环挂载 | 是 |
| `WARNING` | 可以保存但需关注 | 医院无负责人、电话为空、名称疑似重复 | 默认否 |
| `INFO` | 提示性信息 | 已自动补齐层级路径、顺序已归一化 | 否 |

### 12.2 错误码

- `HM_NAME_REQUIRED`：节点名称不能为空；
- `HM_NAME_TOO_LONG`：节点名称超过长度；
- `HM_CATEGORY_INVALID`：类别编码不存在或已停用；
- `HM_PARENT_CATEGORY_INVALID`：父子类别关系不允许；
- `HM_DUPLICATE_NAME`：同级名称重复；
- `HM_DUPLICATE_HOSPITAL_CODE`：医院编码重复；
- `HM_CYCLE_DETECTED`：检测到循环挂载；
- `HM_ORPHAN_NODE`：变更后产生孤儿节点；
- `HM_SORT_INVALID`：顺序不是合法非负整数；
- `HM_PHONE_INVALID`：联系电话格式错误；
- `HM_ROOT_CONFLICT`：根节点数量或根节点规则冲突；
- `HM_HAS_CHILDREN`：节点存在子节点，不能删除/停用；
- `HM_ALREADY_REFERENCED`：节点已被业务数据引用；
- `HM_INACTIVE_PARENT`：不能挂载到停用父节点；
- `HM_VERSION_CONFLICT`：记录已被其他用户修改；
- `HM_PERMISSION_DENIED`：无节点数据权限；
- `HM_IMPORT_DUPLICATE`：导入批次重复（二期）。

### 12.3 字段校验

| 字段 | 校验规则 |
|---|---|
| 名称 | 必填，1～120 字符，去首尾空格，禁止控制字符 |
| 类别编码 | 必须存在于启用字典，且符合父子关系 |
| 顺序 | 非负整数，最大值由数据库和业务配置限制 |
| 负责人 | 可空，最长 64 字符，去除首尾空格 |
| 联系电话 | 可空；手机号、座机和分机按统一正则/号码服务校验 |
| 医院编码 | 医院节点建议必填，长度 1～64，系统范围唯一 |
| 父节点 | 根节点为空；非根节点必须是有效活动节点，不能是自身/后代 |

## 13. 接口设计

### 13.1 统一响应

```json
{
  "code": "OK",
  "message": "success",
  "data": {},
  "traceId": "..."
}
```

错误响应包含稳定错误码、用户可读消息和 `traceId`，不返回 SQL、堆栈或敏感信息。

### 13.2 获取医院树

```http
GET /api/terminal/hospitals/tree?keyword=中医院&status=ACTIVE&lazy=false
```

响应示例：

```json
{
  "items": [
    {
      "id": 1,
      "parentId": null,
      "path": "1",
      "level": 0,
      "name": "以岭药业",
      "categoryCode": "PROVINCE",
      "categoryName": "省份",
      "sortOrder": 1,
      "ownerName": null,
      "ownerPhone": null,
      "status": "ACTIVE",
      "leaf": false,
      "children": [
        {
          "id": 2,
          "parentId": 1,
          "path": "1/2",
          "level": 1,
          "name": "临城县中医医院",
          "categoryCode": "HOSPITAL",
          "categoryName": "医院",
          "sortOrder": 0,
          "ownerName": "张三",
          "ownerPhone": "0311-00000000",
          "status": "ACTIVE",
          "leaf": true,
          "children": []
        }
      ]
    }
  ],
  "treeMeta": {
    "lazy": false,
    "matchedIds": [],
    "expandedIds": [1],
    "categoryVersion": "v1"
  }
}
```

搜索时返回命中节点的祖先路径；`matchedIds` 用于前端高亮。

### 13.3 获取节点详情

```http
GET /api/terminal/hospitals/{id}
```

至少返回完整路径、类别、顺序、负责人、联系电话、子节点数量、下游引用数量、状态、版本和最近变更记录摘要。

### 13.4 获取类别字典

```http
GET /api/terminal/hospitals/categories?parentId=1
```

只返回当前父节点允许且启用的类别。

### 13.5 新增根节点

```http
POST /api/terminal/hospitals
Idempotency-Key: <uuid>
Content-Type: application/json
```

```json
{
  "parentId": null,
  "name": "以岭药业",
  "categoryCode": "GROUP",
  "sortOrder": 1,
  "ownerName": "",
  "ownerPhone": "",
  "hospitalCode": "YLY",
  "status": "DRAFT"
}
```

服务端计算 `path`、`level` 和类别名称，不信任客户端提交的路径和类别名称。

### 13.6 新增下级节点

```http
POST /api/terminal/hospitals/{parentId}/children
Idempotency-Key: <uuid>
Content-Type: application/json
```

请求体不重复提交 `parentId` 或以路径中的 ID 为准；服务端校验 URL 父节点权限和请求体一致性。

### 13.7 修改节点

```http
PUT /api/terminal/hospitals/{id}
Content-Type: application/json
```

```json
{
  "version": 3,
  "name": "临城县中医医院",
  "categoryCode": "HOSPITAL",
  "parentId": 1,
  "sortOrder": 2,
  "ownerName": "李四",
  "ownerPhone": "0311-11111111",
  "hospitalCode": "HB-LC-001",
  "reason": "联系人信息更新"
}
```

若发生父节点或类别变化，响应增加 `affectedDescendantCount`、`oldPath` 和 `newPath`。

### 13.8 删除/停用

```http
DELETE /api/terminal/hospitals/{id}
Content-Type: application/json
```

```json
{
  "version": 3,
  "reason": "医院终止合作",
  "mode": "AUTO"
}
```

服务端根据子节点和引用关系决定软删除或停用；禁止物理删除已被业务引用的节点。

### 13.9 调整顺序/移动节点（二期或预留）

```http
POST /api/terminal/hospitals/{id}/move
POST /api/terminal/hospitals/{id}/sort
```

请求包含目标父节点、目标顺序、版本和原因。服务端执行循环检查、权限检查和子树路径更新。

### 13.10 业务选项接口

供货、销量、库存等模块使用：

```http
GET /api/terminal/hospitals/options?keyword=河北&includePath=true
```

默认只返回 `ACTIVE` 且用户有权限的医院叶子节点，响应包含：

```json
{
  "id": 2,
  "hospitalCode": "HB-LC-001",
  "name": "临城县中医医院",
  "fullPath": "以岭药业 / 河北 / 临城县中医医院",
  "parentId": 1,
  "categoryCode": "HOSPITAL"
}
```

### 13.11 审计和引用查询

```http
GET /api/terminal/hospitals/{id}/references
GET /api/terminal/hospitals/{id}/audit-logs
```

仅具备审计权限的用户可访问，返回引用模块、记录数量、最近使用日期和变更历史摘要。

### 13.12 HTTP 状态码

| 状态 | 含义 |
|---:|---|
| 400 | 参数、字段或树关系格式错误 |
| 401 | 未登录 |
| 403 | 功能或节点数据权限不足 |
| 404 | 节点、类别或父节点不存在 |
| 409 | 名称/编码重复、版本冲突、循环或并发冲突 |
| 422 | 有子节点、已引用或其他业务约束不满足 |
| 500 | 服务端异常 |

## 14. 数据库设计

### 14.1 现有表兼容说明

当前项目已有 `hospital` 表，包含 `id`、`hospital_name`、`status`、`created_at`。该结构只能支持平面医院列表，无法表达截图中的树形层级、类别编码、顺序、负责人和联系电话。

建议在不破坏现有 `hospital_id` 引用的前提下扩展表，或新建组织节点表并通过 `hospital_id` 关联医院业务实体。MVP 推荐直接扩展 `hospital` 表，后续复杂组织结构再拆分。

### 14.2 扩展表 `hospital`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键，保持现有医院 ID不变 |
| `parent_id` | BIGINT NULL | 父节点 ID，根节点为空 |
| `path` | VARCHAR(1000) | 祖先路径，如 `1/23/45` |
| `level` | INT | 树层级 |
| `hospital_name` | VARCHAR(120) | 节点名称，兼容现有字段 |
| `hospital_code` | VARCHAR(64) | 医院/组织编码 |
| `category_code` | VARCHAR(32) | 类别编码 |
| `owner_name` | VARCHAR(64) | 负责人 |
| `owner_phone` | VARCHAR(32) | 联系电话 |
| `sort_order` | INT | 同级顺序 |
| `status` | VARCHAR(20) | `DRAFT`/`ACTIVE`/`INACTIVE`/`RETIRED` |
| `version` | INT | 乐观锁版本 |
| `created_by`/`created_at` | BIGINT/DATETIME | 创建信息 |
| `updated_by`/`updated_at` | BIGINT/DATETIME | 修改信息 |
| `deleted_at` | DATETIME | 软删除时间 |

建议索引：

```text
idx_hospital_parent_sort (parent_id, sort_order, hospital_name, id)
idx_hospital_path (path)
idx_hospital_name_status (hospital_name, status, deleted_at)
uk_hospital_code (hospital_code, deleted_at)
idx_hospital_category (category_code, status)
```

同级名称唯一和有效期内编码唯一需要结合软删除策略在应用层/防重表中实现，不能只依赖允许 NULL 的唯一索引。

### 14.3 类别字典表 `hospital_category`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `category_code` | VARCHAR(32) | 类别编码 |
| `category_name` | VARCHAR(50) | 类别名称 |
| `allowed_parent_categories` | JSON/VARCHAR | 允许的父类别集合 |
| `can_have_children` | BOOLEAN | 是否允许子节点 |
| `is_hospital_leaf` | BOOLEAN | 是否可作为业务医院叶子 |
| `sort_order` | INT | 字典顺序 |
| `status` | VARCHAR(20) | `ACTIVE`/`INACTIVE` |
| `version` | INT | 版本 |

### 14.4 组织树变更记录 `hospital_change_log`

保存节点 ID、变更类型、旧父节点、新父节点、旧路径、新路径、旧字段摘要、新字段摘要、影响后代数量、操作人、时间、原因和 traceId。

### 14.5 引用快照与业务兼容

- 供货、销量、库存等业务表使用 `hospital_id` 外键或逻辑关联；
- 业务记录应保存 `hospital_name` 快照，避免医院改名导致历史报表变化；
- 医院停用不删除历史业务数据；
- 如果现有 `hospital` 表已有数据，迁移脚本需为其补充根节点、类别编码和路径；
- 迁移期间暂时无法识别父节点的记录进入 `MIGRATION_PENDING`，不能直接伪造层级。

## 15. 数据迁移方案

### 15.1 现有平面数据迁移

1. 备份现有 `hospital` 表及所有业务表中的 `hospital_id`。
2. 创建一个临时根节点或业务确认的集团节点。
3. 将现有医院挂载到确认的省份/区域节点下；如果没有区域信息，先挂到根节点并标记“待完善层级”。
4. 回填 `parent_id`、`path`、`level`、`category_code`、`sort_order`。
5. 校验每个医院 ID只有一条有效节点、路径无循环、业务引用不丢失。
6. 完成灰度验证后再开放树形编辑能力。

### 15.2 迁移验收

- 原有医院 ID保持不变；
- 供货、销量、库存、盘点和要货指导查询结果数量不变；
- 默认医院下拉只显示迁移后的 `ACTIVE` 叶子医院；
- 迁移前后名称、编码和状态可对账；
- 迁移日志可追溯每条节点的原始来源。

## 16. 与其他模块关系

### 16.1 医院供货、终端销量和库存

这些模块的医院选择都应调用医院管理的 `options` 接口：

- 只返回启用且有权限的医院叶子节点；
- 返回医院 ID、编码、名称和完整路径；
- 保存业务记录时以医院 ID为准，名称只作快照；
- 医院停用后不能新增业务数据，但历史记录仍可查。

### 16.2 盘点库存、期初库存和要货指导

医院管理变更不会自动改写历史库存快照。医院改名、换区域或停用时：

1. 保留原医院 ID；
2. 保存变更前后路径和名称；
3. 新汇总使用当前有效医院主数据；
4. 历史汇总继续使用快照名称和原业务键；
5. 影响范围在医院详情和审计中可查看。

### 16.3 数据权限

组织树是医院数据权限的基础：

```text
用户授权区域节点
→ 可查看该节点及允许继承的后代医院
→ 可新增/修改的节点范围由角色权限进一步限制
```

服务端每次查询业务数据都按医院 ID集合过滤，不能让前端仅提交区域 ID后由前端自行展开。

### 16.4 建议领域事件

- `HospitalCreated`
- `HospitalUpdated`
- `HospitalMoved`
- `HospitalDeactivated`
- `HospitalReactivated`
- `HospitalTreeChanged`

事件至少包含节点 ID、医院编码、名称、父节点、旧/新路径、类别、状态、影响后代数、操作人和版本。

## 17. 前端交互规范

### 17.1 按钮状态

| 按钮 | 无选择 | 选择 1 个节点 | 选择多个节点 | 权限不足 |
|---|---|---|---|---|
| 新增 | 可用 | 可用 | 可用 | 隐藏/禁用 |
| 展开/折叠 | 可用 | 可用 | 可用 | 可用 |
| 编辑 | 禁用或行级可用 | 按状态可用 | 禁用 | 隐藏/禁用 |
| `+` 新增下级 | 行级可用 | 行级可用 | 不适用 | 隐藏/禁用 |
| 删除/停用 | 禁用或行级可用 | 按引用关系可用 | 二期批量 | 隐藏/禁用 |

截图中主要使用行级操作图标，工具栏修改/删除是否需要显示取决于当前实现，但行为规则必须一致。

### 17.2 表单交互

- 进入新增下级表单时显示完整父级路径；
- 类别下拉只显示当前父节点允许的类别；
- 医院节点选择后提示其是否为业务叶子节点；
- 名称、编码重复时在字段级即时提示，提交时服务端再次校验；
- 电话格式错误显示明确示例；
- 修改父节点前显示受影响后代数量；
- 修改 ACTIVE 节点时提示历史业务记录不随名称变化；
- 关闭未保存表单时二次确认。

### 17.3 树形显示

- 根节点、区域节点和医院节点使用不同的类别标签；
- 类别颜色仅辅助展示，必须显示类别文字；
- 负责人/电话为空显示“—”；
- 名称过长省略显示并悬浮展示完整值；
- 当前选中节点使用背景高亮；
- 搜索命中文本高亮，祖先节点不误标为命中；
- 加载子节点显示局部 loading，不阻塞整棵树。

### 17.4 反馈和错误

- 保存成功提示节点名称和操作结果；
- 删除/停用成功提示“已停用/已删除”，不能笼统提示“操作成功”；
- 有子节点或业务引用时显示具体数量和处理建议；
- 版本冲突提示“数据已被其他用户修改，请刷新后重试”；
- 网络失败保留表单内容和树状态。

## 18. 审计、安全与一致性

### 18.1 审计日志

新增、修改、移动、排序、停用、恢复、删除、查询（如敏感权限）、导出和权限变更必须记录：

- 模块和操作类型；
- 节点 ID、医院编码、旧/新父节点和路径；
- 变更前后名称、类别、顺序、负责人和电话摘要；
- 下游引用数和影响后代数；
- 操作人、IP、时间、traceId和原因。

### 18.2 数据安全

- 所有树、详情、选项和写接口执行医院数据范围过滤；
- 不能通过修改节点 ID、父节点 ID或 `rootId`越权；
- SQL 使用参数绑定，排序字段使用白名单；
- 联系电话按角色脱敏；
- 接口错误不暴露数据库结构和服务器路径；
- 组织树变更不直接删除历史业务数据。

### 18.3 一致性与缓存

- 节点写入、路径更新、版本更新和审计日志使用同一事务；
- 子树移动需要锁定当前节点及目标父节点，避免并发循环；
- 医院选项缓存按组织树版本失效；
- 缓存刷新失败时短期以内存旧版本服务，但接口返回版本信息；
- 事件发布采用可靠消息或事务 outbox，避免业务提交成功但下游未感知。

## 19. 非功能需求

### 19.1 性能

- 5,000 个节点以内完整树首屏目标不超过 2 秒，P95 不超过 4 秒；
- 超过 5,000 个节点启用懒加载；
- 名称搜索响应目标不超过 1 秒（常用索引命中）；
- 子树移动更新 10,000 个节点以内目标不超过 10 秒，超过时转异步任务；
- 医院选项接口在常用关键字下 1 秒内返回；
- 不允许每个树节点单独请求子节点造成 N+1 查询。

### 19.2 可用性

- 页面刷新、网络重试和重新登录不丢失已保存数据；
- 异步树迁移/批量导入任务支持进度、失败原因和重试；
- 组织树版本和最后更新时间可查看；
- 发生结构异常时提供只读降级树，不允许继续写入破坏数据。

### 19.3 兼容性与可访问性

- 支持 Chrome、Edge 最近两个大版本；
- 推荐分辨率 1366×768 及以上；
- 树节点展开、编辑、加号和删除支持键盘操作；
- 展开状态、当前节点和错误提示对屏幕阅读器可识别；
- 状态不能只依靠颜色表达。

## 20. 测试与验收标准

### 20.1 页面验收

1. 面包屑、页签、医院名称查询、搜索/重置、添加和展开/折叠按钮与截图信息架构一致。
2. 树形表格正确展示医院名称、类别编码、顺序、负责人、联系电话和操作列。
3. 根节点和子节点缩进、箭头、展开/折叠状态正确。
4. 行级编辑、加号新增下级和删除图标行为正确。
5. 刷新、搜索、空状态、loading和错误提示完整。

### 20.2 树结构验收

| 场景 | 预期 |
|---|---|
| 新增根节点 | `parentId=null`，生成合法 path 和 level |
| 新增医院下级 | 自动挂载到当前节点并展开父节点 |
| 同级重名 | 阻断并提示重复 |
| 父节点为医院叶子 | 默认禁止新增下级 |
| 修改父节点为自身 | 阻断 |
| 修改父节点为后代 | 阻断循环 |
| 删除有子节点节点 | 阻断并显示子节点数 |
| 删除已引用医院 | 只能停用/退役，不能物理删除 |
| 同级顺序相同 | 按名称和 ID稳定排序 |
| 搜索子节点 | 返回祖先并自动展开路径 |

### 20.3 字段验收

| 场景 | 预期 |
|---|---|
| 名称为空 | `HM_NAME_REQUIRED` |
| 名称超过 120 字符 | 阻断 |
| 类别编码不存在 | `HM_CATEGORY_INVALID` |
| 父子类别不允许 | `HM_PARENT_CATEGORY_INVALID` |
| 顺序为负数/小数 | `HM_SORT_INVALID` |
| 电话格式非法 | `HM_PHONE_INVALID` |
| 医院编码重复 | `HM_DUPLICATE_HOSPITAL_CODE` |
| 负责人和电话为空 | 默认允许，页面显示“—”或产生警告 |

### 20.4 权限验收

- 无权限用户不能查看授权范围外节点；
- 直接修改 URL 的节点 ID或父节点 ID仍不能越权；
- 区域管理员只能新增/修改授权区域内节点；
- 普通业务人员可以读取有效医院选项但不能维护组织树；
- 停用、移动、排序和查看电话按独立权限拦截；
- 批量请求混入无权限节点时整批拒绝或逐条返回阻断，策略统一。

### 20.5 并发和一致性验收

- 两人同时修改同一节点，后提交者收到 409，不覆盖前者；
- 两人同时新增同级同名节点，只成功一条；
- 并发移动节点不能形成循环或孤儿；
- 路径、层级、排序和审计日志事务一致；
- 缓存失效后医院选项能在规定时间内反映新状态。

### 20.6 下游验收

1. 医院供货、终端销量和库存模块只能选择 `ACTIVE` 叶子医院。
2. 医院改名后历史业务记录 ID不变，快照名称不被静默改写。
3. 医院停用后不能新增业务记录，但历史查询和报表仍可访问。
4. 医院区域调整后，权限范围和完整路径正确更新。
5. 组织树变更事件能够触发下游缓存刷新或待处理标记。

## 21. 开发拆分建议

### 21.1 后端与数据

1. 扩展现有 `hospital` 表或建立组织节点表，完成平面数据迁移。
2. 建立类别字典、路径索引、顺序和版本字段。
3. 实现树查询、名称搜索、懒加载和祖先路径返回。
4. 实现新增根节点、新增下级、修改、停用/删除和引用保护。
5. 实现父子类别、名称/编码唯一、循环、孤儿和顺序校验。
6. 实现乐观锁、节点/子树锁和事务路径更新。
7. 实现医院选项、完整路径和数据权限接口。
8. 实现审计、变更事件、缓存失效和失败重试。
9. 预留排序、移动、导入导出和联系人扩展接口。
10. 编写单元、接口、权限、迁移、并发和性能测试。

### 21.2 前端

1. 新建独立医院管理页面和路由。
2. 实现树形表格、缩进、展开/折叠和懒加载。
3. 实现医院名称搜索、祖先路径展开和命中高亮。
4. 实现新增根节点、新增下级和编辑表单。
5. 实现类别联动、负责人、电话、编码和顺序校验。
6. 实现行级编辑、加号、删除/停用确认和引用提示。
7. 实现刷新、loading、空状态、错误重试和状态标签。
8. 实现权限指令和电话脱敏。

### 21.3 测试

- 页面布局、树结构、搜索和展开/折叠；
- 新增、编辑、删除/停用、版本和审计；
- 类别、名称、编码、电话、顺序和父子关系；
- 循环、孤儿、重复、子节点和引用保护；
- 权限、越权、注入和敏感字段脱敏；
- 并发新增、修改、移动、缓存和事件；
- 大树、懒加载、搜索性能和浏览器兼容性。

## 22. 待业务确认项

| 优先级 | 待确认项 | 影响 |
|---|---|---|
| P0 | 根节点是否只有一个，根节点默认类别是什么 | 影响树模型和新增按钮 |
| P0 | “类别编码”具体字典及父子层级规则 | 影响新增、编辑和校验 |
| P0 | 医院名称是否全局唯一或只要求同级唯一 | 影响唯一索引和查询 |
| P0 | 是否必须维护医院编码 | 影响主数据关联和下游接口 |
| P0 | 负责人/联系电话代表医院负责人还是业务联系人 | 影响字段和权限 |
| P0 | 已被供货、销量、库存等模块引用的医院如何删除 | 影响停用、历史和下游 |
| P0 | 有子节点的区域能否停用，是否允许级联 | 影响树安全 |
| P0 | 顺序从 0 还是 1 开始，是否允许重复 | 影响排序和页面显示 |
| P0 | 医院是否允许跨区域挂载或调整父节点 | 影响权限和路径更新 |
| P0 | 医院改名后历史报表显示当前名称还是快照名称 | 影响数据追溯 |
| P1 | 是否需要省/市/区多级行政层级 | 影响类别字典 |
| P1 | 是否需要统一社会信用代码、地址、医院等级等字段 | 影响表单和迁移 |
| P1 | 是否支持批量导入、导出和批量调整顺序 | 影响二期范围 |
| P1 | 是否需要多个负责人和联系人子表 | 影响数据模型 |
| P1 | 是否需要医院启用审批、双人复核和发布 | 影响状态流程 |
| P1 | 区域管理员的数据权限是否随组织树继承 | 影响权限模型 |
| P1 | 医院停用后历史数据是否仍可在默认查询中显示 | 影响查询过滤 |
| P2 | 是否需要拖拽移动、版本对比和组织树导入模板 | 影响后续交互 |

## 23. 发布前检查清单

- [ ] P0 组织树、根节点、类别和医院唯一性规则全部确认；
- [ ] 现有平面医院数据完成备份、迁移和业务对账；
- [ ] `parent_id`、`path`、`level`、`category_code`、`sort_order`和版本字段可用；
- [ ] 页面字段、树形缩进、按钮和行级操作与截图逐项核对；
- [ ] 新增根节点、新增下级、编辑、停用和引用保护测试通过；
- [ ] 名称、编码、类别、顺序、电话和父子关系校验通过；
- [ ] 循环、孤儿、重复、子节点和并发冲突测试通过；
- [ ] 医院选项接口只返回有效且有权限的叶子医院；
- [ ] 供货、销量、库存、盘点和要货指导模块引用验证通过；
- [ ] 医院改名/停用后的历史快照和报表验证通过；
- [ ] 审计日志、缓存失效、事件发布和失败重试验证通过；
- [ ] 性能、懒加载、权限、安全和浏览器兼容性达到要求；
- [ ] 业务完成医院主数据 UAT。

