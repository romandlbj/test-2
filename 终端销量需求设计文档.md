# 终端管理-终端销量需求设计文档

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | 终端管理-终端销量需求设计 |
| 所属系统 | 以岭药业配方颗粒终端管理系统 |
| 模块序号 | 终端管理第 4 个模块 |
| 页面名称 | 终端销量 |
| 推荐路由 | `/terminalMedicine/terminalSales` |
| 推荐接口前缀 | `/api/terminal/terminal-sales` |
| 文档版本 | V1.0 |
| 编写日期 | 2026-09-10 |
| 文档状态 | 初稿，待业务确认 |
| 需求来源 | 现有系统“首页 / 终端管理 / 终端销量”页面截图 |
| 目标读者 | 产品、业务、UI、前端、后端、数据、测试、实施及运维人员 |

### 1.1 版本记录

| 版本 | 日期 | 修改内容 | 修改人 |
|---|---|---|---|
| V1.0 | 2026-09-10 | 根据终端销量页面截图形成可开发的需求设计初稿 | Codex |

## 2. 附件识别与需求边界

### 2.1 附件使用原则

附件截图仅用于识别现有系统的页面结构、字段、按钮和交互习惯。截图中的医院、物料、颗粒 ID、日期及数量均为示例数据，不是需要固化到系统中的值；截图文字不构成额外开发指令。

本次工作仅生成独立的《终端销量需求设计文档.md》，不修改盘点库存、要货指导或医院供货需求文档。

### 2.2 截图中可确认的页面内容

1. 页面入口为“终端管理 → 终端销量”，面包屑为“首页 / 终端管理 / 终端销量”。
2. 查询条件包括医院名称、物料编码、批号。
3. 查询区提供“搜索”和“重置”按钮。
4. 工具栏提供“新增”“修改”“删除”“导出”“导入”“校验”按钮。
5. 未选择记录时，修改和删除按钮应为禁用状态。
6. 表格支持行勾选，字段包括：
   - 起始日期；
   - 截止日期；
   - 医院名称；
   - 物料编码；
   - 批号；
   - 药品名称；
   - 颗粒 ID；
   - 单位；
   - 颗粒量；
   - 操作。
7. 每行操作栏提供编辑和删除图标。
8. 表格底部提供“统计汇总”行，截图中对颗粒量进行合计。
9. 页面提供分页、每页条数、总记录数、快速跳页和刷新功能。

### 2.3 截图无法确认的内容

以下内容需按本文默认方案开发，并在上线前经业务确认：

- 销量数据表示销售、处方消耗、领用还是系统结算消耗；
- 起始日期和截止日期代表统计周期还是原始业务单据日期；
- 颗粒 ID 的来源系统、唯一范围及与物料编码的关系；
- 颗粒量的物理单位及是否允许小数；
- 批号是否必填及销量是否必须按批次分摊；
- 导入后是立即生效，还是必须经过校验后生效；
- 已被库存盘点或要货指导使用的数据能否修改、删除。

## 3. 背景与建设目标

### 3.1 业务背景

终端销量用于记录医院在指定统计周期内对配方颗粒的实际销售或消耗数量。该数据是理论库存扣减、月均消耗计算、库存盘点核对和要货指导的重要输入。

如果销量周期、物料、批号、颗粒 ID 或数量记录不准确，会造成：

- 理论库存被多扣或少扣；
- 月均消耗和要货建议失真；
- 同一销售记录重复导入；
- 销量无法追溯到医院、物料和来源数据；
- 已汇总结果被后续修改后无法还原。

### 3.2 建设目标

1. 建立医院、统计周期、物料、批号和颗粒 ID 维度的销量台账。
2. 支持人工新增、修改、删除/作废、批量导入、校验和导出。
3. 统一销量数量、单位、精度和统计周期口径。
4. 对重复记录、周期重叠、主数据缺失和异常销量进行校验。
5. 只有通过校验的销量数据才进入库存扣减和要货指导计算。
6. 保留来源批次、计算版本和操作日志，保证历史结果可追溯。

### 3.3 成功标准

- 相同查询条件下，列表、统计汇总和导出中的颗粒量一致；
- 重复上传同一文件或重复提交同一记录不会产生重复销量；
- 每条有效销量均可追溯医院、周期、物料、颗粒 ID、来源批次和操作人；
- 无权限用户不能查询、导出或变更其他医院的数据；
- 销量变更能够正确触发下游库存及要货指导的待重算标记。

## 4. 需求范围

### 4.1 本期范围

- 终端销量列表查询及分页；
- 医院名称、物料编码、批号筛选；
- 新增和单条修改；
- 批量删除或作废；
- Excel/CSV 导入预览、校验、提交和错误明细下载；
- 当前查询结果导出；
- 批量校验及校验状态管理；
- 颗粒量全量统计汇总；
- 服务端排序、数据权限、幂等、审计和异常提示；
- 向库存盘点、理论库存和要货指导提供有效销量数据。

### 4.2 本期不包含

- 收银、处方收费和医保结算系统；
- 患者、医生、处方等个人敏感信息管理；
- 销售退货、销售冲销的完整审批流程；
- 财务收入、成本、利润和税务核算；
- 移动端扫码销售；
- 自动采购或自动要货；
- 机器学习销量预测。

### 4.3 后续扩展

- 接入医院处方或 HIS 系统自动同步销量；
- 按科室、医生、剂型和终端设备分析；
- 销售退回和冲销事件；
- 日销量趋势、异常波动和缺数预警；
- 销量明细钻取和来源单据追溯。

## 5. 角色与权限

### 5.1 权限矩阵

| 角色 | 查询 | 新增 | 修改 | 删除/作废 | 导入 | 校验 | 导出 | 数据范围 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| 系统管理员 | 是 | 是 | 是 | 是 | 是 | 是 | 是 | 全部医院 |
| 区域/业务管理员 | 是 | 是 | 是 | 按授权 | 是 | 是 | 是 | 授权区域医院 |
| 医院销量人员 | 是 | 是 | 是 | 本人草稿 | 是 | 是 | 可配置 | 所属医院 |
| 库存管理人员 | 是 | 可配置 | 可配置 | 否 | 可配置 | 是 | 是 | 授权医院 |
| 财务/审计人员 | 是 | 否 | 否 | 否 | 否 | 否 | 是 | 授权医院 |
| 只读用户 | 是 | 否 | 否 | 否 | 否 | 否 | 可配置 | 授权范围 |

### 5.2 权限编码

- `terminal:terminal-sales:view`
- `terminal:terminal-sales:create`
- `terminal:terminal-sales:update`
- `terminal:terminal-sales:delete`
- `terminal:terminal-sales:import`
- `terminal:terminal-sales:validate`
- `terminal:terminal-sales:export`
- `terminal:terminal-sales:override`

所有接口必须在服务端进行功能权限和医院数据权限校验。前端隐藏或禁用按钮不能替代服务端鉴权。

## 6. 页面设计

### 6.1 页面入口

- 左侧导航：终端管理 → 终端销量；
- 页面路由：`/terminalMedicine/terminalSales`；
- 面包屑：首页 / 终端管理 / 终端销量；
- 接口前缀：`/api/terminal/terminal-sales`。

### 6.2 页面布局

1. 面包屑和页签区；
2. 查询区：医院名称、物料编码、批号、搜索、重置；
3. 工具栏：新增、修改、删除、导出、导入、校验；
4. 表格区：行选择、销量字段、行级编辑和删除；
5. 统计行：当前查询结果颗粒量合计；
6. 分页区：总条数、每页条数、页码、跳页；
7. 右上角：表格搜索和刷新按钮。

### 6.3 页面结构示意

```text
首页 / 终端管理 / 终端销量

[医院名称 ▼] [物料编码] [批号] [搜索] [重置]

[新增] [修改] [删除] [导出] [导入] [校验]                [表格搜索] [刷新]

┌选择┬起始日期*┬截止日期*┬医院名称*┬物料编码┬批号┬药品名称*┬颗粒ID*┬单位┬颗粒量*┬操作┐
│ □  │          │          │          │        │    │          │        │    │        │编辑 删除│
├────┼──────────┼──────────┼──────────┼────────┼────┼──────────┼────────┼────┼────────┼─────────┤
│ □  │ ...      │ ...      │ ...      │ ...    │... │ ...      │ ...    │... │ ...    │ ✎  🗑   │
├────┴──────────┴──────────┴──────────┴────────┴────┴──────────┴────────┴────┴────────┴─────────┤
│ 统计汇总                                                            颗粒量合计：...           │
└───────────────────────────────────────────────────────────────────────────────────────────────┘

共 N 条 [10条/页]  < 1 2 3 ... >  前往 [ ] 页
```

### 6.4 固定列与滚动

- 左侧固定选择列、起始日期、截止日期和医院名称；
- 操作列固定在右侧；
- 中间字段支持横向滚动；
- 表头和统计行在垂直滚动时保持可见；
- 最小推荐分辨率 1366×768。

## 7. 查询需求

### 7.1 查询字段

| 页面字段 | API 参数 | 控件 | 必填 | 规则 |
|---|---|---|---:|---|
| 医院名称 | `hospitalId` | 可搜索下拉 | 否 | 仅返回用户有权限的医院，提交医院 ID |
| 物料编码 | `materialCode` | 文本框 | 否 | 去首尾空格，默认模糊匹配，最长 64 字符 |
| 批号 | `batchNo` | 文本框 | 否 | 模糊匹配，最长 64 字符 |
| 页码 | `page` | 分页器 | 否 | 默认 1，最小 1 |
| 每页条数 | `pageSize` | 分页器 | 否 | 默认 10，可选 10/20/50/100/200 |
| 排序字段 | `sortField` | 表头排序 | 否 | 服务端白名单字段 |
| 排序方向 | `sortOrder` | 表头排序 | 否 | `asc`/`desc` |

### 7.2 查询行为

1. 多个条件之间使用 AND 关系。
2. 空条件查询当前用户有权限的全部未作废数据。
3. 默认排序：截止日期倒序、起始日期倒序、医院名称升序、ID 倒序。
4. 点击搜索回到第 1 页；物料编码和批号按回车可搜索。
5. 点击重置清空条件、恢复默认排序和第 1 页。
6. 查询期间显示 loading 并防止重复请求。
7. 无数据时显示空状态，保留查询条件。
8. 刷新保留条件、排序、页码、页大小和横向滚动位置。
9. 分页统计总数和颗粒量汇总均由服务端返回。

### 7.3 建议扩展查询条件

截图未显示但接口建议预留：

- 统计周期起止日期；
- 药品名称；
- 颗粒 ID；
- 校验状态；
- 来源类型；
- 是否包含作废记录。

扩展条件可先用于高级筛选抽屉，不必影响截图主查询区。

## 8. 表格字段设计

### 8.1 字段清单

| 页面列 | API 字段 | 数据库字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---:|---|
| 选择 | `selected` | - | Boolean | - | 表头复选框全选当前页 |
| 起始日期* | `startDate` | `start_date` | Date | 是 | 销量统计周期开始日期，包含当天 |
| 截止日期* | `endDate` | `end_date` | Date | 是 | 销量统计周期结束日期，包含当天 |
| 医院名称* | `hospitalName` | `hospital_name` | String/快照 | 是 | 最长 120 字符 |
| 医院 ID | `hospitalId` | `hospital_id` | Long | 是 | 隐藏字段，用于权限和关联 |
| 物料编码 | `materialCode` | `material_code` | String | 条件必填 | 最长 64 字符，原样保留前导零 |
| 物料 ID | `materialId` | `material_id` | Long | 条件必填 | 与物料主数据关联 |
| 批号 | `batchNo` | `batch_no` | String | 条件必填 | 最长 64 字符；是否业务必填待确认 |
| 药品名称* | `drugName` | `drug_name` | String/快照 | 是 | 由物料主数据或颗粒 ID 带出 |
| 颗粒 ID* | `particleId` | `particle_id` | String | 是 | 来源系统颗粒唯一标识，建议按字符串保存 |
| 单位 | `unitCode`/`unitName` | `unit_code`/`unit_name` | String | 是 | 来源单位字典，例如克、g |
| 颗粒量* | `particleQty` | `particle_qty` | Decimal(18,3) | 是 | 销量/消耗数量，必须大于等于 0 |
| 校验状态 | `validationStatus` | `validation_status` | Enum | 是 | 页面可使用图标/详情展示 |
| 来源类型 | `sourceType` | `source_type` | Enum | 是 | `MANUAL`、`IMPORT`、`API` |
| 操作 | `actions` | - | - | - | 行级编辑、删除/作废 |

### 8.2 必填标识说明

截图中起始日期、截止日期、医院名称、药品名称、颗粒 ID 和颗粒量带星号。物料编码、批号和单位虽未在截图中显示星号，但在完整业务数据中应按以下条件校验：

- `particleId` 能唯一反查物料时，物料编码可由系统补全；
- 无法通过颗粒 ID 反查物料时，物料编码必须填写；
- 销量要求按批次扣减库存时，批号必须填写；
- 单位必须通过颗粒 ID 或物料单位配置确定，不能永久为空。

### 8.3 统计汇总

表格底部“统计汇总”针对当前查询条件的全部有效记录计算：

```text
颗粒量合计 = Σ particleQty
```

接口还应返回：

- `recordCount`：记录数；
- `validatedCount`：已校验记录数；
- `invalidCount`：异常记录数；
- `missingMaterialCount`：缺少物料映射数；
- `missingBatchCount`：缺少批号数。

截图中的 `578.8` 仅用于确认汇总位置，不作为验收固定值。

## 9. 数据口径和业务粒度

### 9.1 统计周期

- 起始日期和截止日期均包含当天；
- 起始日期不得晚于截止日期；
- 默认最大周期为 366 天，可通过配置调整；
- 同一自然日销量建议使用 `startDate=endDate`；
- 如果来源系统提供的是累计销量，必须明确累计周期，不能将累计值当成单日销量。

### 9.2 最小业务粒度

默认业务键：

```text
医院 ID + 颗粒 ID + 起始日期 + 截止日期 + 批号 + 来源系统
```

如果来源系统具有唯一流水号，应优先使用：

```text
来源系统 + 来源业务 ID
```

作为幂等键，业务组合键用于重复候选校验。

### 9.3 颗粒 ID

1. 颗粒 ID 推荐使用字符串保存，避免外部系统出现前导零或超出整数范围。
2. 在同一来源系统中必须唯一映射一个物料和单位。
3. 一个物料可以对应多个颗粒 ID，但一个有效颗粒 ID 在同一时间只能对应一个物料版本。
4. 映射冲突时记录不得进入有效销量。
5. 截图中颗粒 ID 形如 `4918`、`4911`，仅为示例，不能据此限定为四位整数。

### 9.4 颗粒量和单位

- `particleQty` 使用 `DECIMAL(18,3)`，禁止使用浮点型；
- 默认允许最多 3 位小数；
- 0 是否允许保存取决于零销量政策，默认作为警告，不进入库存扣减；
- 负数不能用于表示退货，退货必须使用独立冲销/退货事件；
- 单位由颗粒 ID 或物料单位配置确定；
- 不同单位的数据不能直接相加，统计前必须换算到标准库存单位；
- 页面可沿用“颗粒量”列名，但表头 tooltip、详情和导出必须说明实际单位。

## 10. 核心业务规则

### 10.1 主数据匹配

1. 医院必须存在、启用且在操作人授权范围内。
2. 颗粒 ID 必须在有效映射表中找到物料、药品名称和单位。
3. 如果同时提交物料编码和颗粒 ID，两者必须映射到同一物料。
4. 药品名称由主数据带出并保存快照，不能通过手工修改绕过物料校验。
5. 单位由映射配置带出，前端默认只读。
6. 批号存在时，必须与医院供货模块中的医院、物料及有效供货批次相匹配。

### 10.2 周期校验

```text
startDate <= endDate
periodDays = endDate - startDate + 1
periodDays <= maxSalesPeriodDays
```

默认 `maxSalesPeriodDays=366`。超长周期必须拆分或使用管理员权限提交。

### 10.3 数量校验

- 颗粒量必须是合法十进制定点数；
- 不允许负数、NaN、Infinity、科学计数法或超精度值；
- 默认 `particleQty > 0`；为兼容来源系统零销量数据，可通过配置允许 0 并标记 `ZERO_SALES`；
- 中间换算至少保留 6 位小数，最终按库存标准单位保留 3 位并使用 `HALF_UP`；
- 导入文件中的展示格式不能改变实际数值。

### 10.4 批号扣减规则

如果销量按批号管理：

1. 批号必须在医院供货有效记录中存在；
2. 截止日期不能早于该批次首次供货日期；
3. 销量数量不能导致该批次理论库存小于允许的负库存阈值；
4. 超出批次可用库存时默认阻断，也可按业务配置标记警告；
5. 未提供批号时，系统不能自行静默选择批次，除非启用明确的 FIFO/FEFO 分摊规则；
6. 自动分摊必须保存分摊明细，能够追溯到每个供货批次。

### 10.5 重复记录与周期重叠

以下情况视为完全重复：

```text
医院 + 颗粒 ID + 批号 + 起始日期 + 截止日期 + 来源系统相同
```

完全重复默认阻断。

同一医院、颗粒 ID、批号的统计周期发生交叉时，标记 `PERIOD_OVERLAP`：

```text
newStart <= existingEnd AND newEnd >= existingStart
```

周期重叠可能导致销量重复计算，默认作为阻断错误；如果来源数据为增量而非累计，可通过来源规则允许，但必须保存 `dataMode=INCREMENTAL`。

### 10.6 数据模式

每个来源必须明确：

- `INCREMENTAL`：当前周期内新增销量；
- `CUMULATIVE`：截至某日的累计销量；
- `SNAPSHOT`：指定周期的销量快照。

V1 默认只支持 `INCREMENTAL`/周期快照方式。累计数据必须先在接入层计算增量，不能直接与增量数据相加。

### 10.7 生效规则

只有满足以下条件的记录才进入下游计算：

```text
validationStatus = VALIDATED
AND deletedAt IS NULL
AND particleQty > 0
AND 医院、颗粒 ID、物料和单位映射有效
```

有效销量按截止日期计入库存；如果业务要求按每日分摊，必须提供日明细或明确分摊算法，不能平均分摊后冒充原始销量。

### 10.8 幂等与并发

- 人工新增使用 `Idempotency-Key`；
- API 同步优先使用来源业务 ID；
- 导入使用文件 SHA-256、导入批次和行哈希防重；
- 同一幂等请求重复提交返回首次结果；
- 数据库唯一键或分布式锁阻止并发重复写入；
- 修改使用 `version` 乐观锁，冲突返回 HTTP 409。

## 11. 状态设计

### 11.1 销量记录状态

| 状态 | 含义 | 默认显示 | 可修改 | 可删除/作废 | 进入下游 |
|---|---|---:|---:|---:|---:|
| `DRAFT` | 人工新增或导入后尚未校验 | 是 | 是 | 是 | 否 |
| `VALIDATED` | 已通过阻断性校验 | 是 | 按权限 | 已引用时只能作废 | 是 |
| `INVALID` | 校验失败，等待修正 | 是 | 是 | 是 | 否 |
| `VOID` | 已作废 | 默认否 | 否 | 否 | 否 |

### 11.2 导入任务状态

- `UPLOADED`：已上传；
- `PREVIEWED`：已预览；
- `PROCESSING`：正在提交；
- `SUCCESS`：全部完成；
- `PARTIAL_SUCCESS`：部分成功；
- `FAILED`：失败；
- `CANCELLED`：取消或超时。

## 12. 功能操作设计

### 12.1 新增

点击新增打开抽屉或弹窗。

| 字段 | 控件 | 默认值 | 可编辑 | 规则 |
|---|---|---|---:|---|
| 起始日期 | 日期选择器 | 空/当前月首日 | 是 | 不晚于截止日期 |
| 截止日期 | 日期选择器 | 当前日期 | 是 | 不早于起始日期 |
| 医院名称 | 远程搜索下拉 | 空 | 是 | 限定授权医院 |
| 颗粒 ID | 远程搜索 | 空 | 是 | 选择后带出物料和单位 |
| 物料编码 | 只读/联动搜索 | 自动带出 | 条件可编辑 | 与颗粒 ID 一致 |
| 药品名称 | 只读 | 自动带出 | 否 | 保存主数据快照 |
| 批号 | 下拉/文本 | 空 | 是 | 优先从有效医院供货批次选择 |
| 单位 | 只读标签 | 自动带出 | 否 | 来源于映射配置 |
| 颗粒量 | 数值框 | 空 | 是 | 大于 0，最多 3 位小数 |

保存时服务端重新读取医院、颗粒 ID、物料和单位映射。保存成功默认进入 `DRAFT`；如配置 `validateOnCreate=true`，可即时校验并进入 `VALIDATED`。

### 12.2 修改

- 选中 1 条记录时修改按钮可用；选中 0 条或多条时禁用；
- 行末编辑图标打开同一编辑表单；
- `DRAFT`、`INVALID` 可编辑；
- `VALIDATED` 记录只有具备覆盖权限时可修改；
- 修改有效记录需填写原因，并生成版本或修正事件；
- 修改周期、医院、颗粒 ID、物料、批号、单位或数量后必须重新校验；
- 使用乐观锁版本，冲突时提示刷新。

### 12.3 删除与作废

- 工具栏删除支持批量选择；行级删除处理单条记录；
- 删除前二次确认并显示选中数量、状态和下游影响；
- `DRAFT`、`INVALID` 执行软删除；
- `VALIDATED` 只能作废，不能物理删除；
- 已进入已确认盘点、要货指导或财务流程时，需生成冲销事件或拒绝操作；
- 删除/作废原因必填，长度 2～200 字；
- 操作结果返回成功数、失败数和逐条原因。

### 12.4 校验

校验范围：

1. 已选择记录时校验选择项；
2. 未选择记录时提示是否校验当前查询结果；
3. 超过 5,000 条转异步任务；
4. 校验医院权限、周期、颗粒 ID、物料映射、单位、批号、数量、重复及周期重叠；
5. 通过后状态改为 `VALIDATED`；失败改为 `INVALID` 并保存问题明细；
6. 校验本身不擅自修改原始输入；
7. 自动修正只能处理去空格、标准日期格式等无业务歧义问题，并记录修正日志。

### 12.5 导入

#### 12.5.1 文件规范

- 默认支持 `.xlsx`，可选支持 UTF-8 BOM `.csv`；
- 最大文件 20 MB；
- 单文件最大 50,000 行；
- 禁止宏、公式执行、外部链接和可执行内容；
- 文件名最长 200 字符；
- 服务端校验扩展名、MIME、内容结构和病毒风险。

#### 12.5.2 模板字段

```text
起始日期,截止日期,医院名称,医院ID(可选),物料编码,批号,
药品名称(可选),颗粒ID,单位(可选),颗粒量,来源业务ID(可选)
```

医院名称、药品名称和单位是辅助校验字段，服务端仍以主数据映射为准。

#### 12.5.3 导入流程

```text
下载模板 → 上传文件 → 解析预览 → 逐行校验 → 查看错误/警告
→ 下载错误明细 → 确认提交 → 再次校验 → 写入记录 → 返回结果
```

- 预览阶段不写正式销量台账；
- 同文件内完全重复行默认报错；
- 预览后提交时必须重新校验；
- 默认只写入通过阻断性校验的行；
- 是否将错误行保存为 `INVALID` 由配置决定；
- 相同文件重复提交必须幂等。

### 12.6 导出

- 导出当前筛选条件下的全部记录，不仅当前页；
- 默认 `.xlsx`，可兼容 `.csv`；
- 包含页面字段、校验状态、来源类型、来源批次、异常原因和版本；
- 文件中包含查询条件、导出时间、数据截止时间、单位说明和操作人；
- 颗粒量保留原始定点精度；
- 超过 50,000 行转异步任务；
- 下载地址默认 30 分钟失效；
- 记录导出审计日志。

## 13. 校验规则

### 13.1 错误分级

| 级别 | 含义 | 是否阻断 | 示例 |
|---|---|---:|---|
| `ERROR` | 数据不能生效 | 是 | 医院无权限、颗粒 ID 不存在、周期重叠、负数 |
| `WARNING` | 可保存但需要关注 | 默认否 | 零销量、历史补录、缺少批号但未启用批次扣减 |
| `INFO` | 信息提示 | 否 | 已自动去除首尾空格 |

### 13.2 主要异常场景

| 场景 | 默认处理 | 错误码 |
|---|---|---|
| 起始日期晚于截止日期 | 阻断 | `TS_DATE_RANGE_INVALID` |
| 周期超过最大天数 | 阻断 | `TS_PERIOD_TOO_LONG` |
| 医院无权限 | 阻断并返回 403 | `TS_HOSPITAL_FORBIDDEN` |
| 颗粒 ID 无映射 | 阻断 | `TS_PARTICLE_NOT_FOUND` |
| 颗粒 ID 与物料不一致 | 阻断 | `TS_MATERIAL_MISMATCH` |
| 单位缺失或冲突 | 阻断 | `TS_UNIT_INVALID` |
| 颗粒量为负或超精度 | 阻断 | `TS_QUANTITY_INVALID` |
| 颗粒量为 0 | 默认警告 | `TS_ZERO_SALES` |
| 完全重复记录 | 阻断 | `TS_DUPLICATE_RECORD` |
| 统计周期重叠 | 默认阻断 | `TS_PERIOD_OVERLAP` |
| 批号不存在 | 按批次策略阻断/警告 | `TS_BATCH_NOT_FOUND` |
| 销量超过批次可用量 | 默认阻断 | `TS_BATCH_STOCK_INSUFFICIENT` |
| 记录已被下游引用 | 禁止直接删除 | `TS_ALREADY_REFERENCED` |
| 乐观锁冲突 | 返回 409 | `TS_VERSION_CONFLICT` |

## 14. 数据库设计

### 14.1 销量主表 `terminal_sales_record`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `start_date` | DATE | 起始日期 |
| `end_date` | DATE | 截止日期 |
| `hospital_id` | BIGINT | 医院 ID |
| `hospital_name` | VARCHAR(120) | 医院名称快照 |
| `material_id` | BIGINT | 物料 ID |
| `material_code` | VARCHAR(64) | 物料编码快照 |
| `batch_no` | VARCHAR(64) | 批号，可空策略由配置控制 |
| `drug_name` | VARCHAR(120) | 药品名称快照 |
| `particle_id` | VARCHAR(64) | 来源颗粒 ID |
| `unit_code` | VARCHAR(20) | 单位编码 |
| `unit_name` | VARCHAR(20) | 单位名称快照 |
| `particle_qty` | DECIMAL(18,3) | 销量颗粒量 |
| `standard_particle_qty` | DECIMAL(18,3) | 换算后的标准库存单位数量 |
| `data_mode` | VARCHAR(20) | `INCREMENTAL`/`SNAPSHOT` |
| `validation_status` | VARCHAR(20) | `DRAFT`/`VALIDATED`/`INVALID`/`VOID` |
| `validation_version` | VARCHAR(32) | 校验版本 |
| `warning_codes` | VARCHAR(1000) | 问题编码集合 |
| `source_type` | VARCHAR(20) | `MANUAL`/`IMPORT`/`API` |
| `source_system` | VARCHAR(64) | 来源系统 |
| `source_business_id` | VARCHAR(128) | 来源业务唯一 ID |
| `source_batch_id` | VARCHAR(64) | 导入/同步批次 |
| `source_row_no` | INT | 文件行号 |
| `dedupe_hash` | CHAR(64) | 防重哈希 |
| `version` | INT | 乐观锁版本 |
| `created_by`/`created_at` | BIGINT/DATETIME | 创建信息 |
| `updated_by`/`updated_at` | BIGINT/DATETIME | 更新信息 |
| `deleted_at` | DATETIME | 软删除时间 |

建议索引：

```text
uk_terminal_sales_source (source_system, source_business_id, deleted_at)
idx_terminal_sales_query (hospital_id, end_date, material_code, batch_no, deleted_at)
idx_terminal_sales_particle (particle_id, start_date, end_date, validation_status)
idx_terminal_sales_batch (hospital_id, material_id, batch_no, end_date)
idx_terminal_sales_import (source_batch_id, source_row_no)
```

来源业务 ID 为空时使用 `dedupe_hash` 做重复候选校验。MySQL 对可空唯一键的行为需在实现时明确，建议使用有效状态列或单独防重表，不依赖 `deleted_at=NULL` 的唯一性假设。

### 14.2 颗粒映射表 `particle_material_mapping`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `source_system` | VARCHAR(64) | 来源系统 |
| `particle_id` | VARCHAR(64) | 颗粒 ID |
| `material_id` | BIGINT | 物料 ID |
| `material_code` | VARCHAR(64) | 物料编码 |
| `unit_code` | VARCHAR(20) | 来源单位 |
| `conversion_factor` | DECIMAL(18,6) | 换算到标准库存单位的系数 |
| `effective_from` | DATE | 生效日期 |
| `effective_to` | DATE | 失效日期 |
| `version_no` | VARCHAR(32) | 配置版本 |
| `status` | VARCHAR(20) | 状态 |

同一来源系统、颗粒 ID 和业务日期只能命中一个有效映射版本。

### 14.3 导入批次与问题表

建议建立：

- `terminal_sales_import_batch`：文件名、SHA-256、行数、任务状态、处理结果和操作人；
- `terminal_sales_import_issue`：批次、行号、字段、原值、级别、错误码和修复建议；
- `terminal_sales_validation_issue`：销量记录的当前或历史校验问题。

### 14.4 与现有数据结构关系

- 复用医院主数据和操作审计日志；
- 终端销量使用独立流水表，不写入 `stocktake_detail`；
- `stocktake_detail` 是盘点/汇总快照，终端销量是原始业务输入；
- 盘点库存和要货指导按规则读取 `VALIDATED` 销量并保存来源批次和截止时间；
- 医院供货批次可用于验证销量批号和可用数量。

## 15. 接口设计

### 15.1 列表查询

```http
GET /api/terminal/terminal-sales
```

请求参数：`hospitalId`、`materialCode`、`batchNo`、`startDateFrom`、`endDateTo`、`particleId`、`status`、`page`、`pageSize`、`sortField`、`sortOrder`。

响应示例：

```json
{
  "items": [
    {
      "id": 1,
      "startDate": "2025-08-22",
      "endDate": "2025-12-25",
      "hospital": {
        "id": 1,
        "name": "河北北方学院第二附属医院"
      },
      "material": {
        "id": 2004,
        "code": "02.22.003.1882",
        "drugName": "白前"
      },
      "batchNo": "",
      "particleId": "4854",
      "unit": {
        "code": "G",
        "name": "克"
      },
      "particleQty": 27.37,
      "validationStatus": "VALIDATED",
      "sourceType": "IMPORT",
      "version": 1,
      "warnings": []
    }
  ],
  "summary": {
    "recordCount": 1,
    "particleQty": 27.37,
    "validatedCount": 1,
    "invalidCount": 0
  },
  "page": 1,
  "pageSize": 10,
  "total": 1,
  "queryMeta": {
    "quantityUnitDescription": "按记录单位展示，统计按标准库存单位换算",
    "dataAsOf": "2025-12-25"
  }
}
```

### 15.2 医院选项

```http
GET /api/terminal/terminal-sales/options/hospitals?keyword=河北
```

只返回当前用户有权限的启用医院。

### 15.3 颗粒/物料选项

```http
GET /api/terminal/terminal-sales/options/particles?keyword=4854&hospitalId=1
```

响应包含颗粒 ID、物料 ID、物料编码、药品名称、单位、映射版本及可选有效批次。

### 15.4 新增

```http
POST /api/terminal/terminal-sales
Idempotency-Key: <uuid>
Content-Type: application/json
```

```json
{
  "startDate": "2025-08-22",
  "endDate": "2025-12-25",
  "hospitalId": 1,
  "particleId": "4854",
  "materialId": 2004,
  "batchNo": "B2511001",
  "particleQty": 27.37,
  "dataMode": "INCREMENTAL"
}
```

服务端重新匹配物料、药品名称和单位，不信任前端提交的名称快照。

### 15.5 修改

```http
PUT /api/terminal/terminal-sales/{id}
```

请求体携带全部可编辑字段、`version` 和修改原因。版本冲突返回 409。

### 15.6 删除/作废

```http
DELETE /api/terminal/terminal-sales
```

```json
{
  "ids": [1, 2],
  "reason": "重复导入"
}
```

响应包含 `deletedCount`、`voidedCount`、`blockedCount` 和阻断原因。

### 15.7 校验

```http
POST /api/terminal/terminal-sales/validate
```

支持 `mode=IDS` 和 `mode=QUERY`。响应包含总数、通过数、警告数、失败数、问题列表和异步 `taskId`。

### 15.8 导入

```http
GET  /api/terminal/terminal-sales/import/template?format=xlsx
POST /api/terminal/terminal-sales/import/preview
POST /api/terminal/terminal-sales/import/commit
GET  /api/terminal/terminal-sales/import/{batchId}/issues
```

预览返回字段映射、总行数、有效行、警告行、错误行和逐行问题；提交时再次校验并保证幂等。

### 15.9 导出

```http
POST /api/terminal/terminal-sales/export
GET  /api/exports/tasks/{taskId}
```

小数据量同步返回文件，大数据量返回异步任务。

### 15.10 错误状态码

| HTTP 状态 | 含义 |
|---:|---|
| 400 | 参数格式、日期或数量错误 |
| 401 | 未登录 |
| 403 | 功能或医院数据权限不足 |
| 404 | 记录、医院、颗粒或物料不存在 |
| 409 | 重复记录、周期重叠、乐观锁或并发冲突 |
| 422 | 主数据或批次库存不满足生效条件 |
| 500 | 服务端异常 |

## 16. 下游模块关系

### 16.1 理论库存

默认公式：

```text
理论库存 = 期初库存 + 有效医院供货 - 有效终端销量 ± 其他调整
```

终端销量仅提供有效扣减量，理论库存模块负责按统计时点聚合。

### 16.2 盘点库存

盘点库存使用截止盘点日的有效销量计算账面库存。盘点快照应保存销量数据截止时间、来源批次和聚合版本。

### 16.3 要货指导

要货指导使用终端销量计算月均消耗、本月需求和补货量。有效销量发生新增、修改或作废时，应：

1. 生成销量变更事件；
2. 标记受影响医院、物料和日期范围为待重算；
3. 异步重算草稿结果，已确认结果不得静默覆盖；
4. 保留重算前后的规则版本和来源批次。

### 16.4 建议领域事件

- `TerminalSalesValidated`
- `TerminalSalesChanged`
- `TerminalSalesVoided`
- `TerminalSalesImportFinished`

事件包含记录 ID、医院、物料、颗粒 ID、批号、周期、变更前后数量、来源批次、操作人和版本。

## 17. 前端交互规范

### 17.1 按钮状态

| 按钮 | 无选择 | 选择 1 条 | 选择多条 |
|---|---|---|---|
| 新增 | 可用 | 可用 | 可用 |
| 修改 | 禁用 | 按记录状态可用 | 禁用 |
| 删除 | 禁用 | 按状态可用 | 按状态可用 |
| 导出 | 可用 | 可用 | 可用 |
| 导入 | 可用 | 可用 | 可用 |
| 校验 | 确认后校验当前查询 | 校验选中项 | 校验选中项 |

权限不足时对应按钮隐藏或禁用，但服务端仍需鉴权。

### 17.2 数值和文本

- 空值显示“—”，0 显示 `0`；
- 颗粒量最多 3 位小数，去除无意义尾零；
- 数量使用千分位，复制和导出保留原值；
- 超长医院、物料、批号和药名省略，悬浮显示完整值；
- 校验异常显示图标和文字，不能只使用颜色。

### 17.3 表单行为

- 选择颗粒 ID 后带出物料、药品名称和单位；
- 颗粒 ID 变化时清空原物料和批号，避免保留错误关联；
- 选择物料时只能展示匹配的颗粒 ID；
- 批号优先从医院供货有效批次中选择；
- 日期、数量错误在字段下方即时提示；
- 保存失败保留用户输入；
- 关闭未保存表单时二次确认。

### 17.4 分页

- 显示总条数，如“共 5145 条”；
- 默认每页 10 条，与截图一致；
- 支持页码、上一页、下一页、省略页和跳页；
- 切换页码不清空筛选条件；
- 选择状态默认只保留当前页，如支持跨页选择必须明确显示跨页数量。

## 18. 审计、安全与一致性

### 18.1 审计日志

新增、修改、删除/作废、校验、导入、导出及强制修正必须记录：

- 模块和操作类型；
- 记录 ID、业务键和来源批次；
- 修改前后字段摘要；
- 影响条数和结果；
- 失败原因；
- 操作人、IP、时间和 traceId。

不得在日志中记录患者或处方敏感信息；本模块本期也不应采集此类信息。

### 18.2 安全

- 所有接口按医院数据范围过滤；
- 批量 ID 混入无权限记录时不得忽略越权；
- SQL 使用参数绑定，排序字段使用白名单；
- 上传文件校验格式、MIME、大小和内容；
- 禁止执行 Excel 公式、宏和外部链接；
- 下载地址使用短时效签名；
- 错误响应不暴露 SQL、堆栈和服务器路径。

### 18.3 一致性

- 新增、修改、作废和导入提交使用事务；
- 有效销量变更和下游待重算事件必须在同一事务或可靠事件机制中完成；
- 校验和导入任务支持重试但不得重复入账；
- 历史记录保存校验版本、主数据映射版本和来源批次。

## 19. 非功能需求

### 19.1 性能

- 常规列表首屏目标不超过 2 秒，P95 不超过 4 秒；
- 支持至少 100 万条历史销量分页查询；
- 统计汇总在数据库或分析层完成，不把全量数据加载到前端；
- 50,000 行以内导入预览目标不超过 30 秒；
- 大数据导入、校验和导出使用异步任务；
- 医院及颗粒选项接口常用关键字 1 秒内返回。

### 19.2 可用性

- 网络失败保留列表和表单内容；
- 异步任务可在离开页面后继续运行；
- 任务状态支持 `PENDING`、`RUNNING`、`SUCCESS`、`PARTIAL_SUCCESS`、`FAILED`；
- 失败任务提供原因和安全重试入口；
- 系统统一使用 `Asia/Shanghai` 处理业务日期。

### 19.3 兼容性

- 支持 Chrome、Edge 最近两个大版本；
- 推荐分辨率 1366×768 及以上；
- 表格支持横向滚动和固定关键列；
- 支持键盘操作和清晰焦点；
- 状态不能只靠颜色表达。

## 20. 测试与验收标准

### 20.1 功能验收

1. 页面入口、面包屑、查询条件、工具栏和表格字段与截图的信息架构一致。
2. 医院、物料编码和批号组合查询正确。
3. 新增、修改、行级删除和批量删除状态符合权限规则。
4. 颗粒 ID 可以正确带出物料、药名和单位。
5. 表格分页、刷新、横向滚动、固定列和统计汇总正确。
6. 统计汇总计算当前查询全部有效记录，而非当前页。
7. 导入可以预览、校验、下载错误明细并幂等提交。
8. 校验通过记录才能进入理论库存和要货指导。
9. 导出字段、行数、数量、单位和查询条件与页面一致。
10. 作废有效销量后，下游结果被标记待重算，历史快照不被静默覆盖。

### 20.2 统计验收样例

给定当前筛选结果颗粒量：

```text
100、8.98、6.36、27.37、28、33.81、43.27、292.66、19.6
```

预期：

```text
颗粒量合计 = 560.05
```

验收应使用真实测试数据重新计算，不以截图中可能未完整显示的行数和 `578.8` 固定值为准。

### 20.3 日期与重复验收

| 场景 | 预期 |
|---|---|
| 起始日期等于截止日期 | 允许，表示单日销量 |
| 起始日期晚于截止日期 | 阻断 |
| 周期大于配置上限 | 阻断或管理员确认 |
| 完全相同业务键重复提交 | 只保留一条，返回重复提示 |
| 同颗粒 ID、批号周期交叉 | 默认 `TS_PERIOD_OVERLAP` 阻断 |
| 同一幂等键重试 | 返回首次处理结果，不重复写入 |

### 20.4 数量验收

- `27.37` 可按 `DECIMAL(18,3)` 保存为 `27.370`；
- 负数、科学计数法、NaN 和超精度被拒绝；
- 0 产生警告且默认不参与库存扣减；
- 不同单位的数据先按映射换算，再做汇总；
- 换算中间值至少保留 6 位，最终使用 `HALF_UP`。

### 20.5 导入验收

- 正确模板可以预览并提交；
- 缺表头、非法日期、未知颗粒 ID、单位冲突和非法数量按行报错；
- 同文件重复行、历史重复行和周期重叠均能识别；
- 预览后映射配置变化，提交时重新校验；
- 相同文件重复上传不重复入账；
- 部分成功时统计和错误文件准确。

### 20.6 权限与安全验收

- 无权限医院无法通过列表、ID、导出或导入查询和写入；
- 隐藏前端按钮后直接调用接口仍返回 403；
- 排序字段、批量 ID、文件名和特殊字符不能造成注入；
- 有效且已引用记录不能物理删除；
- 所有变更、导入、校验和导出存在审计日志。

## 21. 开发拆分建议

### 21.1 数据与后端

1. 确认颗粒 ID、物料、单位及批号的主数据映射。
2. 新建销量主表、颗粒映射表、导入批次和问题表。
3. 实现查询、分页、排序、全量统计和医院权限过滤。
4. 实现新增、修改、软删除/作废和乐观锁。
5. 实现周期、重复、重叠、单位、数量和批号库存校验。
6. 实现导入模板、预览、提交、错误文件和幂等。
7. 实现批量校验及异步任务。
8. 实现导出和短时效下载。
9. 实现下游待重算事件和来源版本追踪。
10. 编写计算、权限、并发、接口及仓储自动化测试。

### 21.2 前端

1. 新建独立终端销量页面和路由。
2. 实现医院、物料、批号查询及状态保持。
3. 实现表格、固定列、操作列、分页和统计行。
4. 实现新增/编辑表单及颗粒 ID 联动。
5. 实现批量和行级删除/作废。
6. 实现导入预览、问题筛选、错误下载和提交进度。
7. 实现批量校验、异步任务和结果提示。
8. 实现导出、权限按钮和异常状态展示。

### 21.3 测试

- 查询、分页、排序、汇总；
- CRUD、状态和下游引用；
- 日期周期、重复和重叠；
- 颗粒 ID、物料、单位和批号映射；
- 数量精度、换算和异常值；
- 导入、导出、校验、幂等和并发；
- 权限、审计、安全和性能；
- Chrome、Edge 和 1366×768 兼容性。

## 22. 待业务确认项

| 优先级 | 待确认项 | 影响 |
|---|---|---|
| P0 | 终端销量代表销售、处方消耗、领用还是结算消耗 | 影响模块定义和数据来源 |
| P0 | 起止日期内的数值是增量、累计值还是周期快照 | 影响重复、周期重叠和聚合算法 |
| P0 | 颗粒量的物理单位及标准库存单位 | 影响字段、换算和统计 |
| P0 | 颗粒 ID 的来源、格式、唯一范围和映射方式 | 影响主数据和唯一键 |
| P0 | 批号是否必填，是否必须按供货批次扣减 | 影响表单、校验和库存 |
| P0 | 颗粒量是否允许 0、小数及最多小数位 | 影响数据库和校验 |
| P0 | 导入保存即生效还是校验后生效 | 影响状态和下游计算 |
| P0 | 周期重叠是否一律禁止 | 影响重复计算风险 |
| P0 | 已被盘点/要货指导使用的销量如何修改或冲销 | 影响版本和审计 |
| P1 | 统计周期最大天数和历史补录范围 | 影响日期校验 |
| P1 | 无批号销量是否由系统按 FIFO/FEFO 自动分摊 | 影响批次明细 |
| P1 | 导入错误行是否保存为 `INVALID` | 影响导入流程 |
| P1 | 零销量是否需要保存 | 影响统计和数据完整性 |
| P1 | 是否需要来源业务 ID、处方汇总号或同步批次 | 影响幂等和追溯 |
| P1 | 导出上限、保留时间和水印 | 影响导出实现 |
| P2 | 是否需要日明细、趋势图和异常波动预警 | 影响后续范围 |

## 23. 发布前检查清单

- [ ] P0 业务口径全部确认并写入数据字典；
- [ ] 颗粒 ID、物料、单位和批号映射可用；
- [ ] 销量、导入、问题表及索引创建完成；
- [ ] 页面字段、按钮和分页与截图逐项核对；
- [ ] 服务端权限和医院数据范围测试通过；
- [ ] 日期、重复、周期重叠、数量和单位测试通过；
- [ ] 导入预览、提交、错误文件和幂等测试通过；
- [ ] 全量统计与导出结果一致；
- [ ] 有效销量变更可触发下游待重算；
- [ ] 审计日志、异步任务和下载安全验证通过；
- [ ] 性能及浏览器兼容性达到要求；
- [ ] 业务用户完成 UAT。

