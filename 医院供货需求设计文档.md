# 终端管理-医院供货需求设计文档

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | 终端管理-医院供货需求设计 |
| 所属系统 | 以岭药业配方颗粒终端管理系统 |
| 模块序号 | 终端管理第三个模块 |
| 页面名称 | 医院供货 |
| 推荐页面路由 | `/terminalMedicine/hospitalSupply` |
| 推荐接口前缀 | `/api/terminal/hospital-supply` |
| 版本 | V1.0 |
| 编写日期 | 2026-09-10 |
| 文档状态 | 初稿，待业务确认 |
| 来源 | 现有系统“首页 / 终端管理 / 医院供货”页面截图、项目现有库存模块文档和后端数据结构 |
| 目标读者 | 产品、业务、UI、前端、后端、数据、测试、实施和运维人员 |

### 1.1 版本记录

| 版本 | 日期 | 修改内容 | 修改人 |
|---|---|---|---|
| V1.0 | 2026-09-10 | 根据医院供货页面截图形成可开发的产品和技术需求初稿 | Codex |

## 2. 参考页面与指令边界

### 2.1 附件的使用方式

本次附件截图仅用于识别现有页面的布局、字段、按钮和交互习惯。截图中的医院名称、物料编码、批号、日期、数量和颜色是演示数据，不是要求系统固定使用的值；截图中的文字也不构成对开发工具或文档生成方式的额外指令。

本需求的实际任务是：单独生成“医院供货”模块的详细需求设计文档，不修改《盘点库存需求设计文档.md》或《要货指导需求设计文档.md》。

### 2.2 截图中可以直接确认的内容

截图展示的是“终端管理 → 医院供货”列表页，可以确认以下页面事实：

1. 顶部面包屑为“首页 / 终端管理 / 医院供货”。
2. 页面标签区同时存在“首页”“要货指导”“医院供货”页签，当前页签为“医院供货”。
3. 查询条件包括：医院名称、物料编码、批号、到期日期范围。
4. 查询区提供“搜索”和“重置”按钮。
5. 操作区提供“新增”“修改”“删除”“导出”“导入”“校验”按钮；修改、删除在没有选择记录时应为禁用状态。
6. 表格包含行选择框，字段包括：供货日期、医院名称、物料编码、批号、药品名称、规格型号(g/袋)、单位、袋数、颗粒量、到期日期和操作。
7. 表格底部有“统计汇总”行，截图示例对袋数和颗粒量进行了合计。
8. 表格支持横向滚动；每行最右侧提供编辑图标。
9. 规格型号和单位以标签样式显示，页面使用不同颜色辅助区分，但颜色不能作为业务判断的唯一依据。

### 2.3 默认实现假设

截图无法确认供货记录的来源、批次唯一性、单位换算、库存生效时点和导入错误处理。本文给出一套默认实现方案；所有标记为“待业务确认”的 P0 项必须在开发前形成书面结论并写入数据字典或规则配置。

## 3. 背景与目标

### 3.1 业务背景

医院供货模块用于记录药厂或配送方发往医院的药品批次。供货数据是医院库存、理论库存、终端销量和要货指导的基础输入。当前页面需要同时支持人工新增、批量导入、修改、删除、校验、导出和按批号/到期日查询。

如果供货日期、批号、规格、袋数或到期日期不准确，后续会出现以下问题：

- 医院库存和理论库存计算错误；
- 同一批次重复入账；
- 过期批次继续被当作可用库存；
- 供货数据无法追溯到来源文件和操作人；
- 盘点库存和要货指导使用的数据口径不一致。

### 3.2 建设目标

1. 建立医院、物料、批号、供货日期和到期日期的统一供货台账。
2. 将规格型号、单位、袋数和颗粒量的关系固化为可验证规则。
3. 支持单条录入和 Excel/CSV 批量导入，导入前提供预校验和错误明细。
4. 以服务端为唯一计算和校验中心，避免前端和后端口径不一致。
5. 为医院库存、盘点库存和要货指导提供可追溯、可重算的供货数据。
6. 通过权限、软删除、版本和审计日志保证数据安全及可追溯。

### 3.3 成功标准

- 同一条供货记录在新增、导入、查询、导出和下游库存计算中的数量一致；
- 对每个批次能够追溯医院、物料、供货日期、到期日期、来源批次和操作记录；
- 无权限用户无法查询、导出或修改其他医院的数据；
- 重复导入具有幂等结果，不产生重复供货；
- 关键字段校验、批次重复校验和数量计算均有自动化测试覆盖。

## 4. 范围

### 4.1 本期范围

- 医院供货列表页；
- 医院、物料编码、批号、到期日期范围查询；
- 供货记录新增、单条修改、批量删除/作废；
- Excel/CSV 模板下载、导入预览、校验、提交和错误结果下载；
- 当前筛选结果导出；
- 规格、单位、袋数和颗粒量计算与校验；
- 到期状态、批次重复、主数据缺失和数据异常提示；
- 服务端分页、排序、统计汇总、权限、审计和幂等；
- 向医院库存、盘点库存和要货指导提供标准供货数据接口。

### 4.2 本期不包含

- 采购订单、出库单、物流单或签收单的完整业务流程；
- 供货审批、退货审批和财务结算流程；
- 药品、医院、单位和规格主数据的完整维护；
- 移动端扫码入库；
- 自动生成要货申请或采购订单；
- 复杂批次追踪、序列号管理和冷链温度管理；
- 机器学习需求预测。

### 4.3 后续可扩展

- 关联供货单号、配送单号、签收单和发票；
- 医院签收确认、退货和冲销；
- 批次库存明细及先进先出/近效期先出；
- 供货记录转入库单；
- 手工调整颗粒量并要求双人复核；
- 供货异常看板和近效期预警。

## 5. 角色与权限

### 5.1 角色权限矩阵

| 角色 | 查询 | 新增 | 修改 | 删除/作废 | 导入 | 校验 | 导出 | 数据范围 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| 系统管理员 | 是 | 是 | 是 | 是 | 是 | 是 | 是 | 全部医院 |
| 区域/业务管理员 | 是 | 是 | 是 | 按授权 | 是 | 是 | 是 | 授权区域医院 |
| 医院供货人员 | 是 | 是 | 是 | 否/本人草稿 | 是 | 是 | 可配置 | 所属医院 |
| 仓储/库存人员 | 是 | 可配置 | 可配置 | 否 | 可配置 | 是 | 是 | 授权医院 |
| 财务/结算人员 | 是 | 否 | 否 | 否 | 否 | 否 | 是 | 授权医院 |
| 只读用户 | 是 | 否 | 否 | 否 | 否 | 否 | 可配置 | 授权范围 |

### 5.2 权限编码

- `terminal:hospital-supply:view`：查看列表、统计和详情；
- `terminal:hospital-supply:create`：新增供货记录；
- `terminal:hospital-supply:update`：修改供货记录；
- `terminal:hospital-supply:delete`：删除草稿或作废已生效记录；
- `terminal:hospital-supply:import`：导入供货文件；
- `terminal:hospital-supply:validate`：执行批量校验；
- `terminal:hospital-supply:export`：导出查询结果；
- `terminal:hospital-supply:override`：后续版本允许强制修正已生效记录。

服务端必须校验权限和医院数据范围。前端隐藏或禁用按钮只用于改善交互，不能作为安全控制。

## 6. 页面入口与布局

### 6.1 页面入口

- 左侧导航：终端管理 → 医院供货；
- 页面路径：`/terminalMedicine/hospitalSupply`；
- 面包屑：首页 / 终端管理 / 医院供货；
- 接口前缀：`/api/terminal/hospital-supply`。

### 6.2 页面区域

1. **面包屑和页签区**：显示当前模块以及已打开页签。
2. **查询区**：医院名称、物料编码、批号、到期日期范围、搜索、重置。
3. **操作区**：新增、修改、删除、导出、导入、校验；右侧保留表格搜索/刷新入口。
4. **数据区**：行选择、多列展示、横向滚动、行编辑入口。
5. **统计区**：对当前筛选条件下的全部记录统计袋数和颗粒量。
6. **分页区**：总条数、当前页、每页条数、页码和跳转。

### 6.3 页面结构示意

```text
首页 / 终端管理 / 医院供货

[医院名称 ▼] [物料编码] [批号] [开始日期 - 结束日期] [搜索] [重置]

[新增] [修改] [删除] [导出] [导入] [校验]                  [表格搜索] [刷新]

┌选择┬供货日期*┬医院名称*┬物料编码*┬批号*┬药品名称*┬规格型号(g/袋)*┬单位*┬袋数*┬颗粒量*┬到期日期*┬操作┐
│ □  │          │         │         │      │          │                 │      │      │       │          │编辑│
├────┼──────────┼─────────┼─────────┼──────┼──────────┼─────────────────┼──────┼──────┼───────┼──────────┼────┤
│ □  │ ...      │ ...     │ ...     │ ...  │ ...      │ ...             │ ...  │ ...  │ ...   │ ...      │ ✎  │
├────┴──────────┴─────────┴─────────┴──────┴──────────┴─────────────────┴──────┴──────┴───────┴──────────┴────┤
│                         统计汇总                 袋数合计：...       颗粒量合计：...                          │
└─────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

### 6.4 固定列与滚动

- 左侧固定选择列、供货日期、医院名称、物料编码；
- 其余字段支持横向滚动；
- 底部统计汇总行固定在表格底部；
- 最小推荐分辨率为 1366×768，不能因窗口变窄而隐藏批号和到期日期；
- 长医院名称、批号和物料编码省略显示，悬浮提示显示完整值。

## 7. 查询条件

### 7.1 查询字段

| 页面字段 | API 参数 | 控件 | 必填 | 校验和行为 |
|---|---|---|---:|---|
| 医院名称 | `hospitalId` | 可搜索下拉 | 否 | 只显示当前用户有权限的医院，提交医院 ID |
| 物料编码 | `materialCode` | 文本框 | 否 | 去首尾空格，默认模糊匹配，最长 64 字符 |
| 批号 | `batchNo` | 文本框 | 否 | 默认模糊匹配，最长 64 字符；保留字母、数字、`-`、`/`、`.` 等常用字符 |
| 到期日期开始 | `expiryStartDate` | 日期选择器 | 否 | `YYYY-MM-DD`，包含当天 |
| 到期日期结束 | `expiryEndDate` | 日期选择器 | 否 | `YYYY-MM-DD`，包含当天，不能早于开始日期 |
| 页码 | `page` | 分页器 | 否 | 最小值 1，默认 1 |
| 每页条数 | `pageSize` | 分页器 | 否 | 默认 20，允许 20/50/100/200，最大 200 |
| 排序字段 | `sortField` | 表头触发 | 否 | 只允许服务端白名单字段 |
| 排序方向 | `sortOrder` | 表头触发 | 否 | `asc` 或 `desc`，默认 `desc` |

### 7.2 查询规则

1. 多个条件之间使用 AND 关系。
2. 不输入任何条件时，按用户权限返回未作废的全部记录。
3. 默认排序为供货日期倒序、到期日期升序、医院名称升序、ID 倒序。
4. 物料编码或批号输入框按回车可以触发搜索。
5. 点击搜索后回到第 1 页；查询按钮进入 loading，避免重复提交。
6. 点击重置后清空条件、恢复默认排序并回到第 1 页。
7. 无结果时显示空状态，并保留当前查询条件。
8. 到期日期筛选为闭区间；开始和结束日期相同表示查询当天到期的记录。
9. 默认列表不显示 `VOID` 记录；具备审计查看权限的用户可通过状态过滤查看历史作废记录。

### 7.3 医院下拉

- 支持远程关键字搜索；
- 输入至少 1 个字符后发起请求；
- 每次最多返回 100 条；
- 页面显示医院名称，提交医院 ID；
- 服务端再次根据用户权限过滤，不能信任前端提交的医院 ID。

## 8. 表格字段设计

字段以截图中的列名为准，API 使用英文驼峰命名，数据库使用下划线命名。带 `*` 的字段为业务必填字段。

### 8.1 供货记录字段

| 页面列 | API 字段 | 数据库字段 | 类型 | 必填 | 展示/校验规则 |
|---|---|---|---|---:|---|
| 选择 | `selected` | - | Boolean | - | 当前页选择；表头复选框只全选当前页 |
| 供货日期* | `supplyDate` | `supply_date` | Date | 是 | `YYYY-MM-DD`；不能晚于系统允许的回溯上限 |
| 医院名称* | `hospitalName` | `hospital_name` | String/快照 | 是 | 来源于医院主数据；最长 120 字符 |
| 医院 ID | `hospitalId` | `hospital_id` | Long | 是 | 前端不展示或隐藏；服务端权限校验 |
| 物料编码* | `materialCode` | `material_code` | String | 是 | 原样保留前导零；最长 64 字符 |
| 物料 ID | `materialId` | `material_id` | Long | 是 | 来源于物料主数据 |
| 批号* | `batchNo` | `batch_no` | String | 是 | 最长 64 字符；不允许空白批号 |
| 药品名称* | `drugName` | `drug_name` | String/快照 | 是 | 由物料主数据带出；最长 120 字符 |
| 规格型号(g/袋)* | `specQtyPerBag` | `spec_qty_per_bag` | Decimal(18,3) | 是 | 每袋的规格数量；大于 0；默认展示 0～3 位小数 |
| 单位* | `unitCode`/`unitName` | `unit_code`/`unit_name` | Enum/String | 是 | 如 `G/克`；来自单位字典，不允许任意输入 |
| 袋数* | `bagCount` | `bag_count` | Decimal(18,3) | 是 | 默认要求大于 0；是否允许小数袋由规则配置决定 |
| 颗粒量* | `particleQty` | `particle_qty` | Decimal(18,3) | 是 | 服务端按公式计算；页面默认只读 |
| 到期日期* | `expiryDate` | `expiry_date` | Date | 是 | `YYYY-MM-DD`；原则上晚于供货日期 |
| 校验状态 | `validationStatus` | `validation_status` | Enum | 是 | `DRAFT`、`VALIDATED`、`INVALID`、`VOID`；截图不增加可见列，可用图标/详情展示 |
| 到期状态 | `expiryStatus` | - | Enum | - | `NORMAL`、`EXPIRING_SOON`、`EXPIRED`，按查询时点计算 |
| 操作 | `actions` | - | - | - | 编辑图标；是否可编辑由状态和权限决定 |

### 8.2 单位和“颗粒量”的约定

截图使用“颗粒量”列，但规格显示为 `g/袋`、单位显示为“克”。因此本模块统一约定：

- `particleQty` 是按供货记录的计量单位保存的数量；当前单位为克时，它的物理含义是颗粒克数；
- 页面继续沿用“颗粒量”作为业务显示名称，实际单位必须在表头、详情和导出文件中说明；
- 不允许把“颗粒量”在不同接口中一会儿当作克数、一会儿当作颗粒个数；
- 若后续支持毫升、袋或其他单位，必须通过单位字典和换算规则扩展，不得在前端硬编码。

### 8.3 统计汇总行

统计汇总针对当前筛选条件下的全部有效记录，不是当前页小计。默认汇总：

- `bagCount`：袋数合计；
- `particleQty`：颗粒量合计；
- 可选返回 `recordCount`、`validatedCount`、`invalidCount`、`expiredCount`，用于结果提示；
- 供货日期、医院名称、物料编码、批号、药品名称、规格、单位和到期日期不合计。

截图中的示例合计“袋数 33.0、颗粒量 4000.0”只用于确认统计位置和字段，不作为真实数据验收值。

## 9. 数据来源与业务粒度

### 9.1 最小业务粒度

一条供货记录默认代表一个医院、一个物料、一个批号、一个供货日期和一个到期日期下的一次供货数量：

```text
医院 ID + 物料 ID + 批号 + 供货日期 + 到期日期
```

如果同一批号在同一天分多次供货，系统需要一个来源单号或允许拆分标识。当前截图没有单号字段，V1 默认将完全相同的业务键视为重复候选并提示合并或确认。

### 9.2 数据来源

| 数据 | 推荐来源 | 读取/写入时点 | 缺失处理 |
|---|---|---|---|
| 医院信息 | `hospital` 医院主数据 | 新增、导入和查询 | 医院不存在或停用时拒绝新增 |
| 物料编码和药品名称 | 物料主数据 | 新增、导入和校验 | 编码不存在时行校验失败 |
| 规格型号和单位 | 物料规格/单位配置 | 新增、导入和校验 | 配置缺失时不能自动按 0 处理 |
| 批号 | 供货来源文件或人工录入 | 新增/导入 | 为空时拒绝 |
| 供货日期 | 来源单据或人工录入 | 新增/导入 | 为空或格式错误时拒绝 |
| 袋数 | 来源单据或人工录入 | 新增/导入 | 非正数或超精度时拒绝 |
| 颗粒量 | 服务端计算 | 保存时 | 与公式不一致时拒绝或按规则自动修正 |
| 到期日期 | 来源单据或人工录入 | 新增/导入 | 早于供货日期时拒绝/告警 |

### 9.3 字段快照

供货记录保存医院名称、药品名称、规格和单位的快照，保证主数据后来变更时历史台账仍可还原。查询时可以同时返回当前主数据版本和记录快照差异。

## 10. 核心业务规则

### 10.1 主数据匹配

1. 医院必须为启用状态且在当前用户授权范围内。
2. 物料编码必须唯一匹配一个有效物料；模糊搜索只能用于选择，保存时必须提交物料 ID。
3. 物料编码选择后自动带出药品名称、规格型号和单位；前端不得让用户任意修改药品名称来绕过主数据校验。
4. 如果业务允许同一物料存在多种规格，规格必须按物料 ID、规格版本和供货日期匹配有效配置。
5. 物料规格配置变更后，新记录使用新版本，历史记录保留原规格快照。

### 10.2 数量计算

默认计算公式：

```text
颗粒量 = 袋数 × 规格型号(g/袋)
```

计算规则：

- 中间计算使用 `BigDecimal`，至少保留 6 位小数；
- 最终保存按数据库精度 `DECIMAL(18,3)` 使用 `HALF_UP` 舍入；
- 如果导入文件提供了颗粒量，服务端仍必须重新计算；
- 导入值与计算值的差异大于 `0.001` 时，默认判定为错误，不允许直接入账；
- 若业务选择“系统自动修正”策略，预览中必须同时展示原值、计算值和修正结果，并记录修正日志；
- 页面显示时去除无意义尾零，但导出保留计算后的定点数值。

### 10.3 袋数规则

- `bagCount` 必须大于 0；
- 默认不允许负数、NaN、Infinity、科学计数法和超出数据库精度的值；
- 默认 `allowFractionalBag=false`，袋数必须为整数；若业务允许拆零供货，配置开启后最多 3 位小数；
- 人工录入和导入均使用同一校验规则；
- 删除供货记录不能通过录入 0 袋替代。

### 10.4 日期规则

- `supplyDate` 和 `expiryDate` 使用系统时区 `Asia/Shanghai` 解析；
- 默认 `expiryDate >= supplyDate`，同日到期显示高风险告警；是否禁止同日到期列为 P0 待确认；
- 供货日期不能晚于当前日期加上配置的未来日期上限，默认不允许未来供货；
- 历史补录允许回溯的最大天数由 `supplyBackfillDays` 配置，默认 366 天；
- 查询到期日期为闭区间；
- 供货日期、到期日期均不包含时分秒，避免跨时区重复。

### 10.5 批号规则

- 批号不能为空、不能只包含空格，长度 1～64 个字符；
- 保存时去除首尾空格，保留中间字符和大小写；
- 是否大小写不敏感由规则配置决定，默认比较时转大写；
- 同一医院、物料、批号、供货日期、到期日期和规格完全一致的记录视为重复候选；
- 重复候选默认阻止新增和导入提交，用户可以查看已存在记录并选择取消；
- 如果确实存在分批供货，必须通过来源单号/拆分标识区分，不能靠修改批号绕过。

### 10.6 到期状态

到期状态按查询时点 `asOfDate` 动态计算，不直接把状态永久写死：

```text
EXPIRED       = expiryDate < asOfDate
EXPIRING_SOON = asOfDate <= expiryDate <= asOfDate + warningDays
NORMAL        = expiryDate > asOfDate + warningDays
```

默认 `warningDays=90`，可按物料或区域配置。展示建议：

- `NORMAL`：中性色；
- `EXPIRING_SOON`：橙色并显示“近效期”；
- `EXPIRED`：红色并显示“已过期”；

颜色必须同时配合文字或图标。过期记录可查询和审计，但是否计入当前可用库存由库存模块的生效规则决定，医院供货模块不得物理删除。

### 10.7 供货生效规则

默认只有 `VALIDATED` 记录进入下游供货汇总：

```text
指定统计日的有效供货量
= 供货日期 <= 统计日
  且记录状态 = VALIDATED
  且未作废
```

是否在统计日之后排除已经到期批次，需要与医院库存模块确认。建议默认：库存可用量在 `expiryDate` 之后不再增加，但历史供货累计仍可在追溯报表中查看。

### 10.8 重复和幂等

- 手工新增使用业务键和幂等键双重保护；
- 文件导入使用文件 SHA-256、导入批次号和行哈希防止重复提交；
- 同一请求重复发送时返回第一次的处理结果，不重复插入；
- 并发新增使用数据库唯一索引或分布式锁；
- 修改使用 `version` 乐观锁，版本不一致返回 409，要求用户刷新后再编辑。

## 11. 校验规则与错误分级

### 11.1 校验时机

1. 前端输入时：格式、长度、必填、日期先后关系。
2. 保存时：服务端再次执行所有字段校验、权限校验和主数据匹配。
3. 点击“校验”时：执行批次重复、规格计算、来源一致性、近效期和下游引用检查。
4. 导入预览时：不落正式业务数据，返回逐行逐字段校验结果。
5. 导入提交时：在事务中重复校验，防止预览后主数据或重复记录发生变化。

### 11.2 错误和警告

| 级别 | 含义 | 示例 | 是否阻断 |
|---|---|---|---:|
| ERROR | 数据不能进入有效供货台账 | 医院不存在、袋数为负、物料不存在、颗粒量不一致 | 是 |
| WARNING | 数据可保存但需要业务关注 | 90 天内到期、历史补录、同批号不同规格 | 默认否，是否允许提交可配置 |
| INFO | 提示性信息 | 已使用最新规格版本、已自动去除首尾空格 | 否 |

### 11.3 建议错误码

- `HS_REQUIRED_FIELD_MISSING`：必填字段缺失；
- `HS_HOSPITAL_FORBIDDEN`：医院不在权限范围；
- `HS_HOSPITAL_NOT_FOUND`：医院不存在或已停用；
- `HS_MATERIAL_NOT_FOUND`：物料编码不存在；
- `HS_MATERIAL_CONFIG_MISSING`：规格/单位配置缺失；
- `HS_BATCH_INVALID`：批号格式或长度错误；
- `HS_QUANTITY_INVALID`：袋数不是合法正数；
- `HS_PARTICLE_MISMATCH`：颗粒量与袋数、规格不一致；
- `HS_DATE_INVALID`：供货日期和到期日期关系错误；
- `HS_DUPLICATE_RECORD`：重复供货记录；
- `HS_VERSION_CONFLICT`：记录已被其他人修改；
- `HS_ALREADY_REFERENCED`：记录已被下游流程引用；
- `HS_IMPORT_DUPLICATE`：导入批次重复提交；
- `HS_SOURCE_STALE`：来源数据超过新鲜度阈值。

## 12. 状态模型

### 12.1 供货记录状态

| 状态 | 含义 | 默认列表展示 | 可修改 | 可删除/作废 | 是否进入下游库存 |
|---|---|---:|---:|---:|---:|
| `DRAFT` | 新增或导入后尚未完成业务校验 | 是 | 是 | 是 | 否 |
| `VALIDATED` | 通过全部阻断性校验，可以参与库存汇总 | 是 | 按权限 | 已引用时只能作废 | 是 |
| `INVALID` | 校验失败，保留失败原因等待修正 | 是 | 是 | 是 | 否 |
| `VOID` | 已软删除/作废，不参与默认查询 | 否 | 否 | 否 | 否 |

### 12.2 导入任务状态

| 状态 | 含义 |
|---|---|
| `UPLOADED` | 文件已上传，等待预览 |
| `PREVIEWED` | 已完成预览校验，等待提交 |
| `PROCESSING` | 正在写入有效记录 |
| `SUCCESS` | 全部有效行已写入 |
| `PARTIAL_SUCCESS` | 部分有效行写入，部分行错误 |
| `FAILED` | 任务失败，未完成写入 |
| `CANCELLED` | 用户取消或任务超时 |

## 13. 操作功能设计

### 13.1 新增

#### 13.1.1 入口

点击“新增”打开抽屉或弹窗表单。新增按钮只在拥有 `create` 权限时显示/可用。

#### 13.1.2 表单字段

| 字段 | 控件 | 默认值 | 可编辑 | 说明 |
|---|---|---|---:|---|
| 供货日期 | 日期选择器 | 当前日期 | 是 | 受未来日期和回溯天数限制 |
| 医院名称 | 可搜索下拉 | 空 | 是 | 只能选择有权限且启用的医院 |
| 物料编码 | 可搜索下拉/输入联想 | 空 | 是 | 选择后带出物料 ID、药品名称、规格和单位 |
| 药品名称 | 文本/只读 | 自动带出 | 否 | 不允许与物料主数据不一致 |
| 批号 | 文本框 | 空 | 是 | 1～64 字符 |
| 规格型号(g/袋) | 数值/只读 | 自动带出 | 默认否 | 由物料规格版本确定 |
| 单位 | 标签/只读 | 自动带出 | 默认否 | 来源于单位字典 |
| 袋数 | 数值框 | 空 | 是 | 大于 0，默认整数 |
| 颗粒量 | 数值/只读 | 根据袋数计算 | 否 | 服务端重新计算 |
| 到期日期 | 日期选择器 | 空 | 是 | 不能早于供货日期 |

#### 13.1.3 保存行为

1. 点击保存先执行前端格式校验，再调用服务端创建接口。
2. 服务端重新读取医院、物料和规格配置，不接受前端直接提交的名称和计算结果作为可信值。
3. 保存成功后记录状态为 `DRAFT`，或按业务配置立即校验为 `VALIDATED`；默认使用 `DRAFT`，由“校验”按钮完成生效。
4. 保存成功后关闭表单、刷新列表并保留查询条件。
5. 保存失败时保留用户输入，按字段显示错误，不清空表单。

### 13.2 修改

- 选中 1 条记录后“修改”按钮可用；选中 0 条或多条时禁用；
- 点击行末编辑图标等同于选中该行并打开编辑表单；
- `DRAFT`、`INVALID` 默认可编辑；`VALIDATED` 只有拥有 `override` 权限时可编辑；
- 修改已生效记录会产生新版本或修正事件，不能直接覆盖下游已经使用的历史快照；
- 供应日期、医院、物料、批号、规格、单位、袋数和到期日期任一变化都必须重新校验；
- 修改提交使用乐观锁 `version`，发现并发变更时返回 409 并提示刷新。

### 13.3 删除/作废

- 未选择记录时按钮禁用；
- 支持多选批量处理当前用户有权限的记录；
- 二次确认弹窗显示选中条数、状态分布和影响范围；
- `DRAFT`、`INVALID` 记录执行软删除；
- `VALIDATED` 且未被下游引用的记录可作废；
- 已被医院库存、盘点、要货指导或财务流程引用的记录不能物理删除，只能按权限作废并生成冲销/修正事件；
- 作废原因必填，长度 2～200 字符；
- 结果返回成功数、阻断数和逐条阻断原因；
- 所有删除/作废记录进入审计日志，保留原字段快照。

### 13.4 导入

#### 13.4.1 支持格式

- 默认支持 `.xlsx`；
- 可选支持带 UTF-8 BOM 的 `.csv`；
- 文件大小默认不超过 20 MB；
- 单文件最多 50,000 行，超过上限拒绝并提示拆分；
- 文件名最多 200 个字符，禁止路径穿越和可执行扩展名。

#### 13.4.2 导入流程

```text
下载模板 → 选择文件 → 上传 → 预览解析 → 逐行校验
      → 查看错误/警告 → 下载错误明细 → 确认提交 → 写入有效记录 → 返回结果
```

默认策略：预览不写入正式供货表；提交时只写入通过阻断性校验的行，错误行不入正式表，但保留在导入批次和错误明细中。若业务需要先导入后修改，可通过配置 `saveInvalidRows=true` 将错误行保存为 `INVALID`。

#### 13.4.3 导入列顺序

模板列顺序建议与页面一致：

```text
供货日期, 医院名称, 医院ID(可选), 物料编码, 批号, 药品名称(可选),
规格型号(g/袋)(可选), 单位(可选), 袋数, 颗粒量(可选), 到期日期
```

说明：

- 医院 ID、药品名称、规格型号、单位和颗粒量可以作为辅助列，但服务端以医院/物料主数据和公式重新计算为准；
- 模板首行是表头，不允许合并单元格；
- 日期支持 `YYYY-MM-DD`，不建议使用 Excel 本地化日期字符串；
- 空行跳过并计入跳过数量；
- 同一文件内重复业务键按错误处理，不自动合并数量。

#### 13.4.4 导入结果

导入结果至少返回：总行数、有效行数、错误行数、警告行数、跳过行数、写入数、重复数和 `importBatchId`。错误明细包含行号、字段名、原始值、错误码和修复建议。

### 13.5 校验

校验按钮的范围规则：

1. 选中记录时，只校验选中记录；
2. 未选中记录时，弹窗确认“校验当前查询结果”，确认后校验当前筛选条件下的全部记录；
3. 大于 5,000 条时转异步任务，页面显示进度；
4. 校验只更新状态、版本和问题明细，不改变用户输入的原始字段；
5. 成功后将无阻断错误的记录标为 `VALIDATED`，存在错误的记录标为 `INVALID`；
6. 校验结束后刷新列表和统计汇总，并保留查询条件；
7. 校验任务必须幂等，重复点击不能重复写入供货数据。

### 13.6 导出

- 导出当前筛选条件下的全部记录，不仅是当前页；
- 默认导出 `.xlsx`，必要时兼容 `.csv`；
- 导出列顺序与页面一致，并增加校验状态、到期状态、异常原因、导入批次号和计算版本；
- 导出文件第一行或说明页包含查询条件、导出时间、数据截止时间、单位说明和操作人；
- 数量和规格保留原始定点精度，显示格式不能破坏可计算性；
- 超过 50,000 行转异步任务；
- 下载链接使用短时效签名，默认 30 分钟失效；
- 记录导出人、筛选条件、文件名、行数和下载状态。

### 13.7 刷新和表格搜索

- 刷新只重新请求当前查询、排序、页码和页大小；
- 刷新期间保留旧数据并显示 loading，避免页面闪烁；
- 表格右侧放大镜可以打开列内快速搜索，但不得绕过服务端权限；
- 横向滚动位置在刷新后尽量保持不变；
- 列排序由服务端执行，不能只对当前页排序。

## 14. 接口设计

### 14.1 统一响应约定

建议统一返回：

```json
{
  "code": "OK",
  "message": "success",
  "data": {},
  "traceId": "..."
}
```

错误响应必须包含稳定错误码、用户可读消息和 `traceId`；不得把 SQL、堆栈或敏感数据返回给前端。

### 14.2 查询列表

```http
GET /api/terminal/hospital-supply
```

请求参数：

```text
hospitalId
materialCode
batchNo
expiryStartDate
expiryEndDate
status
page
pageSize
sortField
sortOrder
```

响应示例：

```json
{
  "items": [
    {
      "id": 1,
      "supplyDate": "2025-12-25",
      "hospital": {
        "id": 1,
        "name": "河北北方学院第二附属医院"
      },
      "material": {
        "id": 2001,
        "code": "02.22.003.1880",
        "drugName": "白茅根"
      },
      "batchNo": "B2511001",
      "specQtyPerBag": 100,
      "unit": {
        "code": "G",
        "name": "克",
        "display": "g/袋"
      },
      "bagCount": 10,
      "particleQty": 1000,
      "expiryDate": "2028-12-01",
      "validationStatus": "VALIDATED",
      "expiryStatus": "NORMAL",
      "version": 1,
      "warnings": []
    }
  ],
  "summary": {
    "recordCount": 1,
    "bagCount": 10,
    "particleQty": 1000,
    "validatedCount": 1,
    "invalidCount": 0,
    "expiredCount": 0
  },
  "page": 1,
  "pageSize": 20,
  "total": 1,
  "queryMeta": {
    "asOfDate": "2026-09-10",
    "quantityUnitDescription": "颗粒量按每条记录的单位保存，当前示例为克",
    "calculationVersion": "v1"
  }
}
```

### 14.3 医院选项

```http
GET /api/terminal/hospital-supply/options/hospitals?keyword=河北
```

响应：

```json
[
  { "id": 1, "hospitalName": "河北北方学院第二附属医院" }
]
```

### 14.4 物料选项

```http
GET /api/terminal/hospital-supply/options/materials?keyword=02.22
```

响应至少包含：

```json
[
  {
    "id": 2001,
    "materialCode": "02.22.003.1880",
    "drugName": "白茅根",
    "specQtyPerBag": 100,
    "unitCode": "G",
    "unitName": "克",
    "configVersion": "v3"
  }
]
```

### 14.5 新增

```http
POST /api/terminal/hospital-supply
Idempotency-Key: <uuid>
Content-Type: application/json
```

请求体：

```json
{
  "supplyDate": "2025-12-25",
  "hospitalId": 1,
  "materialId": 2001,
  "batchNo": "B2511001",
  "bagCount": 10,
  "expiryDate": "2028-12-01"
}
```

服务端根据 `hospitalId`、`materialId` 和供货日期读取名称、规格、单位并计算 `particleQty`。客户端提交的名称、规格、单位和颗粒量如果存在，只能作为展示/校验辅助，不能覆盖服务端主数据。

### 14.6 修改

```http
PUT /api/terminal/hospital-supply/{id}
Content-Type: application/json
```

请求体必须携带 `version`，示例：

```json
{
  "version": 1,
  "supplyDate": "2025-12-25",
  "hospitalId": 1,
  "materialId": 2001,
  "batchNo": "B2511001",
  "bagCount": 12,
  "expiryDate": "2028-12-01",
  "reason": "来源单据数量修正"
}
```

### 14.7 删除/作废

```http
DELETE /api/terminal/hospital-supply
Content-Type: application/json
```

请求体：

```json
{
  "ids": [1, 2, 3],
  "reason": "重复导入，保留正式供货记录"
}
```

响应：

```json
{
  "affectedCount": 2,
  "voidedCount": 1,
  "blockedCount": 0,
  "blockedReasons": []
}
```

### 14.8 校验

```http
POST /api/terminal/hospital-supply/validate
Content-Type: application/json
```

按 ID 校验：

```json
{
  "mode": "IDS",
  "ids": [1, 2, 3],
  "ruleVersion": "v1"
}
```

按当前查询校验：

```json
{
  "mode": "QUERY",
  "query": {
    "hospitalId": 1,
    "expiryStartDate": "2025-01-01",
    "expiryEndDate": "2028-12-31"
  },
  "ruleVersion": "v1"
}
```

响应包含：

```json
{
  "taskId": null,
  "totalCount": 3,
  "validatedCount": 2,
  "warningCount": 1,
  "invalidCount": 0,
  "issues": []
}
```

### 14.9 导入预览和提交

```http
POST /api/terminal/hospital-supply/import/preview
Content-Type: multipart/form-data
```

预览响应返回 `importBatchId`、表头映射、总行数、有效行数、错误行数、警告行数和逐行问题。

提交：

```http
POST /api/terminal/hospital-supply/import/commit
Content-Type: application/json
```

请求体：

```json
{
  "importBatchId": "HSI-20260910-00001",
  "confirmWarnings": true,
  "saveInvalidRows": false
}
```

提交接口必须重新执行校验并使用幂等锁，不能直接信任预览结果。

### 14.10 模板下载

```http
GET /api/terminal/hospital-supply/import/template?format=xlsx
```

模板需包含字段说明、示例行、日期格式、单位说明和必填标识；示例行不能被默认导入为正式数据。

### 14.11 导出

```http
POST /api/terminal/hospital-supply/export
Content-Type: application/json
```

请求体包含查询条件、排序、导出格式和是否包含作废记录。同步返回文件流，异步返回 `taskId`。

任务查询沿用：

```http
GET /api/exports/tasks/{taskId}
```

## 15. 数据库设计

### 15.1 供货主表 `hospital_supply_record`

建议字段如下：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `supply_date` | DATE | 供货日期 |
| `hospital_id` | BIGINT | 医院 ID |
| `hospital_name` | VARCHAR(120) | 医院名称快照 |
| `material_id` | BIGINT | 物料 ID |
| `material_code` | VARCHAR(64) | 物料编码快照 |
| `batch_no` | VARCHAR(64) | 批号 |
| `drug_name` | VARCHAR(120) | 药品名称快照 |
| `spec_qty_per_bag` | DECIMAL(18,3) | 每袋规格数量 |
| `unit_code` | VARCHAR(20) | 单位编码，如 `G` |
| `unit_name` | VARCHAR(20) | 单位名称快照，如“克” |
| `bag_count` | DECIMAL(18,3) | 袋数 |
| `particle_qty` | DECIMAL(18,3) | 服务端计算的颗粒量 |
| `expiry_date` | DATE | 到期日期 |
| `validation_status` | VARCHAR(20) | `DRAFT`/`VALIDATED`/`INVALID`/`VOID` |
| `validation_version` | VARCHAR(32) | 校验规则版本 |
| `warning_codes` | VARCHAR(1000) | 警告编码集合 |
| `source_type` | VARCHAR(20) | `MANUAL`/`IMPORT`/`API` |
| `source_batch_id` | VARCHAR(64) | 来源导入批次或外部批次 |
| `source_row_no` | INT | 来源文件行号 |
| `dedupe_hash` | CHAR(64) | 业务键哈希 |
| `version` | INT | 乐观锁版本 |
| `created_by`/`created_at` | BIGINT/DATETIME | 创建信息 |
| `updated_by`/`updated_at` | BIGINT/DATETIME | 更新信息 |
| `deleted_at` | DATETIME | 软删除时间 |

建议索引：

```text
uk_supply_business (hospital_id, material_id, batch_no, supply_date, expiry_date, deleted_at)
idx_supply_query (hospital_id, supply_date, material_code, batch_no, expiry_date, deleted_at)
idx_supply_expiry (expiry_date, validation_status, deleted_at)
idx_supply_source (source_batch_id, source_row_no)
```

如果业务允许同一业务键多次供货，唯一索引必须增加 `source_document_no` 或 `split_no`，不能简单删除唯一约束。

### 15.2 导入批次表 `hospital_supply_import_batch`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `batch_no` | VARCHAR(64) | 导入批次号 |
| `file_name` | VARCHAR(200) | 原始文件名 |
| `file_sha256` | CHAR(64) | 文件指纹 |
| `file_size` | BIGINT | 文件大小 |
| `status` | VARCHAR(20) | 导入任务状态 |
| `total_rows` | INT | 总行数 |
| `valid_rows` | INT | 有效行数 |
| `warning_rows` | INT | 警告行数 |
| `error_rows` | INT | 错误行数 |
| `written_rows` | INT | 实际写入行数 |
| `operator_id` | BIGINT | 操作人 |
| `created_at`/`finished_at` | DATETIME | 任务时间 |
| `error_file_path` | VARCHAR(500) | 错误明细文件位置 |

文件本体应保存到受控对象存储或文件服务，数据库只保存受控地址和摘要，不直接拼接用户输入路径。

### 15.3 导入问题表 `hospital_supply_import_issue`

保存导入批次、行号、字段名、原始值、错误级别、错误码、错误消息和建议。错误文件下载应由任务 ID 生成短时效地址。

### 15.4 校验问题表（可选）

如果需要保留每次校验历史，建议使用 `hospital_supply_validation_issue`，保存记录 ID、校验版本、字段、级别、编码、消息、是否已处理和创建时间；如果只需要当前问题，可将问题编码放在主表并保留最新审计日志。

### 15.5 与现有表的关系

当前项目已有 `hospital`、`material_unit_config`、`operation_audit_log`、`stocktake_summary` 和 `stocktake_detail`。医院供货模块建议：

- 复用 `hospital` 和审计日志；
- 不直接把供货记录写入 `stocktake_detail`，避免盘点快照与供货流水混用；
- 新建 `hospital_supply_record` 保存供货台账；
- 如果 `material_unit_config` 不能表达 `规格型号(g/袋)` 和单位，新增 `material_supply_spec_config` 或扩展物料主数据；
- 盘点库存和要货指导通过服务接口或物化汇总读取 `VALIDATED` 供货数据。

## 16. 与其他模块的关系

### 16.1 医院库存

医院库存可按统计日读取有效供货：

```text
医院供货颗粒量 = Σ(VALIDATED 且供货日期 <= 统计日的 particleQty)
```

退货、冲销和作废不能直接修改历史供货量，应通过反向业务事件或冲销记录体现。

### 16.2 盘点库存

盘点库存使用供货数据构建账面库存或核对库存链路。盘点汇总必须保存供货数据的截止时间、来源批次和计算版本，保证历史结果可复现。

### 16.3 要货指导

要货指导读取供货数据计算指导范围内的供货量和库存消耗。供货记录修改、校验状态变化或作废后，系统应：

1. 记录数据变更事件；
2. 标记受影响医院、物料和日期范围的库存/要货指导结果为“待重算”；
3. 按权限和任务策略异步重算或提示业务人员手工汇总；
4. 不静默覆盖已经确认的历史快照。

### 16.4 建议事件

可发布以下领域事件：

- `HospitalSupplyValidated`：供货记录通过校验；
- `HospitalSupplyChanged`：有效供货记录发生修改；
- `HospitalSupplyVoided`：供货记录作废；
- `HospitalSupplyImportFinished`：导入任务完成。

事件至少包含记录 ID、业务键、变更前后数量、供货日期、医院 ID、物料 ID、来源批次、操作人和计算版本。

## 17. 前端交互与展示规范

### 17.1 数值和日期

- 日期统一显示 `YYYY-MM-DD`；
- 空值显示“—”，0 显示 `0`；
- 规格、袋数和颗粒量默认最多 3 位小数，去除无意义尾零；
- 大数量使用千分位展示，但复制和导出保留原始数字；
- 颗粒量列表头或 tooltip 必须显示单位；
- 计算字段不可直接编辑，修改袋数或规格后实时预览颗粒量。

### 17.2 标签、颜色和异常

- 规格/单位标签可以使用主题色区分，但颜色不是数据值；
- 近效期使用橙色，已过期使用红色，并显示文字或图标；
- `INVALID` 记录显示告警图标，悬浮展示第一条错误；
- 同一行存在多个错误时，详情抽屉展示完整问题清单；
- 负数或异常数值不能仅用颜色表达。

### 17.3 按钮状态

| 按钮 | 无选择 | 选中 1 条 | 选中多条 | 权限不足 |
|---|---|---|---|---|
| 新增 | 可用 | 可用 | 可用 | 隐藏或禁用 |
| 修改 | 禁用 | 可用（按状态） | 禁用 | 隐藏或禁用 |
| 删除 | 禁用 | 可用（按状态） | 可用（按状态） | 隐藏或禁用 |
| 导出 | 可用 | 可用 | 可用 | 隐藏或禁用 |
| 导入 | 可用 | 可用 | 可用 | 隐藏或禁用 |
| 校验 | 校验当前查询需确认 | 校验选中行 | 校验选中行 | 隐藏或禁用 |

### 17.4 表单交互

- 医院和物料选择支持键盘输入和远程搜索；
- 物料变更后清空批号、袋数和颗粒量，避免沿用旧物料数据；
- 袋数输入后即时展示预计颗粒量，服务端返回结果后以服务端值为准；
- 日期关系错误在字段下方提示；
- 关闭未保存表单时二次确认；
- 保存失败不丢失已填数据；
- 编辑已生效记录时明确提示会影响下游重算。

### 17.5 导入预览

- 预览表格固定显示行号、原始值、解析值、校验状态和问题摘要；
- 支持按错误/警告筛选；
- 支持下载错误明细；
- 提交按钮显示“有效行数”和“警告行数”，警告提交需要确认；
- 预览超过前端渲染上限时采用虚拟列表或只展示前 1,000 行，完整结果以文件下载为准。

## 18. 审计、安全与一致性

### 18.1 审计日志

以下动作必须写入 `operation_audit_log` 或等价审计表：

- 新增、修改、删除/作废；
- 导入预览、导入提交和错误文件下载；
- 批量校验；
- 导出；
- 已生效记录的强制修正；
- 规则配置或单位配置变更。

日志至少保存：模块、操作类型、记录 ID/业务键、变更前摘要、变更后摘要、导入批次、影响数量、结果、失败原因、操作人、客户端 IP、时间和 traceId。敏感信息脱敏，避免把完整文件内容写入日志。

### 18.2 权限与越权防护

- 所有查询、详情、修改、删除、导出和导入提交都执行医院范围过滤；
- 不能通过修改 `hospitalId`、`id`、分页、导出条件或批量 ID 绕过权限；
- 批量 ID 中混入无权限记录时，默认整批拒绝或逐条返回阻断结果，策略需统一；
- 接口 SQL 使用参数绑定；排序字段通过白名单映射；
- 上传文件进行扩展名、MIME、大小、内容结构和病毒扫描校验；
- 导出文件使用短时效签名，禁止暴露服务器真实路径。

### 18.3 并发与事务

- 新增、修改和导入提交在事务中完成；
- 批量提交按行锁定或使用唯一索引防止重复；
- 修改使用乐观锁；
- 作废有效记录时同时写入审计和下游变更事件，失败则整体回滚；
- 大批量校验和导入使用任务表，任务状态变更可重试但不能重复写入。

## 19. 非功能需求

### 19.1 性能

- 普通列表查询首屏目标不超过 2 秒，95 分位不超过 4 秒；
- 支持至少 100 万条历史供货记录分页查询；
- 统计汇总使用数据库聚合，不把全量记录加载到前端；
- 物料、医院远程选项接口常用关键字响应目标不超过 1 秒；
- 50,000 行以内导入预览目标不超过 30 秒，超过时转异步；
- 大文件导出不得阻塞在线查询。

### 19.2 可用性

- 导入、校验、导出任务支持断线后查询状态；
- 任务失败显示可读原因并支持重试；
- 网络失败时保留表单和列表旧数据；
- 服务端时间、日期和单位规则统一，避免不同节点计算结果不同。

### 19.3 兼容性与可访问性

- 支持 Chrome、Edge 最近两个大版本；
- 推荐分辨率 1366×768 及以上；
- 表格必须支持横向滚动和固定关键列；
- 颜色不是唯一信息表达方式；
- 按钮、输入框、选择框、表格勾选和弹窗支持键盘操作。

## 20. 测试与验收标准

### 20.1 页面与功能验收

1. 页面面包屑、页签、查询区和工具栏与截图中的信息架构一致。
2. 查询可按医院、物料编码、批号、到期日期任意组合执行。
3. 供货日期、医院、物料编码、批号、药品名称、规格、单位、袋数、颗粒量和到期日期字段显示正确。
4. 多选、单选、编辑图标、分页、刷新、横向滚动和统计汇总正常。
5. 未选择数据时修改/删除按钮禁用；选择 1 条时修改可用；选择多条时删除和校验可用。
6. 统计汇总是全部查询结果的袋数和颗粒量合计，不是当前页合计。
7. 新增和修改时药品名称、规格、单位由物料主数据带出，颗粒量由服务端计算。
8. 导入预览可以展示逐行错误，提交后有效行入库，错误行可下载明细。
9. 校验通过的记录进入 `VALIDATED`，有阻断错误的记录进入 `INVALID`，作废记录不出现在默认列表。
10. 导出结果与当前筛选条件一致，包含单位、状态、校验版本和统计说明。

### 20.2 计算验收样例

给定：

```text
规格型号 = 100 g/袋
袋数 = 10
```

预期：

```text
颗粒量 = 100 × 10 = 1000 g
```

给定：

```text
规格型号 = 200 g/袋
袋数 = 4
```

预期：

```text
颗粒量 = 200 × 4 = 800 g
```

### 20.3 校验验收样例

| 输入场景 | 预期结果 |
|---|---|
| 物料编码不存在 | `HS_MATERIAL_NOT_FOUND`，不能保存/提交 |
| 医院不在权限范围 | 返回 403 或 `HS_HOSPITAL_FORBIDDEN`，不泄露记录是否存在 |
| 袋数为 0 或负数 | `HS_QUANTITY_INVALID`，阻断 |
| 袋数为 `1.2345` 且不允许小数袋 | 超精度/小数袋错误，阻断 |
| 颗粒量与规格×袋数相差超过 0.001 | `HS_PARTICLE_MISMATCH`，阻断 |
| 到期日期早于供货日期 | `HS_DATE_INVALID`，阻断 |
| 到期日期在 90 天内 | `EXPIRING_SOON`，产生警告；是否阻断按配置执行 |
| 到期日期早于查询日期 | `EXPIRED`，历史可查；是否进入库存由下游规则决定 |
| 同医院、物料、批号、供货日期、到期日期重复 | `HS_DUPLICATE_RECORD`，默认阻断 |
| 主数据规格版本冲突 | `HS_MATERIAL_CONFIG_MISSING` 或冲突错误，阻断 |

### 20.4 导入验收

- 正确模板可以预览并返回行数统计；
- 表头缺失、列名重复、日期格式错误、非法数值和未知物料有逐行错误；
- 重复上传同一文件不会重复写入；
- 预览通过后主数据发生变化，提交时会重新校验并阻断不一致行；
- 部分成功时成功行和失败行数量准确，失败明细可下载；
- 导入任务中断后重试不产生重复供货。

### 20.5 权限与安全验收

- 无权限医院无法通过列表、详情、导出、修改、删除或导入获取数据；
- 前端隐藏按钮后，直接调用接口仍会被服务端拦截；
- 不能通过排序字段、批量 ID、文件名或 SQL 特殊字符注入；
- 已被下游引用的有效记录不能被物理删除；
- 所有新增、修改、作废、导入、校验和导出都有审计日志。

### 20.6 性能验收

- 100 万条数据按医院、物料、批号、到期日期分页查询无全表扫描导致的超时；
- 50,000 行导入和导出可显示进度；
- 统计汇总与列表查询在大数据量下仍满足目标响应时间；
- 并发导入同一文件或并发新增同一业务键只生成一份有效记录。

## 21. 开发拆分建议

### 21.1 数据和后端

1. 确认医院、物料、规格、单位主数据字段及版本策略。
2. 新建 `hospital_supply_record`、导入批次和导入问题表。
3. 实现医院/物料远程选项接口和权限过滤。
4. 实现列表分页、服务端排序、到期状态计算和全量统计汇总。
5. 实现新增、修改、软删除/作废和乐观锁。
6. 实现颗粒量计算、重复批次校验和校验版本管理。
7. 实现文件上传、预览、错误明细、提交、幂等和异步任务。
8. 实现导出、下载签名和导出审计。
9. 发布供医院库存、盘点库存、要货指导消费的标准接口或领域事件。
10. 编写服务层单元测试、仓储测试、接口测试和并发测试。

### 21.2 前端

1. 新建医院供货页面路由和独立组件，不修改盘点库存页面组件。
2. 实现查询表单、医院/物料远程下拉、日期范围校验。
3. 实现多列表格、固定列、横向滚动、排序、分页和统计汇总。
4. 实现新增/编辑抽屉、自动带出主数据、颗粒量实时预览和状态提示。
5. 实现批量删除/作废、校验确认、结果反馈和错误明细查看。
6. 实现导入模板下载、文件上传、预览、错误筛选、提交和任务进度。
7. 实现导出任务、数值格式、到期颜色/图标和权限指令。

### 21.3 测试

- 页面布局和字段逐项对照截图验收；
- CRUD、导入、导出、校验、统计和分页；
- 单位换算、规格变更、颗粒量精度和批号重复；
- 日期、近效期、过期和历史补录；
- 权限、越权、审计、并发和幂等；
- 大数据量查询、导入预览、异步导出和断线重试；
- Chrome、Edge、1366×768 和横向滚动兼容性。

## 22. 待业务确认项

以下事项属于 P0/P1，开发前必须确认：

| 优先级 | 待确认项 | 影响 |
|---|---|---|
| P0 | “颗粒量”的真实物理单位是克、颗粒个数还是其他单位 | 影响字段名称、单位字典、计算和下游库存 |
| P0 | 规格型号(g/袋)是否始终等于每袋颗粒量，是否存在多种规格版本 | 影响主数据和数量公式 |
| P0 | 袋数是否允许小数袋、最多几位小数 | 影响录入、导入、数据库精度和采购汇总 |
| P0 | 同一医院、物料、批号、日期重复供货时是否允许多条记录 | 影响唯一键和来源单号设计 |
| P0 | 供货日期是否允许未来日期，历史补录最大回溯天数 | 影响日期校验 |
| P0 | 到期日期等于供货日期是否允许 | 影响日期校验和近效期策略 |
| P0 | 到期批次在库存统计日之后是否继续计入库存 | 影响医院库存、盘点库存和要货指导 |
| P0 | 只有校验通过的记录才进入下游，还是保存即生效 | 影响状态和库存结果 |
| P0 | 修改/作废已被下游引用记录的处理方式 | 影响版本、冲销事件和审计 |
| P1 | 导入错误行是否保存为 `INVALID` 供页面修改 | 影响导入流程和数据表 |
| P1 | 导入遇到警告时是否允许强制提交 | 影响提交按钮和权限 |
| P1 | 规格、单位、药品名称由物料主数据带出后是否允许业务覆盖 | 影响主数据一致性 |
| P1 | 近效期预警天数默认值和是否按物料配置 | 影响到期状态 |
| P1 | 是否需要来源单号、配送单号、签收单号 | 影响业务键、追溯和下游流程 |
| P1 | 导出格式、最大行数、文件留存时间和水印要求 | 影响导出实现 |
| P2 | 是否需要详情抽屉、批次库存、打印和批次追溯 | 影响前端和后续版本范围 |

## 23. 发布前检查清单

- [ ] P0 待确认项已经形成书面结论并录入数据字典/规则配置；
- [ ] 医院、物料、规格和单位主数据映射完成；
- [ ] 供货表、导入表、问题表和索引创建完成；
- [ ] 新增、修改、删除/作废、校验和导入权限测试通过；
- [ ] 颗粒量计算、精度、批号重复和日期规则测试通过；
- [ ] 列表全量统计与截图示例的统计位置一致；
- [ ] 导入模板、预览、错误明细和幂等策略验证通过；
- [ ] 导出字段、单位、状态、规则版本和审计信息完整；
- [ ] 已生效记录的下游影响、重算和冲销策略验证通过；
- [ ] 大数据量查询、异步任务和并发写入达到性能目标；
- [ ] 业务人员完成 UAT 并确认真实样例数据。

