# 盘点库存需求设计文档

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | 终端管理-盘点库存需求设计 |
| 版本 | V1.0 |
| 来源 | 现有系统“终端管理 / 盘点库存”页面截图 |
| 目标读者 | 产品、UI、前端、后端、测试、实施人员 |
| 状态 | 初稿，待业务确认 |

## 2. 背景与目标

系统用于药品终端库存盘点、库存差异核对及要货指导。当前页面以医院、物料和汇总日期为查询条件，以横向分组表格展示基础数据、整袋库存、零散库存、实际库存、亏涨结果和要货指导。

本需求的目标：

1. 将医院端盘点结果统一沉淀为可查询、可追溯的数据。
2. 自动换算整袋、零散粒数与实际库存，减少人工计算错误。
3. 自动计算药厂/医院维度的盈亏数量和金额。
4. 根据历史消耗、当月需求和现有库存生成要货指导。
5. 支持汇总、导出、批量操作，并保证权限、精度和审计完整。

## 3. 范围

### 3.1 本期范围

- 盘点库存列表查询。
- 医院、物料编码、药品名称、汇总日期筛选。
- 明细数据分页、排序、横向滚动、合计行。
- 选择明细后删除、导出、汇总。
- 盘点数据的计算、校验、异常标识。
- 要货指导的自动计算与展示。
- 操作日志、导出记录和数据权限控制。

### 3.2 暂不包含

- 移动端扫码盘点。
- 采购订单、配送单、入库单的完整闭环。
- 药品主数据维护（仅调用既有主数据）。
- 复杂预测模型和机器学习补货建议。

## 4. 页面与导航

### 4.1 入口

左侧导航：终端管理 → 盘点库存。

页面路径建议：`/terminalMedicine/stocktake`。

### 4.2 页面布局

1. 顶部：面包屑“首页 / 终端管理 / 盘点库存”。
2. 查询区：医院名称、物料编码、名称、汇总日期、搜索、重置。
3. 操作区：删除、导出、汇总；右侧提供表格搜索/刷新图标。
4. 数据区：固定左侧选择列和基础数据列，右侧横向滚动展示业务分组列。
5. 底部：分页器、当前页、总条数、每页条数。

## 5. 角色与权限

| 角色 | 查询 | 汇总 | 导出 | 删除 | 数据范围 |
|---|---:|---:|---:|---:|---|
| 系统管理员 | 是 | 是 | 是 | 是 | 全部医院 |
| 区域/业务管理员 | 是 | 是 | 是 | 按权限 | 授权区域医院 |
| 医院盘点人员 | 是 | 可选 | 可选 | 否/本人 | 所属医院 |
| 只读用户 | 是 | 否 | 可选 | 否 | 授权范围 |

权限编码建议：

- `terminal:stocktake:view`
- `terminal:stocktake:aggregate`
- `terminal:stocktake:export`
- `terminal:stocktake:delete`

所有接口必须在服务端进行权限校验，前端隐藏按钮不能替代权限控制。

## 6. 查询条件需求

| 字段 | 控件 | 必填 | 规则 |
|---|---|---:|---|
| 医院名称 | 可搜索下拉框 | 否 | 显示用户有权限访问的医院；支持名称模糊搜索 |
| 物料编码 | 文本框 | 否 | 去除首尾空格；支持精确或前缀匹配 |
| 名称 | 文本框 | 否 | 药品名称模糊匹配，最长 100 字符 |
| 汇总日期 | 日期范围 | 否 | 开始日期≤结束日期；默认不限制；最大查询跨度建议 366 天 |
| 搜索 | 按钮 | - | 按条件查询并回到第 1 页 |
| 重置 | 按钮 | - | 清空条件并恢复默认列表 |

查询行为：

- 多条件之间为 AND 关系。
- 空条件查询按当前用户权限返回数据，默认按汇总日期倒序、药品名称升序。
- 输入物料编码后回车可触发查询。
- 查询期间按钮进入 loading，防止重复提交。
- 无结果时显示空状态，并保留查询条件。

## 7. 表格字段设计

表格支持多级表头、列排序和横向滚动。金额、数量和比例列按统一精度格式化，原始值不因展示格式丢失。

### 7.1 基础数据

| 字段 | 类型 | 说明 |
|---|---|---|
| 选择 | Boolean | 行选择；表头复选框支持当前页全选 |
| 汇总日期 | Date | 本次盘点/汇总业务日期，格式 `YYYY-MM-DD` |
| 医院名称 | String | 盘点医院 |
| 物料编码 | String | 药品/物料唯一编码 |
| 药品名称 | String | 药品通用名或系统名称 |

### 7.2 整袋库存

| 字段 | 类型 | 说明 |
|---|---|---|
| 袋数 | Decimal(18,3) | 整袋数量；截图示例有 0、1、2、13 等值 |
| 颗粒量 | Decimal(18,3) | 整袋折算的颗粒总量 |

### 7.3 零散库存

| 字段 | 类型 | 说明 |
|---|---|---|
| 颗粒量 | Decimal(18,3) | 零散药品颗粒数量 |

### 7.4 实际库存

| 字段 | 类型 | 说明 |
|---|---|---|
| 实际库存（颗粒量） | Decimal(18,3) | `整袋颗粒量 + 零散颗粒量` |

### 7.5 医院库存

| 字段 | 类型 | 说明 |
|---|---|---|
| 欠片量 | Decimal(18,3) | 医院侧欠片数量，可为 0 |
| 颗粒量 | Decimal(18,3) | 医院侧库存颗粒量 |

### 7.6 亏涨结果

| 字段 | 类型 | 说明 |
|---|---|---|
| 药厂涨(袋) | Decimal(18,3) | 药厂维度涨袋数量 |
| 医院亏(袋) | Decimal(18,3) | 医院维度亏袋数量 |
| 颗粒单价 | Decimal(18,6) | 每颗粒结算单价 |
| 医院亏金额 | Decimal(18,2) | 医院亏袋/颗粒折算后的金额 |
| 药厂涨金额 | Decimal(18,2) | 药厂涨袋/颗粒折算后的金额 |

负数显示为红色，正数显示为蓝色或黑色，颜色仅表达状态，不能作为唯一信息来源。

### 7.7 要货指导

| 字段 | 类型 | 说明 |
|---|---|---|
| 要货指导范围 | DateRange | 指导起止日期，格式 `YYYY-MM-DD 至 YYYY-MM-DD` |
| 月均消耗（颗粒量） | Decimal(18,3) | 指导范围内月均消耗 |
| 本月需求量(袋) | Decimal(18,3) | 当前月份预计需求，按袋计 |
| 现场剩余可用时长(月) | Decimal(18,3) | `当前可用库存 / 月均消耗` |
| 30天需进货量(袋) | Decimal(18,3) | 未来 30 天建议进货袋数 |

### 7.8 统计汇总行

表格底部固定“统计汇总”行，对当前查询结果（不是仅当前页）进行合计。可合计字段包括袋数、颗粒量、实际库存、欠片量、医院库存颗粒量、涨亏数量、金额、月均消耗、本月需求和 30 天需进货量。比率/时长字段按业务定义采用加总或加权平均，需在配置中明确。

## 8. 核心业务规则与计算

### 8.1 单位与换算

每个物料必须配置“每袋颗粒数”`pack粒数`，且大于 0。

```text
整袋颗粒量 = 袋数 × pack粒数
实际库存颗粒量 = 整袋颗粒量 + 零散颗粒量
```

若来源数据已经提供整袋颗粒量，则以来源值为准，并校验其与换算值的差异；差异超过配置阈值时标记“数据异常”。

### 8.2 亏涨计算

建议统一以颗粒为内部核算单位：

```text
库存差异颗粒 = 盘点实际库存颗粒量 - 系统账面库存颗粒量
药厂涨(袋) = max(库存差异颗粒, 0) / pack粒数
医院亏(袋) = max(-库存差异颗粒, 0) / pack粒数
医院亏金额 = 医院亏(袋) × pack粒数 × 颗粒单价
药厂涨金额 = 药厂涨(袋) × pack粒数 × 颗粒单价
```

如现行业务的“药厂涨/医院亏”存在特殊归属规则，须由业务确认后写入配置，不允许前后端各自实现不同公式。

### 8.3 要货指导

```text
月均消耗 = 指导范围内有效消耗颗粒总量 / 有效月份数
现场剩余可用时长(月) = max(实际可用库存颗粒量, 0) / max(月均消耗, 最小消耗阈值)
30天需求颗粒量 = 月均消耗 × 30 / 月基准天数
30天需进货量(袋) = ceil(max(30天需求颗粒量 - 实际可用库存颗粒量, 0) / pack粒数)
```

默认月基准天数为 30。月均消耗为 0 时，现场剩余可用时长显示“—”，30 天需进货量为 0，并标记“无消耗数据”。

## 9. 操作功能

### 9.1 汇总

点击“汇总”后：

1. 校验当前筛选条件和盘点数据完整性。
2. 按医院、汇总日期、物料生成/刷新盘点汇总快照。
3. 服务端重新执行全部计算并保存计算版本、操作人、操作时间。
4. 返回汇总成功条数、异常条数和异常原因。
5. 列表自动刷新，统计行重新计算。

若同一业务键已有汇总结果，默认执行幂等更新，不产生重复记录；是否允许覆盖已确认数据由权限控制。

### 9.2 删除

- 未选择数据时按钮置灰，并提示“请先选择数据”。
- 支持批量删除当前用户有权限的数据。
- 删除前弹出二次确认，显示待删除条数。
- 已确认、已结算或已进入财务流程的数据禁止物理删除，只能作废。
- 删除/作废写入审计日志，记录数据主键、业务键、操作人、时间和原因。

### 9.3 导出

- 导出当前查询条件下的全部结果，而非仅当前页。
- 导出前提示预计条数；超过 50,000 行转为异步任务。
- 文件格式默认 `.xlsx`，包含多级表头、统计汇总行和导出条件。
- 导出文件中的数值保留原始精度，展示列按页面精度格式化。
- 记录导出人、条件、文件名、生成时间和下载状态。

### 9.4 排序与刷新

- 数量、金额、日期字段支持升序/降序。
- 排序由服务端执行，避免分页后排序错误。
- 刷新仅重新加载当前条件、当前页和排序状态。
- 列宽支持拖拽或按预设宽度，冻结选择列、基础数据列和统计行。

## 10. 数据模型建议

### 10.1 盘点汇总主表 `stocktake_summary`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| hospital_id | bigint | 医院 ID |
| material_id | bigint | 物料 ID |
| summary_date | date | 汇总日期 |
| status | varchar(20) | DRAFT/CONFIRMED/VOID |
| calculation_version | varchar(32) | 计算规则版本 |
| source_batch_id | varchar(64) | 来源批次/导入批次 |
| created_by/created_at | bigint/datetime | 创建信息 |
| updated_by/updated_at | bigint/datetime | 更新信息 |
| deleted_at | datetime nullable | 软删除时间 |

唯一索引建议：`hospital_id + material_id + summary_date + deleted_at(IS NULL)`。

### 10.2 盘点明细表 `stocktake_detail`

保存袋数、整袋颗粒量、零散颗粒量、医院库存、账面库存、差异、单价和要货指导等快照字段。金额和数量使用定点小数，禁止使用浮点型存储。

### 10.3 物料换算表 `material_unit_config`

保存 `material_id`、`pack粒数`、颗粒单价、生效日期、失效日期、版本号。汇总时按汇总日期匹配有效版本。

### 10.4 审计日志表 `operation_audit_log`

保存模块、操作类型、请求参数摘要、影响记录数、操作结果、失败原因、用户、IP、时间和关联任务 ID。敏感字段脱敏。

## 11. 接口设计建议

### 11.1 查询列表

`GET /api/terminal/stocktake`

请求参数：`hospitalId`、`materialCode`、`drugName`、`startDate`、`endDate`、`page`、`pageSize`、`sortField`、`sortOrder`。

响应：

```json
{
  "items": [],
  "total": 0,
  "summary": {},
  "page": 1,
  "pageSize": 20
}
```

### 11.2 汇总

`POST /api/terminal/stocktake/aggregate`

请求体包含筛选条件或明确的 `ids`。响应包含 `successCount`、`errorCount`、`errors[]`、`taskId`。

### 11.3 删除/作废

`DELETE /api/terminal/stocktake`，请求体为 `ids`、`reason`。服务端按状态决定软删除或作废。

### 11.4 导出

`POST /api/terminal/stocktake/export`，返回同步文件地址或异步 `taskId`；另提供 `GET /api/export/tasks/{taskId}` 查询进度。

### 11.5 医院下拉

`GET /api/hospitals/options?keyword=`，只返回当前用户有权限的医院。

## 12. 前端交互与校验

- 表格加载时显示骨架屏或 loading。
- 数值为空显示“—”，0 显示 `0`，禁止把 0 显示为空。
- 金额保留 2 位小数，单价最多 6 位小数，数量默认 3 位小数。
- 超长文本使用省略号，悬浮显示完整内容。
- 日期范围非法、物料不存在、换算配置缺失时在查询/汇总前给出明确提示。
- 亏损金额、负差异使用红色；异常数据增加告警图标和可查看原因的 tooltip。
- 进入详情（如后续提供）时保留来源筛选条件，返回后恢复列表状态。

## 13. 非功能需求

### 13.1 性能

- 常规查询首屏响应目标 ≤2 秒，95 分位 ≤4 秒。
- 列表接口支持至少 100 万条历史记录的分页查询。
- 导出采用流式/异步方式，不能阻塞在线查询。

### 13.2 安全

- 所有接口使用登录态和权限校验。
- 防止越权查询医院数据；医院 ID 必须由服务端按权限过滤。
- 导出地址使用短时效签名，默认有效期 30 分钟。
- 对操作参数、日志和错误信息进行注入防护及敏感信息脱敏。

### 13.3 一致性与可追溯

- 汇总计算在服务端事务中完成。
- 记录计算规则版本，保证历史结果可复现。
- 删除采用软删除/作废，保留审计链路。
- 并发汇总使用幂等键或分布式锁，避免重复生成。

### 13.4 兼容性

- 支持 Chrome、Edge 最新两个大版本。
- 最小推荐分辨率 1366×768；表格必须支持横向滚动。
- 中文、数字、日期和小数格式按系统统一规范展示。

## 14. 测试与验收标准

### 14.1 功能验收

1. 按医院、物料编码、药品名称、日期范围组合查询结果正确。
2. 分页、服务端排序、横向滚动和固定列正常。
3. 汇总后计算字段与业务公式一致，重复汇总不重复插入。
4. 选择、删除/作废、二次确认、权限拦截和审计日志完整。
5. 导出内容与查询结果一致，包含多级表头和统计汇总行。
6. 无消耗、缺换算、负库存、异常差异等边界场景有明确提示。

### 14.2 计算验收样例

给定 `pack粒数=100`、袋数 `2`、零散颗粒 `68.57`：

```text
整袋颗粒量 = 2 × 100 = 200
实际库存颗粒量 = 200 + 68.57 = 268.57
```

给定盘点实际库存 268.57、账面库存 300：

```text
库存差异颗粒 = -31.43
医院亏(袋) = 31.43 / 100 = 0.3143（页面按配置精度展示）
```

### 14.3 异常验收

- 无权限医院的数据不可通过 URL、接口参数或导出获取。
- `pack粒数=0`、负数、NaN、超精度输入必须被拒绝。
- 大数据量导出显示任务进度，失败可重试且不产生脏文件。
- 汇总过程中网络中断，重试后结果仍保持幂等和一致。

## 15. 待业务确认项

1. “袋数/颗粒量/欠片量”的最终单位和允许小数位。
2. 药厂涨与医院亏的正式业务公式及归属方向。
3. 账面库存、盘点库存和医院库存的来源系统及同步频率。
4. 统计汇总行中“现场剩余可用时长”的汇总方式（加总、平均或加权平均）。
5. 汇总是否允许覆盖已确认/已结算数据。
6. 删除是物理删除、软删除还是统一作废，以及保留期限。
7. 导出最大行数、文件保留时间和是否需要水印。
8. 指导范围的默认起止日期和月均消耗的有效月份定义。
9. 页面是否需要明细钻取、打印和批量导入功能。

## 16. 开发拆分建议

### 后端

- 数据表、索引、物料换算配置读取。
- 查询分页、服务端排序和权限过滤。
- 汇总计算服务及规则版本化。
- 删除/作废、导出任务、审计日志。
- 单元测试覆盖公式和边界条件。

### 前端

- 查询表单和医院远程下拉。
- 多级表头、固定列、排序、分页、合计行。
- 批量选择、删除确认、汇总结果反馈。
- 导出任务状态、异常提示和数值格式化。
- 权限指令和按钮状态控制。

### 测试

- 正常流程、权限、边界数值、并发汇总、导出、兼容性和性能测试。
- 使用固定计算样例与数据库快照进行自动化回归。

