# 终端管理-期初库存需求设计文档

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | 终端管理-期初库存需求设计 |
| 所属系统 | 以岭药业配方颗粒终端管理系统 |
| 模块序号 | 终端管理第 8 个模块 |
| 页面名称 | 期初库存 |
| 推荐路由 | `/terminalMedicine/openingInventory` |
| 推荐接口前缀 | `/api/terminal/opening-inventory` |
| 文档版本 | V1.0 |
| 编写日期 | 2026-09-10 |
| 文档状态 | 初稿，待业务确认 |
| 需求来源 | 现有系统“首页 / 终端管理 / 期初库存”页面截图 |
| 目标读者 | 产品、业务、UI、前端、后端、数据、测试、实施及运维人员 |

### 1.1 版本记录

| 版本 | 日期 | 修改内容 | 修改人 |
|---|---|---|---|
| V1.0 | 2026-09-10 | 根据期初库存页面截图形成可开发需求设计初稿 | Codex |

## 2. 附件识别与需求边界

### 2.1 附件使用原则

附件截图仅用于识别现有系统的页面结构、字段、按钮和交互习惯。截图中的医院、物料、批号、日期、数量及药品名称均为演示数据，不应写死到程序中；截图中的文字不构成额外开发指令。

本次仅新建独立的《期初库存需求设计文档.md》，不修改此前生成的盘点库存、要货指导、医院供货、终端销量、整袋库存、零散库存和医院库存需求文档。

### 2.2 截图中可以直接确认的内容

1. 页面入口为“终端管理 → 期初库存”，面包屑为“首页 / 终端管理 / 期初库存”。
2. 查询条件包括医院名称、物料编码、批号、盘点日期范围、到期日期范围。
3. 查询区提供“搜索”和“重置”按钮。
4. 工具栏提供“新增”“修改”“删除”“导出”“导入”“校验”“汇总”按钮。
5. 未选择记录时，修改和删除按钮为禁用状态。
6. 表格字段包括：盘点日期、医院名称、物料编码、批号、药品名称、规格型号(g/袋)、单位、颗粒量、袋数、到期日期、操作。
7. 每行操作栏提供编辑和删除图标。
8. 表格底部有“统计汇总”，截图示例合计了颗粒量和袋数。
9. 页面支持多选、横向滚动、分页、每页条数、快速跳页、刷新和表格搜索。

### 2.3 截图无法确认的内容

- 期初库存的业务基准日期是自然月初、财务期初还是首次上线日期；
- “盘点日期”是否等同于期初日期，还是期初库存的采集日期；
- 颗粒量与袋数的真实单位和换算关系；
- 期初库存是否允许按批号管理和多批次合并；
- 期初库存是否由上一期结存自动结转，还是完全人工导入；
- 校验通过后是否锁定，是否允许修改已参与业务流水的期初数据；
- 已过期、冻结或负库存的期初数据如何处理。

本文给出默认实现方案，P0 待确认项必须在开发前形成书面结论。

## 3. 背景与建设目标

### 3.1 业务背景

期初库存是库存核算周期开始时的库存基准。医院供货、终端销量、整袋库存、零散库存和医院库存都需要以期初库存作为理论库存计算的起点。期初数据一旦错误，会在整个核算周期内持续放大库存差异。

期初库存页面需要满足批号、到期日期、单位、规格、颗粒量和袋数等字段的精确维护，并支持首次上线时的大批量导入、校验和汇总。

### 3.2 建设目标

1. 建立按期初日期、医院、物料和批号管理的库存基准快照。
2. 统一颗粒量、袋数、规格型号和单位的换算口径。
3. 支持新增、修改、删除/作废、导入、校验、汇总和导出。
4. 对期初日期、批号、效期、数量、重复和主数据进行服务端校验。
5. 确保期初库存只能作为核算起点，避免与后续流水重复入账。
6. 支持期初锁定、版本结转、审计和下游重算。

### 3.3 成功标准

- 相同期初基准下，列表、统计、导出和理论库存计算结果一致；
- 同一医院/物料/批号/期初日期不会重复生成有效基准；
- 期初库存能够追溯来源批次、导入文件、操作人和规则版本；
- 期初锁定后，未经授权不能修改；
- 期初变更能够触发盘点、医院库存和要货指导待重算；
- 无权限用户不能查看或导出其他医院期初数据。

## 4. 需求范围

### 4.1 本期范围

- 期初库存列表和分页；
- 医院、物料编码、批号、盘点日期、到期日期范围查询；
- 新增、修改、批量删除/作废；
- Excel/CSV 模板下载、导入预览、逐行校验、提交和错误明细下载；
- 批量校验、期初汇总和状态管理；
- 颗粒量和袋数全量统计；
- 单位、规格、批号、效期、数量及重复校验；
- 期初锁定和结转版本；
- 与医院供货、终端销量、整袋/零散库存、医院库存、盘点和要货指导联动；
- 服务端权限、审计、幂等、事务和并发控制。

### 4.2 本期不包含

- 采购、入库、出库、配送、退货和财务完整流程；
- 自动替代医院库存系统；
- 仓库库位、序列号和托盘管理；
- 移动端扫码盘点；
- 自动采购和预测模型；
- 复杂审批流（可预留状态）。

### 4.3 后续扩展

- 上期末库存自动结转为本期期初；
- 期初审批、双人复核和锁定；
- 期初差异调整单；
- 批次 FIFO/FEFO；
- 期初库存对账看板；
- 多账套、多核算周期和历史版本比较。

## 5. 角色与权限

### 5.1 权限矩阵

| 角色 | 查询 | 新增 | 修改 | 删除/作废 | 导入 | 校验 | 汇总/锁定 | 导出 | 数据范围 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 系统管理员 | 是 | 是 | 是 | 是 | 是 | 是 | 是 | 是 | 全部医院 |
| 区域/业务管理员 | 是 | 是 | 是 | 按授权 | 是 | 是 | 按授权 | 是 | 授权区域医院 |
| 医院库存人员 | 是 | 是 | 是 | 本人草稿 | 是 | 是 | 否 | 可配置 | 所属医院 |
| 库存主管 | 是 | 是 | 是 | 按授权 | 是 | 是 | 是 | 是 | 授权医院 |
| 财务/审计人员 | 是 | 否 | 否 | 否 | 否 | 否 | 否 | 是 | 授权医院 |
| 只读用户 | 是 | 否 | 否 | 否 | 否 | 否 | 否 | 可配置 | 授权范围 |

### 5.2 权限编码

- `terminal:opening-inventory:view`
- `terminal:opening-inventory:create`
- `terminal:opening-inventory:update`
- `terminal:opening-inventory:delete`
- `terminal:opening-inventory:import`
- `terminal:opening-inventory:validate`
- `terminal:opening-inventory:aggregate`
- `terminal:opening-inventory:lock`
- `terminal:opening-inventory:export`
- `terminal:opening-inventory:override`

服务端必须同时校验功能权限、医院数据权限和期初锁定状态。

## 6. 页面设计

### 6.1 页面入口

- 左侧导航：终端管理 → 期初库存；
- 页面路由：`/terminalMedicine/openingInventory`；
- 面包屑：首页 / 终端管理 / 期初库存；
- 接口前缀：`/api/terminal/opening-inventory`。

### 6.2 页面区域

1. 面包屑和页签区；
2. 查询区：医院名称、物料编码、批号、盘点日期范围、到期日期范围；
3. 工具栏：新增、修改、删除、导出、导入、校验、汇总；
4. 表格区：行选择、期初字段、行级编辑和删除；
5. 统计区：颗粒量和袋数合计；
6. 分页区：总条数、每页条数、页码和跳页；
7. 右侧工具：表格搜索、刷新。

### 6.3 页面结构示意

```text
首页 / 终端管理 / 期初库存

[医院名称 ▼] [物料编码] [批号] [盘点日期开始-结束] [到期日期开始-结束] [搜索] [重置]

[新增] [修改] [删除] [导出] [导入] [校验] [汇总]       [搜索] [刷新]

┌选择┬盘点日期*┬医院名称*┬物料编码*┬批号┬药品名称*┬规格(g/袋)┬单位*┬颗粒量*┬袋数┬到期日期*┬操作┐
│ □  │          │          │         │    │          │          │     │        │    │          │编辑 删除│
├────┼──────────┼──────────┼─────────┼────┼──────────┼──────────┼─────┼────────┼────┼──────────┼─────────┤
│ □  │ ...      │ ...      │ ...     │... │ ...      │ ...      │ ... │ ...    │... │ ...      │ ✎  🗑   │
├────┴──────────┴──────────┴─────────┴────┴──────────┴──────────┴─────┴────────┴────┴──────────┴─────────┤
│ 统计汇总                                             颗粒量：...       袋数：...                      │
└────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

### 6.4 固定列与滚动

- 左侧固定选择列、盘点日期、医院名称；
- 右侧固定操作列；
- 中间字段支持横向滚动；
- 表头和统计行垂直滚动时保持可见；
- 推荐支持 1366×768 及以上分辨率。

## 7. 查询需求

### 7.1 查询字段

| 页面字段 | API 参数 | 控件 | 必填 | 规则 |
|---|---|---|---:|---|
| 医院名称 | `hospitalId` | 可搜索下拉 | 否 | 仅显示授权医院 |
| 物料编码 | `materialCode` | 文本框 | 否 | 去首尾空格，模糊匹配，最长 64 字符 |
| 批号 | `batchNo` | 文本框 | 否 | 模糊匹配，最长 64 字符 |
| 盘点日期开始 | `stocktakeStartDate` | 日期选择器 | 否 | 闭区间起点 |
| 盘点日期结束 | `stocktakeEndDate` | 日期选择器 | 否 | 不早于开始日期 |
| 到期日期开始 | `expiryStartDate` | 日期选择器 | 否 | 闭区间起点 |
| 到期日期结束 | `expiryEndDate` | 日期选择器 | 否 | 不早于开始日期 |
| 页码 | `page` | 分页器 | 否 | 默认 1 |
| 每页条数 | `pageSize` | 分页器 | 否 | 默认 10，可选 10/20/50/100/200 |
| 排序字段 | `sortField` | 表头 | 否 | 服务端白名单 |
| 排序方向 | `sortOrder` | 表头 | 否 | `asc`/`desc` |

### 7.2 查询行为

1. 多条件之间使用 AND 关系。
2. 无筛选条件时返回当前用户权限范围内全部未作废期初记录。
3. 默认排序：盘点日期倒序、到期日期升序、医院名称升序、ID 倒序。
4. 点击搜索回到第 1 页；物料编码和批号支持回车查询。
5. 重置清空条件、恢复默认排序和第 1 页。
6. 查询期间显示 loading，防止重复请求。
7. 无结果时显示空状态并保留条件。
8. 两个日期范围均为闭区间；非法范围在前端和服务端同时阻断。
9. 默认只显示当前期初版本和未作废记录；审计用户可查看历史版本。
10. 已锁定期初记录在列表显示锁定标签，普通用户不可编辑。

## 8. 表格字段设计

| 页面列 | API 字段 | 数据库字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---:|---|
| 选择 | `selected` | - | Boolean | - | 表头只全选当前页 |
| 盘点日期* | `stocktakeDate` | `stocktake_date` | Date | 是 | 期初库存采集/基准日期 |
| 期初基准日期 | `openingDate` | `opening_date` | Date | 是 | 进入核算周期的生效日期，可与盘点日期不同 |
| 医院名称* | `hospitalName` | `hospital_name` | String/快照 | 是 | 最长 120 字符 |
| 医院 ID | `hospitalId` | `hospital_id` | Long | 是 | 权限及关联字段 |
| 物料编码* | `materialCode` | `material_code` | String | 是 | 保留前导零 |
| 物料 ID | `materialId` | `material_id` | Long | 是 | 主数据关联 |
| 批号 | `batchNo` | `batch_no` | String | 条件必填 | 严格批次模式必填 |
| 药品名称* | `drugName` | `drug_name` | String/快照 | 是 | 物料主数据快照 |
| 规格型号(g/袋) | `specQtyPerBag` | `spec_qty_per_bag` | Decimal(18,3) | 条件必填 | 每袋规格 |
| 单位* | `unitCode`/`unitName` | `unit_code`/`unit_name` | String | 是 | 颗粒量或库存单位 |
| 颗粒量* | `particleQty` | `particle_qty` | Decimal(18,3) | 是 | 期初颗粒库存量 |
| 标准颗粒量 | `standardParticleQty` | `standard_particle_qty` | Decimal(18,3) | 是 | 换算到标准库存单位 |
| 袋数 | `bagCount` | `bag_count` | Decimal(18,3) | 否 | 期初整袋数量；若无袋数可为空 |
| 到期日期* | `expiryDate` | `expiry_date` | Date | 是 | 批次效期 |
| 锁定状态 | `lockStatus` | `lock_status` | Enum | 是 | `UNLOCKED`/`LOCKED` |
| 校验状态 | `validationStatus` | `validation_status` | Enum | 是 | `DRAFT`/`VALIDATED`/`INVALID`/`VOID` |
| 来源类型 | `sourceType` | `source_type` | Enum | 是 | `MANUAL`/`IMPORT`/`ROLLOVER`/`API` |
| 版本 | `version` | `version` | Integer | 是 | 乐观锁 |
| 操作 | `actions` | - | - | - | 行级编辑、删除/作废 |

### 8.1 期初日期和盘点日期

- `openingDate` 是库存进入核算周期的生效日期；
- `stocktakeDate` 是数据采集或盘点发生日期；
- 默认两者相同；若不同，详情必须显示差异天数和原因；
- `openingDate` 一经锁定不得修改；
- 截图只展示“盘点日期”，接口和数据库仍建议保留两个字段，避免将业务基准和采集时间混为一谈。

### 8.2 数量和统计

默认统计当前查询全部有效记录：

```text
颗粒量合计 = Σ standardParticleQty
袋数合计 = Σ bagCount（非空记录）
```

如果袋数是由颗粒量反推，必须使用：

```text
袋数 = standardParticleQty / specQtyPerBag
```

是否向上取整、是否允许余数由业务确认。截图中的颗粒量 `4801.1`、袋数 `0.0` 仅为界面示例，不作为固定验收值。

## 9. 数据口径与业务粒度

### 9.1 模块定义

期初库存是指定核算周期开始时的库存基准快照。它不是供货流水、销量流水，也不是当前实时库存；同一周期只能有一个当前有效期初版本。

### 9.2 最小业务粒度

默认业务键：

```text
openingDate + hospitalId + materialId + batchNo + expiryDate + unitVersion
```

如果同一批号按科室、库位或库存形态区分，需要增加 `locationId`/`inventoryForm`；截图未展示，V1 默认不拆库位。

### 9.3 期初与期末结转

- 上一期期末库存可以作为下一期期初候选值；
- 自动结转时必须生成 `sourceType=ROLLOVER`、来源周期和来源快照 ID；
- 自动结转结果必须经过校验，不能绕过业务确认直接锁定；
- 期初数量一旦锁定，不得因后续供货/销量变化自动变化；
- 后续调整通过库存调整事件或新版本期初修正完成。

### 9.4 颗粒量、袋数和单位

- 颗粒量保存原始单位值和标准库存单位值；
- 袋数只表示整袋数量，不应包含零散库存；
- 若期初库存包含整袋和零散两种形态，应分别在整袋/零散模块记录，再由医院库存汇总；
- 本模块的“颗粒量”用于期初基准，不能与后续供货或销量重复入账。

## 10. 核心业务规则

### 10.1 主数据规则

1. 医院必须存在、启用且在用户授权范围内。
2. 物料编码必须匹配有效物料。
3. 药品名称由物料主数据带出并保存快照。
4. 单位、规格和换算版本按 `openingDate` 匹配。
5. 批号存在时必须与医院供货或批次主数据匹配。
6. 主数据版本冲突时不能进入 `VALIDATED`。

### 10.2 数量计算

若已知袋数和每袋规格：

```text
particleQty = bagCount × specQtyPerBag
```

若只提供标准颗粒量：

```text
bagCount = standardParticleQty / specQtyPerBag
```

规则：

- 中间计算至少保留 6 位小数；
- 最终保存按 `DECIMAL(18,3)`、`HALF_UP`；
- 导入文件提供的计算字段必须由服务端重新计算；
- 计算值与导入值差异超过 `0.001` 默认阻断；
- 不允许浮点、NaN、Infinity、科学计数法和超精度。

### 10.3 袋数规则

- 默认允许 `bagCount >= 0`；
- 默认只允许整数袋；
- 如果期初仅维护颗粒量，袋数可为空，不得强行按 0 写入；
- 如果同时维护袋数和颗粒量，两者必须符合规格换算；
- 零袋记录默认保留并标记 `ZERO_BAG`；
- 负袋数禁止，损耗/冲销使用独立调整事件。

### 10.4 日期规则

- `openingDate` 默认不能晚于当前日期；
- `stocktakeDate` 默认不能早于 `openingDate`，除非业务允许提前采集；
- 历史补录最大回溯天数默认 366 天；
- 到期日期原则上不早于供货日期和期初日期；
- 到期日当天是否有效需业务确认；
- 日期全部按 `Asia/Shanghai` 解析。

### 10.5 批号规则

- 批号长度 1～64 字符，去除首尾空格；
- 同一医院、物料、批号不可对应多个冲突效期；
- 严格批次模式下批号为空阻断；
- 宽松模式下允许空批号但产生警告；
- 同一业务键重复不自动合并数量。

### 10.6 期初锁定

锁定是期初库存的关键控制：

1. 只有通过校验且无阻断错误的记录才可锁定；
2. 锁定可按医院、核算周期或当前筛选结果批量执行；
3. 锁定前显示记录数、异常数、影响周期和确认提示；
4. `LOCKED` 记录普通用户不可修改和删除；
5. 管理员修正锁定数据必须走解锁/新版本流程并填写原因；
6. 锁定后发布 `OpeningInventoryLocked` 事件，通知库存和要货指导使用该基准；
7. 同一医院和核算周期只能有一个当前有效锁定版本。

### 10.7 重复和版本

- 同一业务键重复新增默认阻断；
- 同一核算周期只能有一个当前有效期初版本；
- 修正锁定数据生成新版本，旧版本保留并标记 `REPLACED`；
- 列表默认展示当前有效版本，审计用户可查看历史；
- 导入使用文件 SHA-256、导入批次和行哈希防重。

### 10.8 生效和下游计算

只有以下记录进入理论库存：

```text
validationStatus = VALIDATED
AND lockStatus = LOCKED
AND deletedAt IS NULL
AND openingDate <= 统计日期
```

理论库存默认计算：

```text
理论库存 = 期初库存 + 有效医院供货 - 有效终端销量 ± 库存调整
```

期初库存不会随供货或销量自动修改。

## 11. 状态模型

### 11.1 业务状态

| 状态 | 含义 | 默认显示 | 可修改 | 可删除/作废 | 进入下游 |
|---|---|---:|---:|---:|---:|
| `DRAFT` | 新增或导入未校验 | 是 | 是 | 是 | 否 |
| `VALIDATED` | 校验通过未锁定 | 是 | 是 | 是 | 否 |
| `LOCKED` | 已锁定的期初基准 | 是 | 仅新版本 | 只能作废/替换 | 是 |
| `INVALID` | 存在阻断错误 | 是 | 是 | 是 | 否 |
| `VOID` | 已作废 | 默认否 | 否 | 否 | 否 |
| `REPLACED` | 已被新版本替换 | 审计可见 | 否 | 否 | 否 |

### 11.2 到期和库存状态

库存状态独立于业务状态：`AVAILABLE`、`EXPIRED`、`FROZEN`、`ZERO`。例如 `LOCKED + EXPIRED` 允许追溯，但可用库存为 0。

## 12. 操作功能设计

### 12.1 新增

点击“新增”打开抽屉或弹窗。

| 字段 | 控件 | 默认值 | 可编辑 | 规则 |
|---|---|---|---:|---|
| 盘点日期 | 日期选择器 | 当前日期 | 是 | 受日期回溯限制 |
| 期初基准日期 | 日期选择器 | 与盘点日期相同 | 是 | 锁定后不可改 |
| 医院名称 | 远程搜索下拉 | 空 | 是 | 仅授权医院 |
| 物料编码 | 远程搜索 | 空 | 是 | 带出药品和规格 |
| 药品名称 | 只读 | 自动带出 | 否 | 保存快照 |
| 批号 | 批次下拉/文本 | 空 | 是 | 严格模式必填 |
| 规格型号 | 只读/选择 | 自动带出 | 条件可编辑 | 取有效版本 |
| 单位 | 只读标签 | 自动带出 | 否 | 单位字典值 |
| 颗粒量 | 数值框 | 空 | 是 | 按精度输入 |
| 袋数 | 数值框 | 空 | 条件可编辑 | 与颗粒量校验 |
| 到期日期 | 日期选择器 | 批次带出 | 条件可编辑 | 与批号一致 |

保存流程：

1. 前端检查必填、日期和数值格式；
2. 服务端重新匹配医院、物料、单位、规格和批号；
3. 计算标准颗粒量、检查重复和周期；
4. 默认保存 `DRAFT`；
5. 成功后刷新列表和统计行，保留筛选条件。

### 12.2 修改

- 选择 1 条记录时修改按钮可用；选择 0 条或多条时禁用；
- `DRAFT`、`INVALID`、未锁定 `VALIDATED` 可修改；
- `LOCKED` 记录普通修改被禁止；管理员必须走新版本或解锁流程；
- 修改数量、物料、单位、批号或日期后重新校验；
- 修改必须携带 `version` 和原因；
- 已被下游使用的锁定记录不能直接覆盖。

### 12.3 删除/作废

- 工具栏支持批量处理，行级图标处理单条；
- `DRAFT`、`INVALID` 可软删除；
- `VALIDATED` 未锁定可删除；
- `LOCKED` 只能作废/替换，不能物理删除；
- 已被理论库存、盘点或要货指导引用时必须保留历史并触发待重算；
- 原因必填，长度 2～200 字；
- 返回成功数、作废数、阻断数及逐条原因。

### 12.4 校验

1. 有选择时校验选中记录；
2. 无选择时确认后校验当前查询结果；
3. 超过 5,000 条转异步任务；
4. 校验主数据、日期、批号、数量、规格、单位、重复、效期和期初版本；
5. 通过后改为 `VALIDATED`，失败改为 `INVALID`；
6. 校验不擅自修改业务字段；
7. 结果返回通过、警告、失败数和问题明细。

### 12.5 汇总与锁定

“汇总”用于按医院/核算周期生成期初结果并检查完整性：

1. 校验当前筛选条件、业务键和数据权限；
2. 检查是否存在多个当前有效期初版本；
3. 计算颗粒量、袋数合计及未映射数量；
4. 返回通过数、异常数和重复数；
5. 汇总成功不等于锁定；锁定需要单独权限和确认；
6. 锁定后生成不可直接编辑的当前有效版本；
7. 锁定操作写审计并发布事件。

### 12.6 导入

#### 文件要求

- 默认支持 `.xlsx`，可选 UTF-8 BOM `.csv`；
- 最大文件 20 MB，单文件最多 50,000 行；
- 禁止宏、公式执行和外部链接；
- 校验扩展名、MIME、内容结构和病毒风险。

#### 模板字段

```text
期初基准日期,盘点日期,医院名称,医院ID(可选),物料编码,批号,
药品名称(可选),规格型号(g/袋)(可选),单位,颗粒量,袋数(可选),到期日期
```

#### 导入流程

```text
下载模板 → 上传 → 解析预览 → 逐行校验 → 查看错误/警告
→ 下载错误明细 → 确认提交 → 再次校验 → 写入草稿 → 汇总/锁定
```

默认预览不写正式数据；提交时重新校验；重复业务键不自动相加；错误行是否保存为 `INVALID` 由配置决定。

### 12.7 导出

- 导出当前查询条件下全部记录；
- 默认 `.xlsx`，可兼容 `.csv`；
- 包含期初日期、盘点日期、医院、物料、批号、药名、规格、单位、颗粒量、袋数、到期状态、锁定状态、来源、版本和问题摘要；
- 文件中包含查询条件、导出时间、单位说明和规则版本；
- 超过 50,000 行转异步任务；
- 下载地址默认 30 分钟失效；
- 记录导出审计。

## 13. 校验规则与错误码

| 场景 | 默认处理 | 错误码 |
|---|---|---|
| 医院无权限 | 阻断/403 | `OI_HOSPITAL_FORBIDDEN` |
| 物料不存在 | 阻断 | `OI_MATERIAL_NOT_FOUND` |
| 单位/规格缺失 | 阻断 | `OI_UNIT_CONFIG_MISSING` |
| 期初日期晚于当前日期 | 阻断 | `OI_OPENING_DATE_INVALID` |
| 盘点日期早于期初日期 | 阻断/警告 | `OI_STOCKTAKE_DATE_INVALID` |
| 批号不匹配 | 阻断/警告 | `OI_BATCH_MISMATCH` |
| 到期日期早于期初日期 | 阻断/警告 | `OI_EXPIRY_INVALID` |
| 颗粒量为负 | 阻断 | `OI_PARTICLE_QTY_NEGATIVE` |
| 袋数为负 | 阻断 | `OI_BAG_COUNT_NEGATIVE` |
| 颗粒量与袋数×规格不一致 | 阻断 | `OI_QTY_MISMATCH` |
| 数量超精度 | 阻断 | `OI_QTY_PRECISION_INVALID` |
| 业务键重复 | 阻断 | `OI_DUPLICATE_RECORD` |
| 同周期多个有效版本 | 阻断 | `OI_MULTIPLE_ACTIVE_VERSION` |
| 已锁定记录修改 | 阻断 | `OI_LOCKED_RECORD` |
| 已被下游引用 | 禁止直接删除 | `OI_ALREADY_REFERENCED` |
| 乐观锁冲突 | 409 | `OI_VERSION_CONFLICT` |
| 导入文件重复 | 返回历史结果 | `OI_IMPORT_DUPLICATE` |

## 14. 数据库设计

### 14.1 主表 `opening_inventory_snapshot`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `opening_date` | DATE | 期初基准日期 |
| `stocktake_date` | DATE | 盘点/采集日期 |
| `hospital_id` | BIGINT | 医院 ID |
| `hospital_name` | VARCHAR(120) | 医院名称快照 |
| `material_id` | BIGINT | 物料 ID |
| `material_code` | VARCHAR(64) | 物料编码快照 |
| `batch_no` | VARCHAR(64) | 批号 |
| `drug_name` | VARCHAR(120) | 药品名称快照 |
| `spec_qty_per_bag` | DECIMAL(18,3) | 每袋规格 |
| `unit_code` | VARCHAR(20) | 单位编码 |
| `unit_name` | VARCHAR(20) | 单位快照 |
| `particle_qty` | DECIMAL(18,3) | 原始颗粒量 |
| `standard_particle_qty` | DECIMAL(18,3) | 标准颗粒量 |
| `bag_count` | DECIMAL(18,3) | 袋数，可空 |
| `conversion_factor` | DECIMAL(18,6) | 换算系数 |
| `expiry_date` | DATE | 到期日期 |
| `inventory_status` | VARCHAR(20) | `AVAILABLE`/`EXPIRED`/`FROZEN`/`ZERO` |
| `lock_status` | VARCHAR(20) | `UNLOCKED`/`LOCKED` |
| `validation_status` | VARCHAR(20) | `DRAFT`/`VALIDATED`/`INVALID`/`VOID`/`REPLACED` |
| `validation_version` | VARCHAR(32) | 校验版本 |
| `warning_codes` | VARCHAR(1000) | 告警编码集合 |
| `snapshot_version` | INT | 同周期版本 |
| `source_type` | VARCHAR(20) | `MANUAL`/`IMPORT`/`ROLLOVER`/`API` |
| `source_snapshot_id` | BIGINT | 上期结转来源 |
| `source_batch_id` | VARCHAR(64) | 导入/同步批次 |
| `source_row_no` | INT | 文件行号 |
| `dedupe_hash` | CHAR(64) | 防重哈希 |
| `version` | INT | 乐观锁版本 |
| `created_by`/`created_at` | BIGINT/DATETIME | 创建信息 |
| `updated_by`/`updated_at` | BIGINT/DATETIME | 修改信息 |
| `deleted_at` | DATETIME | 软删除时间 |

建议索引：

```text
uk_oi_business (opening_date, hospital_id, material_id, batch_no, expiry_date, snapshot_version, deleted_at)
idx_oi_query (hospital_id, opening_date, stocktake_date, material_code, batch_no, expiry_date, deleted_at)
idx_oi_lock (opening_date, hospital_id, lock_status, validation_status, deleted_at)
idx_oi_source (source_batch_id, source_snapshot_id)
```

如果批号允许为空，不能依赖 MySQL 可空唯一索引实现防重，应使用规范化值或独立业务键表。

### 14.2 周期表 `opening_inventory_period`

建议保存医院、核算周期、当前版本、锁定状态、锁定人、锁定时间和来源周期：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `hospital_id` | BIGINT | 医院 ID |
| `period_start` | DATE | 周期开始 |
| `period_end` | DATE | 周期结束 |
| `current_version` | INT | 当前有效期初版本 |
| `lock_status` | VARCHAR(20) | 周期锁定状态 |
| `locked_by`/`locked_at` | BIGINT/DATETIME | 锁定信息 |
| `source_period_id` | BIGINT | 结转来源周期 |

### 14.3 导入批次与问题表

建议建立：

- `opening_inventory_import_batch`：文件哈希、状态、行数、写入数和操作人；
- `opening_inventory_import_issue`：批次、行号、字段、原值、级别、错误码和修复建议；
- `opening_inventory_validation_issue`：记录当前/历史校验问题。

### 14.4 与现有模块关系

- 复用医院主数据、物料/单位配置和操作审计日志；
- 期初库存使用独立基准表，不写入供货、销量、整袋、零散或医院库存流水表；
- 理论库存以期初快照为起点读取；
- 盘点和要货指导保存期初来源 ID、版本和截止时间；
- 期初锁定后不因后续流水自动变化。

## 15. 接口设计

### 15.1 列表查询

```http
GET /api/terminal/opening-inventory
```

参数：`hospitalId`、`materialCode`、`batchNo`、`openingStartDate`、`openingEndDate`、`stocktakeStartDate`、`stocktakeEndDate`、`expiryStartDate`、`expiryEndDate`、`status`、`lockStatus`、`page`、`pageSize`、`sortField`、`sortOrder`。

响应示例：

```json
{
  "items": [
    {
      "id": 1,
      "openingDate": "2025-12-25",
      "stocktakeDate": "2025-12-25",
      "hospital": { "id": 1, "name": "河北北方学院第二附属医院" },
      "material": {
        "id": 482,
        "code": "02.22.003.0482",
        "drugName": "麻黄根"
      },
      "batchNo": "",
      "specQtyPerBag": null,
      "unit": { "code": "G", "name": "克" },
      "particleQty": 6.8,
      "standardParticleQty": 6.8,
      "bagCount": null,
      "expiryDate": "2028-07-14",
      "lockStatus": "LOCKED",
      "validationStatus": "VALIDATED",
      "inventoryStatus": "AVAILABLE",
      "snapshotVersion": 1,
      "version": 1,
      "warnings": []
    }
  ],
  "summary": {
    "recordCount": 1,
    "particleQty": 6.8,
    "standardParticleQty": 6.8,
    "bagCount": 0,
    "lockedCount": 1,
    "invalidCount": 0
  },
  "page": 1,
  "pageSize": 10,
  "total": 1,
  "queryMeta": {
    "inventoryNature": "OPENING_SNAPSHOT",
    "calculationVersion": "v1",
    "standardUnit": "G"
  }
}
```

### 15.2 选项接口

```http
GET /api/terminal/opening-inventory/options/hospitals?keyword=河北
GET /api/terminal/opening-inventory/options/materials?keyword=02.22
GET /api/terminal/opening-inventory/options/batches?hospitalId=1&materialId=482
```

选项返回主数据、规格/单位版本、批号、供货日期和到期日期。

### 15.3 新增

```http
POST /api/terminal/opening-inventory
Idempotency-Key: <uuid>
Content-Type: application/json
```

```json
{
  "openingDate": "2025-12-25",
  "stocktakeDate": "2025-12-25",
  "hospitalId": 1,
  "materialId": 482,
  "batchNo": "B2507001",
  "particleQty": 6.8,
  "bagCount": null,
  "expiryDate": "2028-07-14"
}
```

### 15.4 修改

```http
PUT /api/terminal/opening-inventory/{id}
Content-Type: application/json
```

请求包含可编辑字段、`version`、修改原因和可选 `createNewVersion`。锁定记录默认要求 `createNewVersion=true`。

### 15.5 删除/作废

```http
DELETE /api/terminal/opening-inventory
Content-Type: application/json
```

```json
{
  "ids": [1, 2],
  "reason": "期初导入批次修正"
}
```

### 15.6 校验

```http
POST /api/terminal/opening-inventory/validate
```

支持 `IDS` 和 `QUERY` 模式，返回通过、告警、失败数量、问题明细及异步任务 ID。

### 15.7 汇总和锁定

```http
POST /api/terminal/opening-inventory/aggregate
POST /api/terminal/opening-inventory/lock
```

锁定请求示例：

```json
{
  "hospitalId": 1,
  "openingDate": "2025-12-25",
  "version": 1,
  "confirm": true,
  "reason": "月初期初库存确认"
}
```

### 15.8 导入

```http
GET  /api/terminal/opening-inventory/import/template?format=xlsx
POST /api/terminal/opening-inventory/import/preview
POST /api/terminal/opening-inventory/import/commit
GET  /api/terminal/opening-inventory/import/{batchId}/issues
```

### 15.9 导出

```http
POST /api/terminal/opening-inventory/export
GET  /api/exports/tasks/{taskId}
```

### 15.10 HTTP 状态码

| 状态 | 含义 |
|---:|---|
| 400 | 参数、日期、单位或数量格式错误 |
| 401 | 未登录 |
| 403 | 功能/医院/锁定权限不足 |
| 404 | 记录或主数据不存在 |
| 409 | 重复、版本、锁定或并发冲突 |
| 422 | 业务校验不通过 |
| 500 | 服务端异常 |

## 16. 与其他模块关系

### 16.1 医院供货和终端销量

期初库存作为理论库存起点：

```text
理论库存 = 期初库存 + 有效医院供货 - 有效终端销量 ± 调整
```

期初数据不应重复包含期初日期之后的供货和销量。

### 16.2 整袋库存和零散库存

如果期初库存按形态拆分：

```text
期初实际库存标准量 = 期初整袋标准量 + 期初零散标准量
```

建议整袋/零散模块保留明细，期初模块保存汇总来源和基准关系；不得在多个模块重复录入同一数量。

### 16.3 医院库存

医院库存可作为期初数据来源或对账结果。若从医院库存生成期初候选，必须保存来源库存快照 ID、日期和版本，生成后仍需校验和锁定。

### 16.4 盘点库存

盘点库存使用期初库存核对账面库存，并将期初来源版本写入盘点快照。期初修正后相关盘点结果标记待重算，历史确认结果不静默覆盖。

### 16.5 要货指导

要货指导读取包含期初在内的理论库存。期初锁定、修正或作废会影响月均消耗、可用时长和补货量，应发布待重算事件。

### 16.6 建议领域事件

- `OpeningInventoryValidated`
- `OpeningInventoryAggregated`
- `OpeningInventoryLocked`
- `OpeningInventoryChanged`
- `OpeningInventoryVoided`
- `OpeningInventoryRolledOver`

事件包含医院、核算周期、物料、批号、期初日期、数量、版本、来源和操作人。

## 17. 前端交互规范

### 17.1 按钮状态

| 按钮 | 无选择 | 选择 1 条 | 选择多条 |
|---|---|---|---|
| 新增 | 可用 | 可用 | 可用 |
| 修改 | 禁用 | 按状态可用 | 禁用 |
| 删除 | 禁用 | 按状态可用 | 按状态可用 |
| 导出 | 可用 | 可用 | 可用 |
| 导入 | 可用 | 可用 | 可用 |
| 校验 | 确认后校验当前查询 | 校验选中项 | 校验选中项 |
| 汇总 | 确认后汇总当前查询 | 汇总选中项 | 汇总选中项 |
| 锁定 | 按周期确认 | 按周期确认 | 按周期确认 |

### 17.2 展示规范

- 空值显示“—”，0 显示 `0`；
- 颗粒量、袋数最多显示 3 位小数，去除无意义尾零；
- 锁定状态显示锁图标和“已锁定”；
- 过期、近效期、冻结和校验异常显示图标及文字；
- 规格和单位标签颜色只用于辅助展示；
- 数量使用千分位，复制和导出保留定点原值。

### 17.3 表单联动

- 选择医院后限制可选物料和批次；
- 选择物料后带出药品名称、规格、单位和映射版本；
- 选择批号后带出到期日期；
- 修改物料或批号时清空不兼容数量和日期；
- 同时输入颗粒量和袋数时实时显示差异；
- 服务端返回的计算值为最终值；
- 关闭未保存表单时二次确认。

### 17.4 锁定交互

- 锁定前弹窗显示医院、期初日期、记录数、异常数和影响范围；
- 存在阻断错误时锁定按钮不可提交；
- 锁定成功后刷新列表并显示版本、锁定人和时间；
- 解锁/修正必须进入管理员流程并填写原因；
- 普通用户点击锁定记录的编辑按钮时显示只读详情。

## 18. 审计、安全与一致性

### 18.1 审计日志

新增、修改、删除/作废、导入、校验、汇总、锁定、解锁、版本替换和导出必须记录：

- 模块、操作类型和 traceId；
- 医院、期初日期、记录 ID、业务键和版本；
- 变更前后数量、单位、批号、日期和状态摘要；
- 来源批次、来源快照、影响数量和结果；
- 操作人、IP、时间和失败原因。

### 18.2 安全

- 全部接口按医院数据范围过滤；
- 锁定、解锁和覆盖操作必须服务端鉴权；
- SQL 参数化，排序字段白名单；
- 文件上传检查扩展名、MIME、大小和内容安全；
- 禁止执行 Excel 宏、公式和外部链接；
- 下载地址短时效签名；
- 错误响应不暴露 SQL、堆栈和服务器路径。

### 18.3 一致性

- 汇总和锁定使用事务；
- 同一医院和期初周期使用唯一键/分布式锁；
- 锁定后旧版本只读；
- 期初变更与下游待重算事件可靠提交；
- 重试不重复写入；
- 历史版本不可被物理删除。

## 19. 非功能需求

### 19.1 性能

- 普通列表首屏目标不超过 2 秒，P95 不超过 4 秒；
- 支持至少 100 万条历史期初快照分页查询；
- 全量汇总使用数据库聚合；
- 50,000 行以内导入预览目标不超过 30 秒；
- 大批量导入、校验、汇总和导出转异步任务；
- 选项接口常用查询 1 秒内返回。

### 19.2 可用性

- 网络失败保留列表和表单内容；
- 异步任务离开页面后继续运行；
- 任务失败显示原因并支持安全重试；
- 页面可查看期初周期、锁定时间、数据截止时间和规则版本；
- 日期和定点数计算在各服务节点一致。

### 19.3 兼容性

- 支持 Chrome、Edge 最近两个大版本；
- 推荐分辨率 1366×768 及以上；
- 支持横向滚动、固定关键列和键盘操作；
- 状态不能只依赖颜色表达。

## 20. 测试与验收标准

### 20.1 功能验收

1. 页面入口、查询条件、工具栏和表格字段与截图信息架构一致。
2. 可按医院、物料、批号、盘点日期和到期日期组合查询。
3. 新增、修改、删除/作废、导入、校验、汇总和锁定符合权限及状态规则。
4. 物料/批号选择可正确带出药品名称、规格、单位和效期。
5. 颗粒量、袋数和标准量计算一致。
6. 锁定后普通用户不能修改或删除记录。
7. 列表、统计汇总和导出结果一致。
8. 重复快照、重复导入和并发提交不会造成期初重复。
9. 期初修正可触发盘点和要货指导待重算。
10. 历史版本可审计、可查询但不参与当前计算。

### 20.2 计算验收

样例一：

```text
规格 = 100 g/袋
袋数 = 17
颗粒量 = 100 × 17 = 1700 g
```

样例二：

```text
规格 = 200 g/袋
袋数 = 5
颗粒量 = 200 × 5 = 1000 g
```

样例三：

```text
期初库存 = 1000
有效供货 = 500
有效销量 = 200
理论库存 = 1000 + 500 - 200 = 1300
```

### 20.3 锁定验收

| 场景 | 预期 |
|---|---|
| 存在 INVALID 记录时锁定 | 阻断并显示错误数 |
| 校验通过未锁定 | 可修改、可删除 |
| 已锁定记录普通编辑 | 禁止并显示只读详情 |
| 管理员修正锁定记录 | 生成新版本，保留旧版本和原因 |
| 同医院同周期重复锁定 | 返回 409，不生成第二个当前版本 |
| 锁定请求重复提交 | 返回首次结果，不重复发布事件 |

### 20.4 边界验收

| 场景 | 预期 |
|---|---|
| 期初日期晚于当前日期 | 阻断 |
| 盘点日期早于期初日期 | 按配置阻断或警告 |
| 颗粒量为负 | 阻断 |
| 袋数为小数且不允许 | 阻断 |
| 颗粒量与规格×袋数不一致 | 阻断 |
| 批号为空且严格批次 | 阻断 |
| 到期日期早于期初日期 | 阻断或产生过期警告 |
| 同业务键重复 | 阻断，不自动相加 |
| 不同周期查询 | 分别统计，并提示跨周期合计 |

### 20.5 导入、权限和安全验收

- 正确模板可预览、校验、提交和汇总；
- 非法日期、物料、批号、单位、数量和重复行逐行报错；
- 相同文件重复提交不重复写入；
- 无权限医院不能通过列表、导出、详情或导入获取数据；
- 锁定/解锁接口严格按权限拦截；
- 已被下游引用的锁定版本不能物理删除；
- 所有新增、修改、作废、导入、校验、汇总、锁定和导出均有审计日志。

### 20.6 性能验收

- 百万级数据分页查询命中索引；
- 全量汇总不将所有数据加载到应用内存；
- 50,000 行任务显示进度；
- 并发提交同一业务键只生成一条有效记录；
- 异步任务失败可重试且不产生重复版本。

## 21. 开发拆分建议

### 21.1 后端与数据

1. 确认期初日期、盘点日期、周期和结转口径。
2. 确认物料、规格、单位、批号和换算版本。
3. 新建期初快照、周期、导入和问题表。
4. 实现查询、分页、排序、统计和医院权限过滤。
5. 实现新增、修改、软删除、版本和乐观锁。
6. 实现数量、日期、批号、效期、重复和主数据校验。
7. 实现导入预览、提交、错误文件、幂等和异步任务。
8. 实现汇总、锁定、解锁和版本替换。
9. 实现与供货、销量、整袋、零散、医院库存、盘点和要货指导的待重算关系。
10. 编写单元、接口、并发、权限和性能测试。

### 21.2 前端

1. 新建独立期初库存页面和路由。
2. 实现医院、物料、批号、盘点日期和到期日期查询。
3. 实现表格、固定列、操作列、分页和双指标统计行。
4. 实现新增/编辑表单及主数据联动。
5. 实现颗粒量/袋数联动预览和单位提示。
6. 实现批量/行级删除、校验、汇总和锁定确认。
7. 实现导入预览、错误筛选、下载和提交进度。
8. 实现导出、权限、锁定、版本和异常状态展示。

### 21.3 测试

- 页面字段、查询、分页、统计和滚动；
- CRUD、状态、锁定、版本和审计；
- 数量、单位、规格、批号、效期和重复；
- 期初结转、理论库存和下游待重算；
- 导入、导出、校验、幂等、并发和任务重试；
- 权限、越权、注入、文件安全和性能；
- Chrome、Edge、1366×768 兼容性。

## 22. 待业务确认项

| 优先级 | 待确认项 | 影响 |
|---|---|---|
| P0 | 期初基准日期与盘点日期是否相同 | 影响字段和计算时点 |
| P0 | 期初库存是首次上线导入、自然月初还是财务期初 | 影响周期模型 |
| P0 | 颗粒量真实单位、标准单位和换算规则 | 影响字段和统计 |
| P0 | 袋数是否必填、是否允许小数、是否允许由颗粒量反推 | 影响表单和公式 |
| P0 | 批号是否必填及是否严格按批次管理 | 影响业务键和追溯 |
| P0 | 到期日当天及已过期期初库存是否计入可用量 | 影响下游库存 |
| P0 | 保存即生效还是校验后生效 | 影响状态 |
| P0 | 期初是否必须锁定，锁定后如何修正 | 影响权限、版本和审计 |
| P0 | 上期末是否自动结转为本期期初 | 影响来源和任务 |
| P0 | 期初库存与医院库存/整袋/零散库存的主数据来源 | 影响重复入账风险 |
| P1 | 期初是否按库位、科室或库存形态拆分 | 影响业务键和表结构 |
| P1 | 颗粒量与袋数差异阈值 | 影响校验策略 |
| P1 | 负库存、零库存和冻结库存处理 | 影响状态和可用量 |
| P1 | 导入错误行是否保存为 `INVALID` | 影响导入流程 |
| P1 | 锁定是否需要审批/双人复核 | 影响流程和权限 |
| P1 | 导出上限、保留时间和水印 | 影响导出实现 |
| P2 | 是否需要调整单、结转任务和历史版本对比 | 影响后续范围 |

## 23. 发布前检查清单

- [ ] P0 业务口径全部确认并写入数据字典/规则配置；
- [ ] 期初日期、盘点日期和核算周期模型确定；
- [ ] 医院、物料、规格、单位、批号和效期映射可用；
- [ ] 期初快照、周期、导入和问题表创建完成；
- [ ] 页面字段、按钮、分页和统计行与截图逐项核对；
- [ ] 颗粒量、袋数、单位和精度测试通过；
- [ ] 重复、日期、批号、效期、锁定和版本测试通过；
- [ ] 导入预览、提交、错误文件和幂等通过；
- [ ] 理论库存和下游待重算关系验证通过；
- [ ] 权限、审计、异步任务、下载安全和性能验证通过；
- [ ] 业务 UAT 完成并确认真实期初数据。

