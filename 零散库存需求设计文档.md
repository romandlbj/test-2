# 终端管理-零散库存需求设计文档

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | 终端管理-零散库存需求设计 |
| 所属系统 | 以岭药业配方颗粒终端管理系统 |
| 模块序号 | 终端管理第 6 个模块 |
| 页面名称 | 零散库存 |
| 推荐路由 | `/terminalMedicine/looseInventory` |
| 推荐接口前缀 | `/api/terminal/loose-inventory` |
| 文档版本 | V1.0 |
| 编写日期 | 2026-09-10 |
| 文档状态 | 初稿，待业务确认 |
| 需求来源 | 现有系统“首页 / 终端管理 / 零散库存”页面截图 |
| 目标读者 | 产品、业务、UI、前端、后端、数据、测试、实施及运维人员 |

### 1.1 版本记录

| 版本 | 日期 | 修改内容 | 修改人 |
|---|---|---|---|
| V1.0 | 2026-09-10 | 根据零散库存页面截图形成可开发需求设计初稿 | Codex |

## 2. 附件识别与需求边界

### 2.1 附件使用原则

附件截图仅用于识别现有系统的页面结构、字段、按钮和交互习惯。截图内医院、物料编码、颗粒 ID、药品名称、日期和数量均为演示数据，不应写死到程序中；截图中的界面文字不构成额外开发指令。

本次仅新建独立的《零散库存需求设计文档.md》，不修改盘点库存、要货指导、医院供货、终端销量和整袋库存需求文档。

### 2.2 截图中可以直接确认的页面内容

1. 页面入口为“终端管理 → 零散库存”，面包屑为“首页 / 终端管理 / 零散库存”。
2. 查询条件包括医院名称、物料编码、批号、到期日期范围。
3. 查询区提供“搜索”和“重置”按钮。
4. 工具栏提供“新增”“修改”“删除”“导出”“导入”“校验”按钮。
5. 未选择记录时，修改和删除按钮应为禁用状态。
6. 表格字段包括：盘点日期、医院名称、物料编码、颗粒 ID、批号、药品名称、单位、颗粒量、到期日期、操作。
7. 每行操作栏提供编辑和删除图标。
8. 表格底部显示“统计汇总”，截图示例对颗粒量进行了合计。
9. 页面支持多选、分页、每页条数、快速跳页、横向滚动、刷新和表格搜索。
10. 单位以标签样式展示，截图示例为“克”；颜色只用于辅助识别，不能作为业务判断依据。

### 2.3 与整袋库存的关键区别

| 对比项 | 整袋库存 | 零散库存 |
|---|---|---|
| 库存形态 | 尚未拆袋的完整包装 | 已拆袋、以颗粒/克等单位保存的余量 |
| 核心数量 | 袋数，颗粒量由规格换算 | 直接录入或换算得到的零散颗粒量 |
| 规格依赖 | 通常必须有每袋规格 | 可不需要袋数，但必须有单位和颗粒 ID 映射 |
| 是否允许小数 | 默认不允许小数袋 | 默认允许数量小数，精度由单位配置决定 |
| 拆袋联动 | 拆袋后整袋减少 | 拆袋后零散库存增加 |
| 统计方式 | 可按袋数及颗粒量合计 | 主要按标准颗粒/克单位合计 |
| 盘点含义 | 整包现场库存快照 | 已开包余量现场库存快照 |

### 2.4 截图无法确认的内容

以下内容需按本文默认方案开发，并在上线前经业务确认：

- 零散库存数量的真实物理单位，是克、颗粒个数还是其他标准单位；
- 颗粒 ID 是否唯一代表物料和单位；
- 批号是否必填、是否必须与医院供货批次绑定；
- 零散库存是否来自拆袋事件、人工盘点还是外部库存接口；
- 零散库存是否允许为 0 或负数；
- 盘点日期相同的重复记录是覆盖、合并还是新建版本；
- 到期日当天以及已过期零散库存是否计入可用库存；
- 零散库存是否需要和整袋库存自动进行拆袋扣减联动。

## 3. 背景与建设目标

### 3.1 业务背景

零散库存用于记录已经打开包装、不能再按完整袋管理的配方颗粒余量。它通常来自整袋拆开、医院调剂、盘点修正或外部系统同步，是现场实际库存的重要组成部分。

零散库存数量通常带小数，且需要同时追踪医院、盘点日期、物料、颗粒 ID、批号、单位和到期日期。若缺少统一管理，会产生：

- 整袋和零散库存重复计算；
- 拆袋后整袋数量未减少、零散数量未增加；
- 颗粒量单位不一致导致库存和要货建议错误；
- 零散库存跨批号或跨物料混用；
- 已过期或校验失败的零散库存进入可用库存；
- 同一盘点日期重复录入导致统计虚增。

### 3.2 建设目标

1. 建立医院、盘点日期、物料、颗粒 ID、批号和到期日期维度的零散库存快照。
2. 支持克数/颗粒数等配置单位下的精确小数库存管理。
3. 支持新增、修改、删除/作废、批量导入、校验和导出。
4. 对物料映射、单位、批号、效期、重复记录和数量精度进行服务端校验。
5. 将有效零散库存与有效整袋库存相加，供盘点库存和要货指导使用。
6. 对整袋拆零和零散回装等库存形态变化保留可追溯关系。

### 3.3 成功标准

- 列表、统计汇总和导出中的零散颗粒量保持一致；
- 相同业务键和相同导入文件不会重复增加库存；
- 零散库存可以追溯到医院、物料、颗粒 ID、批号、盘点日期及来源；
- 过期、近效期、零库存和异常单位均有明确提示；
- 无权限用户不能查询或修改其他医院的零散库存；
- 零散库存变更能触发实际库存和要货指导待重算。

## 4. 需求范围

### 4.1 本期范围

- 零散库存列表和分页；
- 医院、物料编码、批号、到期日期范围查询；
- 新增、修改、批量删除/作废；
- Excel/CSV 模板下载、导入预览、逐行校验、提交和错误明细下载；
- 当前查询结果导出；
- 批量校验和状态管理；
- 颗粒量全量统计汇总；
- 单位、颗粒 ID、批号、效期和重复校验；
- 与整袋库存的库存形态联动；
- 服务端权限、审计、幂等和并发控制。

### 4.2 本期不包含

- 处方、收费、患者和医生明细；
- 完整拆袋、回装、调拨和退货审批流；
- 仓库库位、货架、托盘和序列号管理；
- 移动端扫码盘点；
- 自动采购、自动要货和复杂预测模型；
- 质量检验、冷链和温湿度管理。

### 4.3 后续扩展

- 整袋拆零操作单；
- 零散回装整袋操作单；
- 批次 FIFO/FEFO 自动扣减；
- 扫码识别颗粒 ID 和批号；
- 零散库存低于安全库存预警；
- 零散库存变化流水和来源单据钻取。

## 5. 角色与权限

### 5.1 权限矩阵

| 角色 | 查询 | 新增 | 修改 | 删除/作废 | 导入 | 校验 | 导出 | 数据范围 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| 系统管理员 | 是 | 是 | 是 | 是 | 是 | 是 | 是 | 全部医院 |
| 区域/业务管理员 | 是 | 是 | 是 | 按授权 | 是 | 是 | 是 | 授权区域医院 |
| 医院盘点人员 | 是 | 是 | 是 | 本人草稿 | 是 | 是 | 可配置 | 所属医院 |
| 库存管理人员 | 是 | 是 | 是 | 按授权 | 是 | 是 | 是 | 授权医院 |
| 审计/财务人员 | 是 | 否 | 否 | 否 | 否 | 否 | 是 | 授权医院 |
| 只读用户 | 是 | 否 | 否 | 否 | 否 | 否 | 可配置 | 授权范围 |

### 5.2 权限编码

- `terminal:loose-inventory:view`
- `terminal:loose-inventory:create`
- `terminal:loose-inventory:update`
- `terminal:loose-inventory:delete`
- `terminal:loose-inventory:import`
- `terminal:loose-inventory:validate`
- `terminal:loose-inventory:export`
- `terminal:loose-inventory:override`
- `terminal:loose-inventory:adjust`

服务端必须执行功能权限、医院数据范围和记录状态校验；前端隐藏/禁用按钮不能作为唯一安全措施。

## 6. 页面设计

### 6.1 页面入口

- 左侧导航：终端管理 → 零散库存；
- 页面路由：`/terminalMedicine/looseInventory`；
- 面包屑：首页 / 终端管理 / 零散库存；
- 接口前缀：`/api/terminal/loose-inventory`。

### 6.2 页面区域

1. 面包屑和页签区；
2. 查询区：医院名称、物料编码、批号、到期日期范围；
3. 工具栏：新增、修改、删除、导出、导入、校验；
4. 表格区：选择、字段展示、行级编辑和删除；
5. 统计区：颗粒量合计；
6. 分页区：总条数、每页条数、页码和跳页；
7. 右侧操作：表格搜索、刷新。

### 6.3 页面结构示意

```text
首页 / 终端管理 / 零散库存

[医院名称 ▼] [物料编码] [批号] [到期开始 - 到期结束] [搜索] [重置]

[新增] [修改] [删除] [导出] [导入] [校验]                  [搜索] [刷新]

┌选择┬盘点日期*┬医院名称*┬物料编码┬颗粒ID*┬批号┬药品名称*┬单位*┬颗粒量*┬到期日期*┬操作┐
│ □  │          │          │        │      │    │          │     │        │          │编辑 删除│
├────┼──────────┼──────────┼────────┼──────┼────┼──────────┼─────┼────────┼──────────┼─────────┤
│ □  │ ...      │ ...      │ ...    │ ...  │... │ ...      │ ... │ ...    │ ...      │ ✎  🗑   │
├────┴──────────┴──────────┴────────┴──────┴────┴──────────┴─────┴────────┴──────────┴─────────┤
│ 统计汇总                                                   颗粒量合计：...                      │
└───────────────────────────────────────────────────────────────────────────────────────────────┘
```

### 6.4 表格固定与滚动

- 左侧固定选择列、盘点日期、医院名称；
- 右侧固定操作列；
- 物料、颗粒 ID、批号、药品、单位、颗粒量和到期日期支持横向滚动；
- 表头和统计行垂直滚动时保持可见；
- 推荐支持 1366×768 及以上分辨率。

## 7. 查询需求

### 7.1 查询字段

| 页面字段 | API 参数 | 控件 | 必填 | 规则 |
|---|---|---|---:|---|
| 医院名称 | `hospitalId` | 可搜索下拉 | 否 | 仅显示授权医院，提交医院 ID |
| 物料编码 | `materialCode` | 文本框 | 否 | 去首尾空格，模糊匹配，最长 64 字符 |
| 批号 | `batchNo` | 文本框 | 否 | 模糊匹配，最长 64 字符 |
| 到期日期开始 | `expiryStartDate` | 日期选择器 | 否 | 闭区间起点 |
| 到期日期结束 | `expiryEndDate` | 日期选择器 | 否 | 不早于开始日期，闭区间终点 |
| 页码 | `page` | 分页器 | 否 | 默认 1，最小 1 |
| 每页条数 | `pageSize` | 分页器 | 否 | 默认 10，可选 10/20/50/100/200 |
| 排序字段 | `sortField` | 表头 | 否 | 服务端白名单 |
| 排序方向 | `sortOrder` | 表头 | 否 | `asc`/`desc` |

### 7.2 查询行为

1. 多条件之间使用 AND 关系。
2. 无筛选条件时返回当前用户权限范围内全部未作废记录。
3. 默认排序：盘点日期倒序、到期日期升序、医院名称升序、ID 倒序。
4. 点击搜索回到第 1 页；物料编码和批号支持回车搜索。
5. 点击重置清空条件、恢复默认排序和第 1 页。
6. 查询期间显示 loading 并防止重复请求。
7. 无数据时显示空状态并保留查询条件。
8. 到期日期筛选为闭区间。
9. 列表、总数、颗粒量汇总使用相同查询条件、状态过滤和权限过滤。
10. 跨多个盘点日期查询时，在统计区域提示“合计包含多个时点，不代表单一时点库存”。

### 7.3 高级筛选预留

接口预留盘点日期、药品名称、颗粒 ID、校验状态、到期状态、来源类型和是否包含作废记录，首版可放在高级筛选抽屉中。

## 8. 表格字段设计

| 页面列 | API 字段 | 数据库字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---:|---|
| 选择 | `selected` | - | Boolean | - | 表头只全选当前页 |
| 盘点日期* | `stocktakeDate` | `stocktake_date` | Date | 是 | 零散库存时点快照日期 |
| 医院名称* | `hospitalName` | `hospital_name` | String/快照 | 是 | 最长 120 字符 |
| 医院 ID | `hospitalId` | `hospital_id` | Long | 是 | 权限与关联字段 |
| 物料编码 | `materialCode` | `material_code` | String | 条件必填 | 可由颗粒 ID 反查 |
| 物料 ID | `materialId` | `material_id` | Long | 条件必填 | 关联物料主数据 |
| 颗粒 ID* | `particleId` | `particle_id` | String | 是 | 来源颗粒主数据唯一标识 |
| 批号 | `batchNo` | `batch_no` | String | 条件必填 | 严格批次管理时必填 |
| 药品名称* | `drugName` | `drug_name` | String/快照 | 是 | 由物料/颗粒主数据带出 |
| 单位* | `unitCode`/`unitName` | `unit_code`/`unit_name` | String | 是 | 示例为克；必须来自单位字典 |
| 颗粒量* | `particleQty` | `particle_qty` | Decimal(18,3) | 是 | 允许小数，服务端按单位精度校验 |
| 标准颗粒量 | `standardParticleQty` | `standard_particle_qty` | Decimal(18,3) | 是 | 换算到标准库存单位后的数量 |
| 到期日期* | `expiryDate` | `expiry_date` | Date | 是 | 批次/颗粒有效期 |
| 校验状态 | `validationStatus` | `validation_status` | Enum | 是 | `DRAFT`/`VALIDATED`/`INVALID`/`VOID` |
| 到期状态 | `expiryStatus` | - | Enum | - | 按盘点日期动态计算 |
| 来源类型 | `sourceType` | `source_type` | Enum | 是 | `MANUAL`/`IMPORT`/`API`/`SPLIT` |
| 版本 | `version` | `version` | Integer | 是 | 乐观锁 |
| 操作 | `actions` | - | - | - | 行级编辑、删除/作废 |

### 8.1 颗粒量与标准颗粒量

- `particleQty` 保存页面和来源记录中的数量及其单位；
- `standardParticleQty` 保存换算到系统标准库存单位后的数量；
- 如果系统标准单位就是克，两者数值可以相同，但字段语义仍需区分；
- 页面必须在列头 tooltip、详情和导出说明中标明单位；
- 不能在不同模块中把“颗粒量”一会儿理解为克、一会儿理解为颗粒个数。

### 8.2 统计汇总

默认对当前查询下全部有效记录计算：

```text
颗粒量合计 = Σ standardParticleQty
```

如果所有记录单位一致，可同时返回页面单位下的 `particleQty` 合计。若单位不一致，必须按标准单位换算后合计，并返回 `unconvertibleCount`。

## 9. 数据口径与业务粒度

### 9.1 模块定义

零散库存表示医院现场已经拆袋、按颗粒/克等单位保管的余量，是某一盘点日期的时点快照，不是医院历史消耗流水。

### 9.2 最小业务粒度

默认业务键：

```text
盘点日期 + 医院 ID + 颗粒 ID + 批号 + 到期日期 + 单位版本
```

如果同一颗粒 ID、批号和日期存在多个库位或容器，需要增加 `locationId`/`containerId` 作为业务键；当前截图未展示库位，V1 默认不分库位。

### 9.3 快照与流水

- 零散库存是某日现场余量，不是终端销量；
- 拆袋事件是流水，零散库存是拆袋后的结果快照；
- 同一天重复盘点不得直接累加；
- 不同盘点日期的零散量不能直接相加作为当前库存；
- 盘点汇总应选择指定日期最近一次有效快照或业务确认版本。

### 9.4 与整袋库存的关系

默认库存公式：

```text
实际库存标准量 = 有效整袋库存标准量 + 有效零散库存标准量
```

整袋拆零时：

```text
整袋数量减少 = 拆袋数量
零散数量增加 = 拆袋数量 × 每袋标准量 - 实际损耗量
```

拆袋产生的损耗、称量误差或盘点调整必须有独立事件，不允许在零散库存页面直接隐式修改整袋数量。

## 10. 核心业务规则

### 10.1 主数据匹配

1. 医院必须存在、启用且在当前用户授权范围内。
2. 颗粒 ID 必须有效映射到一个物料、药品名称和单位。
3. 如果同时提交物料编码和颗粒 ID，两者必须属于同一物料。
4. 药品名称、单位由主数据带出并保存快照，不允许手工改名绕过映射。
5. 物料或颗粒 ID 发生版本变化时，新记录使用新版本，历史记录保留旧快照。
6. 颗粒 ID 映射冲突时，记录不能进入 `VALIDATED`。

### 10.2 数量与精度

- `particleQty` 必须是合法十进制定点数；
- 默认允许 0～3 位小数，具体精度由单位字典决定；
- 负数不能用于表示损耗或退货，损耗和退货使用独立事件；
- 不能使用 NaN、Infinity、科学计数法或超出数据库精度的值；
- 中间换算至少保留 6 位小数，最终标准量按 `DECIMAL(18,3)`、`HALF_UP` 保存；
- 单位精度不足时阻断，不应静默截断。

### 10.3 颗粒量换算

默认换算：

```text
standardParticleQty = particleQty × conversionFactor
```

其中 `conversionFactor` 根据颗粒 ID、单位和生效日期匹配，不能由前端固定写死。

若单位为克且系统标准单位为克：

```text
conversionFactor = 1
```

若单位为其他可换算单位，必须有明确配置。缺少换算配置时记录为 `INVALID`，不能按 1 处理。

### 10.4 盘点日期

- 统一使用 `Asia/Shanghai` 业务日期；
- 盘点日期默认不能晚于当前日期；
- 历史补录最大回溯天数默认 366 天，可配置；
- 同一医院、颗粒 ID、批号和日期的重复记录默认阻断；
- 同一天重新盘点应更新已有草稿或生成新版本，由业务确认后替换原有效版本；
- 不同日期记录分别保存，不能用后一天记录覆盖前一天历史。

### 10.5 批号与效期

- 批号长度 1～64 字符，去除首尾空格；
- 批号应与医院供货批次、物料和颗粒 ID 匹配；
- 同一医院、颗粒 ID、批号对应多个到期日时标记冲突；
- 严格批次模式下批号为空阻断；非严格模式下允许保存但产生警告；
- 到期日期原则上不早于供货日期；
- 过期记录可保存为实际现场库存，但不计入可用库存。

### 10.6 到期状态

以盘点日期为基准动态计算：

```text
EXPIRED       = expiryDate < stocktakeDate
EXPIRING_SOON = stocktakeDate <= expiryDate <= stocktakeDate + warningDays
NORMAL        = expiryDate > stocktakeDate + warningDays
```

默认 `warningDays=90`。列表显示红色/橙色图标和文字，导出输出状态文本。到期日当天是否有效需要业务确认。

### 10.7 零库存与负库存

- `particleQty=0` 默认可保存为盘点结果，并标记 `ZERO_STOCK`；
- 零库存是否保留在列表取决于配置，默认保留以便审计；
- 负数默认禁止；
- 若要表达损耗、冲销或退货，必须使用库存调整事件，不能录入负零散库存。

### 10.8 过期和可用库存

```text
availableStandardParticleQty =
  0,                          当 expiryDate < stocktakeDate
  standardParticleQty,        其他情况且状态为 VALIDATED
```

冻结、质量异常或召回批次同样不进入可用库存，但应保留现场库存数和冻结原因。

### 10.9 重复与幂等

- 手工新增通过业务键查询重复候选；
- 文件导入使用文件 SHA-256、导入批次和行哈希防重；
- API 同步优先使用来源系统和来源业务 ID；
- 同一幂等键重复提交返回首次结果；
- 数据库唯一约束或分布式锁防止并发重复写入；
- 重复不自动累加数量。

## 11. 状态模型

| 状态 | 含义 | 默认显示 | 可修改 | 可删除/作废 | 进入下游 |
|---|---|---:|---:|---:|---:|
| `DRAFT` | 新增或导入后未校验 | 是 | 是 | 是 | 否 |
| `VALIDATED` | 通过阻断性校验 | 是 | 按权限 | 已引用时只能作废 | 是 |
| `INVALID` | 存在阻断错误 | 是 | 是 | 是 | 否 |
| `VOID` | 已作废 | 默认否 | 否 | 否 | 否 |

问题级别为 `ERROR`、`WARNING`、`INFO`。有 `ERROR` 时必须为 `INVALID`；仅有允许的警告时可以进入 `VALIDATED`。

## 12. 操作功能设计

### 12.1 新增

点击“新增”打开抽屉或弹窗。

| 字段 | 控件 | 默认值 | 可编辑 | 规则 |
|---|---|---|---:|---|
| 盘点日期 | 日期选择器 | 当前日期 | 是 | 受未来日期和回溯限制 |
| 医院名称 | 远程搜索下拉 | 空 | 是 | 仅授权医院 |
| 颗粒 ID | 远程搜索下拉 | 空 | 是 | 选择后带出物料、药品和单位 |
| 物料编码 | 只读/联动 | 自动带出 | 条件可编辑 | 与颗粒 ID 必须一致 |
| 药品名称 | 只读 | 自动带出 | 否 | 保存快照 |
| 批号 | 批次下拉/文本 | 空 | 是 | 严格批次模式必填 |
| 单位 | 只读标签 | 自动带出 | 否 | 由映射配置确定 |
| 颗粒量 | 数值框 | 空/0 | 是 | 按单位精度输入，不能为负 |
| 到期日期 | 日期选择器 | 批次带出 | 条件可编辑 | 与批号一致 |

保存流程：

1. 前端检查必填、日期和数值格式；
2. 服务端重新匹配医院、颗粒 ID、物料、单位和批次；
3. 服务端计算标准颗粒量；
4. 检查重复业务键及批次关系；
5. 默认保存为 `DRAFT`，或按配置完成即时校验；
6. 成功后关闭表单并刷新当前列表，保留查询条件。

### 12.2 修改

- 选择 1 条记录时修改按钮可用；选择 0 条或多条时禁用；
- 行级编辑图标打开同一编辑表单；
- `DRAFT`、`INVALID` 默认可修改；
- `VALIDATED` 只有覆盖权限可修改，并必须填写原因；
- 修改颗粒 ID、批号、日期、单位或数量后必须重新校验；
- 已被盘点或要货指导引用的记录不得直接覆盖历史版本；
- 使用 `version` 乐观锁，版本冲突返回 HTTP 409。

### 12.3 删除/作废

- 工具栏支持批量删除/作废，行级操作处理单条记录；
- 删除前显示记录数量、状态和下游影响；
- `DRAFT`、`INVALID` 软删除；
- `VALIDATED` 只能作废；
- 已被确认盘点、库存汇总或要货指导引用时，禁止物理删除并触发待重算；
- 作废原因必填，长度 2～200 字；
- 返回成功数、作废数、阻断数和逐条原因。

### 12.4 校验

1. 有选择时校验选中记录；
2. 无选择时确认后校验当前查询结果；
3. 超过 5,000 条转异步任务；
4. 校验医院、颗粒 ID、物料、单位、批号、效期、数量、精度、重复及整袋联动关系；
5. 通过后状态改为 `VALIDATED`，失败改为 `INVALID`；
6. 校验不擅自修改业务数量；
7. 结果返回通过、警告、失败数量和逐条问题。

### 12.5 导入

#### 12.5.1 文件要求

- 默认支持 `.xlsx`，可选支持 UTF-8 BOM `.csv`；
- 最大文件 20 MB；
- 单文件最大 50,000 行；
- 禁止宏、公式执行、外部链接和可执行内容；
- 校验扩展名、MIME、内容结构和病毒风险。

#### 12.5.2 模板字段

```text
盘点日期,医院名称,医院ID(可选),物料编码(可选),颗粒ID,批号,
药品名称(可选),单位(可选),颗粒量,到期日期
```

物料编码、药品名称和单位可以作为辅助列，但服务端以颗粒 ID 映射为准。

#### 12.5.3 导入流程

```text
下载模板 → 上传 → 解析预览 → 逐行校验 → 查看错误/警告
→ 下载错误明细 → 确认提交 → 再次校验 → 写入快照 → 返回结果
```

- 预览不写入正式库存表；
- 完全重复行不自动累加；
- 提交阶段重新校验主数据、重复和版本；
- 默认只写入无阻断错误的行；
- 是否将错误行保存为 `INVALID` 由配置决定；
- 相同文件重复提交必须幂等。

### 12.6 导出

- 导出当前查询条件下全部有效记录，不仅当前页；
- 默认 `.xlsx`，可兼容 `.csv`；
- 包含页面字段、标准颗粒量、校验状态、到期状态、来源、版本和问题摘要；
- 文件包含查询条件、导出时间、盘点日期范围和单位说明；
- 超过 50,000 行转异步任务；
- 下载地址默认 30 分钟失效；
- 记录导出人、条件、文件名和下载状态。

## 13. 校验规则与错误码

| 场景 | 默认处理 | 错误码 |
|---|---|---|
| 医院无权限 | 阻断/403 | `LI_HOSPITAL_FORBIDDEN` |
| 颗粒 ID 不存在 | 阻断 | `LI_PARTICLE_NOT_FOUND` |
| 物料与颗粒 ID 不一致 | 阻断 | `LI_MATERIAL_MISMATCH` |
| 单位缺失/冲突 | 阻断 | `LI_UNIT_INVALID` |
| 换算配置缺失 | 阻断 | `LI_CONVERSION_MISSING` |
| 颗粒量为负 | 阻断 | `LI_QTY_NEGATIVE` |
| 数量超过单位精度 | 阻断 | `LI_QTY_PRECISION_INVALID` |
| 批号不匹配 | 阻断/警告 | `LI_BATCH_MISMATCH` |
| 到期日期缺失 | 阻断 | `LI_EXPIRY_REQUIRED` |
| 已过期 | 警告，不计可用量 | `LI_EXPIRED` |
| 近效期 | 警告 | `LI_EXPIRING_SOON` |
| 业务键重复 | 阻断 | `LI_DUPLICATE_RECORD` |
| 整袋/零散联动不平衡 | 按配置阻断或警告 | `LI_SPLIT_BALANCE_INVALID` |
| 已被下游引用 | 禁止直接删除 | `LI_ALREADY_REFERENCED` |
| 乐观锁冲突 | 返回 409 | `LI_VERSION_CONFLICT` |
| 文件重复提交 | 返回历史结果 | `LI_IMPORT_DUPLICATE` |

## 14. 数据库设计

### 14.1 主表 `loose_inventory_snapshot`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `stocktake_date` | DATE | 盘点日期 |
| `hospital_id` | BIGINT | 医院 ID |
| `hospital_name` | VARCHAR(120) | 医院名称快照 |
| `material_id` | BIGINT | 物料 ID |
| `material_code` | VARCHAR(64) | 物料编码快照 |
| `particle_id` | VARCHAR(64) | 颗粒 ID |
| `batch_no` | VARCHAR(64) | 批号 |
| `drug_name` | VARCHAR(120) | 药品名称快照 |
| `unit_code` | VARCHAR(20) | 页面单位编码 |
| `unit_name` | VARCHAR(20) | 页面单位名称快照 |
| `particle_qty` | DECIMAL(18,3) | 原单位零散量 |
| `standard_particle_qty` | DECIMAL(18,3) | 标准库存单位数量 |
| `conversion_factor` | DECIMAL(18,6) | 换算系数快照 |
| `expiry_date` | DATE | 到期日期 |
| `available_particle_qty` | DECIMAL(18,3) | 可用标准量 |
| `validation_status` | VARCHAR(20) | `DRAFT`/`VALIDATED`/`INVALID`/`VOID` |
| `validation_version` | VARCHAR(32) | 校验规则版本 |
| `warning_codes` | VARCHAR(1000) | 告警编码集合 |
| `source_type` | VARCHAR(20) | `MANUAL`/`IMPORT`/`API`/`SPLIT` |
| `source_document_no` | VARCHAR(128) | 来源单号/拆袋单号 |
| `source_batch_id` | VARCHAR(64) | 导入或同步批次 |
| `source_row_no` | INT | 文件行号 |
| `dedupe_hash` | CHAR(64) | 防重哈希 |
| `version` | INT | 乐观锁版本 |
| `created_by`/`created_at` | BIGINT/DATETIME | 创建信息 |
| `updated_by`/`updated_at` | BIGINT/DATETIME | 修改信息 |
| `deleted_at` | DATETIME | 软删除时间 |

建议索引：

```text
uk_li_business (stocktake_date, hospital_id, particle_id, batch_no, expiry_date, deleted_at)
idx_li_query (hospital_id, stocktake_date, material_code, particle_id, batch_no, expiry_date, deleted_at)
idx_li_expiry (expiry_date, validation_status, deleted_at)
idx_li_source (source_batch_id, source_row_no)
```

如果批号允许为空，不能依赖 MySQL 可空唯一索引实现防重，应使用规范化空值、有效状态列或单独业务键表。

### 14.2 单位映射表 `particle_unit_mapping`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `source_system` | VARCHAR(64) | 来源系统 |
| `particle_id` | VARCHAR(64) | 颗粒 ID |
| `material_id` | BIGINT | 物料 ID |
| `unit_code` | VARCHAR(20) | 来源单位 |
| `standard_unit_code` | VARCHAR(20) | 标准库存单位 |
| `conversion_factor` | DECIMAL(18,6) | 换算系数 |
| `scale` | INT | 允许小数位 |
| `effective_from` | DATE | 生效日期 |
| `effective_to` | DATE | 失效日期 |
| `version_no` | VARCHAR(32) | 版本 |
| `status` | VARCHAR(20) | 状态 |

同一颗粒 ID、业务日期只能命中一个有效单位映射版本。

### 14.3 导入批次和问题表

建议建立：

- `loose_inventory_import_batch`：文件名、SHA-256、状态、总行数、有效行数、错误行数、写入数和操作人；
- `loose_inventory_import_issue`：批次、行号、字段名、原值、问题级别、错误码和修复建议；
- `loose_inventory_validation_issue`：库存记录当前/历史校验问题。

### 14.4 拆袋联动表（后续或本期预留）

建议预留 `inventory_split_event`：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `hospital_id` | BIGINT | 医院 ID |
| `material_id` | BIGINT | 物料 ID |
| `batch_no` | VARCHAR(64) | 批号 |
| `event_date` | DATE | 拆袋日期 |
| `whole_bag_delta` | DECIMAL(18,3) | 整袋变化，通常为负 |
| `loose_particle_delta` | DECIMAL(18,3) | 零散变化 |
| `loss_particle_qty` | DECIMAL(18,3) | 损耗量 |
| `source_document_no` | VARCHAR(128) | 来源单号 |
| `status` | VARCHAR(20) | 状态 |

V1 即使不开放拆袋操作，也应允许零散记录保存 `sourceType=SPLIT` 和来源单号。

### 14.5 与现有模块关系

- 复用医院、物料/颗粒映射和操作审计日志；
- 零散库存使用独立快照表，不写入整袋库存表、供货流水或销量流水；
- 盘点库存读取指定日期的有效整袋和零散库存；
- 要货指导使用有效可用标准库存量；
- 统一保存来源批次、映射版本和规则版本。

## 15. 接口设计

### 15.1 列表查询

```http
GET /api/terminal/loose-inventory
```

请求参数：`hospitalId`、`materialCode`、`particleId`、`batchNo`、`expiryStartDate`、`expiryEndDate`、`stocktakeStartDate`、`stocktakeEndDate`、`status`、`page`、`pageSize`、`sortField`、`sortOrder`。

响应示例：

```json
{
  "items": [
    {
      "id": 1,
      "stocktakeDate": "2025-12-23",
      "hospital": { "id": 1, "name": "衡水市人民医院" },
      "material": {
        "id": 1602,
        "code": "02.22.003.1602",
        "drugName": "阿胶"
      },
      "particleId": "4633",
      "batchNo": "B2511001",
      "unit": {
        "code": "G",
        "name": "克",
        "standardCode": "G"
      },
      "particleQty": 288.5,
      "standardParticleQty": 288.5,
      "expiryDate": "2028-09-24",
      "expiryStatus": "NORMAL",
      "availableParticleQty": 288.5,
      "validationStatus": "VALIDATED",
      "sourceType": "MANUAL",
      "version": 1,
      "warnings": []
    }
  ],
  "summary": {
    "recordCount": 1,
    "particleQty": 288.5,
    "standardParticleQty": 288.5,
    "availableParticleQty": 288.5,
    "expiredCount": 0,
    "invalidCount": 0,
    "unconvertibleCount": 0
  },
  "page": 1,
  "pageSize": 10,
  "total": 1,
  "queryMeta": {
    "inventoryNature": "SNAPSHOT",
    "standardUnit": "G",
    "asOfDate": "2025-12-23",
    "calculationVersion": "v1"
  }
}
```

### 15.2 选项接口

```http
GET /api/terminal/loose-inventory/options/hospitals?keyword=衡水
GET /api/terminal/loose-inventory/options/particles?keyword=4633&hospitalId=1
GET /api/terminal/loose-inventory/options/batches?hospitalId=1&particleId=4633
```

颗粒选项至少返回颗粒 ID、物料编码、药品名称、单位、标准单位、换算系数、配置版本和可选有效批次。

### 15.3 新增

```http
POST /api/terminal/loose-inventory
Idempotency-Key: <uuid>
Content-Type: application/json
```

```json
{
  "stocktakeDate": "2025-12-23",
  "hospitalId": 1,
  "particleId": "4633",
  "materialId": 1602,
  "batchNo": "B2511001",
  "particleQty": 288.5,
  "expiryDate": "2028-09-24",
  "sourceType": "MANUAL"
}
```

服务端重新匹配物料、药品、单位和换算系数，计算标准颗粒量并校验重复。

### 15.4 修改

```http
PUT /api/terminal/loose-inventory/{id}
Content-Type: application/json
```

请求体必须包含 `version` 和修改原因。已生效且被下游引用的记录按权限生成新版本或修正事件。

### 15.5 删除/作废

```http
DELETE /api/terminal/loose-inventory
Content-Type: application/json
```

```json
{
  "ids": [1, 2],
  "reason": "重复盘点，保留最新版本"
}
```

### 15.6 校验

```http
POST /api/terminal/loose-inventory/validate
Content-Type: application/json
```

支持按 `IDS` 或当前 `QUERY` 校验，返回通过、告警、失败数量及问题明细。大批量操作返回异步任务 ID。

### 15.7 导入

```http
GET  /api/terminal/loose-inventory/import/template?format=xlsx
POST /api/terminal/loose-inventory/import/preview
POST /api/terminal/loose-inventory/import/commit
GET  /api/terminal/loose-inventory/import/{batchId}/issues
```

### 15.8 导出

```http
POST /api/terminal/loose-inventory/export
GET  /api/exports/tasks/{taskId}
```

### 15.9 错误状态码

| HTTP 状态 | 含义 |
|---:|---|
| 400 | 参数、日期、单位或数量格式错误 |
| 401 | 未登录 |
| 403 | 功能或医院数据权限不足 |
| 404 | 记录、颗粒或主数据不存在 |
| 409 | 重复、并发或版本冲突 |
| 422 | 业务校验不通过 |
| 500 | 服务端异常 |

## 16. 与其他模块关系

### 16.1 整袋库存

整袋和零散库存共同组成医院现场实际库存：

```text
实际库存标准量 = 有效整袋库存标准量 + 有效零散库存标准量
```

拆袋操作必须遵循数量守恒：

```text
整袋减少的标准量 = 零散增加的标准量 + 损耗量
```

没有拆袋事件时，两个模块各自维护快照，不允许通过页面查询时自动互相修改。

### 16.2 医院供货

零散库存批号、到期日和物料应优先匹配医院供货记录。供货记录是来源流水，零散库存是盘点时点结果，不能把供货累计量直接作为零散库存。

### 16.3 终端销量

终端销量扣减实际库存，但不能直接修改零散库存快照。若销量发生在拆袋颗粒上，应由库存计算服务根据库存形态和批次策略扣减，并保存分摊结果。

### 16.4 盘点库存

盘点库存读取同一盘点日有效整袋和零散快照，保存来源记录 ID、版本及单位换算版本。如果多个日期混查，统计行不得被误认为单日库存。

### 16.5 要货指导

要货指导使用有效、未过期、未冻结的标准库存量。零散库存变更、作废或单位配置变化时，相关医院/物料/日期范围标记为待重算。

### 16.6 建议领域事件

- `LooseInventoryValidated`
- `LooseInventoryChanged`
- `LooseInventoryVoided`
- `LooseInventoryImportFinished`
- `InventorySplitEventCreated`

事件至少包含医院、物料、颗粒 ID、批号、盘点日期、变更前后数量、标准单位量、来源批次和版本。

## 17. 前端交互规范

### 17.1 按钮状态

| 按钮 | 无选择 | 选择 1 条 | 选择多条 |
|---|---|---|---|
| 新增 | 可用 | 可用 | 可用 |
| 修改 | 禁用 | 按状态可用 | 禁用 |
| 删除 | 禁用 | 按状态可用 | 按状态可用 |
| 导出 | 可用 | 可用 | 可用 |
| 导入 | 可用 | 可用 | 可用 |
| 校验 | 确认后校验当前查询 | 校验选中项 | 校验选中项 |

权限不足时隐藏或禁用按钮，但接口仍必须服务端鉴权。

### 17.2 数值和单位展示

- 空值显示“—”，0 显示 `0`；
- 颗粒量按单位配置最多显示 3 位小数，去除无意义尾零；
- 页面单位标签和导出说明必须明确物理单位；
- 标准颗粒量可在详情或导出中展示；
- 大数显示千分位，复制/导出保留原始值；
- 负数不作为正常库存展示，异常应显示错误状态。

### 17.3 表单联动

- 选择医院后限制可选批次和颗粒范围；
- 选择颗粒 ID 后自动带出物料、药品、单位和换算系数；
- 选择批号后带出到期日期；
- 颗粒 ID 变化时清空批号、到期日期和数量；
- 输入颗粒量后实时预览标准颗粒量；
- 服务端返回的标准量为最终值；
- 关闭未保存表单时二次确认。

### 17.4 到期和异常提示

- 近效期显示橙色图标和“近效期”；
- 已过期显示红色图标和“已过期”；
- 颗粒 ID/单位映射异常显示告警图标；
- 多个问题通过详情抽屉展示完整列表；
- 颜色不能成为唯一的状态表达方式。

### 17.5 分页和选择

- 默认每页 10 条；
- 显示总条数、页码、省略页和跳页输入；
- 表头复选框默认只全选当前页；
- 若支持跨页选择，必须展示筛选范围和选中总数；
- 翻页后默认清除当前页选择，防止误操作。

## 18. 审计、安全与一致性

### 18.1 审计日志

新增、修改、删除/作废、校验、导入、导出、单位配置变更和拆袋联动必须记录：

- 模块、操作类型、traceId；
- 记录 ID、业务键、来源批次和来源单号；
- 修改前后数量、单位、批号、日期和状态摘要；
- 影响数量、结果和失败原因；
- 操作人、IP 和时间。

### 18.2 数据安全

- 所有接口按医院数据范围过滤；
- 不能通过 ID、医院参数、批量请求或导出条件越权；
- SQL 使用参数绑定，排序字段白名单；
- 上传文件进行扩展名、MIME、大小、内容和病毒校验；
- 禁止执行 Excel 公式、宏和外部链接；
- 下载地址使用短时效签名；
- 错误响应不暴露堆栈、SQL 和服务器路径。

### 18.3 一致性与事务

- 新增、修改、作废和导入提交使用事务；
- 同一业务键加唯一约束或分布式锁；
- 零散/整袋联动事件和快照更新使用可靠事务/事件机制；
- 重试不重复写入；
- 记录保存单位映射版本、校验版本和来源批次。

## 19. 非功能需求

### 19.1 性能

- 普通列表首屏目标不超过 2 秒，P95 不超过 4 秒；
- 支持至少 100 万条历史零散库存快照分页查询；
- 统计汇总在服务端执行，不将全量数据加载到前端；
- 50,000 行以内导入预览目标不超过 30 秒；
- 大批量导入、校验和导出转异步任务；
- 医院、颗粒和批次选项接口常用查询 1 秒内返回。

### 19.2 可用性

- 网络失败保留列表和表单内容；
- 异步任务离开页面后继续运行；
- 任务失败显示原因并支持安全重试；
- 页面可查看数据截止时间和单位/映射版本；
- 所有服务节点使用统一时区和定点数计算。

### 19.3 兼容性

- 支持 Chrome、Edge 最近两个大版本；
- 推荐分辨率 1366×768 及以上；
- 支持横向滚动、固定关键列和键盘操作；
- 状态不只依赖颜色表达。

## 20. 测试与验收标准

### 20.1 功能验收

1. 页面入口、查询条件、工具栏和表格字段与截图信息架构一致。
2. 可按医院、物料编码、批号和到期日期任意组合查询。
3. 新增、修改、删除/作废、导入、导出和校验符合权限及状态规则。
4. 选择颗粒 ID 后正确带出物料、药品、单位和可选批次。
5. 颗粒量支持小数且按单位精度保存。
6. 标准颗粒量换算正确，缺少换算配置时阻断。
7. 统计汇总针对当前查询全部有效记录，不是当前页。
8. 过期、近效期、零库存和映射异常有明确提示。
9. 重复导入、重复业务键和并发提交不会造成库存虚增。
10. 有效零散库存变化能触发实际库存和要货指导待重算。

### 20.2 统计验收样例

给定截图示例颗粒量：

```text
288.5、464.1、567.5、389.9、331.9、359.4、12.9、194.9、64.6、376.8
```

预期：

```text
颗粒量合计 = 3050.5
```

该数值用于验证统计行算法；正式验收还需覆盖不同单位和换算系数。

### 20.3 单位和精度验收

| 场景 | 预期 |
|---|---|
| 单位为克，数量 288.5 | 标准量为 288.5 克 |
| 数量为 0.001 且单位允许 3 位小数 | 可以保存 |
| 数量为 0.0001 且单位只允许 3 位小数 | 阻断，不静默截断 |
| 数量为负数 | 阻断 |
| 单位无换算配置 | `LI_CONVERSION_MISSING` |
| 不同单位混合统计 | 先换算到标准单位，再合计 |
| 标准换算结果超数据库精度 | 阻断并提示 |

### 20.4 业务键和日期验收

| 场景 | 预期 |
|---|---|
| 同医院、颗粒 ID、批号、盘点日完全重复 | 阻断，不自动累加 |
| 同一天重新盘点 | 更新草稿或生成新版本，不能直接新增重复有效记录 |
| 盘点日期晚于当前日期 | 默认阻断 |
| 到期日期早于盘点日期 | 允许保存现场库存，标记过期且可用量为 0 |
| 到期日期在预警期内 | 标记近效期 |
| 批号与供货批次不一致 | 严格模式阻断，宽松模式警告 |
| 颗粒 ID 与物料不一致 | 阻断 |

### 20.5 整袋联动验收

给定：

```text
每袋标准量 = 1000 克
拆袋 1 袋
实际零散增加 = 998 克
损耗 = 2 克
```

预期：

```text
整袋库存减少 = 1 袋
零散库存增加 = 998 克
损耗记录 = 2 克
变化前后标准量守恒：1000 = 998 + 2
```

如果没有拆袋事件，不允许通过零散库存页面直接修改整袋库存数量。

### 20.6 导入、权限和安全验收

- 正确模板可预览、校验和提交；
- 非法颗粒 ID、单位、日期、数量和重复行逐行报错；
- 相同文件重复提交不重复入账；
- 无权限医院无法通过列表、导出、详情或导入获取数据；
- 前端隐藏按钮后直接调用接口仍返回 403；
- 已被下游引用记录不能物理删除；
- 新增、修改、作废、校验、导入和导出均有审计日志。

### 20.7 性能验收

- 百万级快照查询命中索引并满足分页响应目标；
- 大批量统计不加载全量到应用内存；
- 50,000 行任务显示进度；
- 并发提交同一业务键只生成一条有效记录；
- 异步任务失败可以重试且不产生重复快照。

## 21. 开发拆分建议

### 21.1 后端与数据

1. 确认颗粒 ID、物料、单位和批次映射关系。
2. 新建零散库存快照、单位映射、导入批次和问题表。
3. 实现查询、分页、排序、全量统计和医院权限过滤。
4. 实现新增、修改、作废、乐观锁和业务键防重。
5. 实现单位换算、数量精度、日期、批号和效期校验。
6. 实现导入模板、预览、提交、错误文件和幂等。
7. 实现批量校验及异步任务。
8. 实现导出、短时效下载和审计。
9. 预留整袋拆零联动事件和下游待重算机制。
10. 编写单元、接口、权限、并发和性能测试。

### 21.2 前端

1. 新建独立零散库存页面和路由。
2. 实现医院、物料、颗粒 ID、批号和效期查询。
3. 实现表格、固定列、操作列、分页和统计行。
4. 实现新增/编辑表单及颗粒 ID、批号联动。
5. 实现小数输入、单位提示和标准量预览。
6. 实现批量/行级删除、校验和异常详情。
7. 实现导入预览、问题筛选、错误下载和进度。
8. 实现导出、权限、效期状态和空状态展示。

### 21.3 测试

- 查询、分页、排序和全量统计；
- CRUD、状态、审计和下游引用；
- 颗粒 ID、物料、单位和批号映射；
- 小数精度、单位换算、零值和负值；
- 效期、重复、盘点日版本和拆袋守恒；
- 导入、导出、校验、幂等、并发和任务重试；
- 权限、越权、注入、文件安全和性能；
- Chrome、Edge、1366×768 和横向滚动兼容性。

## 22. 待业务确认项

| 优先级 | 待确认项 | 影响 |
|---|---|---|
| P0 | 零散库存的真实物理单位及系统标准单位 | 影响字段、换算和汇总 |
| P0 | 颗粒 ID 是否唯一映射物料和单位 | 影响主数据和业务键 |
| P0 | 零散库存是现场盘点、拆袋结果还是外部库存快照 | 影响来源和状态 |
| P0 | 批号是否必填，是否必须匹配医院供货批次 | 影响校验和追溯 |
| P0 | 同医院、颗粒、批号、日期重复时覆盖、版本还是阻断 | 影响唯一键和历史 |
| P0 | 数量允许小数位及是否允许 0 | 影响数据库和输入校验 |
| P0 | 过期库存是否计入实际库存和可用库存 | 影响下游计算 |
| P0 | 到期日当天的有效性 | 影响状态算法 |
| P0 | 整袋与零散是否需要正式拆袋联动 | 影响事件表和守恒校验 |
| P0 | 已被盘点/要货指导使用的记录如何修正 | 影响版本和重算 |
| P1 | 是否按库位/容器拆分零散库存 | 影响业务键和表结构 |
| P1 | 单位换算配置由物料、颗粒 ID 还是批次决定 | 影响映射表 |
| P1 | 导入错误行是否保存为 `INVALID` | 影响导入流程 |
| P1 | 近效期预警天数 | 影响页面状态 |
| P1 | 是否需要冻结量、损耗量和质量状态 | 影响可用量字段 |
| P1 | 导出上限、文件保留时间和水印 | 影响导出实现 |
| P2 | 是否需要扫码、拆袋、回装和库存流水详情 | 影响后续版本 |

## 23. 发布前检查清单

- [ ] P0 业务口径全部确认并写入数据字典/规则配置；
- [ ] 颗粒 ID、物料、单位、批号和效期映射可用；
- [ ] 零散快照、单位映射、导入和问题表创建完成；
- [ ] 页面字段、按钮、分页和统计行与截图逐项核对；
- [ ] 小数精度、单位换算、零值和负值测试通过；
- [ ] 重复、日期、效期和批次校验通过；
- [ ] 整袋/零散联动及库存守恒规则验证通过；
- [ ] 导入预览、提交、错误文件和幂等通过；
- [ ] 列表、统计汇总和导出结果一致；
- [ ] 下游盘点和要货指导待重算机制通过；
- [ ] 权限、审计、异步任务、下载安全和性能验证通过；
- [ ] 业务 UAT 完成并确认真实样例数据。

