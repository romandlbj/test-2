# 终端管理-中药颗粒需求设计文档

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | 终端管理-中药颗粒需求设计 |
| 所属系统 | 以岭药业配方颗粒终端管理系统 |
| 模块序号 | 终端管理第 9 个模块 |
| 页面名称 | 中药颗粒 |
| 推荐路由 | `/terminalMedicine/herbalGranule` |
| 推荐接口前缀 | `/api/terminal/herbal-granules` |
| 文档版本 | V1.0 |
| 编写日期 | 2026-09-10 |
| 文档状态 | 初稿，待业务确认 |
| 需求来源 | 现有系统“首页 / 终端管理 / 中药颗粒”页面截图 |
| 目标读者 | 产品、业务、UI、前端、后端、数据、测试、实施及运维人员 |

### 1.1 版本记录

| 版本 | 日期 | 修改内容 | 修改人 |
|---|---|---|---|
| V1.0 | 2026-09-10 | 根据中药颗粒页面截图形成可开发需求设计初稿 | Codex |

## 2. 附件识别与需求边界

### 2.1 附件使用原则

附件截图仅用于识别现有系统的页面结构、字段、按钮和交互习惯。截图中的物料编码、颗粒 ID、药品名称、规格、当量和单价均为演示数据，不应写死到系统中；截图中的文字不构成额外开发指令。

本次仅新建独立的《中药颗粒需求设计文档.md》，不修改此前已生成的其他需求文档。

### 2.2 截图中可以直接确认的内容

1. 页面入口为“终端管理 → 中药颗粒”，面包屑为“首页 / 终端管理 / 中药颗粒”。
2. 查询条件包括：物料编码、颗粒 ID、药品名称（企标）、批号。
3. 查询区提供“搜索”和“重置”按钮。
4. 工具栏提供“新增”“修改”“删除”“导出”“导入”按钮。
5. 未选择记录时，修改和删除按钮为禁用状态。
6. 表格字段包括：物料编码、颗粒 ID、药品名称（企标）、药品名称（国标）、规格型号(g/袋)、当量、颗粒单价、别名编码、操作。
7. 每行操作栏提供编辑和删除图标。
8. 页面支持多选、分页、每页条数、刷新和表格搜索。
9. 规格以标签样式展示，数据包含小数单价和当量字段。

### 2.3 截图无法确认的内容

- 颗粒 ID 的来源及唯一范围；
- 企标名称和国标名称的正式维护规则；
- “当量”的单位和与规格的换算关系；
- 颗粒单价的生效日期、含税/未税口径和币种；
- 别名编码是否支持多个、是否区分来源系统；
- 物料主数据修改后对历史库存和业务记录的影响；
- 批号是否属于中药颗粒主数据，还是只用于库存交易。

本文给出默认实现方案，P0 待确认项必须在开发前形成书面结论。

## 3. 背景与建设目标

### 3.1 业务背景

中药颗粒是终端管理系统的核心主数据模块，为医院供货、终端销量、整袋库存、零散库存、医院库存、期初库存、盘点库存和要货指导提供统一的物料及单位基础。

截图显示同一颗粒需要同时维护企业标准名称、国家标准名称、物料编码、颗粒 ID、规格型号、当量、单价和别名编码。主数据不一致会导致库存、结算和跨系统对账出现错误。

### 3.2 建设目标

1. 建立唯一、可版本化的中药颗粒主数据。
2. 统一企标名称、国标名称、物料编码和颗粒 ID 的映射。
3. 维护规格、当量、颗粒单价和别名编码，并记录生效版本。
4. 支持新增、修改、删除/停用、导入和导出。
5. 对编码唯一性、名称、数值、价格、生效期和引用关系进行校验。
6. 为所有库存及销量模块提供稳定的主数据查询和版本快照。

### 3.3 成功标准

- 一个有效颗粒 ID 在同一生效期内只能映射一个物料和单位；
- 物料编码、颗粒 ID、别名编码可追溯到版本和操作人；
- 主数据变更不会静默修改历史供货、库存和销量记录；
- 重复导入、重复编码和冲突版本可被阻断；
- 无权限用户无法新增、修改、停用或导出未授权主数据。

## 4. 需求范围

### 4.1 本期范围

- 中药颗粒列表、查询、分页和表格搜索；
- 新增、单条修改、批量删除/停用；
- 物料编码、颗粒 ID、企标名称、国标名称、规格、当量、单价和别名编码维护；
- Excel/CSV 模板下载、导入预览、逐行校验、提交和错误明细下载；
- 当前筛选结果导出；
- 编码唯一、映射关系、数值精度、价格和生效期校验；
- 主数据版本、启用/停用、引用保护和审计日志；
- 向库存、供货、销量和要货指导提供标准查询接口。

### 4.2 本期不包含

- 中药材质量标准、检验报告和批检数据；
- 采购、生产、包装、配送和财务结算完整流程；
- 医院、物料和价格审批流；
- 处方、患者和医生明细；
- 药品图片、说明书和临床知识库；
- 复杂多语言和机器学习名称识别。

### 4.3 后续扩展

- 主数据审批和双人复核；
- 国家药监码、医保码和统一编码对接；
- 规格、剂型、包装层级和厂商维护；
- 价格历史和区域价目表；
- 药品标准、质量、禁忌和说明书管理；
- 主数据变更影响分析。

## 5. 角色与权限

### 5.1 权限矩阵

| 角色 | 查询 | 新增 | 修改 | 删除/停用 | 导入 | 导出 | 启用/发布 | 数据范围 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| 系统管理员 | 是 | 是 | 是 | 是 | 是 | 是 | 是 | 全部颗粒 |
| 主数据管理员 | 是 | 是 | 是 | 按流程 | 是 | 是 | 是 | 全部颗粒 |
| 区域/业务管理员 | 是 | 可配置 | 可配置 | 否 | 可配置 | 是 | 否 | 授权范围 |
| 库存/供货人员 | 是 | 否 | 否 | 否 | 否 | 是 | 否 | 授权范围 |
| 财务/审计人员 | 是 | 否 | 否 | 否 | 否 | 是 | 否 | 全部或授权 |
| 只读用户 | 是 | 否 | 否 | 否 | 否 | 可配置 | 否 | 授权范围 |

### 5.2 权限编码

- `terminal:herbal-granule:view`
- `terminal:herbal-granule:create`
- `terminal:herbal-granule:update`
- `terminal:herbal-granule:delete`
- `terminal:herbal-granule:import`
- `terminal:herbal-granule:export`
- `terminal:herbal-granule:publish`
- `terminal:herbal-granule:override`

服务端必须同时校验功能权限、主数据状态和下游引用关系。

## 6. 页面设计

### 6.1 页面入口

- 左侧导航：终端管理 → 中药颗粒；
- 页面路由：`/terminalMedicine/herbalGranule`；
- 面包屑：首页 / 终端管理 / 中药颗粒；
- 接口前缀：`/api/terminal/herbal-granules`。

### 6.2 页面区域

1. 面包屑和页签区；
2. 查询区：物料编码、颗粒 ID、药品名称（企标）、批号；
3. 工具栏：新增、修改、删除、导出、导入；
4. 表格区：多选、主数据字段、行级编辑和删除；
5. 分页区：总条数、每页条数、页码和跳页；
6. 右侧工具：表格搜索、刷新。

### 6.3 页面结构示意

```text
首页 / 终端管理 / 中药颗粒

[物料编码] [颗粒ID] [药品名称(企标)] [批号] [搜索] [重置]

[新增] [修改] [删除] [导出] [导入]                       [搜索] [刷新]

┌选择┬物料编码┬颗粒ID┬药品名称(企标)┬药品名称(国标)┬规格(g/袋)┬当量┬颗粒单价┬别名编码┬操作┐
│ □  │        │      │              │              │          │    │        │        │编辑 删除│
├────┼────────┼──────┼──────────────┼──────────────┼──────────┼────┼────────┼────────┼─────────┤
│ □  │ ...    │ ...  │ ...          │ ...          │ 100      │... │ ...    │ ...    │ ✎  🗑   │
└────┴────────┴──────┴──────────────┴──────────────┴──────────┴────┴────────┴────────┴─────────┘

共 N 条 [10条/页]  < 1 2 3 ... >  前往 [ ] 页
```

### 6.4 固定列与滚动

- 左侧固定选择列、物料编码、颗粒 ID；
- 右侧固定操作列；
- 名称、规格、当量、价格和别名编码支持横向滚动；
- 超长名称和别名编码省略并悬浮展示；
- 推荐支持 1366×768 及以上分辨率。

## 7. 查询需求

### 7.1 查询字段

| 页面字段 | API 参数 | 控件 | 必填 | 规则 |
|---|---|---|---:|---|
| 物料编码 | `materialCode` | 文本框 | 否 | 去首尾空格，模糊匹配，最长 64 字符 |
| 颗粒 ID | `particleId` | 文本框 | 否 | 默认精确或前缀匹配，最长 64 字符 |
| 药品名称（企标） | `enterpriseName` | 文本框 | 否 | 模糊匹配，最长 120 字符 |
| 批号 | `batchNo` | 文本框 | 否 | 如批号属于库存交易，查询时通过关联记录过滤 |
| 状态 | `status` | 高级筛选 | 否 | `ACTIVE`/`INACTIVE`/`DRAFT` |
| 页码 | `page` | 分页器 | 否 | 默认 1 |
| 每页条数 | `pageSize` | 分页器 | 否 | 默认 10，可选 10/20/50/100/200 |
| 排序字段 | `sortField` | 表头 | 否 | 服务端白名单 |
| 排序方向 | `sortOrder` | 表头 | 否 | `asc`/`desc` |

### 7.2 查询行为

1. 多条件使用 AND 关系。
2. 无条件查询返回当前用户可见范围内的有效主数据。
3. 默认只显示 `ACTIVE`，审计或主数据管理员可筛选停用和草稿。
4. 默认排序：物料编码升序、颗粒 ID 升序、ID 倒序。
5. 点击搜索回到第 1 页；物料编码、颗粒 ID 和名称支持回车搜索。
6. 点击重置清空条件、恢复默认排序和第 1 页。
7. 查询期间显示 loading 并防止重复请求。
8. 无结果时显示空状态并保留查询条件。
9. 批号不是中药颗粒主数据字段时，批号查询必须通过库存/供货关联查询，不能在主表中伪造批号。

## 8. 表格字段设计

| 页面列 | API 字段 | 数据库字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---:|---|
| 选择 | `selected` | - | Boolean | - | 表头只全选当前页 |
| 物料编码 | `materialCode` | `material_code` | String | 是 | 业务物料唯一编码，保留前导零 |
| 物料 ID | `materialId` | `material_id` | Long | 是 | 内部主键 |
| 颗粒 ID | `particleId` | `particle_id` | String | 是 | 来源系统颗粒唯一标识 |
| 药品名称（企标） | `enterpriseName` | `enterprise_name` | String | 是 | 企业标准名称 |
| 药品名称（国标） | `nationalName` | `national_name` | String | 否 | 国家标准名称，可为空但需说明原因 |
| 规格型号(g/袋) | `specQtyPerBag` | `spec_qty_per_bag` | Decimal(18,3) | 是 | 每袋规格数量，大于 0 |
| 当量 | `equivalentQty` | `equivalent_qty` | Decimal(18,6) | 是 | 当量值，单位和口径必须由规则配置说明 |
| 当量单位 | `equivalentUnit` | `equivalent_unit` | String | 是 | 如 g、mg、饮片等 |
| 颗粒单价 | `particleUnitPrice` | `particle_unit_price` | Decimal(18,6) | 是 | 当前生效价格，不含税/含税由配置标识 |
| 价格币种 | `currency` | `currency` | String | 是 | 默认 CNY |
| 别名编码 | `aliasCodes` | 独立子表 | Array[String] | 否 | 支持多个别名编码，逗号展示或详情展示 |
| 状态 | `status` | `status` | Enum | 是 | `DRAFT`/`ACTIVE`/`INACTIVE` |
| 生效日期 | `effectiveFrom` | `effective_from` | Date | 是 | 主数据版本生效日 |
| 失效日期 | `effectiveTo` | `effective_to` | Date | 否 | 为空表示持续有效 |
| 版本 | `version` | `version` | Integer | 是 | 乐观锁 |
| 操作 | `actions` | - | - | - | 行级编辑、停用/删除 |

### 8.1 字段展示规则

- 物料编码和颗粒 ID 原样展示，不能丢失前导零；
- 企标名称、国标名称超长时省略并悬浮显示完整内容；
- 规格、当量和单价按定点小数展示，去除无意义尾零；
- 单价默认最多 6 位小数，页面可按系统金额规范显示；
- 别名编码多个时默认以“、”分隔，详情展示完整列表；
- 停用记录显示灰色状态标签，不默认进入业务选项。

## 9. 主数据模型与业务规则

### 9.1 主数据唯一性

默认唯一约束：

```text
有效期内：particleId 只能映射一个 materialId
有效期内：materialCode 只能对应一个物料主记录
有效期内：同一 aliasCode 不能映射多个物料
```

允许历史版本存在，但生效日期区间不能重叠。若不同来源系统使用相同颗粒 ID，应以 `sourceSystem + particleId` 作为外部唯一键。

### 9.2 企标与国标名称

- 企业标准名称是系统业务首选显示名称；
- 国家标准名称用于对照、检索和外部接口；
- 国标名称为空时允许保存，但必须填写缺失原因或状态标记；
- 名称去除首尾空格，不能包含控制字符；
- 同一物料名称修改生成新版本，历史交易保留原名称快照；
- 不允许通过修改名称改变物料编码或颗粒 ID 的身份。

### 9.3 规格和当量

默认定义：

```text
specQtyPerBag > 0
equivalentQty > 0
```

当量不应在前端硬编码。必须保存 `equivalentUnit` 和计算口径，例如：

```text
当量 = 原饮片折算量 / 颗粒包装数量
```

具体公式需业务确认。规格变更后不得覆盖历史库存和供货记录中的规格快照。

### 9.4 颗粒单价

- 单价必须大于等于 0；
- 默认保存 6 位小数，页面按金额规范显示；
- 价格必须有生效日期和价格版本；
- 同一物料在同一日期只能有一个有效价格版本；
- 必须明确含税/未税、币种和价格单位；
- 修改已生效价格生成新版本，历史结算和亏涨结果继续引用旧版本；
- 价格为 0 是否允许由业务配置决定，默认允许保存但产生警告。

### 9.5 别名编码

- 一个物料可有多个别名编码；
- 同一来源系统中别名编码必须唯一；
- 别名编码长度 1～64 字符，去除首尾空格；
- 别名编码支持来源系统、编码类型和生效期；
- 删除别名编码前检查下游引用；
- 不能用修改别名编码的方式绕过物料唯一性校验。

### 9.6 状态和引用保护

| 状态 | 含义 | 业务可选 | 可编辑 | 可删除 |
|---|---|---:|---:|---:|
| `DRAFT` | 新增/导入未发布 | 否 | 是 | 是 |
| `ACTIVE` | 当前有效主数据 | 是 | 生成新版本 | 只能停用 |
| `INACTIVE` | 已停用 | 否 | 可复制生成新版本 | 否 |
| `RETIRED` | 已退役，仅历史可查 | 否 | 否 | 否 |

已被医院供货、终端销量、库存或盘点引用的主数据不能物理删除，只能停用或退役。停用前显示影响记录数量。

## 10. 操作功能设计

### 10.1 新增

点击“新增”打开抽屉或弹窗。

| 字段 | 控件 | 默认值 | 可编辑 | 规则 |
|---|---|---|---:|---|
| 物料编码 | 文本框 | 空 | 是 | 唯一、最长 64 字符 |
| 颗粒 ID | 文本框 | 空 | 是 | 唯一映射、最长 64 字符 |
| 企标名称 | 文本框 | 空 | 是 | 必填，最长 120 字符 |
| 国标名称 | 文本框 | 空 | 是 | 可空，最长 120 字符 |
| 规格型号 | 数值框 | 空 | 是 | 大于 0，最多 3 位小数 |
| 当量 | 数值框 | 空 | 是 | 大于 0，最多 6 位小数 |
| 当量单位 | 下拉 | 默认单位 | 是 | 单位字典值 |
| 颗粒单价 | 数值框 | 空 | 是 | 大于等于 0，最多 6 位小数 |
| 价格单位/币种 | 下拉 | CNY | 是 | 需与价格配置一致 |
| 别名编码 | 可添加列表 | 空 | 是 | 编码去重、最长 64 字符 |
| 生效日期 | 日期选择器 | 当前日期 | 是 | 不得造成版本重叠 |

保存流程：

1. 前端校验必填、长度和数字格式；
2. 服务端校验编码、颗粒 ID、别名和有效期唯一性；
3. 服务端保存名称、规格、当量、价格和版本快照；
4. 默认保存为 `DRAFT`；
5. 发布/启用后才进入供货、库存和销量选项；
6. 保存成功刷新列表并保留筛选条件。

### 10.2 修改

- 选择 1 条记录时修改按钮可用，选择 0 条或多条时禁用；
- `DRAFT` 可直接修改；
- `ACTIVE` 不能直接覆盖身份字段，修改生成新版本；
- 修改物料编码、颗粒 ID 或别名编码需检查所有下游引用；
- 修改价格、规格或当量必须填写原因并记录前后值；
- 提交携带 `version`，冲突返回 HTTP 409；
- 历史交易记录保留原主数据快照。

### 10.3 删除/停用

- 工具栏支持批量停用，行级操作支持停用/退役；
- `DRAFT` 且无引用记录可软删除；
- `ACTIVE` 记录只能停用或退役，不能物理删除；
- 已被业务引用时显示引用模块和记录数量；
- 停用前二次确认，原因必填，长度 2～200 字；
- 停用后不再出现在新增供货、库存和销量的主数据选项中；
- 历史查询和审计仍可查看停用记录。

### 10.4 导入

#### 文件要求

- 默认支持 `.xlsx`，可选 UTF-8 BOM `.csv`；
- 最大文件 20 MB，单文件最多 50,000 行；
- 禁止宏、公式执行和外部链接；
- 校验扩展名、MIME、内容结构和病毒风险。

#### 模板字段

```text
物料编码,颗粒ID,药品名称(企标),药品名称(国标),规格型号(g/袋),
当量,当量单位,颗粒单价,价格单位,币种,别名编码,生效日期,失效日期
```

#### 导入流程

```text
下载模板 → 上传 → 解析预览 → 逐行校验 → 查看错误/警告
→ 下载错误明细 → 确认提交 → 再次校验 → 写入草稿 → 发布/启用
```

- 预览不写正式主数据；
- 完全重复行不自动覆盖；
- 同文件内编码/颗粒 ID冲突逐行报错；
- 提交阶段重新校验主数据和版本；
- 默认只写入无阻断错误的行；
- 导入提交支持幂等，重复文件返回历史结果。

### 10.5 导出

- 导出当前筛选条件下全部主数据；
- 默认 `.xlsx`，可兼容 `.csv`；
- 包含物料编码、颗粒 ID、企标/国标名称、规格、当量、单价、别名、生效期、状态和版本；
- 文件包含导出时间、查询条件、操作人和数据版本；
- 超过 50,000 行转异步任务；
- 下载地址默认 30 分钟失效；
- 导出操作写入审计日志。

## 11. 校验规则与错误码

| 场景 | 默认处理 | 错误码 |
|---|---|---|
| 物料编码为空/非法 | 阻断 | `HG_MATERIAL_CODE_INVALID` |
| 颗粒 ID为空/重复 | 阻断 | `HG_PARTICLE_ID_INVALID` |
| 企标名称为空 | 阻断 | `HG_ENTERPRISE_NAME_REQUIRED` |
| 国标名称缺失 | 警告/按配置 | `HG_NATIONAL_NAME_MISSING` |
| 规格小于等于 0 | 阻断 | `HG_SPEC_INVALID` |
| 当量小于等于 0 | 阻断 | `HG_EQUIVALENT_INVALID` |
| 单位不存在 | 阻断 | `HG_UNIT_INVALID` |
| 单价为负或超精度 | 阻断 | `HG_PRICE_INVALID` |
| 价格版本重叠 | 阻断 | `HG_PRICE_PERIOD_OVERLAP` |
| 别名编码重复 | 阻断 | `HG_ALIAS_DUPLICATE` |
| 生效期重叠 | 阻断 | `HG_EFFECTIVE_PERIOD_OVERLAP` |
| 已被下游引用 | 禁止物理删除 | `HG_ALREADY_REFERENCED` |
| 已停用主数据被新业务选择 | 阻断 | `HG_INACTIVE_NOT_SELECTABLE` |
| 乐观锁冲突 | 409 | `HG_VERSION_CONFLICT` |
| 导入文件重复 | 返回历史结果 | `HG_IMPORT_DUPLICATE` |

## 12. 数据库设计

### 12.1 主表 `herbal_granule_master`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `material_code` | VARCHAR(64) | 物料编码 |
| `particle_id` | VARCHAR(64) | 颗粒 ID |
| `enterprise_name` | VARCHAR(120) | 企标名称 |
| `national_name` | VARCHAR(120) | 国标名称 |
| `spec_qty_per_bag` | DECIMAL(18,3) | 规格型号/g/袋 |
| `equivalent_qty` | DECIMAL(18,6) | 当量 |
| `equivalent_unit` | VARCHAR(20) | 当量单位 |
| `particle_unit_price` | DECIMAL(18,6) | 颗粒单价 |
| `price_unit` | VARCHAR(20) | 价格单位 |
| `currency` | VARCHAR(10) | 币种，默认 CNY |
| `effective_from` | DATE | 生效日期 |
| `effective_to` | DATE | 失效日期 |
| `status` | VARCHAR(20) | `DRAFT`/`ACTIVE`/`INACTIVE`/`RETIRED` |
| `source_type` | VARCHAR(20) | `MANUAL`/`IMPORT`/`API` |
| `source_batch_id` | VARCHAR(64) | 来源批次 |
| `version` | INT | 乐观锁版本 |
| `created_by`/`created_at` | BIGINT/DATETIME | 创建信息 |
| `updated_by`/`updated_at` | BIGINT/DATETIME | 修改信息 |
| `deleted_at` | DATETIME | 软删除时间 |

建议索引：

```text
uk_hg_material_effective (material_code, effective_from, deleted_at)
uk_hg_particle_effective (particle_id, effective_from, deleted_at)
idx_hg_name (enterprise_name, national_name)
idx_hg_status (status, effective_from, effective_to)
```

应用层必须额外检查生效日期区间重叠，不能只依赖简单唯一索引。

### 12.2 价格版本表 `herbal_granule_price`

如果价格频繁变化，建议独立保存：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `granule_id` | BIGINT | 主数据 ID |
| `unit_price` | DECIMAL(18,6) | 单价 |
| `price_unit` | VARCHAR(20) | 价格单位 |
| `currency` | VARCHAR(10) | 币种 |
| `tax_mode` | VARCHAR(20) | `TAX_INCLUDED`/`TAX_EXCLUDED` |
| `effective_from` | DATE | 生效日期 |
| `effective_to` | DATE | 失效日期 |
| `version_no` | VARCHAR(32) | 价格版本 |
| `status` | VARCHAR(20) | 状态 |

### 12.3 别名表 `herbal_granule_alias`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `granule_id` | BIGINT | 主数据 ID |
| `alias_code` | VARCHAR(64) | 别名编码 |
| `alias_type` | VARCHAR(20) | 来源编码类型 |
| `source_system` | VARCHAR(64) | 来源系统 |
| `effective_from`/`effective_to` | DATE | 生效区间 |
| `status` | VARCHAR(20) | 状态 |

### 12.4 导入批次和审计

建议建立：

- `herbal_granule_import_batch`：文件哈希、状态、行数、写入数和操作人；
- `herbal_granule_import_issue`：批次、行号、字段、原值、错误码和建议；
- 复用 `operation_audit_log` 记录新增、修改、停用、导入、导出和发布。

## 13. 接口设计

### 13.1 列表查询

```http
GET /api/terminal/herbal-granules
```

参数：`materialCode`、`particleId`、`enterpriseName`、`batchNo`、`status`、`page`、`pageSize`、`sortField`、`sortOrder`。

响应示例：

```json
{
  "items": [
    {
      "id": 1,
      "materialCode": "02.22.003.0306",
      "particleId": "4370",
      "enterpriseName": "补骨脂",
      "nationalName": "补骨脂",
      "specQtyPerBag": 100,
      "equivalentQty": 6.7,
      "equivalentUnit": "g",
      "particleUnitPrice": 0.6,
      "priceUnit": "g",
      "currency": "CNY",
      "aliasCodes": [],
      "status": "ACTIVE",
      "effectiveFrom": "2025-01-01",
      "effectiveTo": null,
      "version": 1
    }
  ],
  "total": 1,
  "page": 1,
  "pageSize": 10,
  "queryMeta": {
    "dataVersion": "v1",
    "priceTaxMode": "TAX_INCLUDED"
  }
}
```

### 13.2 颗粒/物料选项

```http
GET /api/terminal/herbal-granules/options?keyword=02.22
```

仅返回 `ACTIVE` 且在当前日期有效的数据，供医院供货、库存和销量录入使用。

### 13.3 新增

```http
POST /api/terminal/herbal-granules
Idempotency-Key: <uuid>
Content-Type: application/json
```

```json
{
  "materialCode": "02.22.003.0306",
  "particleId": "4370",
  "enterpriseName": "补骨脂",
  "nationalName": "补骨脂",
  "specQtyPerBag": 100,
  "equivalentQty": 6.7,
  "equivalentUnit": "g",
  "particleUnitPrice": 0.6,
  "priceUnit": "g",
  "currency": "CNY",
  "aliasCodes": [],
  "effectiveFrom": "2025-01-01"
}
```

### 13.4 修改和版本

```http
PUT /api/terminal/herbal-granules/{id}
```

请求必须携带 `version`、修改原因和 `createNewVersion`。ACTIVE 数据默认创建新版本，不直接覆盖历史。

### 13.5 停用/退役

```http
POST /api/terminal/herbal-granules/{id}/deactivate
POST /api/terminal/herbal-granules/{id}/retire
```

请求体包含原因和 `version`。服务端返回下游引用统计和操作结果。

### 13.6 导入

```http
GET  /api/terminal/herbal-granules/import/template?format=xlsx
POST /api/terminal/herbal-granules/import/preview
POST /api/terminal/herbal-granules/import/commit
GET  /api/terminal/herbal-granules/import/{batchId}/issues
```

### 13.7 导出

```http
POST /api/terminal/herbal-granules/export
GET  /api/exports/tasks/{taskId}
```

### 13.8 HTTP 状态码

| 状态 | 含义 |
|---:|---|
| 400 | 参数、编码、名称或数值格式错误 |
| 401 | 未登录 |
| 403 | 权限不足 |
| 404 | 主数据不存在 |
| 409 | 重复、版本或生效期冲突 |
| 422 | 已被下游引用或业务校验不通过 |
| 500 | 服务端异常 |

## 14. 与其他模块关系

### 14.1 医院供货、终端销量和库存

医院供货、终端销量、整袋库存、零散库存、医院库存和期初库存均通过 `materialId`/`particleId` 引用中药颗粒主数据。交易记录保存主数据名称、规格、单位和价格快照，主数据后续变更不能修改历史交易。

### 14.2 盘点库存和要货指导

盘点和要货指导读取当前或指定日期有效的主数据版本。规格、当量或价格变更后，历史盘点和要货指导结果继续使用原计算版本；新汇总使用新版本。

### 14.3 停用影响

停用颗粒后：

1. 不能新增供货、销量或库存记录；
2. 历史记录仍可查询和导出；
3. 已存在的期初、盘点和要货结果不自动删除；
4. 如需要替代物料，必须建立新物料映射并记录替代关系。

### 14.4 建议领域事件

- `HerbalGranuleCreated`
- `HerbalGranuleVersionPublished`
- `HerbalGranulePriceChanged`
- `HerbalGranuleDeactivated`
- `HerbalGranuleAliasChanged`

事件包含主数据 ID、物料编码、颗粒 ID、版本、生效日期、变更前后摘要、操作人和影响模块。

## 15. 前端交互规范

### 15.1 按钮状态

| 按钮 | 无选择 | 选择 1 条 | 选择多条 |
|---|---|---|---|
| 新增 | 可用 | 可用 | 可用 |
| 修改 | 禁用 | 按状态可用 | 禁用 |
| 删除/停用 | 禁用 | 按状态可用 | 按状态可用 |
| 导出 | 可用 | 可用 | 可用 |
| 导入 | 可用 | 可用 | 可用 |
| 发布/启用 | 按权限 | 按状态可用 | 按状态可用 |

### 15.2 表单联动

- 物料编码和颗粒 ID输入后检查已有映射；
- 颗粒 ID已存在时禁止重复新增，并提供查看已有记录入口；
- 生效日期变化时实时提示版本重叠；
- 规格、当量和单价输入显示精度提示；
- 别名编码支持新增、删除和去重；
- ACTIVE 数据编辑时提示将生成新版本；
- 关闭未保存表单时二次确认。

### 15.3 数值和文本

- 空值显示“—”；
- 单价、当量和规格去除无意义尾零；
- 物料编码、颗粒 ID和别名编码保留原始字符串；
- 长名称和别名列表省略并悬浮展示；
- 状态使用标签和文字，不仅依赖颜色。

## 16. 审计、安全与一致性

### 16.1 审计

新增、修改、停用、退役、导入、导出、发布、价格变更和别名变更必须记录：

- 主数据 ID、物料编码、颗粒 ID和版本；
- 变更前后字段摘要；
- 生效区间和价格口径；
- 来源批次、影响模块和引用数量；
- 操作人、IP、时间、traceId和结果。

### 16.2 安全

- 全部写接口服务端鉴权；
- 不能通过 ID、编码或导入文件绕过权限；
- SQL 参数绑定，排序字段白名单；
- 上传文件校验扩展名、MIME、大小和内容安全；
- 导出链接短时效签名；
- 错误响应不暴露 SQL、堆栈和服务器路径。

### 16.3 一致性

- 主数据版本发布与缓存刷新可靠提交；
- 下游服务读取指定版本，不依赖页面缓存；
- 修改 ACTIVE 数据不直接覆盖历史快照；
- 同一颗粒 ID和物料编码的生效期不能重叠；
- 重复导入、并发新增和重试均保证幂等。

## 17. 非功能需求

### 17.1 性能

- 列表首屏目标不超过 2 秒，P95 不超过 4 秒；
- 支持至少 10 万条颗粒主数据分页查询；
- 物料/颗粒选项接口常用查询 1 秒内返回；
- 50,000 行以内导入预览目标不超过 30 秒；
- 大批量导入和导出转异步任务。

### 17.2 可用性

- 网络失败保留表单和列表内容；
- 版本冲突提供刷新和差异提示；
- 导入、导出任务可离开页面后继续运行；
- 停用前展示下游影响范围；
- 缓存更新失败不能导致新旧版本混用。

### 17.3 兼容性

- 支持 Chrome、Edge 最近两个大版本；
- 推荐分辨率 1366×768 及以上；
- 支持横向滚动、固定关键列和键盘操作；
- 状态和错误不能只依赖颜色表达。

## 18. 测试与验收标准

### 18.1 功能验收

1. 页面入口、查询条件、工具栏和字段与截图信息架构一致。
2. 物料编码、颗粒 ID、企标名称、国标名称、规格、当量、单价和别名显示正确。
3. 新增、修改、停用、导入和导出符合权限及状态规则。
4. 物料编码和颗粒 ID唯一性、别名唯一性和生效期冲突能够校验。
5. ACTIVE 数据修改生成新版本，历史交易不受影响。
6. 停用记录不能被新供货、销量和库存业务选择。
7. 导入预览逐行显示错误，提交幂等且可下载错误明细。
8. 下游模块可以按版本读取主数据和价格。

### 18.2 数据校验样例

| 场景 | 预期 |
|---|---|
| 物料编码 `02.22.003.0306`、颗粒 ID `4370` | 可以保存并建立唯一映射 |
| 同一颗粒 ID重复新增 | 阻断 |
| 同一物料不同版本生效期重叠 | 阻断 |
| 规格为 100、当量为 6.7、单价为 0.6 | 按配置精度保存 |
| 单价为负数 | 阻断 |
| 当量为 0 | 阻断 |
| 国标名称为空 | 警告或按配置阻断 |
| 别名编码已被其他物料使用 | 阻断 |
| ACTIVE 记录修改价格 | 生成新价格版本，旧版本保留 |
| 已被库存引用的物料停用 | 允许停用但禁止物理删除 |

### 18.3 导入验收

- 正确模板可预览并提交；
- 缺少物料编码、颗粒 ID、企标名称或规格时逐行报错；
- 重复编码、颗粒 ID、别名和重叠生效期可识别；
- 预览后主数据发生变化，提交时重新校验；
- 同文件重复提交不重复写入；
- 部分成功时有效行、错误行和警告行数量准确。

### 18.4 权限与安全验收

- 无权限用户不能写入、停用或导出未授权主数据；
- 前端隐藏按钮后直接调用接口仍返回 403；
- 不能通过编码、ID、排序字段或上传文件注入；
- 已被下游引用的记录不能物理删除；
- 所有变更、发布、导入、导出均有审计日志。

### 18.5 性能验收

- 十万级主数据分页和关键字查询命中索引；
- 远程选项查询在 1 秒内返回；
- 50,000 行导入/导出显示进度；
- 并发新增相同颗粒 ID只生成一条有效主数据；
- 缓存刷新和版本读取最终一致且可追踪。

## 19. 开发拆分建议

### 19.1 后端与数据

1. 确认物料编码、颗粒 ID、企标/国标名称、规格、当量和价格口径。
2. 新建主数据、价格版本、别名、导入和问题表。
3. 实现列表、选项、分页、排序和状态过滤。
4. 实现新增、版本化修改、停用、退役和引用保护。
5. 实现编码唯一、生效期、数值、价格和别名校验。
6. 实现导入预览、提交、错误文件、幂等和异步任务。
7. 实现导出、短时效下载和审计。
8. 提供库存、供货、销量和要货指导读取接口/事件。
9. 实现主数据缓存和版本失效策略。
10. 编写单元、接口、权限、并发和性能测试。

### 19.2 前端

1. 新建独立中药颗粒页面和路由。
2. 实现物料编码、颗粒 ID、企标名称和批号查询。
3. 实现表格、固定列、分页、行选择和操作列。
4. 实现新增/编辑表单、别名列表和版本提示。
5. 实现规格、当量、单价精度和单位提示。
6. 实现停用/退役确认及引用影响展示。
7. 实现导入预览、问题筛选、错误下载和提交进度。
8. 实现导出、状态标签、权限按钮和异常提示。

### 19.3 测试

- 页面字段、查询、分页、排序和滚动；
- CRUD、版本、状态、发布和审计；
- 编码/颗粒 ID/别名唯一性；
- 名称、规格、当量、价格、单位和精度；
- 生效期重叠、价格版本和历史快照；
- 导入、导出、校验、幂等、并发和缓存；
- 下游供货、销量、库存和要货指导引用；
- 权限、注入、文件安全和性能。

## 20. 待业务确认项

| 优先级 | 待确认项 | 影响 |
|---|---|---|
| P0 | 颗粒 ID 的来源系统、唯一范围和格式 | 影响主键及接口 |
| P0 | 物料编码与颗粒 ID是否一对一 | 影响唯一约束 |
| P0 | 企标名称、国标名称的权威来源和维护权限 | 影响主数据治理 |
| P0 | 规格型号(g/袋)的真实含义和允许精度 | 影响库存换算 |
| P0 | “当量”的单位、公式和业务口径 | 影响页面和下游计算 |
| P0 | 颗粒单价的单位、含税/未税、币种和生效方式 | 影响结算和亏涨 |
| P0 | 别名编码是否支持多个及来源系统 | 影响子表和唯一性 |
| P0 | ACTIVE 数据修改是否必须新版本 | 影响历史可追溯 |
| P0 | 停用/退役后历史库存、供货、销量如何处理 | 影响引用保护 |
| P0 | 批号是否属于主数据，还是仅属于库存交易 | 影响查询和模型 |
| P1 | 国标名称缺失是警告还是阻断 | 影响导入和发布 |
| P1 | 单价为 0 是否允许 | 影响价格校验 |
| P1 | 是否需要审批、双人复核和发布流程 | 影响状态模型 |
| P1 | 是否需要国家药监码、医保码等外部编码 | 影响字段和接口 |
| P1 | 导入上限、错误行保存和文件保留时间 | 影响导入实现 |
| P2 | 是否需要说明书、质量标准和药品图片 | 影响后续范围 |

## 21. 发布前检查清单

- [ ] P0 主数据口径全部确认并录入数据字典；
- [ ] 物料编码、颗粒 ID、企标/国标名称映射可用；
- [ ] 规格、当量、单位、价格和别名规则确定；
- [ ] 主表、价格表、别名表、导入表和问题表创建完成；
- [ ] 页面字段、按钮、分页和操作与截图逐项核对；
- [ ] 唯一性、生效期、数值精度和价格版本测试通过；
- [ ] ACTIVE 版本修改、停用、退役和引用保护测试通过；
- [ ] 导入预览、提交、错误文件和幂等测试通过；
- [ ] 下游供货、销量、库存和要货指导读取版本验证通过；
- [ ] 权限、审计、缓存、异步任务、下载安全和性能验证通过；
- [ ] 业务完成主数据 UAT 并确认真实样例。

